<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>daily review</title>
    <url>/2020/09/10/daily-review/</url>
    <content><![CDATA[<a id="more"></a>



<h1 id="2020-09-16"><a href="#2020-09-16" class="headerlink" title="2020.09.16"></a>2020.09.16</h1><h2 id="算法"><a href="#算法" class="headerlink" title="算法"></a>算法</h2><h3 id="1-最大矩形"><a href="#1-最大矩形" class="headerlink" title="1. 最大矩形"></a><a href="https://leetcode-cn.com/problems/maximal-rectangle/" target="_blank" rel="noopener">1. 最大矩形</a></h3><figure class="highlight prolog"><table><tr><td class="code"><pre><span class="line">输入:</span><br><span class="line">[</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"0"</span>],</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>],</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>],</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"0"</span>,<span class="string">"1"</span>,<span class="string">"0"</span>]</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>分析： 该题目可以有两种方法来做，动态规划和 栈</p>
<p><strong>动态规划的话</strong>，我们要获得<strong>每个结点作为 矩形的 右下角</strong> 形成的矩形的最大 宽度 进一步获得 相应的面积</p>
<p><strong>栈</strong>： 本题和之前的 <a href="https://leetcode-cn.com/problems/largest-rectangle-in-histogram/" target="_blank" rel="noopener">2. 柱状图中最大的矩形</a>很相似 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">maximalRectangle</span><span class="params">(<span class="keyword">char</span>[][] matrix)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> m=matrix.length;</span><br><span class="line">        <span class="keyword">if</span>(m==<span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> n=matrix[<span class="number">0</span>].length;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span>[][]dp = <span class="keyword">new</span> <span class="keyword">int</span>[m][n];</span><br><span class="line">        <span class="keyword">int</span> res=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;m;i++)&#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">0</span>;j&lt;n;j++)&#123;</span><br><span class="line">                <span class="keyword">if</span>(matrix[i][j]==<span class="string">'0'</span>)</span><br><span class="line">                    dp[i][j]=<span class="number">0</span>;</span><br><span class="line">                <span class="keyword">else</span> </span><br><span class="line">                    dp[i][j]= j==<span class="number">0</span>? <span class="number">1</span>: dp[i][j-<span class="number">1</span>]+<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">int</span> width=dp[i][j];</span><br><span class="line">                <span class="keyword">for</span>(<span class="keyword">int</span> k=i;k&gt;=<span class="number">0</span>;k--)&#123;</span><br><span class="line">                width=Math.min(dp[k][j],width);</span><br><span class="line">                res = Math.max(res,width*(i-k+<span class="number">1</span>));  </span><br><span class="line">                &#125;                                     </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">help</span><span class="params">(<span class="keyword">int</span>[] heights)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">int</span> len=heights.length;</span><br><span class="line">        <span class="keyword">if</span>(len==<span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> res=<span class="number">0</span>;</span><br><span class="line">        LinkedList&lt;Integer&gt; stack = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">            <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; heights[i]&lt;heights[stack.getLast()])&#123;</span><br><span class="line">                <span class="keyword">int</span> curHeight = heights[stack.removeLast()];</span><br><span class="line">                <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; curHeight==heights[stack.getLast()])</span><br><span class="line">                    stack.removeLast();</span><br><span class="line">                </span><br><span class="line">                <span class="keyword">int</span> width;</span><br><span class="line">                <span class="keyword">if</span>(stack.isEmpty())</span><br><span class="line">                    width=i;</span><br><span class="line">                <span class="keyword">else</span> width = i- stack.getLast()-<span class="number">1</span>;</span><br><span class="line">                res = Math.max(res,(width*curHeight));</span><br><span class="line">            &#125;</span><br><span class="line">            stack.add(i);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(!stack.isEmpty())&#123;</span><br><span class="line">            <span class="keyword">int</span> curHeight = heights[stack.removeLast()];</span><br><span class="line">            <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; curHeight== heights[stack.getLast()])</span><br><span class="line">                stack.removeLast();</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">int</span> width;</span><br><span class="line">            <span class="keyword">if</span>(stack.isEmpty())  width=len;</span><br><span class="line">            <span class="keyword">else</span> width=len-stack.getLast()-<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            res=Math.max(res,(width*curHeight));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">maximalRectangle</span><span class="params">(<span class="keyword">char</span>[][] matrix)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (matrix.length == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> maxarea = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[matrix[<span class="number">0</span>].length];</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; matrix.length; i++) &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; matrix[<span class="number">0</span>].length; j++) &#123;</span><br><span class="line">                dp[j] = matrix[i][j] == <span class="string">'1'</span> ? dp[j] + <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            maxarea = Math.max(maxarea, help(dp));</span><br><span class="line">        &#125; <span class="keyword">return</span> maxarea;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





<h1 id="2020-09-17"><a href="#2020-09-17" class="headerlink" title="2020.09.17"></a>2020.09.17</h1><h2 id="记录"><a href="#记录" class="headerlink" title="记录"></a>记录</h2><h3 id="线程状态转换图"><a href="#线程状态转换图" class="headerlink" title="线程状态转换图"></a>线程状态转换图</h3><ol>
<li><a href="https://imgchr.com/i/wR8PgJ" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2020/09/17/wR8PgJ.jpg" alt="wR8PgJ.jpg"></a></li>
</ol>
<p>wait和 await方法会 让出锁，所以执行 notify或者 signal后 会去通知 另外一个<strong>阻塞</strong>的线程，线程获得锁进入 Runnable状态。</p>
<p>而sleep方法不会让出锁，而是到一定时间后继续执行。</p>
<h3 id="mysql-中full-outer-join等效"><a href="#mysql-中full-outer-join等效" class="headerlink" title="mysql 中full outer join等效"></a>mysql 中full outer join等效</h3><p>MySQL中不支持  完全外联， 所以要有等效替代：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">from</span> t_emp A <span class="keyword">LEFT</span> <span class="keyword">JOIN</span> t_dept B <span class="keyword">ON</span> A.deptId=B.id </span><br><span class="line"><span class="keyword">union</span></span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">from</span> t_emp A <span class="keyword">RIGHt</span> <span class="keyword">JOIN</span> t_dept B <span class="keyword">ON</span> A.deptId=B.id ;</span><br></pre></td></tr></table></figure>

<p>即： union操作既可以 联合 起来 又可以 去重.</p>
<h3 id="Linkedlist和-stack-等可以-for-each语法"><a href="#Linkedlist和-stack-等可以-for-each语法" class="headerlink" title="Linkedlist和 stack 等可以 for each语法"></a>Linkedlist和 stack 等可以 for each语法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LinkedList&lt;Integer&gt; linkedList = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">6</span>; i++) &#123;</span><br><span class="line">    linkedList.add(<span class="number">6</span>-i);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span>(Integer i:linkedList)</span><br><span class="line">    System.out.println(i);</span><br></pre></td></tr></table></figure>

<h3 id="docker-MySQL"><a href="#docker-MySQL" class="headerlink" title="docker MySQL"></a>docker MySQL</h3><ol>
<li><p>配置端口映射启动：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker run -p 3308:3306 --name mysql02 -e MYSQL_ROOT_PASSWORD=123456 -d mysql</span><br></pre></td></tr></table></figure>



</li>
</ol>
<ol start="2">
<li><p>进入 交互式界面 :</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker ps      <span class="comment">##查看 正在运行的容器</span></span><br><span class="line">docker <span class="built_in">exec</span> -it 2d05a8a48c48  bash</span><br></pre></td></tr></table></figure>







</li>
</ol>
<h2 id="题目"><a href="#题目" class="headerlink" title="题目"></a>题目</h2><h3 id="1-去除重复字母"><a href="#1-去除重复字母" class="headerlink" title="1. 去除重复字母"></a><a href="https://leetcode-cn.com/problems/remove-duplicate-letters/" target="_blank" rel="noopener">1. 去除重复字母</a></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">removeDuplicateLetters</span><span class="params">(String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> len=s.length();</span><br><span class="line">        <span class="keyword">if</span>(len==<span class="number">0</span>) <span class="keyword">return</span> <span class="string">""</span>;</span><br><span class="line">        LinkedList&lt;Character&gt; stack = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">        HashSet&lt;Character&gt; set = <span class="keyword">new</span> HashSet&lt;&gt;();</span><br><span class="line">        HashMap&lt;Character,Integer&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++) map.put(s.charAt(i),i);</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">            <span class="keyword">char</span> c=s.charAt(i);</span><br><span class="line">            <span class="keyword">if</span>(!set.contains(c) )&#123;    <span class="comment">//包含了就不要加了，防止重复</span></span><br><span class="line">                <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; c&lt;stack.getLast() &amp;&amp; map.get(stack.getLast())&gt;i)</span><br><span class="line">                    set.remove(stack.removeLast());</span><br><span class="line">                </span><br><span class="line">                set.add(c);</span><br><span class="line">                stack.add(c);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">         StringBuilder sb = <span class="keyword">new</span> StringBuilder(stack.size());</span><br><span class="line">         <span class="keyword">while</span>(stack.size()&gt;<span class="number">0</span>)&#123;</span><br><span class="line">             sb.append(stack.removeFirst());</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> sb.toString();    </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>用栈来存储<strong>最终返回的字符串</strong>，并维持字符串的最小字5</p>
<h1 id="2020-09-18"><a href="#2020-09-18" class="headerlink" title="2020.09.18"></a>2020.09.18</h1><h2 id="记录-1"><a href="#记录-1" class="headerlink" title="记录"></a>记录</h2><h3 id="docker-启动网络报错"><a href="#docker-启动网络报错" class="headerlink" title="docker 启动网络报错"></a>docker 启动网络报错</h3><figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="symbol">WARNING: </span>IPv4 forwarding is disabled. Networking will not work</span><br></pre></td></tr></table></figure>

<p>原因：网络配置出了问题：</p>
<p>解决方法</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">vi /usr/lib/sysctl.d/00-system.conf</span><br><span class="line"><span class="meta">#</span><span class="bash">添加如下代码：</span></span><br><span class="line">net.ipv4.ip_forward=1</span><br><span class="line"><span class="meta">#</span><span class="bash">重启network服务</span></span><br><span class="line">systemctl restart network</span><br></pre></td></tr></table></figure>



<h3 id="sql执行顺序练习"><a href="#sql执行顺序练习" class="headerlink" title="sql执行顺序练习"></a>sql执行顺序练习</h3><p><img src="https://s1.ax1x.com/2020/09/18/whFpHH.png" alt="whFpHH.png"></p>
<p><img src="https://s1.ax1x.com/2020/09/18/whiy7j.png" alt="whiy7j.png"></p>
<h3 id="Arrays排序"><a href="#Arrays排序" class="headerlink" title="Arrays排序"></a>Arrays排序</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Arrays.sort(arr_str,(first,second)-&gt;&#123;<span class="keyword">return</span> first.length()-second.length();&#125;); </span><br><span class="line">Arrays.sort(arr_num,(first,second)-&gt;&#123;<span class="keyword">return</span> second.intValue()-first.intValue() ;&#125;);</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Arrays.sort(arr_str,String::compareTo);   <span class="comment">//简单的String排序</span></span><br><span class="line">Arrays.sort(arr_num,Integer::compareUnsigned);   <span class="comment">//绝对值 升序排列</span></span><br></pre></td></tr></table></figure>

<p>comparator+lambda表达式</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> Arrays.sort(staff,Comparator.comparing(Employee::getName,(f,s)-&gt;&#123;<span class="keyword">return</span> Integer.compare(s.length(),f.length());&#125;)</span><br><span class="line">           <span class="comment">//根据员工姓名长度排序</span></span><br><span class="line"> .thenComparing(Employee::getId,(f,s)-&gt;&#123;<span class="keyword">return</span> Integer.compare(f.intValue(),s.intValue());&#125;));</span><br><span class="line"><span class="comment">// .thenComparing 表示前面条件若相同，则进一步判断的标准</span></span><br></pre></td></tr></table></figure>



<h3 id="单索引优化"><a href="#单索引优化" class="headerlink" title="单索引优化"></a>单索引优化</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 单表索引 优化 练习</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">IF</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">`article`</span>(</span><br><span class="line"><span class="string">`id`</span> <span class="built_in">INT</span>(<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> PRIMARY <span class="keyword">KEY</span> AUTO_INCREMENT,</span><br><span class="line"><span class="string">`author_id`</span> <span class="built_in">INT</span> (<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line"><span class="string">`category_id`</span> <span class="built_in">INT</span>(<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> , </span><br><span class="line"><span class="string">`views`</span> <span class="built_in">INT</span>(<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> , </span><br><span class="line"><span class="string">`comments`</span> <span class="built_in">INT</span>(<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line"><span class="string">`title`</span> VARBINARY(<span class="number">255</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span>,</span><br><span class="line"><span class="string">`content`</span> <span class="built_in">TEXT</span> <span class="keyword">NOT</span> <span class="literal">NULL</span></span><br><span class="line">);</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> <span class="string">`article`</span>(<span class="string">`author_id`</span>,<span class="string">`category_id`</span> ,<span class="string">`views`</span> ,<span class="string">`comments`</span> ,<span class="string">`title`</span> ,<span class="string">`content`</span> )<span class="keyword">VALUES</span></span><br><span class="line">(<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>,<span class="string">'1'</span>,<span class="string">'1'</span>),</span><br><span class="line">(<span class="number">2</span>,<span class="number">2</span>,<span class="number">2</span>,<span class="number">2</span>,<span class="string">'2'</span>,<span class="string">'2'</span>),</span><br><span class="line">(<span class="number">3</span>,<span class="number">3</span>,<span class="number">3</span>,<span class="number">3</span>,<span class="string">'3'</span>,<span class="string">'3'</span>);</span><br><span class="line">(1,1,4,5,'d','文学')</span><br><span class="line"> </span><br><span class="line"> <span class="comment">-- 要求：查询 category_id=1 且 comments &gt;1 且 views 最多文章的 id, author_id </span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">SELECT</span> * <span class="keyword">FROM</span> article;</span><br><span class="line"></span><br><span class="line"> <span class="comment">-- 根据 category_id comments  views 建立索引</span></span><br><span class="line"> <span class="keyword">create</span>  <span class="keyword">index</span> idx_atc_ccv <span class="keyword">on</span> article(category_id, comments, views );</span><br><span class="line">  </span><br><span class="line"> <span class="comment">-- 查看索引</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">INDEX</span> <span class="keyword">from</span> article;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 再次进行查询   type 变为 range</span></span><br><span class="line"><span class="keyword">explain</span> <span class="keyword">select</span> <span class="keyword">id</span>, author_id <span class="keyword">from</span> article <span class="keyword">WHERE</span> category_id=<span class="number">1</span> <span class="keyword">AND</span> comments &gt;<span class="number">1</span> <span class="keyword">ORDER</span> <span class="keyword">BY</span> views <span class="keyword">DESC</span> <span class="keyword">LIMIT</span> <span class="number">1</span>; </span><br><span class="line"><span class="comment">-- 仍然有 using filesort 问题 ： 虽然建立了 索引 但是由于使用了范围（comments&gt;1)，导致 索引失效</span></span><br><span class="line"> 	Extra</span><br><span class="line"> 	Using index condition; Using filesort</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 解决方法; </span></span><br><span class="line"><span class="comment">-- 删除原来建立的索引</span></span><br><span class="line"><span class="keyword">drop</span> <span class="keyword">index</span> idx_atc_ccv <span class="keyword">on</span> article;</span><br><span class="line"><span class="keyword">show</span> <span class="keyword">INDEX</span> <span class="keyword">from</span> article;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 新建索引， 避免将 范围查询的项 加入索引创建(抛弃 comments选项)， 可以避免索引失效</span></span><br><span class="line"><span class="keyword">create</span>  <span class="keyword">index</span> idx_article_cv <span class="keyword">on</span> article(category_id,  views );</span><br><span class="line"><span class="keyword">explain</span> <span class="keyword">select</span> <span class="keyword">id</span>, author_id <span class="keyword">from</span> article <span class="keyword">WHERE</span> category_id=<span class="number">1</span> <span class="keyword">AND</span> comments &gt;<span class="number">1</span> <span class="keyword">ORDER</span> <span class="keyword">BY</span> views <span class="keyword">DESC</span> <span class="keyword">LIMIT</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<h3 id="双表索引优化"><a href="#双表索引优化" class="headerlink" title="双表索引优化"></a>双表索引优化</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 双表索引优化</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">IF</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">`class`</span>(</span><br><span class="line"><span class="string">`id`</span> <span class="built_in">INT</span>(<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> PRIMARY <span class="keyword">KEY</span> AUTO_INCREMENT,</span><br><span class="line"><span class="string">`card`</span> <span class="built_in">INT</span> (<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span></span><br><span class="line">);</span><br><span class="line"> </span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">IF</span> <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> <span class="string">`book`</span>(</span><br><span class="line"><span class="string">`bookid`</span> <span class="built_in">INT</span>(<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span> PRIMARY <span class="keyword">KEY</span> AUTO_INCREMENT,</span><br><span class="line"><span class="string">`card`</span> <span class="built_in">INT</span> (<span class="number">10</span>) <span class="keyword">UNSIGNED</span> <span class="keyword">NOT</span> <span class="literal">NULL</span></span><br><span class="line">);</span><br><span class="line"> </span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> <span class="keyword">class</span>(card)<span class="keyword">VALUES</span>(<span class="keyword">FLOOR</span>(<span class="number">1</span>+(<span class="keyword">RAND</span>()*<span class="number">20</span>)));</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> <span class="keyword">class</span>(card)<span class="keyword">VALUES</span>(<span class="keyword">FLOOR</span>(<span class="number">1</span>+(<span class="keyword">RAND</span>()*<span class="number">20</span>)));</span><br><span class="line"> </span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> book(card)<span class="keyword">VALUES</span>(<span class="keyword">FLOOR</span>(<span class="number">1</span>+(<span class="keyword">RAND</span>()*<span class="number">20</span>)));</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> book(card)<span class="keyword">VALUES</span>(<span class="keyword">FLOOR</span>(<span class="number">1</span>+(<span class="keyword">RAND</span>()*<span class="number">20</span>)));</span><br><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> book(card)<span class="keyword">VALUES</span>(<span class="keyword">FLOOR</span>(<span class="number">1</span>+(<span class="keyword">RAND</span>()*<span class="number">20</span>)));</span><br><span class="line"> </span><br><span class="line"><span class="keyword">select</span>  * <span class="keyword">from</span> book <span class="keyword">inner</span> <span class="keyword">join</span> <span class="keyword">class</span> <span class="keyword">on</span> book.card = class.card;</span><br><span class="line"><span class="keyword">select</span>  * <span class="keyword">from</span> book <span class="keyword">left</span> <span class="keyword">join</span> <span class="keyword">class</span> <span class="keyword">on</span> book.card = class.card;</span><br><span class="line"></span><br><span class="line"><span class="keyword">explain</span> <span class="keyword">select</span>  * <span class="keyword">from</span> book <span class="keyword">left</span> <span class="keyword">join</span> <span class="keyword">class</span> <span class="keyword">on</span> book.card = class.card;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 左表 card 建立索引</span></span><br><span class="line"><span class="keyword">ALTER</span>  <span class="keyword">TABLE</span> book <span class="keyword">ADD</span> <span class="keyword">INDEX</span> Y  (card);</span><br><span class="line"><span class="keyword">show</span> <span class="keyword">index</span> <span class="keyword">from</span> book;</span><br><span class="line"><span class="keyword">explain</span> <span class="keyword">select</span>  * <span class="keyword">from</span> book <span class="keyword">left</span> <span class="keyword">join</span> <span class="keyword">class</span> <span class="keyword">on</span> book.card = class.card;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">1	SIMPLE	book		index		Y	4		20	100.00	Using index</span></span><br><span class="line"><span class="comment">1	SIMPLE	class		ALL					20	100.00	Using where; Using join buffer (hash join)</span></span><br><span class="line"><span class="comment">**/</span></span><br><span class="line"><span class="keyword">drop</span> <span class="keyword">index</span> Y <span class="keyword">on</span> book;  <span class="comment">--丢弃</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 右表 class 建立索引</span></span><br><span class="line"><span class="keyword">ALTER</span>  <span class="keyword">TABLE</span> <span class="keyword">class</span> <span class="keyword">ADD</span> <span class="keyword">INDEX</span> Y  (card);</span><br><span class="line"><span class="keyword">show</span> <span class="keyword">index</span> <span class="keyword">from</span> <span class="keyword">class</span>;</span><br><span class="line"><span class="keyword">explain</span> <span class="keyword">select</span>  * <span class="keyword">from</span> book <span class="keyword">left</span> <span class="keyword">join</span> <span class="keyword">class</span> <span class="keyword">on</span> book.card = class.card;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">1	SIMPLE	book		ALL					20	100.00	</span></span><br><span class="line"><span class="comment">1	SIMPLE	class		ref	Y	Y	4	Test.book.card	1	100.00	Using index</span></span><br><span class="line"><span class="comment">*/</span> </span><br><span class="line"></span><br><span class="line"><span class="comment">-- 结论： 左连接 建在右边  右连接建在左边</span></span><br></pre></td></tr></table></figure>



<h3 id="like"><a href="#like" class="headerlink" title="like"></a>like</h3><p> 左右两边写% 索引失效,  最好只写右边</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">explain</span> <span class="keyword">select</span> * <span class="keyword">from</span> staffs <span class="keyword">WHERE</span> <span class="keyword">name</span> <span class="keyword">like</span> <span class="string">"%z%"</span>;</span><br><span class="line">1	SIMPLE	staffs		ALL					3	33.33	Using where</span><br><span class="line"><span class="keyword">explain</span> <span class="keyword">select</span> * <span class="keyword">from</span> staffs <span class="keyword">WHERE</span> <span class="keyword">name</span> <span class="keyword">like</span> <span class="string">"z%"</span>;</span><br><span class="line">1	SIMPLE	staffs		range	index_staffs_nameAgePos	index_staffs_nameAgePos	74	 1	100.00	Using index condition</span><br></pre></td></tr></table></figure>

<p>不得不用了两个% 时， 应该使用  <strong>覆盖索引</strong>来解决：即对要<strong>查询的所有部分</strong>建立起索引 即可防止索引失效。</p>
<h3 id="索引是否有效小测"><a href="#索引是否有效小测" class="headerlink" title="索引是否有效小测"></a>索引是否有效小测</h3><p><img src="https://s1.ax1x.com/2020/09/18/w4UTij.png" alt="w4UTij.png"></p>
<p><img src="https://s1.ax1x.com/2020/09/18/w4B5Pe.png" alt="w4B5Pe.png"></p>
<h3 id="order-by使用的问题"><a href="#order-by使用的问题" class="headerlink" title="order by使用的问题"></a>order by使用的问题</h3><p>使用 order by有多个选项时，要按照  <strong>建立索引的顺序 去书写</strong>。否则会产生<code>File sort</code></p>
<p>但是 order by选项 在 前面 选择也有时，就不会产生<code>File sort</code></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> c1, c2 <span class="keyword">from</span> t <span class="keyword">where</span> c1=<span class="string">'a1'</span> <span class="keyword">AND</span> c2=<span class="string">'a2'</span> <span class="keyword">order</span> <span class="keyword">by</span> c3, c2  <span class="comment">-- 因为前面 使用了 c2所以 变成了一个常量</span></span><br><span class="line"><span class="comment">-- 不会产生 File sort</span></span><br></pre></td></tr></table></figure>



<h3 id="口诀"><a href="#口诀" class="headerlink" title="口诀"></a>口诀</h3><p><img src="https://s1.ax1x.com/2020/09/18/w4Bh5D.png" alt="w4Bh5D.png"></p>
<h2 id="题目-1"><a href="#题目-1" class="headerlink" title="题目"></a>题目</h2><h3 id="1-丑数-II"><a href="#1-丑数-II" class="headerlink" title="1 丑数 II"></a><a href="https://leetcode-cn.com/problems/ugly-number-ii/" target="_blank" rel="noopener">1 丑数 II</a></h3><figure class="highlight angelscript"><table><tr><td class="code"><pre><span class="line">丑数就是质因数只包含 <span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span> 的正整数。</span><br><span class="line"><span class="number">1</span>是丑数</span><br></pre></td></tr></table></figure>

<ul>
<li>三指针方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Ugly u = <span class="keyword">new</span> Ugly();</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">nthUglyNumber</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;      </span><br><span class="line">        <span class="keyword">return</span> u.nums[n-<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ugly</span></span>&#123;</span><br><span class="line">     <span class="keyword">public</span> <span class="keyword">int</span>[] nums = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">1690</span>];</span><br><span class="line">    Ugly()&#123;</span><br><span class="line">        nums[<span class="number">0</span>]=<span class="number">1</span>; </span><br><span class="line">        <span class="keyword">int</span> i2=<span class="number">0</span>, i3=<span class="number">0</span>, i5=<span class="number">0</span>;    <span class="comment">//三指针， 每一个 步阶 对应一个指针。</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;<span class="number">1690</span>;i++)&#123;</span><br><span class="line">            <span class="keyword">int</span> tmp = Math.min(nums[i2]*<span class="number">2</span>,Math.min(nums[i3]*<span class="number">3</span>,nums[i5]*<span class="number">5</span>));   <span class="comment">//对应的 乘以相应的因子</span></span><br><span class="line">            nums[i]=tmp;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span>(tmp==nums[i2]*<span class="number">2</span>) i2++;</span><br><span class="line">            <span class="keyword">if</span>(tmp==nums[i3]*<span class="number">3</span>) i3++;</span><br><span class="line">            <span class="keyword">if</span>(tmp==nums[i5]*<span class="number">5</span>) i5++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;      </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>堆（优先队列）</li>
</ul>
<p>要始终保持 <strong>弹出最小的</strong> 加入到结果集中取，堆结构恰好符合。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ugly</span> </span>&#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">int</span>[] nums = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">1690</span>];</span><br><span class="line">  Ugly() &#123;</span><br><span class="line">    HashSet&lt;Long&gt; seen = <span class="keyword">new</span> HashSet();</span><br><span class="line">    PriorityQueue&lt;Long&gt; heap = <span class="keyword">new</span> PriorityQueue&lt;Long&gt;();</span><br><span class="line">    seen.add(<span class="number">1L</span>);</span><br><span class="line">    heap.add(<span class="number">1L</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">long</span> currUgly, newUgly;</span><br><span class="line">    <span class="keyword">int</span>[] primes = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>&#125;;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">1690</span>; ++i) &#123;</span><br><span class="line">      currUgly = heap.poll();    <span class="comment">//取出堆 顶 元素</span></span><br><span class="line">      nums[i] = (<span class="keyword">int</span>)currUgly;</span><br><span class="line">      <span class="keyword">for</span>(<span class="keyword">int</span> j : primes) &#123;</span><br><span class="line">        newUgly = currUgly * j;</span><br><span class="line">        <span class="keyword">if</span> (!seen.contains(newUgly)) &#123;    <span class="comment">//当前堆中不存在 新的这个丑数就加入</span></span><br><span class="line">          seen.add(newUgly);</span><br><span class="line">          heap.add(newUgly);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> Ugly u = <span class="keyword">new</span> Ugly();</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">nthUglyNumber</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> u.nums[n - <span class="number">1</span>];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="学习优先队列（堆）"><a href="#学习优先队列（堆）" class="headerlink" title="学习优先队列（堆）"></a>学习优先队列（堆）</h4><p> <code>PriorityQueue</code>：优先队列，也叫 堆。 分为大顶堆和小顶堆， 该规则由数据结构自动维持。</p>
<p>其基本操作</p>
<p><code>pq.add()</code> :加入</p>
<p><code>pq.poll()</code>:取出 <strong>堆 顶元素</strong></p>
<h3 id="2-前-K-个高频元素"><a href="#2-前-K-个高频元素" class="headerlink" title="2. 前 K 个高频元素"></a><a href="https://leetcode-cn.com/problems/top-k-frequent-elements/" target="_blank" rel="noopener">2. 前 K 个高频元素</a></h3><p>堆的经典使用方法， 维护一个<strong>小顶堆</strong>， </p>
<p>当堆中数目&lt;k，则加入。</p>
<p>大于k时， 当前元素频率比顶部高，则弹出顶部元素，将新元素加入， 否则不处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[] topKFrequent(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k) &#123;</span><br><span class="line">    HashMap&lt;Integer,Integer&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> num:nums)</span><br><span class="line">        map.put(num,map.getOrDefault(num,<span class="number">0</span>)+<span class="number">1</span>);   <span class="comment">//获取 元素及出现的次数信息 </span></span><br><span class="line">    </span><br><span class="line">    PriorityQueue&lt;<span class="keyword">int</span>[]&gt; queue = <span class="keyword">new</span> PriorityQueue&lt;&gt;(<span class="keyword">new</span> Comparator&lt;<span class="keyword">int</span>[]&gt;() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(<span class="keyword">int</span>[] m, <span class="keyword">int</span>[] n)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> m[<span class="number">1</span>] - n[<span class="number">1</span>];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);    <span class="comment">//必须重写 compare方法， 因为要维护小顶堆</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(Map.Entry&lt;Integer,Integer&gt; entry: map.entrySet())&#123;</span><br><span class="line">            <span class="keyword">int</span> curnum=entry.getKey(), curcount=entry.getValue();</span><br><span class="line">            <span class="keyword">if</span>(queue.size()==k)&#123;     	<span class="comment">//数目等于 k则 进行判断</span></span><br><span class="line">                <span class="keyword">if</span>(curcount&gt;queue.peek()[<span class="number">1</span>])&#123;</span><br><span class="line">                    queue.poll();</span><br><span class="line">                    queue.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;curnum,curcount&#125;);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                 queue.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;curnum,curcount&#125;);  <span class="comment">//不足k时 直接加入</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">         <span class="keyword">int</span>[] ret = <span class="keyword">new</span> <span class="keyword">int</span>[k];</span><br><span class="line">         <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;k;i++)</span><br><span class="line">             ret[i]=queue.poll()[<span class="number">0</span>];</span><br><span class="line">       </span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="本题的语法使用"><a href="#本题的语法使用" class="headerlink" title="本题的语法使用"></a>本题的语法使用</h4><ol>
<li>HashMap 的 put方法中 ，value可以使用 <code>getOrDefault(num,0)</code>加入</li>
</ol>
<p>如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">map.getOrDefault(num,<span class="number">0</span>)+<span class="number">1</span>  ;  <span class="comment">//有数据，则获得 map[num]的数据加一 否则默认 为0 加一</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li><ul>
<li><code>PriorityQueue&lt;int[]&gt;</code>重写 compare 方法。</li>
</ul>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">PriorityQueue&lt;<span class="keyword">int</span>[]&gt; queue = <span class="keyword">new</span> PriorityQueue&lt;&gt;(<span class="keyword">new</span> Comparator&lt;<span class="keyword">int</span>[]&gt;() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(<span class="keyword">int</span>[] m, <span class="keyword">int</span>[] n)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> m[<span class="number">1</span>] - n[<span class="number">1</span>];     <span class="comment">//递增 ， 小顶堆</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure>

<ul>
<li><code>queue.add()和queue.poll()方法</code></li>
<li><code>new int[]{1,2}</code> 的使用</li>
</ul>
<h1 id="2020-09-19"><a href="#2020-09-19" class="headerlink" title="2020.09.19"></a>2020.09.19</h1><h2 id="题目-2"><a href="#题目-2" class="headerlink" title="题目"></a>题目</h2><h4 id="1-有序矩阵中第K小的元素"><a href="#1-有序矩阵中第K小的元素" class="headerlink" title="1. 有序矩阵中第K小的元素"></a><a href="https://leetcode-cn.com/problems/kth-smallest-element-in-a-sorted-matrix/" target="_blank" rel="noopener">1. 有序矩阵中第K小的元素</a></h4><figure class="highlight angelscript"><table><tr><td class="code"><pre><span class="line">给定一个 n x n 矩阵，其中每行和每列元素均按升序排序，找到矩阵中第 k 小的元素。</span><br><span class="line">请注意，它是排序后的第 k 小元素，而不是第 k 个不同的元素。</span><br><span class="line">matrix = [</span><br><span class="line">   [ <span class="number">1</span>,  <span class="number">5</span>,  <span class="number">9</span>],</span><br><span class="line">   [<span class="number">10</span>, <span class="number">11</span>, <span class="number">13</span>],</span><br><span class="line">   [<span class="number">12</span>, <span class="number">13</span>, <span class="number">15</span>]</span><br><span class="line">],</span><br><span class="line">k = <span class="number">8</span>,</span><br><span class="line"></span><br><span class="line">返回 <span class="number">13</span>。</span><br></pre></td></tr></table></figure>

<ul>
<li>方法一： 使用 堆， 一列一列的进行选择， 第一个最小值肯定是左上角，把它从堆中弹出后 ，该加入哪个值呢？ 很明显是被弹出元素的右边第一个的值， 因为排序的特点，右边第一个值是剩余的元素中 <strong>最小或者相等小的值</strong>。 将它加入 堆中 ，如此重复k次即可。</li>
<li>使用 <strong>堆 保存的内容有</strong>： 值，x下标，y下标，故使用<code>new PriorityQueue&lt;int[]&gt;</code></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">kthSmallest</span><span class="params">(<span class="keyword">int</span>[][] matrix, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">           PriorityQueue&lt;<span class="keyword">int</span>[]&gt; pq = <span class="keyword">new</span> PriorityQueue&lt;<span class="keyword">int</span>[]&gt;(<span class="keyword">new</span> Comparator&lt;<span class="keyword">int</span>[]&gt;() &#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(<span class="keyword">int</span>[] a, <span class="keyword">int</span>[] b)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> a[<span class="number">0</span>] - b[<span class="number">0</span>];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        <span class="keyword">int</span> m=matrix.length;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;m;i++)&#123;</span><br><span class="line">            pq.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;matrix[i][<span class="number">0</span>],i,<span class="number">0</span>&#125;);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;k-<span class="number">1</span>;i++)&#123;</span><br><span class="line">           <span class="keyword">int</span>[] tmp= pq.poll();</span><br><span class="line">           <span class="keyword">if</span>(tmp[<span class="number">2</span>]!=m-<span class="number">1</span>)&#123;</span><br><span class="line">               pq.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;matrix[tmp[<span class="number">1</span>]][tmp[<span class="number">2</span>]+<span class="number">1</span>],tmp[<span class="number">1</span>],tmp[<span class="number">2</span>]+<span class="number">1</span> &#125; );</span><br><span class="line">           &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> pq.peek()[<span class="number">0</span>];</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><strong>二分法</strong>：check的作用就是计算图中 <strong>红线左边的 元素的个数。</strong></p>
<p><img src="https://assets.leetcode-cn.com/solution-static/378/378_fig2.png" alt=""></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">kthSmallest</span><span class="params">(<span class="keyword">int</span>[][] matrix, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> n = matrix.length;</span><br><span class="line">        <span class="keyword">int</span> left = matrix[<span class="number">0</span>][<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">int</span> right = matrix[n - <span class="number">1</span>][n - <span class="number">1</span>];</span><br><span class="line">        <span class="keyword">while</span> (left &lt; right) &#123;</span><br><span class="line">            <span class="keyword">int</span> mid = left + ((right - left) &gt;&gt; <span class="number">1</span>);</span><br><span class="line">            <span class="keyword">if</span> (check(matrix, mid, k, n)) &#123;</span><br><span class="line">                right = mid;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                left = mid + <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> left;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">check</span><span class="params">(<span class="keyword">int</span>[][] matrix, <span class="keyword">int</span> mid, <span class="keyword">int</span> k, <span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> i = n - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> num = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (i &gt;= <span class="number">0</span> &amp;&amp; j &lt; n) &#123;</span><br><span class="line">            <span class="keyword">if</span> (matrix[i][j] &lt;= mid) &#123;</span><br><span class="line">                num += i + <span class="number">1</span>;</span><br><span class="line">                j++;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                i--;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> num &gt;= k;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





<h4 id="2-数据流的中位数"><a href="#2-数据流的中位数" class="headerlink" title="2. 数据流的中位数"></a><a href="https://leetcode-cn.com/problems/find-median-from-data-stream/" target="_blank" rel="noopener">2. 数据流的中位数</a></h4><p>本题巧妙运用了 两个堆，一个大顶堆保存中位数左边的数据， 小顶堆保存 中位数右边的数据，  小顶堆中的数据都比大顶堆中的数据大。</p>
<p>要始终保持维护这个特点</p>
<p><img src="https://pic.leetcode-cn.com/94d648b40f1a72ed7984e6fc0828f52d8947761582071a2c1268d4e750006664-295-4.png" alt=""></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MedianFinder</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">private</span> PriorityQueue&lt;Integer&gt; maxheap;</span><br><span class="line">    <span class="keyword">private</span> PriorityQueue&lt;Integer&gt; minheap;     </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MedianFinder</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        count = <span class="number">0</span>;</span><br><span class="line">        maxheap = <span class="keyword">new</span> PriorityQueue&lt;&gt;((x, y) -&gt; y - x);</span><br><span class="line">        minheap = <span class="keyword">new</span> PriorityQueue&lt;&gt;();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addNum</span><span class="params">(<span class="keyword">int</span> num)</span> </span>&#123;</span><br><span class="line">        count += <span class="number">1</span>;</span><br><span class="line">        minheap.offer(num);       <span class="comment">//先加入 右边</span></span><br><span class="line">        maxheap.add(minheap.poll()); <span class="comment">// 把右边的最小数据 加入到左边， 保证右边的数据始终大于左边  </span></span><br><span class="line"> </span><br><span class="line">        <span class="keyword">if</span> ((count &amp; <span class="number">1</span>) != <span class="number">0</span>) &#123;   <span class="comment">//奇数时， 始终保证 中位数右边的堆 大</span></span><br><span class="line">            minheap.add(maxheap.poll());   </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">findMedian</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> ((count &amp; <span class="number">1</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">             <span class="keyword">return</span> (<span class="keyword">double</span>) (maxheap.peek() + minheap.peek()) / <span class="number">2</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">             <span class="keyword">return</span> (<span class="keyword">double</span>) minheap.peek();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>









<h2 id="记录-2"><a href="#记录-2" class="headerlink" title="记录"></a>记录</h2><h3 id="小表驱动大表"><a href="#小表驱动大表" class="headerlink" title="小表驱动大表"></a>小表驱动大表</h3><p><img src="https://s1.ax1x.com/2020/09/19/w5M5r9.png" alt="w5M5r9.png"></p>
<p>exists和in的区别：</p>
<ul>
<li>前者 是先选 <strong>外面</strong> 再验证 是否和括号里面有交集。</li>
<li>后者 是先从 <strong>括号里面选</strong>，再向括号外部。</li>
</ul>
<p>至于什么情况下哪个性能优秀，取决于具体的相对大小， <strong>总原则就是：先选出的表要小，后选的要大，小表驱动大表。</strong></p>
<h3 id="exists语法"><a href="#exists语法" class="headerlink" title="exists语法"></a>exists语法</h3><p><img src="https://s1.ax1x.com/2020/09/19/w5QUd1.png" alt="w5QUd1.png"></p>
<h3 id="慢查询日志"><a href="#慢查询日志" class="headerlink" title="慢查询日志"></a>慢查询日志</h3><p>开启慢查询日志</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">VARIABLES</span> <span class="keyword">like</span> <span class="string">'%slow_query_log%'</span>;</span><br><span class="line"><span class="keyword">set</span> <span class="keyword">GLOBAL</span> slow_query_log=<span class="number">1</span>;    <span class="comment">-- 开启慢查询日志</span></span><br><span class="line"> </span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">VARIABLES</span> <span class="keyword">like</span> <span class="string">'%long_query_time%'</span>; <span class="comment">--查看阈值时间</span></span><br><span class="line"><span class="keyword">set</span> <span class="keyword">GLOBAL</span> long_query_time=<span class="number">2</span>;   <span class="comment">-- 设置 阈值 为2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> <span class="keyword">sleep</span>(<span class="number">11</span>);   <span class="comment">-- 休眠查询</span></span><br></pre></td></tr></table></figure>





<h3 id="SQL更改表的语句"><a href="#SQL更改表的语句" class="headerlink" title="SQL更改表的语句"></a>SQL更改表的语句</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 1. 修改id为主键</span></span><br><span class="line"><span class="keyword">alter</span>  <span class="keyword">table</span> newtable <span class="keyword">MODIFY</span> <span class="keyword">id</span> <span class="built_in">int</span> <span class="keyword">UNSIGNED</span> <span class="keyword">not</span> <span class="literal">null</span>  primary <span class="keyword">key</span>;</span><br><span class="line"><span class="comment">-- 2. 更新表</span></span><br><span class="line"><span class="keyword">Update</span> newtable <span class="keyword">set</span> deptName=<span class="string">'华山派'</span> <span class="keyword">WHERE</span> <span class="keyword">id</span>=<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 3. 新增加主键</span></span><br><span class="line"><span class="keyword">alter</span>  <span class="keyword">table</span> newtable <span class="keyword">add</span> <span class="keyword">id</span> <span class="built_in">int</span> <span class="keyword">UNSIGNED</span> <span class="keyword">not</span> <span class="literal">null</span>  primary <span class="keyword">key</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 4. 增加列</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> newtable <span class="keyword">add</span> newcol <span class="built_in">varchar</span>(<span class="number">10</span>) <span class="keyword">not</span> <span class="literal">Null</span>;  <span class="comment">--新加 newcol的列</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 5. 删除某列</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> newtable <span class="keyword">DROP</span> 字段名;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 6. 增加索引</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> newtable <span class="keyword">ADD</span> <span class="keyword">INDEX</span> ( <span class="string">`id`</span> );</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">INDEX</span> idx <span class="keyword">ON</span> newtable (col1,col2);</span><br><span class="line"><span class="comment">-- 7. 删除索引</span></span><br><span class="line"><span class="keyword">drop</span> <span class="keyword">index</span> idx_1_2 <span class="keyword">on</span> newtable;</span><br></pre></td></tr></table></figure>

<h3 id="自动执行脚本"><a href="#自动执行脚本" class="headerlink" title="自动执行脚本"></a>自动执行脚本</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">delimiter $$</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">function</span> ran_string(n <span class="built_in">int</span>) <span class="keyword">returns</span> <span class="built_in">varchar</span>(<span class="number">255</span>)</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line"><span class="keyword">declare</span> chars_str <span class="built_in">varchar</span>(<span class="number">100</span>) <span class="keyword">default</span> <span class="string">'abcdefghijklmnopqrstuvwxyz'</span>;</span><br><span class="line"><span class="keyword">declare</span> return_str <span class="built_in">varchar</span>(<span class="number">255</span>) <span class="keyword">default</span> <span class="string">''</span>;</span><br><span class="line"><span class="keyword">declare</span> i <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">0</span>;</span><br><span class="line">while i &lt; n do</span><br><span class="line"><span class="keyword">set</span> return_str = <span class="keyword">concat</span>(return_str,<span class="keyword">substring</span>(chars_str,<span class="keyword">floor</span>(<span class="number">1</span>+<span class="keyword">rand</span>()*<span class="number">52</span>),<span class="number">1</span>));</span><br><span class="line"><span class="keyword">set</span> i=i+<span class="number">1</span>;</span><br><span class="line"><span class="keyword">end</span> <span class="keyword">while</span>;</span><br><span class="line">return return_str;</span><br><span class="line"><span class="keyword">end</span> $$</span><br><span class="line"></span><br><span class="line">delimiter $$</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">function</span> rand_num() <span class="keyword">returns</span> <span class="built_in">int</span>(<span class="number">5</span>)</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line"><span class="keyword">declare</span> i <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">set</span> i=<span class="keyword">floor</span>(<span class="number">100</span>+<span class="keyword">rand</span>()*<span class="number">10</span>);   <span class="comment">-- 100-109范围</span></span><br><span class="line">return i;</span><br><span class="line"><span class="keyword">end</span> $$</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">delimiter $$ </span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> insert_emp(<span class="keyword">in</span> <span class="keyword">start</span> <span class="built_in">int</span>(<span class="number">10</span>),<span class="keyword">in</span> max_num <span class="built_in">int</span>(<span class="number">10</span>))</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line"><span class="keyword">declare</span> i <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">set</span> autocommit = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">repeat</span><br><span class="line"><span class="keyword">set</span> i = i+<span class="number">1</span>;</span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> emp(empno,ename,job,mgr,hiredate,sal,comm,deptno) <span class="keyword">values</span>((<span class="keyword">start</span>+i),ran_string(<span class="number">6</span>),<span class="string">'salesman'</span>,<span class="number">0001</span>,<span class="keyword">curdate</span>(),<span class="number">2000</span>,<span class="number">400</span>,rand_num());</span><br><span class="line">until i=max_num</span><br><span class="line"><span class="keyword">end</span> <span class="keyword">repeat</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">commit</span>;</span><br><span class="line"><span class="keyword">end</span> $$</span><br><span class="line"></span><br><span class="line">delimiter $$ </span><br><span class="line"><span class="keyword">create</span> <span class="keyword">procedure</span> insert_dept(<span class="keyword">in</span> <span class="keyword">start</span> <span class="built_in">int</span>(<span class="number">10</span>),<span class="keyword">in</span> max_num <span class="built_in">int</span>(<span class="number">10</span>))</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line"><span class="keyword">declare</span> i <span class="built_in">int</span> <span class="keyword">default</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">set</span> autocommit = <span class="number">0</span>;</span><br><span class="line">repeat</span><br><span class="line"><span class="keyword">set</span> i = i+<span class="number">1</span>;</span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span> dept(deptno,dname,loc) <span class="keyword">values</span>((<span class="keyword">start</span>+i),ran_string(<span class="number">10</span>),ran_string(<span class="number">8</span>));</span><br><span class="line">until i=max_num</span><br><span class="line"><span class="keyword">end</span> <span class="keyword">repeat</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">commit</span>;</span><br><span class="line"><span class="keyword">end</span> $$</span><br><span class="line"></span><br><span class="line">delimiter ;</span><br><span class="line"></span><br><span class="line"><span class="keyword">CALL</span> insert_dept(<span class="number">100</span>,<span class="number">10</span>);</span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> dept;</span><br><span class="line"><span class="keyword">CALL</span> insert_dept(<span class="number">50</span>,<span class="number">1000</span>);</span><br></pre></td></tr></table></figure>



<h3 id="profile的内容"><a href="#profile的内容" class="headerlink" title="profile的内容"></a>profile的内容</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">VARIABLES</span> <span class="keyword">like</span> <span class="string">'%profiling%'</span>;   <span class="comment">-- 查看 是否开启 （默认关闭）</span></span><br><span class="line"><span class="keyword">set</span> profiling=<span class="keyword">on</span>;   <span class="comment">-- 开启 profiling</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">profiles</span>;   <span class="comment">-- 查看 profile内容</span></span><br><span class="line"><span class="keyword">show</span> profile cpu ,<span class="keyword">block</span> io <span class="keyword">for</span> <span class="keyword">QUERY</span> <span class="number">10</span>;  <span class="comment">--proFile查看选项 和 相关的语句</span></span><br></pre></td></tr></table></figure>



<p>当出现 下面几项时，就严重问题（常用的）<strong>严重拖垮性能</strong>。</p>
<p><img src="https://s1.ax1x.com/2020/09/19/wIQ11O.png" alt="wIQ11O.png"></p>
<h3 id="数据库锁理论"><a href="#数据库锁理论" class="headerlink" title="数据库锁理论"></a>数据库锁理论</h3><h4 id="读锁"><a href="#读锁" class="headerlink" title="读锁"></a>读锁</h4><p>加了读锁。另外一个session不可以修改 被加锁的内容， <strong>会阻塞等待，直到解锁。</strong></p>
<p><img src="https://s1.ax1x.com/2020/09/19/wI1RpV.png" alt="wI1RpV.png"></p>
<h4 id="读写锁区别"><a href="#读写锁区别" class="headerlink" title="读写锁区别"></a>读写锁区别</h4><p>简而言之： <strong>读锁会阻塞 写不阻塞读， 写锁 阻塞 读写。</strong></p>
<h4 id="加锁解锁"><a href="#加锁解锁" class="headerlink" title="加锁解锁"></a>加锁解锁</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">how open tables;    <span class="comment">-- 查看加锁情况  值为1 的说明有锁</span></span><br><span class="line"><span class="keyword">lock</span> <span class="keyword">table</span> t_emp <span class="keyword">read</span>, t_dept write;    <span class="comment">-- 加锁</span></span><br><span class="line"><span class="keyword">unlock</span> <span class="keyword">tables</span>;   <span class="comment">--解锁</span></span><br></pre></td></tr></table></figure>

<h4 id="索引失效"><a href="#索引失效" class="headerlink" title="索引失效"></a>索引失效</h4><p>索引失效可能导致 行锁变表锁 ， 性能大大下降，</p>
<p>比如 给 varchar类型变量建立索引后，插入数据时，<strong>varchar类型没有加单引号</strong>， 不会报错（MySQL的自动类型转换） 但是 索引会失效导致性能下降，带来严重的问题。</p>
<h4 id="间隙锁"><a href="#间隙锁" class="headerlink" title="间隙锁"></a>间隙锁</h4><p><img src="https://s1.ax1x.com/2020/09/19/wIaxnx.png" alt="wIaxnx.png"></p>
<h4 id="手动锁定某一行"><a href="#手动锁定某一行" class="headerlink" title="手动锁定某一行"></a>手动锁定某一行</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> tb <span class="keyword">where</span> <span class="keyword">id</span>=<span class="number">8</span> <span class="keyword">for</span> <span class="keyword">update</span>;</span><br><span class="line"><span class="comment">-- 锁定后其他对这行的操作会被阻塞。</span></span><br></pre></td></tr></table></figure>

<h4 id="分析行锁竞争情况"><a href="#分析行锁竞争情况" class="headerlink" title="分析行锁竞争情况"></a>分析行锁竞争情况</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">status</span> <span class="keyword">like</span> <span class="string">'%innodb_row_lock%'</span>;   <span class="comment">-- 分析系统上 行锁的竞争情况</span></span><br></pre></td></tr></table></figure>
<p><img src="https://s1.ax1x.com/2020/09/19/woAUgA.png" alt="woAUgA.png"></p>
<h1 id="2020-09-20"><a href="#2020-09-20" class="headerlink" title="2020.09.20"></a>2020.09.20</h1><h2 id="题目-3"><a href="#题目-3" class="headerlink" title="题目"></a>题目</h2><h3 id="1-加油站"><a href="#1-加油站" class="headerlink" title="1. 加油站"></a><a href="https://leetcode-cn.com/problems/gas-station/" target="_blank" rel="noopener">1. 加油站</a></h3><p>本题的最核心思路： </p>
<p>一个站的收益如果小于0，<strong>肯定不能作为起点</strong>；</p>
<p>而连续的多个站也可以等效地看做一个站，如果其累积收益小于0，就跳过，寻找下一个。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">canCompleteCircuit</span><span class="params">(<span class="keyword">int</span>[] gas, <span class="keyword">int</span>[] cost)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> n=gas.length;</span><br><span class="line">        <span class="keyword">int</span> total_tank=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> cur_tank=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> start_station=<span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;i++)&#123;</span><br><span class="line">            total_tank+=gas[i]-cost[i];</span><br><span class="line">            cur_tank+=gas[i]-cost[i];</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span>(cur_tank&lt;<span class="number">0</span>)&#123;</span><br><span class="line">                start_station=i+<span class="number">1</span>;</span><br><span class="line">                cur_tank=<span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> total_tank&lt;<span class="number">0</span>? -<span class="number">1</span> : start_station;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-跳跃游戏"><a href="#2-跳跃游戏" class="headerlink" title="2. 跳跃游戏"></a><a href="https://leetcode-cn.com/problems/jump-game/" target="_blank" rel="noopener">2. 跳跃游戏</a></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">canJump</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> rightmost=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> n=nums.length;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;i++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(rightmost&gt;=i)&#123;</span><br><span class="line">                rightmost=Math.max(rightmost,i+nums[i]);</span><br><span class="line">                <span class="keyword">if</span>(rightmost&gt;=n-<span class="number">1</span>)</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">true</span>;                    </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="2020-09-22"><a href="#2020-09-22" class="headerlink" title="2020.09.22"></a>2020.09.22</h1><h2 id="题目-4"><a href="#题目-4" class="headerlink" title="题目"></a>题目</h2><h3 id="1-跳跃游戏-II"><a href="#1-跳跃游戏-II" class="headerlink" title="1. 跳跃游戏 II"></a><a href="https://leetcode-cn.com/problems/jump-game-ii/" target="_blank" rel="noopener">1. 跳跃游戏 II</a></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">jump</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> len=nums.length;</span><br><span class="line">        <span class="keyword">int</span> maxIndex=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> step=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> end=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len-<span class="number">1</span>;i++)&#123;    <span class="comment">//不访问最后一个数组元素，因为能到最后一个元素，上一次的距离一定覆盖到了</span></span><br><span class="line">            maxIndex=Math.max(maxIndex,i+nums[i]);</span><br><span class="line">            <span class="keyword">if</span>(i==end)&#123;          <span class="comment">//到了 end 说明已经到了上一次可以到达的最远 地方，必须要step++了</span></span><br><span class="line">                end=maxIndex;</span><br><span class="line">                step++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> step;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该解法妙就妙在：每次记录当前位置可以跳到的最远距离，并不断更新整体可以到达的最远下标。</p>
<p>用end记录上一次跳跃 可以到的最远位置，当i==end时， 说明step必须要加一了。</p>
<h3 id="135-分发糖果"><a href="#135-分发糖果" class="headerlink" title="135. 分发糖果"></a><a href="https://leetcode-cn.com/problems/candy/" target="_blank" rel="noopener">135. 分发糖果</a></h3><p>感悟： 这题目要求 保证分数高的 分得的糖果比两边都高， 所以一开始感觉无从下手， 但是拆分开来看就简单了，先保证一变，再去考虑另外一边， 这样考虑就简单了很多。 将问题拆分，就变得有思绪了。 任何事情不都是这样吗？</p>
<ol>
<li>暴力解法</li>
</ol>
<p>学习使用 <strong>flag 标志思想</strong>。 有更新时，将flag 跟新为true， 进入循环先设置为false，这样就能保证没有数据变换时， 跳出循环。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">candy</span><span class="params">(<span class="keyword">int</span>[] ratings)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> len=ratings.length;</span><br><span class="line">        <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span>[]candies = <span class="keyword">new</span> <span class="keyword">int</span>[len];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">boolean</span> flag=<span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">while</span>(flag)&#123;</span><br><span class="line">            flag=<span class="keyword">false</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">                <span class="keyword">if</span>(i!=len-<span class="number">1</span> &amp;&amp; ratings[i]&gt;ratings[i+<span class="number">1</span>] &amp;&amp; candies[i]&lt;=candies[i+<span class="number">1</span>])&#123;</span><br><span class="line">                    flag=<span class="keyword">true</span>;</span><br><span class="line">                    candies[i]=candies[i+<span class="number">1</span>]+<span class="number">1</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                 <span class="keyword">if</span>(i&gt;<span class="number">0</span> &amp;&amp; ratings[i]&gt;ratings[i-<span class="number">1</span>] &amp;&amp; candies[i]&lt;=candies[i-<span class="number">1</span>])&#123;</span><br><span class="line">                    flag=<span class="keyword">true</span>;</span><br><span class="line">                    candies[i]=candies[i-<span class="number">1</span>]+<span class="number">1</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> tmp:candies)</span><br><span class="line">            sum+=tmp;</span><br><span class="line">            </span><br><span class="line">        <span class="keyword">return</span> sum+len;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<ol start="2">
<li>两遍扫描</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">candy</span><span class="params">(<span class="keyword">int</span>[] ratings)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> len=ratings.length;</span><br><span class="line">        <span class="keyword">int</span> []candies = <span class="keyword">new</span> <span class="keyword">int</span>[len];</span><br><span class="line">       </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;len;i++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(ratings[i]&gt;ratings[i-<span class="number">1</span>])</span><br><span class="line">                candies[i]=candies[i-<span class="number">1</span>]+<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=len-<span class="number">2</span>;i&gt;=<span class="number">0</span>;i--)&#123;</span><br><span class="line">            <span class="keyword">if</span>(ratings[i]&gt;ratings[i+<span class="number">1</span>])</span><br><span class="line">                candies[i]=Math.max(candies[i+<span class="number">1</span>]+<span class="number">1</span>,candies[i]);   <span class="comment">//一定要用 max保证，取最大的。</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i:candies)</span><br><span class="line">            sum+=i;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> sum+len;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="2020-09-23"><a href="#2020-09-23" class="headerlink" title="2020.09.23"></a>2020.09.23</h1><h2 id="题目-5"><a href="#题目-5" class="headerlink" title="题目"></a>题目</h2><p>两道差不多的题目</p>
<p><a href="https://leetcode-cn.com/problems/create-maximum-number/solution/yi-zhao-chi-bian-li-kou-si-dao-ti-ma-ma-zai-ye-b-7/" target="_blank" rel="noopener">参考链接</a></p>
<h3 id="1-移掉K位数字"><a href="#1-移掉K位数字" class="headerlink" title="1. 移掉K位数字"></a><a href="https://leetcode-cn.com/problems/remove-k-digits/" target="_blank" rel="noopener">1. 移掉K位数字</a></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">removeKdigits</span><span class="params">(String num, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">        LinkedList&lt;Character&gt; stack = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">        stack.add(num.charAt(<span class="number">0</span>));</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;num.length();i++)&#123;</span><br><span class="line">            Character c = num.charAt(i);           </span><br><span class="line">            <span class="keyword">while</span>(k&gt;<span class="number">0</span> &amp;&amp; !stack.isEmpty() &amp;&amp; c&lt;stack.getLast())&#123;</span><br><span class="line">                k--;</span><br><span class="line">                stack.removeLast();            </span><br><span class="line">            &#125;</span><br><span class="line">              stack.add(c);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(k&gt;<span class="number">0</span> &amp;&amp; !stack.isEmpty()) &#123;</span><br><span class="line">        stack.removeLast();</span><br><span class="line">        k--;</span><br><span class="line">        &#125; </span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span>(stack.isEmpty()) <span class="keyword">return</span> <span class="string">"0"</span>;</span><br><span class="line">        StringBuilder stb = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">boolean</span> start=<span class="keyword">true</span>;</span><br><span class="line">       </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">char</span> c:stack)&#123;</span><br><span class="line">            <span class="keyword">if</span>(start &amp;&amp; c==<span class="string">'0'</span>) <span class="keyword">continue</span>;</span><br><span class="line">            start=<span class="keyword">false</span>;</span><br><span class="line">            stb.append(c);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> stb.length()==<span class="number">0</span>? <span class="string">"0"</span> : stb.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="2-拼接最大数"><a href="#2-拼接最大数" class="headerlink" title="2 拼接最大数"></a><a href="https://leetcode-cn.com/problems/create-maximum-number/" target="_blank" rel="noopener">2 拼接最大数</a></h3><p>把 求  <strong>选择k个元素组合最大</strong>，转换成 <strong>删除 length-k个数</strong> 使组合最大。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[] maxNumber(<span class="keyword">int</span>[] nums1, <span class="keyword">int</span>[] nums2, <span class="keyword">int</span> k) &#123;</span><br><span class="line">        <span class="keyword">int</span>[] ans = <span class="keyword">new</span> <span class="keyword">int</span>[k];</span><br><span class="line">        <span class="keyword">if</span>(nums1.length + nums2.length &lt; k) <span class="keyword">return</span> ans;</span><br><span class="line">		</span><br><span class="line">        <span class="comment">//s的定义域 是 nums1可以取得的长度范围， 一共取k长, 这样就容易理解下面写的了</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> s = Math.max(k - nums2.length, <span class="number">0</span>); s &lt;= Math.min(nums1.length , k); s++)&#123;  <span class="comment">//下限 取最大，上限取最小</span></span><br><span class="line">            List&lt;Integer&gt; l1 = getKLargestNumber(nums1, s);   <span class="comment">//O(n1)</span></span><br><span class="line">            List&lt;Integer&gt; l2 = getKLargestNumber(nums2, k - s);  <span class="comment">//O(n2)</span></span><br><span class="line">            <span class="keyword">int</span>[] merge = merge(l1, l2);    <span class="comment">//O(k)</span></span><br><span class="line">            <span class="keyword">if</span>(isBigger(merge, ans)) ans = merge;  <span class="comment">//O(k)</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//获取长度为k的最大子序列</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Integer&gt; <span class="title">getKLargestNumber</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> n = nums.length;</span><br><span class="line">        <span class="keyword">int</span> popCnt = n - k;   <span class="comment">//要丢弃的数目</span></span><br><span class="line">        List&lt;Integer&gt; res = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">if</span>(k == <span class="number">0</span>)  <span class="keyword">return</span> res;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">            <span class="comment">//新加入的比较大</span></span><br><span class="line">            <span class="keyword">while</span>(!res.isEmpty() &amp;&amp; res.get(res.size() - <span class="number">1</span>) &lt; nums[i] &amp;&amp; popCnt &gt; <span class="number">0</span>)&#123;</span><br><span class="line">                res.remove(res.size() - <span class="number">1</span>);  <span class="comment">//删除末尾 比较小的元素</span></span><br><span class="line">                popCnt--;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(res.size() &lt; k) res.add(nums[i]);  <span class="comment">//</span></span><br><span class="line">            <span class="keyword">else</span> popCnt--;  <span class="comment">// 如果结果集已经 满了 则不添加到数组， pop--</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//合并两个有序子序列， 成为一个最大值</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[] merge(List&lt;Integer&gt; l1, List&lt;Integer&gt; l2)&#123;</span><br><span class="line">        <span class="keyword">int</span>[] res = <span class="keyword">new</span> <span class="keyword">int</span>[l1.size() + l2.size()];</span><br><span class="line">        <span class="keyword">int</span> l = <span class="number">0</span>, r = <span class="number">0</span>, idx = <span class="number">0</span>;</span><br><span class="line">        </span><br><span class="line">        StringBuilder sb1 = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">        StringBuilder sb2 = <span class="keyword">new</span> StringBuilder();		</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = l; i &lt; l1.size(); i++) sb1.append(l1.get(i));</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = r; i &lt; l2.size(); i++) sb2.append(l2.get(i));</span><br><span class="line">        String a = sb1.toString();</span><br><span class="line">        String b = sb2.toString();</span><br><span class="line">		</span><br><span class="line">        <span class="keyword">while</span> (l &lt; l1.size() || r &lt; l2.size())&#123;</span><br><span class="line">            <span class="keyword">if</span>(l &gt;= l1.size()) res[idx++] = l2.get(r++);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(r &gt;= l2.size()) res[idx++] = l1.get(l++);</span><br><span class="line">        </span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>( a.substring(l).compareTo(b.substring(r))&gt;<span class="number">0</span> )&#123; </span><br><span class="line">                res[idx++] = l1.get(l++);</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                res[idx++] = l2.get(r++);</span><br><span class="line">         &#125;</span><br><span class="line">      &#125;</span><br><span class="line">          <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//前面的数是否大于后面的数</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isBigger</span><span class="params">(<span class="keyword">int</span>[] list, <span class="keyword">int</span>[] ans)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(ans.length == <span class="number">0</span>) <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; list.length; i++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(list[i] &gt; ans[i])    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(list[i] &lt; ans[i])   <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个题目在merger里面，拼接成String类进行操作的原因：  <strong>数组头部相等的情况时，还要进行后面的大小判断，只判断两个数组的第一个元素是不行的。</strong></p>
<p>如：</p>
<figure class="highlight angelscript"><table><tr><td class="code"><pre><span class="line">nums1: <span class="number">6</span>,<span class="number">7</span></span><br><span class="line">nums2: <span class="number">6</span>,<span class="number">0</span>,<span class="number">1</span>,<span class="number">2</span></span><br></pre></td></tr></table></figure>

<p>如果取掉nums2[0]，那么后面， 将会是 nums1[0]  ( =6)和 nums2[1] ( =0)的比较，就会选出  6，显然是不对的，因为应<strong>该选出7 才对，所以仅靠比较第一个的大小是不行的</strong>。</p>
<h1 id="2020-09-24"><a href="#2020-09-24" class="headerlink" title="2020.09.24"></a>2020.09.24</h1><h2 id="题目-6"><a href="#题目-6" class="headerlink" title="题目"></a>题目</h2><h3 id="1-划分字母区间"><a href="#1-划分字母区间" class="headerlink" title="1.划分字母区间"></a><a href="https://leetcode-cn.com/problems/partition-labels/" target="_blank" rel="noopener">1.划分字母区间</a></h3><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">同一个字母只会出现在其中的一个片段。返回一个表示每个字符串片段的长度的列表。</span><br><span class="line"></span><br><span class="line">输入：S = <span class="string">"ababcbacadefegdehijhklij"</span></span><br><span class="line">输出：[<span class="number">9</span>,<span class="number">7</span>,<span class="number">8</span>]</span><br><span class="line">解释：</span><br><span class="line">划分结果为 <span class="string">"ababcbaca"</span>, <span class="string">"defegde"</span>, <span class="string">"hijhklij"</span>。</span><br><span class="line">每个字母最多出现在一个片段中。</span><br><span class="line">像 <span class="string">"ababcbacadefegde"</span>, <span class="string">"hijhklij"</span> 的划分是错误的，因为划分的片段数较少。</span><br></pre></td></tr></table></figure>

<p>这个题目可以类比为 之前的 <strong>跳跃问题Ⅱ</strong>， 每个字母 最后一次出现的地方，就是应该截取的最远距离，在这一段距离中，可能中间的字母最后一次出现的地方比现在的标记还远，那么就更新这个最远距离， 直到遍历到最远的地方，这就是应该截取的位置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Integer&gt; <span class="title">partitionLabels</span><span class="params">(String S)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> len=S.length();</span><br><span class="line">        <span class="keyword">int</span>[] lastPos = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">26</span>];   <span class="comment">//记录 字母最后一次出现的地方</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">            lastPos[S.charAt(i)-<span class="string">'a'</span>]=i;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        List&lt;Integer&gt; res = <span class="keyword">new</span> ArrayList&lt;&gt;();      </span><br><span class="line">        <span class="keyword">int</span> []barrier=<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;-<span class="number">1</span>,<span class="number">0</span>&#125;;   <span class="comment">//barrier[0]：上一次的界限， barrier[1]:新界限</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">            barrier[<span class="number">1</span>]=Math.max(barrier[<span class="number">1</span>], lastPos[S.charAt(i)-<span class="string">'a'</span>]);  <span class="comment">//遍历的过程中不断更新， 类比跳跃问题Ⅱ</span></span><br><span class="line">            <span class="keyword">if</span>(barrier[<span class="number">1</span>]==i)&#123;    <span class="comment">//已经到了边界           </span></span><br><span class="line">                res.add(barrier[<span class="number">1</span>]-barrier[<span class="number">0</span>]);                                     </span><br><span class="line">                barrier[<span class="number">0</span>]=barrier[<span class="number">1</span>];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





<h3 id="2-两地调度"><a href="#2-两地调度" class="headerlink" title="2. 两地调度"></a><a href="https://leetcode-cn.com/problems/two-city-scheduling/" target="_blank" rel="noopener">2. 两地调度</a></h3><figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">公司计划面试 2N 人。第 i 人飞往 A 市的费用为 costs[<span class="string">i</span>][<span class="symbol">0</span>]，飞往 B 市的费用为 costs[<span class="string">i</span>][<span class="symbol">1</span>]。</span><br><span class="line"></span><br><span class="line">返回将每个人都飞到某座城市的最低费用，要求每个城市都有 N 人抵达。</span><br></pre></td></tr></table></figure>



<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">twoCitySchedCost</span><span class="params">(<span class="keyword">int</span>[][] costs)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> m=costs.length;</span><br><span class="line">        <span class="keyword">int</span> n=costs[<span class="number">0</span>].length;</span><br><span class="line">        <span class="keyword">int</span> res=<span class="number">0</span>; </span><br><span class="line">        List&lt;Integer&gt; note1 = <span class="keyword">new</span> ArrayList&lt;&gt;();  <span class="comment">//记录哪些人去了 A</span></span><br><span class="line">        List&lt;Integer&gt; note2 = <span class="keyword">new</span> ArrayList&lt;&gt;();  <span class="comment">//记录哪些人去了 B</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;m;i++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(costs[i][<span class="number">0</span>]&lt;costs[i][<span class="number">1</span>])&#123;</span><br><span class="line">                res+=costs[i][<span class="number">0</span>];</span><br><span class="line">                note1.add(i);</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                res+=costs[i][<span class="number">1</span>];   </span><br><span class="line">                note2.add(i);        </span><br><span class="line">            &#125;            </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(note1.size()==m/<span class="number">2</span>) <span class="keyword">return</span> res; <span class="comment">//去 A 的人正好一半，直接返回</span></span><br><span class="line"></span><br><span class="line">        List&lt;Integer&gt; note = note1.size()&gt;note2.size() ? note1 : note2;  <span class="comment">//找到 去哪个地方的人多 </span></span><br><span class="line">        <span class="keyword">int</span> []diff=<span class="keyword">new</span> <span class="keyword">int</span>[note.size()];  <span class="comment">//计算需要被 剔除人的集合中的人去两地的差值</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;diff.length;i++)&#123;</span><br><span class="line">            <span class="keyword">int</span> index=note.get(i);</span><br><span class="line">            diff[i]=Math.abs(costs[index][<span class="number">0</span>]-costs[index][<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        Arrays.sort(diff);   <span class="comment">//排序，取前面较小的值     </span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;(note.size()- (m/<span class="number">2</span>));i++)</span><br><span class="line">            res+=diff[i];    <span class="comment">//补加 这些值</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="记录-3"><a href="#记录-3" class="headerlink" title="记录"></a>记录</h2><figure class="highlight angelscript"><table><tr><td class="code"><pre><span class="line"><span class="number">1.</span> 编写一个 SQL 查询，获取 Employee 表中第二高的薪水（Salary）</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    <span class="keyword">IFNULL</span>(</span><br><span class="line">     		 (<span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span> Salary <span class="keyword">FROM</span> Employee <span class="keyword">ORDER</span> <span class="keyword">BY</span> Salary <span class="keyword">DESC</span> <span class="keyword">LIMIT</span> <span class="number">1</span> <span class="keyword">OFFSET</span> <span class="number">1</span>),<span class="literal">NULL</span></span><br><span class="line">          )  <span class="keyword">AS</span> SecondHighestSalary</span><br><span class="line"> <span class="comment">-- 这里 使用了 IFNULL（如果非空） 的判断， 防止出现工资数目不到2的情况</span></span><br><span class="line"> <span class="comment">-- 注意要使用 DIstinct</span></span><br></pre></td></tr></table></figure>



<ol start="2">
<li><strong>limit 和 offset的值 都 不能有算数运算。</strong></li>
</ol>
<h1 id="2020-09-25"><a href="#2020-09-25" class="headerlink" title="2020.09.25"></a>2020.09.25</h1><h2 id="题目-7"><a href="#题目-7" class="headerlink" title="题目"></a>题目</h2><h3 id="1-不含-AAA-或-BBB-的字符串"><a href="#1-不含-AAA-或-BBB-的字符串" class="headerlink" title="1. 不含 AAA 或 BBB 的字符串"></a><a href="https://leetcode-cn.com/problems/string-without-aaa-or-bbb/" target="_blank" rel="noopener">1. 不含 AAA 或 BBB 的字符串</a></h3><p>该解法思路很奇妙： </p>
<ul>
<li>谁剩余多就优先写谁</li>
<li>writeA标志位 来标记应该写哪一个。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">strWithout3a3b</span><span class="params">(<span class="keyword">int</span> A, <span class="keyword">int</span> B)</span> </span>&#123;</span><br><span class="line">        StringBuilder stb = <span class="keyword">new</span> StringBuilder();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(A&gt;<span class="number">0</span> || B&gt;<span class="number">0</span>)&#123;</span><br><span class="line">            <span class="keyword">boolean</span> writeA=<span class="keyword">false</span>;</span><br><span class="line">            <span class="keyword">int</span> len=stb.length();</span><br><span class="line">            <span class="keyword">if</span>(len&gt;=<span class="number">2</span> &amp;&amp; stb.charAt(len-<span class="number">1</span>)==stb.charAt(len-<span class="number">2</span>))&#123;  </span><br><span class="line">                <span class="comment">//长度大于等于2 并且前两位写的是 b，那么应该写a,不能再写 b了</span></span><br><span class="line">                <span class="keyword">if</span>(stb.charAt(len-<span class="number">1</span>)==<span class="string">'b'</span>)</span><br><span class="line">                    writeA=<span class="keyword">true</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">if</span>(A&gt;=B)   <span class="comment">//否则的话， 就选择 多的那个 </span></span><br><span class="line">                    writeA=<span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span>(A&gt;<span class="number">0</span> &amp;&amp; writeA)&#123;</span><br><span class="line">                A--;</span><br><span class="line">                stb.append(<span class="string">'a'</span>);</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">if</span>(B&gt;<span class="number">0</span>)&#123;</span><br><span class="line">                B--;</span><br><span class="line">                stb.append(<span class="string">'b'</span>);</span><br><span class="line">                &#125;        </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> stb.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-单调递增的数字"><a href="#2-单调递增的数字" class="headerlink" title="2. 单调递增的数字"></a><a href="https://leetcode-cn.com/problems/monotone-increasing-digits/" target="_blank" rel="noopener">2. 单调递增的数字</a></h3><figure class="highlight gml"><table><tr><td class="code"><pre><span class="line">给定一个非负整数 N，找出小于或等于 N 的最大的整数，同时这个整数需要满足其各个位数上的数字是单调递增。</span><br><span class="line"></span><br><span class="line">（当且仅当每个相邻位数上的数字 <span class="symbol">x</span> 和 <span class="symbol">y</span> 满足 <span class="symbol">x</span> &lt;= <span class="symbol">y</span> 时，我们称这个整数是单调递增的。）</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">monotoneIncreasingDigits</span><span class="params">(<span class="keyword">int</span> N)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">char</span>[] S = String.valueOf(N).toCharArray();</span><br><span class="line">        <span class="keyword">int</span> i=<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span>(i&lt;S.length &amp;&amp; S[i-<span class="number">1</span>]&lt;=S[i]) ++i;</span><br><span class="line">        <span class="keyword">while</span>(i&gt;<span class="number">0</span> &amp;&amp; i&lt;S.length &amp;&amp; S[i-<span class="number">1</span>]&gt;S[i]) S[--i]--;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = i+<span class="number">1</span>; j &lt; S.length; ++j) S[j] = <span class="string">'9'</span>;</span><br><span class="line">        <span class="keyword">return</span> Integer.parseInt(String.valueOf(S));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>先找到第一个悬崖 <code>s[i-1]&gt;s[i]</code>。然后，当<strong>悬崖存在时</strong>，前面的数字减去1，那么为了维护单调性，之前的数字也要进行判断， 。再把剩下的数字补上 <code>9</code>，最后完成扫描。</p>
<p>学习使用：  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String.valueOf(Char []S);  <span class="comment">//参数可以是 char型数组</span></span><br><span class="line">Integer.parseInt(...); <span class="comment">//将其他包装类型转换为 Integer类型</span></span><br><span class="line"><span class="keyword">char</span> 类型也可以进行 ++ --操作</span><br></pre></td></tr></table></figure>



<h2 id="SQL练习"><a href="#SQL练习" class="headerlink" title="SQL练习"></a>SQL练习</h2><ol>
<li><figure class="highlight excel"><table><tr><td class="code"><pre><span class="line">编写一个 SQL 查询来实现分数排名。</span><br><span class="line">如果两个分数相同，则两个分数排名（<span class="built_in">Rank</span>）相同。请注意，平分后的下一个名次应该是下一个连续的整数值。换句话说，名次之间不应该有“间隔”。</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>排名的问题 使用 <strong>窗口函数</strong> 来完成。</p>
<p>有三个窗口函数 ：</p>
<p><code>rank  dense_rank   row_number</code></p>
<p>区别：</p>
<p><img src="https://pic.leetcode-cn.com/729cc8ee48f55e4c4c448d764e6c0c1e1de50a7cb1674fd557abff50519651a8-1.png" alt=""></p>
<p>所以根据题意，本题应该使用 dese_rank()函数:  <code>DENSE_RANK() OVER(...)</code></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> Score, </span><br><span class="line">    <span class="keyword">DENSE_RANK</span>() <span class="keyword">OVER</span>(<span class="keyword">ORDER</span> <span class="keyword">BY</span> Score <span class="keyword">DESC</span>) <span class="keyword">AS</span> <span class="string">'Rank'</span></span><br><span class="line"><span class="keyword">FROM</span> Scores</span><br><span class="line"></span><br><span class="line"> <span class="keyword">select</span> city_id ,</span><br><span class="line">    -&gt; <span class="keyword">DENSE_RANK</span>() <span class="keyword">OVER</span>(<span class="keyword">ORDER</span> <span class="keyword">BY</span> city <span class="keyword">desc</span>) <span class="keyword">AS</span> citydesc</span><br><span class="line">    -&gt; <span class="keyword">from</span> city;</span><br></pre></td></tr></table></figure>



<ol start="2">
<li>编写一个 SQL 查询，查找所有至少连续出现三次的数字。</li>
</ol>
<p>感悟： 巧妙利用<strong>三表查询</strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DISTINCT</span></span><br><span class="line">l1.Num </span><br><span class="line"><span class="keyword">AS</span> </span><br><span class="line"> ConsecutiveNums</span><br><span class="line"><span class="keyword">FROM</span> </span><br><span class="line">    <span class="keyword">Logs</span> l1,</span><br><span class="line">    <span class="keyword">Logs</span> l2,</span><br><span class="line">    <span class="keyword">Logs</span> l3</span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">    l1.Id=l2.Id<span class="number">-1</span></span><br><span class="line">    <span class="keyword">AND</span> l2.Id=l3.Id<span class="number">-1</span></span><br><span class="line">    <span class="keyword">AND</span> l1.Num=l2.Num</span><br><span class="line">    <span class="keyword">AND</span> l2.Num=l3.Num</span><br><span class="line">;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>比经理工资高的员工</li>
</ol>
<figure class="highlight gherkin"><table><tr><td class="code"><pre><span class="line">+----+-------+--------+-----------+</span><br><span class="line">|<span class="string"> Id </span>|<span class="string"> Name  </span>|<span class="string"> Salary </span>|<span class="string"> ManagerId </span>|</span><br><span class="line">+----+-------+--------+-----------+</span><br><span class="line">|<span class="string"> 1  </span>|<span class="string"> Joe   </span>|<span class="string"> 70000  </span>|<span class="string"> 3         </span>|</span><br><span class="line">|<span class="string"> 2  </span>|<span class="string"> Henry </span>|<span class="string"> 80000  </span>|<span class="string"> 4         </span>|</span><br><span class="line">|<span class="string"> 3  </span>|<span class="string"> Sam   </span>|<span class="string"> 60000  </span>|<span class="string"> NULL      </span>|</span><br><span class="line">|<span class="string"> 4  </span>|<span class="string"> Max   </span>|<span class="string"> 90000  </span>|<span class="string"> NULL      </span>|</span><br><span class="line">+----+-------+--------+-----------+</span><br><span class="line">查找 Salary比其对应的经历薪资大 的员工</span><br></pre></td></tr></table></figure>



<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> a.Name <span class="keyword">AS</span> Employee</span><br><span class="line"><span class="keyword">FROM</span> </span><br><span class="line">    Employee a,</span><br><span class="line">    Employee b</span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">    a.ManagerId=b.Id</span><br><span class="line">    <span class="keyword">AND</span></span><br><span class="line">    a.Salary&gt;b.Salary;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">a.Name <span class="keyword">as</span> Employee</span><br><span class="line"><span class="keyword">FROM</span>  Employee a <span class="keyword">JOIN</span> Employee b</span><br><span class="line"><span class="keyword">ON</span>  a.ManagerId =b.Id</span><br><span class="line"><span class="keyword">AND</span> a.Salary&gt;b.Salary;</span><br></pre></td></tr></table></figure>



<ol start="4">
<li>查找重复的Email</li>
</ol>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> Email <span class="keyword">FROM</span>(</span><br><span class="line"><span class="keyword">SELECT</span> Email, <span class="keyword">COUNT</span>(Email) <span class="keyword">AS</span> <span class="keyword">num</span></span><br><span class="line"><span class="keyword">FROM</span> Person</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> Email</span><br><span class="line">) <span class="keyword">AS</span> statistic   <span class="comment">-- 这里必须要有 AS </span></span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line"><span class="keyword">num</span>&gt;=<span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">select</span> Email <span class="keyword">from</span> Person</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> Email</span><br><span class="line"><span class="keyword">HAVING</span> <span class="keyword">cOUNT</span>(Email)&gt;<span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p>学习 <strong>count（Email）</strong> 的使用。</p>
<ol start="5">
<li>从 不订购东西的客户</li>
</ol>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">Name</span> <span class="keyword">AS</span> Customers </span><br><span class="line"><span class="keyword">FROM</span> Customers c</span><br><span class="line"><span class="keyword">WHERE</span> c.Id <span class="keyword">NOT</span> <span class="keyword">IN</span>(</span><br><span class="line">    <span class="keyword">SELECT</span> CustomerId  <span class="keyword">FROM</span> Orders </span><br><span class="line">);</span><br></pre></td></tr></table></figure>







<p>6.<a href="https://leetcode-cn.com/problems/department-highest-salary/" target="_blank" rel="noopener">部门工资最高的员工</a></p>
<p>特点： 两个字段也可以用 IN来使用</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    Department.name <span class="keyword">AS</span> <span class="string">'Department'</span>,</span><br><span class="line">    Employee.name <span class="keyword">AS</span> <span class="string">'Employee'</span>,</span><br><span class="line">    Salary</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">    Employee</span><br><span class="line">    <span class="keyword">JOIN</span></span><br><span class="line">    Department <span class="keyword">ON</span> Employee.DepartmentId = Department.Id</span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">    (Employee.DepartmentId , Salary) <span class="keyword">IN</span></span><br><span class="line">    (   <span class="keyword">SELECT</span></span><br><span class="line">            DepartmentId, <span class="keyword">MAX</span>(Salary)</span><br><span class="line">        <span class="keyword">FROM</span></span><br><span class="line">            Employee</span><br><span class="line">        <span class="keyword">GROUP</span> <span class="keyword">BY</span> DepartmentId</span><br><span class="line">	)</span><br><span class="line">;</span><br></pre></td></tr></table></figure>



<h2 id="记录-4"><a href="#记录-4" class="headerlink" title="记录"></a>记录</h2><h3 id="count-1"><a href="#count-1" class="headerlink" title="count(1)"></a>count(1)</h3><ol>
<li>count(1)不比count(*)快。</li>
</ol>
<h3 id="前缀索引"><a href="#前缀索引" class="headerlink" title="前缀索引"></a>前缀索引</h3><p>   前缀索引，就是用<strong>列的前缀代替整个列</strong>作为索引  key，当前缀长度合适时，可以做到既使得前缀索引的选择<strong>性接近全列索引</strong>，同时因为索引key变短而  <strong>减少了索引文件的大小和维护开销</strong>。</p>
<p>如： <code>create index idx_last_name ont employee (last_name(4))</code>  使用last_name的后四位代替 整个 last_name 建立索引。</p>
<h3 id="limit偏移量过大时"><a href="#limit偏移量过大时" class="headerlink" title="limit偏移量过大时"></a>limit偏移量过大时</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> employee a <span class="keyword">inner</span> <span class="keyword">join</span> (<span class="keyword">select</span> <span class="keyword">id</span> <span class="keyword">from</span> employee <span class="keyword">limit</span> <span class="number">100000</span>,<span class="number">10</span>) b  <span class="keyword">on</span> a.id=b.id;</span><br></pre></td></tr></table></figure>

<h3 id="MySQL索引背后的数据结构及算法原理"><a href="#MySQL索引背后的数据结构及算法原理" class="headerlink" title="MySQL索引背后的数据结构及算法原理"></a><a href="http://blog.codinglabs.org/articles/theory-of-mysql-index.html" target="_blank" rel="noopener">MySQL索引背后的数据结构及算法原理</a></h3><h3 id="行转列"><a href="#行转列" class="headerlink" title="行转列"></a>行转列</h3><h3 id="自定义变量"><a href="#自定义变量" class="headerlink" title="自定义变量"></a>自定义变量</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span> @min_actor :=(<span class="keyword">select</span> <span class="keyword">min</span>(actor_id) <span class="keyword">from</span> actor)</span><br><span class="line"><span class="keyword">set</span> @<span class="keyword">last</span>:=(<span class="keyword">select</span> dep_name <span class="keyword">from</span> department <span class="keyword">where</span> <span class="keyword">id</span>=<span class="number">4</span>);</span><br><span class="line"><span class="keyword">select</span> @<span class="keyword">last</span>;</span><br></pre></td></tr></table></figure>





<h3 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h3><p>更新视图时，对视图的修改是作用在  基表上的， 不是直接添加在 已有的视图中。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">create</span> <span class="keyword">view</span> selctid3 <span class="keyword">AS</span> <span class="keyword">select</span> * <span class="keyword">from</span> department <span class="keyword">where</span> <span class="keyword">id</span>=<span class="number">3</span>;</span><br><span class="line"> <span class="keyword">insert</span> <span class="keyword">into</span> selctid3(dep_name,dep_location) <span class="keyword">values</span>(<span class="string">'adf'</span>,<span class="string">'weihai'</span>);</span><br><span class="line"> </span><br><span class="line"> <span class="comment">-- 执行下面语句</span></span><br><span class="line"> <span class="keyword">select</span> * <span class="keyword">from</span> selctid3;   </span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">| id | dep_name | dep_location |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">|  3 | c        | cccc         |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">不会出现上面插入的数据， 因为 插入的语句是直接作用在 了基表中，在基表里有新增的数据</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> selIdBiggerThan7;</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">| id | dep_name | dep_location |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">|  8 | adf      | weihai       |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">1 row in <span class="keyword">set</span> (<span class="number">0.00</span> sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; <span class="keyword">insert</span> <span class="keyword">into</span> selIdBiggerThan7(dep_name,dep_location) <span class="keyword">values</span>(<span class="string">'dfadfda'</span>,<span class="string">'qingdao'</span>);</span><br><span class="line">Query OK, 1 row affected (0.00 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; select * from selIdBiggerThan7;</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">| id | dep_name | dep_location |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">|  8 | adf      | weihai       |</span><br><span class="line">|  9 | dfadfda  | qingdao      |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">2 rows in <span class="keyword">set</span> (<span class="number">0.00</span> sec)</span><br></pre></td></tr></table></figure>





<h3 id="存储过程"><a href="#存储过程" class="headerlink" title="存储过程"></a>存储过程</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">mysql&gt; CREATE PROCEDURE selectit()</span><br><span class="line">    -&gt; BEGIN</span><br><span class="line">    -&gt; select dep_name FROM department;</span><br><span class="line">    -&gt; END ;;</span><br><span class="line">Query OK, 0 rows affected (0.03 sec)</span><br><span class="line"></span><br><span class="line"> </span><br><span class="line">mysql&gt; call selectit();</span><br><span class="line">+<span class="comment">----------+</span></span><br><span class="line">| dep_name |</span><br><span class="line">+<span class="comment">----------+</span></span><br><span class="line">| a        |</span><br><span class="line">| adf      |</span><br><span class="line">| b        |</span><br><span class="line">+<span class="comment">----------+</span></span><br></pre></td></tr></table></figure>



<h1 id="2020-09-26"><a href="#2020-09-26" class="headerlink" title="2020.09.26"></a>2020.09.26</h1><h2 id="题目-8"><a href="#题目-8" class="headerlink" title="题目"></a>题目</h2><h3 id="1-合并区间"><a href="#1-合并区间" class="headerlink" title=" 1.合并区间"></a><a href="https://leetcode-cn.com/problems/merge-intervals/" target="_blank" rel="noopener"> 1.合并区间</a></h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[][] merge(<span class="keyword">int</span>[][] intervals) &#123;</span><br><span class="line">       List&lt;<span class="keyword">int</span>[]&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">       List&lt;<span class="keyword">int</span>[]&gt; tmp = <span class="keyword">new</span> ArrayList&lt;&gt;();       </span><br><span class="line">       <span class="keyword">int</span> m=intervals.length;</span><br><span class="line">       <span class="keyword">if</span>(m==<span class="number">0</span>) <span class="keyword">return</span> <span class="keyword">new</span> <span class="keyword">int</span>[][]&#123;&#125;;</span><br><span class="line">       <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;m;i++)</span><br><span class="line">           list.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;intervals[i][<span class="number">0</span>],intervals[i][<span class="number">1</span>]&#125;);</span><br><span class="line">        </span><br><span class="line">        Collections.sort(list, (o1,o2)-&gt;  o1[<span class="number">0</span>]-o2[<span class="number">0</span>]);</span><br><span class="line">        <span class="keyword">int</span> start=list.get(<span class="number">0</span>)[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">int</span> end  =list.get(<span class="number">0</span>)[<span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;m;i++)&#123;</span><br><span class="line">          <span class="keyword">if</span>(list.get(i)[<span class="number">0</span>]&gt;end)&#123;</span><br><span class="line">              tmp.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;start,end&#125;);</span><br><span class="line">              start=list.get(i)[<span class="number">0</span>];</span><br><span class="line">              end=list.get(i)[<span class="number">1</span>];             </span><br><span class="line">          &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">               end=Math.max(end,list.get(i)[<span class="number">1</span>]);</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        tmp.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;start,end&#125;);   <span class="comment">//最后也要加上，否则会漏掉最后一个区间</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span>[][]res=<span class="keyword">new</span> <span class="keyword">int</span>[tmp.size()][<span class="number">2</span>];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;tmp.size();i++)&#123;</span><br><span class="line">            res[i][<span class="number">0</span>]=tmp.get(i)[<span class="number">0</span>];</span><br><span class="line">            res[i][<span class="number">1</span>]=tmp.get(i)[<span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;       </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>本题逻辑和射气球差不多， 需要根据起始位置进行排序，并且不要忘掉<strong>最后一个区间要额外加上</strong>。</p>
<ul>
<li>使用<code>Collections.sort()</code>对 list 进行排序</li>
<li>使用lambda表达式 代替 comparator。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Collections.sort(list, (o1,o2)-&gt;  o1[<span class="number">0</span>]-o2[<span class="number">0</span>]);  <span class="comment">//注意：没有return</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>List&lt;int[]&gt;</code>: list中的泛型 可以 <strong>使用 数组</strong>。</li>
</ul>
<h3 id="2-插入区间"><a href="#2-插入区间" class="headerlink" title="2.插入区间"></a><a href="https://leetcode-cn.com/problems/insert-interval/" target="_blank" rel="noopener">2.插入区间</a></h3><p>先确定 开头，再确定结尾。</p>
<p>也可以 把 <code>newInterval</code>加入 <code>intervals</code>，用上一题代码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[][] insert(<span class="keyword">int</span>[][] intervals, <span class="keyword">int</span>[] newInterval) &#123;</span><br><span class="line">        <span class="keyword">int</span> m=intervals.length;</span><br><span class="line">        <span class="keyword">if</span>(m==<span class="number">0</span>) <span class="keyword">return</span> <span class="keyword">new</span> <span class="keyword">int</span>[][]&#123;newInterval&#125;;       </span><br><span class="line">        List&lt;<span class="keyword">int</span>[]&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">int</span> start=newInterval[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">int</span> end  =newInterval[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;m;i++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(start&gt;intervals[i][<span class="number">1</span>])&#123;</span><br><span class="line">            list.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;intervals[i][<span class="number">0</span>],intervals[i][<span class="number">1</span>]&#125;);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">            &#125;               </span><br><span class="line">           <span class="keyword">if</span>(intervals[i][<span class="number">0</span>]&lt;=start &amp;&amp; start&lt;=intervals[i][<span class="number">1</span>])</span><br><span class="line">                start=intervals[i][<span class="number">0</span>];</span><br><span class="line"></span><br><span class="line">           <span class="keyword">if</span>(end&lt;intervals[i][<span class="number">0</span>])</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">if</span>(end&lt;=intervals[i][<span class="number">1</span>]) </span><br><span class="line">            &#123;</span><br><span class="line">                end=intervals[i][<span class="number">1</span>];</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;           </span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">         list.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;start,end&#125;);</span><br><span class="line">         <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;m;i++)&#123;</span><br><span class="line">             <span class="keyword">if</span>(intervals[i][<span class="number">0</span>]&gt;end)</span><br><span class="line">                list.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;intervals[i][<span class="number">0</span>],intervals[i][<span class="number">1</span>]&#125;);</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="keyword">int</span> [][]res=<span class="keyword">new</span> <span class="keyword">int</span>[list.size()][<span class="number">2</span>];</span><br><span class="line">         <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;list.size();i++)&#123;</span><br><span class="line">             res[i][<span class="number">0</span>]=list.get(i)[<span class="number">0</span>];</span><br><span class="line">             res[i][<span class="number">1</span>]=list.get(i)[<span class="number">1</span>];</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>







<h2 id="SQL练习-1"><a href="#SQL练习-1" class="headerlink" title="SQL练习"></a>SQL练习</h2><h3 id="1-删除重复的电子邮箱"><a href="#1-删除重复的电子邮箱" class="headerlink" title="1.删除重复的电子邮箱"></a><a href="https://leetcode-cn.com/problems/delete-duplicate-emails/" target="_blank" rel="noopener">1.删除重复的电子邮箱</a></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> p1 <span class="keyword">FROM</span> </span><br><span class="line">    Person p1,</span><br><span class="line">    Person p2</span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">    p1.Email = p2.Email <span class="keyword">AND</span> p1.Id &gt; p2.Id</span><br></pre></td></tr></table></figure>

<p>感悟： 使用同一个表的两个声明，可以用自己来查自己</p>
<h3 id="2-部门工资前三高的所有员工"><a href="#2-部门工资前三高的所有员工" class="headerlink" title="2.部门工资前三高的所有员工"></a><a href="https://leetcode-cn.com/problems/department-top-three-salaries/" target="_blank" rel="noopener">2.部门工资前三高的所有员工</a></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">B.Name <span class="keyword">AS</span> Department,</span><br><span class="line">A.Name <span class="keyword">AS</span> Employee,</span><br><span class="line">A.Salary </span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">(<span class="keyword">SELECT</span> <span class="keyword">DENSE_RANK</span>() <span class="keyword">OVER</span>(<span class="keyword">partition</span> <span class="keyword">by</span> DepartmentId <span class="keyword">order</span> <span class="keyword">by</span> Salary <span class="keyword">desc</span>) <span class="keyword">AS</span> ranking ,</span><br><span class="line">DepartmentId,<span class="keyword">Name</span>,Salary <span class="keyword">FROM</span> Employee) </span><br><span class="line">A</span><br><span class="line"><span class="keyword">JOIN</span></span><br><span class="line">Department B </span><br><span class="line"><span class="keyword">ON</span></span><br><span class="line">A.DepartmentId=B.id</span><br><span class="line"><span class="keyword">WHERE</span> A.ranking&lt;=<span class="number">3</span>;</span><br></pre></td></tr></table></figure>

<p>前三高，<strong>涉及排名的问题，一般使用窗函数</strong>, <code>partition by</code>是<strong>按照什么进行</strong> 分类。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">DENSE_RANK</span>() <span class="keyword">OVER</span>(<span class="keyword">partition</span> <span class="keyword">by</span> DepartmentId <span class="keyword">order</span> <span class="keyword">by</span> Salary <span class="keyword">desc</span>) <span class="keyword">AS</span> ranking,DepartmentId,<span class="keyword">Name</span>,Salary <span class="keyword">FROM</span> Employee) </span><br><span class="line"><span class="comment">-- 根据 Employee表 将工资进行排名，并且按照部门分开计算 ， 该表内容如下：</span></span><br><span class="line">[<span class="number">1</span>, <span class="number">1</span>, <span class="string">"Max"</span>, <span class="number">90000</span>], </span><br><span class="line">[<span class="number">2</span>, <span class="number">1</span>, <span class="string">"Joe"</span>, <span class="number">85000</span>], </span><br><span class="line">[<span class="number">2</span>, <span class="number">1</span>, <span class="string">"Randy"</span>, <span class="number">85000</span>], </span><br><span class="line">[<span class="number">3</span>, <span class="number">1</span>, <span class="string">"Will"</span>, <span class="number">70000</span>], </span><br><span class="line">[<span class="number">4</span>, <span class="number">1</span>, <span class="string">"Janet"</span>, <span class="number">69000</span>], </span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="string">"Henry"</span>, <span class="number">80000</span>], </span><br><span class="line">[<span class="number">2</span>, <span class="number">2</span>, <span class="string">"Sam"</span>, <span class="number">60000</span>]]&#125;</span><br></pre></td></tr></table></figure>



<h3 id="3-上升的温度"><a href="#3-上升的温度" class="headerlink" title="3. 上升的温度"></a><a href="https://leetcode-cn.com/problems/rising-temperature/" target="_blank" rel="noopener">3. 上升的温度</a></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">给定一个 Weather 表，编写一个 SQL 查询，来查找与之前（昨天的）日期相比温度更高的所有日期的 Id。</span><br><span class="line">+<span class="comment">---------+------------------+------------------+</span></span><br><span class="line">| Id(INT) | RecordDate(DATE) | Temperature(INT) |</span><br><span class="line">+<span class="comment">---------+------------------+------------------+</span></span><br><span class="line">|       1 |       2015-01-01 |               10 |</span><br><span class="line">|       2 |       2015-01-02 |               25 |</span><br><span class="line">|       3 |       2015-01-03 |               20 |</span><br><span class="line">|       4 |       2015-01-04 |               30 |</span><br><span class="line">+<span class="comment">---------+------------------+------------------+</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECt</span> w2.Id</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">    Weather w1, Weather w2</span><br><span class="line"><span class="keyword">WHERE</span> </span><br><span class="line">    <span class="keyword">DATEDIFF</span>(w2.RecordDate,w1.RecordDate)=<span class="number">1</span></span><br><span class="line">    <span class="keyword">AND</span></span><br><span class="line">    w2.Temperature&gt;w1.Temperature;</span><br><span class="line">    </span><br><span class="line"><span class="comment">-- 学习 使用 DATEDIFF 函数，日期做差 函数（参数一 减去 参数二的间隔数）</span></span><br></pre></td></tr></table></figure>





<h2 id="记录-5"><a href="#记录-5" class="headerlink" title="记录"></a>记录</h2><h3 id="MySQL5后的触发器"><a href="#MySQL5后的触发器" class="headerlink" title="MySQL5后的触发器"></a>MySQL5后的触发器</h3><h4 id="INSERT触发器"><a href="#INSERT触发器" class="headerlink" title="INSERT触发器"></a>INSERT触发器</h4><p>MySQL5以后的触发器不允许返回值，应该如下使用：把返回值插入变量中</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> newDepName <span class="keyword">AFTER</span> <span class="keyword">INSERT</span> <span class="keyword">ON</span> department</span><br><span class="line"><span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="keyword">ROW</span> <span class="keyword">SELECT</span> NEW.dep_name <span class="keyword">INTO</span>@asd;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">select</span> @asd;</span><br><span class="line">+<span class="comment">------------+</span></span><br><span class="line">| @asd       |</span><br><span class="line">+<span class="comment">------------+</span></span><br><span class="line">| 0x         |</span><br><span class="line">+<span class="comment">------------+</span></span><br><span class="line">1 row in <span class="keyword">set</span> (<span class="number">0.00</span> sec)</span><br><span class="line">mysql&gt; <span class="keyword">insert</span> <span class="keyword">into</span> department(dep_name,dep_location) <span class="keyword">values</span>(<span class="string">'shehaijiudian'</span>,<span class="string">'weihai'</span>);</span><br><span class="line">Query OK, 1 row affected (0.02 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; select @asd;</span><br><span class="line">+<span class="comment">---------------+</span></span><br><span class="line">| @asd          |</span><br><span class="line">+<span class="comment">---------------+</span></span><br><span class="line">| shehaijiudian |</span><br><span class="line">+<span class="comment">---------------+</span></span><br></pre></td></tr></table></figure>



<h4 id="DELETE触发器"><a href="#DELETE触发器" class="headerlink" title="DELETE触发器"></a>DELETE触发器</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> depArchive(</span><br><span class="line"><span class="keyword">id</span> <span class="built_in">INT</span>,</span><br><span class="line">dep_name <span class="built_in">VARCHAR</span>(<span class="number">20</span>),</span><br><span class="line">dep_location <span class="built_in">VARCHAR</span>(<span class="number">20</span>)</span><br><span class="line">);  <span class="comment">-- 创建 用于保存 删除的记录的表</span></span><br><span class="line"><span class="comment">---------------------------------------------------------------</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> DeleDepartment <span class="keyword">BEFORE</span> <span class="keyword">DELETE</span> <span class="keyword">on</span> department  <span class="comment">-- BEFORE DELETE 在删除前触发</span></span><br><span class="line"><span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="keyword">ROW</span></span><br><span class="line"><span class="keyword">BEGIN</span> </span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span>  depArchive(<span class="keyword">id</span>,dep_name,dep_location) <span class="keyword">VALUES</span>   <span class="comment">-- 删除的值在 OLD表中 ，同new（新插入的）类似</span></span><br><span class="line">(OLD.id,OLD.dep_name,OLD.dep_location);</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-----------------------------------------------------------------</span></span><br><span class="line"><span class="keyword">delete</span>  <span class="keyword">from</span> department <span class="keyword">where</span> dep_name = <span class="string">'huaweihaisi'</span>;</span><br><span class="line"><span class="comment">------------------------------------------------------</span></span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> deparchive;</span><br><span class="line">+<span class="comment">------+-------------+--------------+</span></span><br><span class="line">| id   | dep_name    | dep_location |</span><br><span class="line">+<span class="comment">------+-------------+--------------+</span></span><br><span class="line">|   12 | huaweihaisi | shanghai     |</span><br><span class="line">+<span class="comment">------+-------------+--------------+</span></span><br></pre></td></tr></table></figure>

<p><strong>OLD表中的 值是只读的，不允许更新。</strong></p>
<h3 id="ACID特性及原理"><a href="#ACID特性及原理" class="headerlink" title="ACID特性及原理"></a>ACID特性及原理</h3><p><a href="https://www.cnblogs.com/kismetv/p/10331633.html" target="_blank" rel="noopener">参考链接</a>：  关注一下  MVCC 和锁</p>
<p><a href="https://cloud.tencent.com/developer/article/1640798" target="_blank" rel="noopener">参考（简略）</a></p>
<h3 id="MVCC为什么不能解决幻读"><a href="#MVCC为什么不能解决幻读" class="headerlink" title="MVCC为什么不能解决幻读"></a>MVCC为什么不能解决幻读</h3><p>不能从根本上解决，其他是事务还是可以插入 新的数据，只是MVCC 控制不读取罢了。</p>
<p>加上 <code>Next key locks</code>可以从根本上解决。</p>
<h1 id="2020-09-27"><a href="#2020-09-27" class="headerlink" title="2020.09.27"></a>2020.09.27</h1><h2 id="题目-9"><a href="#题目-9" class="headerlink" title="题目"></a>题目</h2><h3 id="1-最大数"><a href="#1-最大数" class="headerlink" title="1. 最大数"></a><a href="https://leetcode-cn.com/problems/largest-number/" target="_blank" rel="noopener">1. 最大数</a></h3><ul>
<li>巧妙利用 <strong>字符串的大小比较</strong>方式， 我们总希望高位上的数字较大</li>
<li>对于 9, 90这样的情况， 需要<code>990</code>而不是<code>909</code>, 所以应该自定义一个比较规则： <code>(sw+s1).compareTo(s1+s2)</code></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">largestNumber</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        String[] asStrs = <span class="keyword">new</span> String[nums.length];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; nums.length; i++) &#123;</span><br><span class="line">            asStrs[i] = String.valueOf(nums[i]);</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">        Arrays.sort(asStrs,(s1,s2)-&gt; (s2+s1).compareTo(s1+s2));</span><br><span class="line">        <span class="keyword">if</span> (asStrs[<span class="number">0</span>].equals(<span class="string">"0"</span>)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"0"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">   </span><br><span class="line">        StringBuilder largestNumberStr = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">for</span> (String numAsStr : asStrs) &#123;</span><br><span class="line">            largestNumberStr.append(numAsStr);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> largestNumberStr.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="2-排序链表"><a href="#2-排序链表" class="headerlink" title="2. 排序链表"></a><a href="https://leetcode-cn.com/problems/sort-list/" target="_blank" rel="noopener">2. 排序链表</a></h3><p>将一个链表 按照 增大的顺序排序， 要求 ： <strong>空间复杂度： 常数</strong>，<strong>时间复杂度<code>nlogn</code></strong></p>
<p>思路： 想到归并排序可以适合链表的这种操作，也 满足复杂度要求。  所以借鉴<strong>归并排序</strong>的思想，先拆分再合并。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ListNode <span class="title">sortList</span><span class="params">(ListNode head)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(head==<span class="keyword">null</span> || head.next==<span class="keyword">null</span>)    <span class="comment">//递归终止条件</span></span><br><span class="line">            <span class="keyword">return</span> head;</span><br><span class="line">        </span><br><span class="line">        ListNode slow=head;</span><br><span class="line">        ListNode fast=head.next;</span><br><span class="line">        <span class="keyword">while</span>(fast!=<span class="keyword">null</span> &amp;&amp; fast.next!=<span class="keyword">null</span>)&#123;  <span class="comment">//使用快慢指针， 快指针走到末尾，慢指针在中间</span></span><br><span class="line">            slow=slow.next;</span><br><span class="line">            fast=fast.next.next;</span><br><span class="line">        &#125;</span><br><span class="line">        ListNode righthead=slow.next;       <span class="comment">//获得右边 头结点</span></span><br><span class="line">        slow.next=<span class="keyword">null</span>;						<span class="comment">//左边链表 隔断</span></span><br><span class="line"></span><br><span class="line">        ListNode left =sortList(head);</span><br><span class="line">        ListNode right=sortList(righthead);</span><br><span class="line">        ListNode dummy = <span class="keyword">new</span> ListNode(<span class="number">0</span>);</span><br><span class="line">        ListNode res=dummy;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(left!=<span class="keyword">null</span> &amp;&amp; right!=<span class="keyword">null</span>)&#123;   <span class="comment">//选取两个中 小的</span></span><br><span class="line">            <span class="keyword">if</span>(left.val&lt;right.val)&#123;</span><br><span class="line">                dummy.next=left;              </span><br><span class="line">                left=left.next;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                dummy.next=right;</span><br><span class="line">                right=right.next;</span><br><span class="line">            &#125;</span><br><span class="line">             dummy=dummy.next;</span><br><span class="line">        &#125;</span><br><span class="line">        dummy.next = left==<span class="keyword">null</span>?right:left;  <span class="comment">//处理链表中可能剩余的部分。</span></span><br><span class="line">        <span class="keyword">return</span> res.next;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="SQL练习-2"><a href="#SQL练习-2" class="headerlink" title="SQL练习"></a>SQL练习</h2><h3 id="1-超过5名学生的课"><a href="#1-超过5名学生的课" class="headerlink" title="1. 超过5名学生的课"></a><a href="https://leetcode-cn.com/problems/classes-more-than-5-students/" target="_blank" rel="noopener">1. 超过5名学生的课</a></h3><p>注意: 使用 <strong>DISTINCT 关键字</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">class</span> </span><br><span class="line"><span class="keyword">FROM</span> courses</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> <span class="keyword">class</span></span><br><span class="line"><span class="keyword">HAVING</span> <span class="keyword">COUNT</span>(<span class="keyword">DISTINCT</span> (student))&gt;=<span class="number">5</span>;   <span class="comment">-- group by  的方式</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> <span class="keyword">class</span> <span class="keyword">FROM</span>   <span class="comment">-- 子查询的方式</span></span><br><span class="line">(</span><br><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">   <span class="keyword">class</span> ,<span class="keyword">COUNT</span>(<span class="keyword">DISTINCT</span>(student)) <span class="keyword">AS</span> <span class="keyword">num</span>   </span><br><span class="line">   <span class="keyword">FROM</span> courses</span><br><span class="line">   <span class="keyword">GROUP</span> <span class="keyword">BY</span> <span class="keyword">class</span></span><br><span class="line">) A</span><br><span class="line"><span class="keyword">WHERE</span> A.num&gt;=<span class="number">5</span></span><br></pre></td></tr></table></figure>





<h3 id="2-换座位"><a href="#2-换座位" class="headerlink" title="2.换座位"></a><a href="https://leetcode-cn.com/problems/exchange-seats/" target="_blank" rel="noopener">2.换座位</a></h3><p><code>CASE  WHEN  MOD</code> <strong>的使用</strong>：</p>
<p><strong>MOD 表示取余</strong></p>
<p><strong>CASE 表示各种状况， 以 END结束</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">    (<span class="keyword">CASE</span></span><br><span class="line">        <span class="keyword">WHEN</span> <span class="keyword">MOD</span>(<span class="keyword">id</span>, <span class="number">2</span>) != <span class="number">0</span> <span class="keyword">AND</span> counts != <span class="keyword">id</span> <span class="keyword">THEN</span> <span class="keyword">id</span> + <span class="number">1</span></span><br><span class="line">        <span class="keyword">WHEN</span> <span class="keyword">MOD</span>(<span class="keyword">id</span>, <span class="number">2</span>) != <span class="number">0</span> <span class="keyword">AND</span> counts = <span class="keyword">id</span> <span class="keyword">THEN</span> <span class="keyword">id</span></span><br><span class="line">        <span class="keyword">ELSE</span> <span class="keyword">id</span> - <span class="number">1</span></span><br><span class="line">    <span class="keyword">END</span>) <span class="keyword">AS</span> <span class="keyword">id</span>,     <span class="comment">-- 选出来的是 id 和 student， 选id之前 用 CASE when 进行处理</span></span><br><span class="line">    student</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">    seat,</span><br><span class="line">    (<span class="keyword">SELECT</span> <span class="keyword">COUNT</span>(*) <span class="keyword">AS</span> counts <span class="keyword">FROM</span> seat) A   <span class="comment">-- 创建的 统计座位个数的 表， 后面必须要起一个名字</span></span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> <span class="keyword">id</span>  ;</span><br></pre></td></tr></table></figure>



<h2 id="记录-6"><a href="#记录-6" class="headerlink" title="记录"></a>记录</h2><h3 id="innodb锁算法"><a href="#innodb锁算法" class="headerlink" title="innodb锁算法"></a>innodb锁算法</h3><ol>
<li>innodb对于行的查询使用<strong>next-key lock</strong>（record  locks+gap locks）</li>
<li><strong>Next-locking keying</strong>为了解决Phantom Problem<strong>幻读问题</strong></li>
<li>当查询的索引含有<strong>唯一属性时</strong>，将next-key lock降级为record key</li>
<li>Gap锁设计的目的是为了<strong>阻止多个事务将记录插入到同一范围内</strong>，而这会导致幻读问题的产生</li>
</ol>
<h3 id="mysql如何解决幻读"><a href="#mysql如何解决幻读" class="headerlink" title="mysql如何解决幻读"></a>mysql如何解决幻读</h3><p><img src="https://img-blog.csdnimg.cn/20190531203108362.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3UwMTMwNjc3NTY=,size_16,color_FFFFFF,t_70" alt=""></p>
<h3 id="MYISAM和INNODB"><a href="#MYISAM和INNODB" class="headerlink" title="MYISAM和INNODB"></a>MYISAM和INNODB</h3><p><a href="http://www.expertphp.in/article/what-are-the-main-differences-between-innodb-and-myisam#:~:text=InnoDB%20does%20not%20support%20FULLTEXT,is%20suitable%20for%20small%20project." target="_blank" rel="noopener">参考链接</a></p>
<p>Mysql 数据库中，最常用的两种引擎是 innordb 和 myisam。InnoDB 是 Mysql 的默<br>认存储引擎。</p>
<ol>
<li><p>事务处理上方面<br>MyISAM 强调的是<strong>性能</strong>，查询的速度比 InnoDB 类型更快，但是<strong>不提供事务支持</strong>。<br><strong>InnoDB 提供事务支持事务。</strong></p>
</li>
<li><p>外键<br>MyISAM 不支持外键，InnoDB 支持外键。</p>
</li>
<li><p><strong>锁</strong><br>MyISAM <strong>只支持表级锁</strong>，InnoDB 支持<strong>行级锁和表级锁</strong>，默认是<strong>行级锁</strong>，行锁大幅度提高了多用户并发操作的性能。innodb 比较适合于<strong>插入和更新操作比较多的情况</strong>，而 myisam 则适合用于<strong>频繁查询</strong>的情况。另外，InnoDB 表的行锁也不是绝对的，如果在执行一个 SQL 语句时，MySQL <strong>不能确定要扫描的范围</strong>，InnoDB 表同样会锁全表。</p>
</li>
<li><p>全文索引<br>MyISAM 支持全文索引， InnoDB 不支持全文索引。innodb 从 mysql5.6 版本开始提供对全文索引的支持。</p>
</li>
<li><p><strong>表主键</strong><br>MyISAM：允许没有主键的表存在。<br>InnoDB：如果没有设定主键，就会自动生成一个 6 字节的主键(用户不可见)。</p>
</li>
<li><p><strong>表的具体行数</strong><br>MyISAM：select count(<em>) from table,MyISAM 只要简单的读出保存好的行数。因为<br>MyISAM 内置了一个计数器，count(</em>)时它直接从计数器中读。<br>InnoDB：不保存表的具体行数，也就是说，执行 select count(*) from table 时，InnoDB要扫描一遍整个表来计算有多少行。</p>
</li>
<li><p>索引的结构</p>
</li>
</ol>
<p>​        MyISAM 的索引是<strong>非聚簇索引</strong>，<strong>可以加载更多索引</strong> ，内存利用率高</p>
<p>​        InnoDB 的索引是 <strong>聚簇索引</strong> ， 体积庞大。</p>
<h1 id="2020-09-28"><a href="#2020-09-28" class="headerlink" title="2020.09.28"></a>2020.09.28</h1><h2 id="题目-10"><a href="#题目-10" class="headerlink" title="题目"></a>题目</h2><h3 id="1-车队"><a href="#1-车队" class="headerlink" title="1 车队"></a><a href="https://leetcode-cn.com/problems/car-fleet/" target="_blank" rel="noopener">1 车队</a></h3><p>感悟：</p>
<p>​    把同一个对象的属性 定义到一个类中，再使用 ，更方便</p>
<p>​    <strong>从右向左计算</strong>，距离终点近的 用时又少，那么肯定<strong>单独成为一个车队</strong>，ans++</p>
<p>​                                距离终点远，但是用时少，那么它<strong>会和前面合并 成一个车队</strong>, ans暂时不变， 更新用时为前面的车队一样</p>
<p>最后 ans++，因为对当前处理的车队还没有统计进去。</p>
<p>从左向右实在不好算，那就换个角度，从右向左</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">carFleet</span><span class="params">(<span class="keyword">int</span> target, <span class="keyword">int</span>[] position, <span class="keyword">int</span>[] speed)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> N = position.length;</span><br><span class="line">        Car[] cars = <span class="keyword">new</span> Car[N];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; ++i)</span><br><span class="line">            cars[i] = <span class="keyword">new</span> Car(position[i], (<span class="keyword">double</span>) (target - position[i]) / speed[i]);</span><br><span class="line"></span><br><span class="line">        Arrays.sort(cars, (a, b) -&gt; Integer.compare(a.position, b.position));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> ans = <span class="number">0</span>, t = N;</span><br><span class="line">        <span class="keyword">while</span> (--t &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (cars[t].time &lt; cars[t-<span class="number">1</span>].time) ans++; <span class="comment">// 后面的追不上前面的，前面的成为一个车队   </span></span><br><span class="line">            <span class="keyword">else</span> cars[t-<span class="number">1</span>] = cars[t];   <span class="comment">//后面的的能追上，同前面 的合并为一个车队   </span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> ans + (t == <span class="number">0</span> ? <span class="number">1</span> : <span class="number">0</span>);    <span class="comment">//=-1是 对之前 t=0的处理</span></span><br><span class="line">        <span class="comment">//最后加一 是因为 当前处理的车队 还没算上</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Car</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> position;</span><br><span class="line">    <span class="keyword">double</span> time;</span><br><span class="line">    Car(<span class="keyword">int</span> p, <span class="keyword">double</span> t) &#123;</span><br><span class="line">        position = p;</span><br><span class="line">        time = t;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





<h2 id="SQL练习-3"><a href="#SQL练习-3" class="headerlink" title="SQL练习"></a>SQL练习</h2><h3 id="1-薪水变动超过15次的员工"><a href="#1-薪水变动超过15次的员工" class="headerlink" title="1.薪水变动超过15次的员工"></a><a href="https://www.nowcoder.com/practice/6d4a4cff1d58495182f536c548fee1ae?tpId=82&&tqId=29759&rp=1&ru=/ta/sql&qru=/ta/sql/question-ranking" target="_blank" rel="noopener">1.薪水变动超过15次的员工</a></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">emp_no, <span class="keyword">COUNT</span>(emp_no)<span class="keyword">AS</span> t</span><br><span class="line"><span class="keyword">FROM</span> salaries</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span></span><br><span class="line">emp_no</span><br><span class="line"><span class="keyword">HAVING</span> t&gt;<span class="number">15</span>;</span><br></pre></td></tr></table></figure>

<p>使用 group by 以后， <strong>使用主键</strong>统计个数。</p>
<h2 id="记录-7"><a href="#记录-7" class="headerlink" title="记录"></a>记录</h2><h3 id="马士兵MySQL知识点"><a href="#马士兵MySQL知识点" class="headerlink" title="马士兵MySQL知识点"></a>马士兵MySQL知识点</h3><h4 id="1-尽量使用自增ID"><a href="#1-尽量使用自增ID" class="headerlink" title="1. 尽量使用自增ID"></a>1. 尽量使用自增ID</h4><h4 id="2-分布式中的iD"><a href="#2-分布式中的iD" class="headerlink" title="2.分布式中的iD"></a>2.分布式中的iD</h4><h4 id="3-聚簇索引和非聚簇索引"><a href="#3-聚簇索引和非聚簇索引" class="headerlink" title="3. 聚簇索引和非聚簇索引"></a>3. 聚簇索引和非聚簇索引</h4><p>根据B+ 树的叶子节点是否存放数据来区分。</p>
<p>InnoDB <strong>主键 索引是聚簇</strong>， <strong>非主键不是聚簇</strong></p>
<p>MyIsam 是非主键索引, InnoDB的二级索引（辅助索引（叶子节点 <strong>存放了 主键ID</strong>））。</p>
<h4 id="4-6字节的rowid"><a href="#4-6字节的rowid" class="headerlink" title="4. 6字节的rowid?"></a>4. 6字节的rowid?</h4><h4 id="5-基数的概念"><a href="#5-基数的概念" class="headerlink" title="5. 基数的概念"></a>5. 基数的概念</h4><p>hyperloglog算法</p>
<p>用处： MySQL ，redis</p>
<p><img src="https://s1.ax1x.com/2020/09/28/0EQE4S.png" alt="0EQE4S.png"></p>
<h4 id="6-ACID"><a href="#6-ACID" class="headerlink" title="6. ACID"></a>6. ACID</h4><ul>
<li>各个实现原理</li>
<li>最重要的性质： 一致性</li>
</ul>
<p>undo  log保证原子性的同时 ， 也有MVCC</p>
<h5 id="可重复读和读已提交"><a href="#可重复读和读已提交" class="headerlink" title="可重复读和读已提交"></a>可重复读和读已提交</h5><p><img src="https://s1.ax1x.com/2020/09/28/0ElqJO.png" alt="0ElqJO.png"></p>
<h4 id="7-预写日志"><a href="#7-预写日志" class="headerlink" title="7. 预写日志"></a>7. 预写日志</h4><p>WAL：write ahead  log</p>
<p>在使用WAL的系统中，所有的修改在<strong>提交之前都要先写入log文件中</strong>，log文件中通常包括<strong>redo和undo信息</strong>，通过<strong>日志记录</strong>描述好数据的改变后(redo和undo)，再<strong>写入缓存</strong>，等缓存区写满后，最后再往持久层修改数据。</p>
<h4 id="8-主从复制原理"><a href="#8-主从复制原理" class="headerlink" title="8. 主从复制原理"></a>8. 主从复制原理</h4><h4 id="9-主从复制延迟原因-解决方案"><a href="#9-主从复制延迟原因-解决方案" class="headerlink" title="9. 主从复制延迟原因 + 解决方案"></a>9. 主从复制延迟原因 + 解决方案</h4><h4 id="10-日志数据写过程"><a href="#10-日志数据写过程" class="headerlink" title="10. 日志数据写过程"></a>10. 日志数据写过程</h4><p><img src="https://s1.ax1x.com/2020/09/28/0VujTf.png" alt="0VujTf.png"></p>
<h4 id="11-二阶段提交"><a href="#11-二阶段提交" class="headerlink" title="11. 二阶段提交"></a>11. 二阶段提交</h4><h1 id="2020-09-29"><a href="#2020-09-29" class="headerlink" title="2020.09.29"></a>2020.09.29</h1><h2 id="题目-11"><a href="#题目-11" class="headerlink" title="题目"></a>题目</h2><h3 id="1-数组的相对排序"><a href="#1-数组的相对排序" class="headerlink" title="1. 数组的相对排序"></a><a href="https://leetcode-cn.com/problems/relative-sort-array/" target="_blank" rel="noopener">1. 数组的相对排序</a></h3><p>主要练习 <strong>hashmap  的使用</strong>， 以及注意： 不在arr2出现的元素，可能也不止一个，所以添加的时候也应该注意。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[] relativeSortArray(<span class="keyword">int</span>[] arr1, <span class="keyword">int</span>[] arr2) &#123;</span><br><span class="line">        <span class="keyword">int</span> len=arr1.length;</span><br><span class="line">        HashMap&lt;Integer,Integer&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)</span><br><span class="line">            map.put(arr1[i],map.getOrDefault(arr1[i],<span class="number">0</span>)+<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">        ArrayList&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> num:arr2)&#123;</span><br><span class="line">            <span class="keyword">if</span>(map.containsKey(num))&#123;</span><br><span class="line">                <span class="keyword">int</span> count=map.get(num);</span><br><span class="line">                <span class="keyword">while</span>(count&gt;<span class="number">0</span>)&#123;</span><br><span class="line">                list.add(num); </span><br><span class="line">                count--; </span><br><span class="line">                &#125;</span><br><span class="line">                map.remove(num);             </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        ArrayList&lt;Integer&gt; tmp = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">if</span>(!map.isEmpty())&#123;</span><br><span class="line">            <span class="keyword">for</span>(Integer i:map.keySet())&#123;</span><br><span class="line">                <span class="keyword">int</span> count=map.get(i);</span><br><span class="line">                <span class="keyword">while</span>(count--&gt;<span class="number">0</span>)</span><br><span class="line">                    tmp.add(i);</span><br><span class="line">            &#125;             </span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Collections.sort(tmp);</span><br><span class="line">        <span class="keyword">for</span>(Integer i:tmp)</span><br><span class="line">        list.add(i);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span>[]res=<span class="keyword">new</span> <span class="keyword">int</span>[list.size()];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;list.size();i++)&#123;</span><br><span class="line">            res[i]=list.get(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="2-重构字符串"><a href="#2-重构字符串" class="headerlink" title="2. 重构字符串"></a><a href="https://leetcode-cn.com/problems/reorganize-string/" target="_blank" rel="noopener">2. 重构字符串</a></h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">给定一个字符串S，检查是否能重新排布其中的字母，使得两相邻的字符不同。</span><br><span class="line">若可行，输出任意可行的结果。若不可行，返回空字符串。</span><br></pre></td></tr></table></figure>

<p>思路： 记录 字母以及对应出现的频率， 根据频率降序排序。</p>
<p>设频率最高为x, 那么我们可以发现， 把这些字母先分为x组，循环给这些组分配字母， 第一轮循环分配字母频率最高的字母（由于在不同的组中，所以一定不会重复）， 第二轮分配字母频率第二高的字母…一直到分配完毕。</p>
<p>将这些分组合并起来， 检查是否有相邻相同， 有，则返回<code>&quot;&quot;</code>， 否则返回拼接好的字符串。</p>
<p>注意： 使用 StringBuilder数组时， 要先new好了，否则会造成空引用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">reorganizeString</span><span class="params">(String S)</span> </span>&#123;           </span><br><span class="line">        <span class="keyword">int</span> len = S.length();</span><br><span class="line">        <span class="keyword">int</span> [][]arr=<span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">26</span>][<span class="number">2</span>];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">            arr[S.charAt(i)-<span class="string">'a'</span>][<span class="number">0</span>]=S.charAt(i);   <span class="comment">//记录 字符</span></span><br><span class="line">            arr[S.charAt(i)-<span class="string">'a'</span>][<span class="number">1</span>]++;             <span class="comment">//记录频率</span></span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        Arrays.sort(arr,(o1,o2)-&gt; Integer.compare(o2[<span class="number">1</span>],o1[<span class="number">1</span>]));  <span class="comment">//频率降序排列</span></span><br><span class="line">        StringBuilder tmp = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">26</span>;i++)&#123;</span><br><span class="line">            <span class="keyword">int</span> count=arr[i][<span class="number">1</span>];</span><br><span class="line">            <span class="keyword">while</span>(count--&gt;<span class="number">0</span>)</span><br><span class="line">                tmp.append((<span class="keyword">char</span>)arr[i][<span class="number">0</span>]);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> max=arr[<span class="number">0</span>][<span class="number">1</span>];</span><br><span class="line">        StringBuilder []stb=<span class="keyword">new</span> StringBuilder[max]; </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;max;i++) stb[i]=<span class="keyword">new</span> StringBuilder(); <span class="comment">//这里要先 new 好了，否则会有 NULLpointerException     </span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> total=<span class="number">0</span>;total&lt;len;total++)&#123;</span><br><span class="line">            stb[total%max].append(tmp.charAt(total));</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        StringBuilder res=<span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;max;i++)</span><br><span class="line">            res.append(stb[i]);</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len-<span class="number">1</span>;i++)    <span class="comment">//检查是否有重复字符串</span></span><br><span class="line">            <span class="keyword">if</span>(res.charAt(i)==res.charAt(i+<span class="number">1</span>))</span><br><span class="line">                <span class="keyword">return</span> <span class="string">""</span>;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> res.toString();                </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="SQL练习-4"><a href="#SQL练习-4" class="headerlink" title="SQL练习"></a>SQL练习</h2><h3 id="not-in"><a href="#not-in" class="headerlink" title="not in"></a>not in</h3><p>查找非 manager 的员工 id， 使用not  in</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> </span><br><span class="line">E.emp_no</span><br><span class="line"><span class="keyword">FROM</span></span><br><span class="line">employees E  </span><br><span class="line"><span class="keyword">WHERE</span></span><br><span class="line">E.emp_no </span><br><span class="line"><span class="keyword">NOT</span> <span class="keyword">IN</span></span><br><span class="line">(<span class="keyword">SELECT</span> dept_manager.emp_no <span class="keyword">FRom</span> dept_manager) ;</span><br></pre></td></tr></table></figure>

<h3 id="group-by"><a href="#group-by" class="headerlink" title="group by"></a>group by</h3><p>获取所有部门中当前(dept_emp.to_date = ‘9999-01-01’)员工当前(salaries.to_date=’9999-01-01’)薪水最高的相关信息，给出dept_no, emp_no以及其对应的salary，<strong>按照部门升序排列</strong>。</p>
<p>按照部门分类的，要使用到 group by</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> </span><br><span class="line">e.dept_no,e.emp_no,<span class="keyword">MAX</span>(s.salary)</span><br><span class="line"><span class="keyword">from</span> </span><br><span class="line">dept_emp e  </span><br><span class="line"><span class="keyword">join</span> </span><br><span class="line">salaries s</span><br><span class="line"><span class="keyword">on</span> </span><br><span class="line">e.emp_no = s.emp_no</span><br><span class="line"><span class="keyword">where</span> </span><br><span class="line">s.to_date=<span class="string">'9999-01-01'</span> <span class="keyword">and</span> e.to_date = <span class="string">'9999-01-01'</span></span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span></span><br><span class="line">e.dept_no;   <span class="comment">-- 根据部门进行分类</span></span><br></pre></td></tr></table></figure>



<h3 id="HAVING"><a href="#HAVING" class="headerlink" title="HAVING"></a>HAVING</h3><p>where 在 group by前面， group by之后只能用 HAVING</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span></span><br><span class="line">title , <span class="keyword">COUNT</span>(emp_no)<span class="keyword">AS</span> t</span><br><span class="line"><span class="keyword">FROM</span> titles <span class="keyword">GROUP</span> <span class="keyword">BY</span> title</span><br><span class="line"><span class="keyword">HAVING</span>  t&gt;=<span class="number">2</span>;</span><br></pre></td></tr></table></figure>



<h3 id="奇偶操作"><a href="#奇偶操作" class="headerlink" title="奇偶操作"></a>奇偶操作</h3><p>同Java语法一样。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">emp_no%2=1  <span class="comment">--奇数</span></span><br><span class="line">emp_no%2=0  <span class="comment">--偶数</span></span><br></pre></td></tr></table></figure>

<h3 id="limit-操作"><a href="#limit-操作" class="headerlink" title="limit 操作"></a>limit 操作</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">limit 1,4  ;    <span class="comment">-- 从下标为1 开始取4个（从下标为0 开始计数）</span></span><br><span class="line">limit 4 offset 1; <span class="comment">-- 同上面等价</span></span><br></pre></td></tr></table></figure>



<h2 id="记录-8"><a href="#记录-8" class="headerlink" title="记录"></a>记录</h2>]]></content>
  </entry>
  <entry>
    <title>javaEE开篇</title>
    <url>/2020/08/06/javaEE%E5%BC%80%E7%AF%87/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>javaEE</category>
      </categories>
      <tags>
        <tag>javaEE</tag>
      </tags>
  </entry>
  <entry>
    <title>Java面向对象</title>
    <url>/2020/08/25/Java%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="所有类的超类-Object"><a href="#所有类的超类-Object" class="headerlink" title="所有类的超类-Object"></a>所有类的超类-Object</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>类 <code>Object</code> 是类层次结构的根类。每个类都使用 <code>Object</code>  作为超类。所有对象（包括数组）都实现这个类的方法。</p>
<a id="more"></a>

<h2 id="常用的方法"><a href="#常用的方法" class="headerlink" title="常用的方法"></a>常用的方法</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">native</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object obj)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">native</span> Object <span class="title">clone</span><span class="params">()</span> <span class="keyword">throws</span> CloneNotSupportedException</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> Class&lt;?&gt; <span class="title">getClass</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">finalize</span><span class="params">()</span> <span class="keyword">throws</span> Throwable </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> <span class="keyword">void</span> <span class="title">notify</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> <span class="keyword">void</span> <span class="title">notifyAll</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> <span class="keyword">void</span> <span class="title">wait</span><span class="params">(<span class="keyword">long</span> timeout)</span> <span class="keyword">throws</span> InterruptedException</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">wait</span><span class="params">(<span class="keyword">long</span> timeout, <span class="keyword">int</span> nanos)</span> <span class="keyword">throws</span> InterruptedException</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">wait</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException</span></span><br></pre></td></tr></table></figure>

<h3 id="hashCode"><a href="#hashCode" class="headerlink" title="hashCode()"></a>hashCode()</h3><ul>
<li>hashCode() 返回<strong>哈希值</strong>，而 equals() 是用来判断两个对象是否等价。</li>
<li>等价的两个对象散列值一定相同，但是散列值相同的两个对象不一定等价。</li>
<li><code>hashCode</code>和<code>equals</code>方法一同重写。在覆盖 equals() 方法时应当总是覆盖 hashCode() 方法，目的：<strong>保证等价的两个对象哈希值也相等</strong>。</li>
</ul>
<p>应用：<code>HashSet</code>  和<code>HashMap</code>等集合类使用了 <strong>hashCode()</strong>  方法来<strong>计算对象应该存储的位置</strong>，需要重写<code>hashCode</code>和<code>equals</code>方法。</p>
<ul>
<li>如果 类不重写 hashCode方法，那么其值 默认是 <strong>对象的地址值</strong>。</li>
<li><strong>String</strong>类<strong>重写</strong>了hashCode, 而 StringBUffer没有重写 hashCode方法。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Person chris = <span class="keyword">new</span> Person(<span class="string">"chris"</span>, <span class="number">21</span>,);</span><br><span class="line">Person chris1 = <span class="keyword">new</span> Person(<span class="string">"chris"</span>, <span class="number">21</span>);  <span class="comment">//在重写之后，chris和chris1等价</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Objects.hash(name, age);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<h3 id="equals"><a href="#equals" class="headerlink" title="equals()"></a>equals()</h3><ol>
<li><code>equals</code>方法五大特点：<ol>
<li>自反性   <code>x.equals(x);</code></li>
<li>对称性   (<code>a.equals(b)</code>则(<code>b.equals(a)</code>))</li>
<li>传递性    <code>if (x.equals(y) &amp;&amp; y.equals(z))</code>   则<code>x.equals(z);</code></li>
<li>一致性   不论调用多少次，<strong>结果一致</strong></li>
<li>空类型返回 false   对任何不是 null 的对象 x 调用 x.equals(null) 结果都为 false</li>
</ol>
</li>
<li><code>equals</code>标准步骤<ol>
<li>检查二者是否是<strong>同一对象</strong></li>
<li>检查参数对象<strong>是否为空</strong>， 是，则返回 false</li>
<li>检查二者是否是<strong>同一类</strong></li>
<li>将<code>Object</code>类进行<strong>转换</strong></li>
<li>检查对应的<strong>属性</strong>是否相同</li>
</ol>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span> == o) <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">if</span> (o == <span class="keyword">null</span> || getClass() != o.getClass()) <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    Person person = (Person) o;</span><br><span class="line">    <span class="keyword">return</span> Objects.equals(name, person.name) &amp;&amp;</span><br><span class="line">            Objects.equals(age, person.age);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意事项</strong>：<code>equals</code>方法 括号内的 参数必须是<code>Object</code>类型，否则就不是对  <code>Object</code>类方法的重载了。</p>
<ol start="3">
<li><strong>== 和 <code>equals()</code></strong></li>
</ol>
<ul>
<li>对于基本类型，== 判断两个<strong>值是否相等</strong> 。</li>
<li>对于引用类型，== 判断两个变量<strong>是否引用同一个对象</strong>，而 equals() 判断引用的对象<strong>是否等价</strong>。</li>
</ul>
<h3 id="clone"><a href="#clone" class="headerlink" title="clone()"></a>clone()</h3><ul>
<li>clone() 是 Object 的 <strong>protected</strong> 方法，它不是 public，若不显式去重写 clone()，其它类就<strong>不能直接去调用</strong>该类实例的 clone() 方法。</li>
<li>clone 方法 的类必须继承<code>Cloneable</code>接口</li>
</ul>
<ol>
<li>重写 clone方法</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ClonePrac</span> <span class="keyword">implements</span> <span class="title">Cloneable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> a;</span><br><span class="line">    <span class="keyword">private</span> String s;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> ClonePrac <span class="title">clone</span><span class="params">()</span> <span class="keyword">throws</span> CloneNotSupportedException </span>&#123;   <span class="comment">//这里有强制转换</span></span><br><span class="line">        <span class="keyword">return</span> (ClonePrac)<span class="keyword">super</span>.clone();  <span class="comment">//super.clone()返回 object类型，强制转换</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>深浅拷贝</li>
</ol>
<ul>
<li><p>浅拷贝就是创建一个新对象，新对象的<strong>属性</strong>和原来对象<strong>完全相同</strong>，对于非基本类型属性，仍指<strong>向原有属性所指向的对象的内存地址</strong>。</p>
<p>克隆操作<strong>默认就是浅拷贝</strong>。</p>
</li>
<li><p>深拷贝就是 创建一个新对象，属性中引用的<strong>其他对象也会被克隆</strong>，重新拷贝一份，不再指向原有对象地址。</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//深拷贝的clone实现</span></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> DeepCopy <span class="title">clone</span><span class="params">()</span> <span class="keyword">throws</span> CloneNotSupportedException </span>&#123;</span><br><span class="line">        DeepCopy res = (DeepCopy) <span class="keyword">super</span>.clone();</span><br><span class="line">        res.arr=<span class="keyword">new</span> <span class="keyword">int</span>[arr.length];</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;arr.length;i++)</span><br><span class="line">            res.arr[i]=<span class="keyword">this</span>.arr[i];</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<ol start="3">
<li>clone 来复制一个对象不仅复杂，且涉及<strong>强制类型转换</strong>，通常推荐使用 <strong>构造方法</strong> 来复制一个对象。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CloneConstructorExample</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] arr;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CloneConstructorExample</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">10</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">            arr[i] = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">CloneConstructorExample</span><span class="params">(CloneConstructorExample original)</span> </span>&#123;</span><br><span class="line">        arr = <span class="keyword">new</span> <span class="keyword">int</span>[original.arr.length];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; original.arr.length; i++) &#123;</span><br><span class="line">            arr[i] = original.arr[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="面向对象的特性"><a href="#面向对象的特性" class="headerlink" title="面向对象的特性"></a>面向对象的特性</h1><p>面向对象的<strong>目的</strong>：解决软件系统的<strong>可扩展性、可维护性、可重用性</strong>；</p>
<h2 id="（1）封装："><a href="#（1）封装：" class="headerlink" title="（1）封装："></a>（1）封装：</h2><p>通常认为封装是把<strong>数据</strong>和<strong>操作数据的方法</strong>绑定起来，对数据的访问<strong>只能通过已定义的接口</strong>。简单说，封装就是<strong>隐藏一切可隐藏的东西</strong>，只向外界提供最简单的<strong>编程接口</strong>。</p>
<h2 id="（2）继承："><a href="#（2）继承：" class="headerlink" title="（2）继承："></a>（2）继承：</h2><p>继承是<strong>从已有类得到继承信息创建新类</strong>的过程。提供继承信息的类被称为父类（超类、基类）；得到继承信息的类被称为子类（派生类）。继承让变化中的软件系统有了一定的延续性。</p>
<h2 id="（3）多态："><a href="#（3）多态：" class="headerlink" title="（3）多态："></a>（3）多态：</h2><p>多态性是指允许<strong>不同子类型</strong>的对象<strong>对同一消息作出不同的响应</strong>。</p>
<p><strong>方法重载</strong>（overload）实现的是<strong>编译时的多态性</strong>（也称为前绑定）</p>
<p><strong>方法重写</strong>（override）实现的是<strong>运行时的多态性</strong>（也称为后绑定）。</p>
<p><strong>运行时的多态是面向对象最精髓的东西</strong>，含义：定义的<strong>引用变量所指向的具体类型</strong>和<strong>通过该引用变量发出的方法调用</strong>在编译时并不确定，而是在<strong>程序运行期间</strong>才确定。不用修改源代码，就可以让<strong>引用变量</strong>绑定到<strong>各种不同的对象上</strong>。</p>
<p>要实现多态需要做两件事：</p>
<ul>
<li><p>第一：方法重写（子类继承父类并重写父类中已有的或抽象的方法）；</p>
</li>
<li><p>第二：对象造型（用<strong>父类型</strong>引用<strong>指向子类型</strong>对象，这样同样的引用调用同样的方法就会<strong>根据子类对象的不同</strong>而表现出不同的行为）。</p>
</li>
</ul>
<h1 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h1><h2 id="继承权限"><a href="#继承权限" class="headerlink" title="继承权限"></a>继承权限</h2><ol>
<li>三个关键字 ：<code>public</code> <code>protected</code> <code>private</code></li>
<li>访问权限对比表格</li>
</ol>
<p><img src="https://uploadfiles.nowcoder.com/files/20170518/6589111_1495116648005_163162-20160121105831078-570590712.png" alt=""></p>
<h2 id="抽象类和接口"><a href="#抽象类和接口" class="headerlink" title="抽象类和接口"></a>抽象类和接口</h2><ol>
<li>概念、使用实例</li>
</ol>
<ul>
<li>抽象类：类中有未实现的方法，类需要用<code>abstract</code>修饰，抽象方法也要用<code>abstract</code>修饰，不可直接实例化对象，被继承后需要实现抽象方法（若不实现，则仍然为抽象类）。 实现类用<code>extends</code>继承抽象类。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractClassExample</span> </span>&#123;  <span class="comment">//abstract在class前面</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">int</span> x;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> y;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">func1</span><span class="params">()</span></span>;    <span class="comment">//抽象方法 </span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">func2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"func2"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractExtendClassExample</span> <span class="keyword">extends</span> <span class="title">AbstractClassExample</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">func1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"func1"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<ul>
<li>接口：使用interface修饰。实现类 使用 <code>implements</code>继承。<ul>
<li>可以有 <strong>常量</strong> 、 <strong>抽象方法</strong>、 <strong>默认方法、 静态方法</strong> ， 不可以有 <strong>实例域</strong>。</li>
<li>方法默认<code>public</code>属性，  常量默认 为<code>public static final</code></li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">InterfaceExample</span> </span>&#123;</span><br><span class="line">   </span><br><span class="line">     <span class="keyword">int</span> a=<span class="number">10</span>;    </span><br><span class="line">     <span class="function"><span class="keyword">void</span> <span class="title">func1</span><span class="params">()</span></span>; </span><br><span class="line">     <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">print1</span><span class="params">()</span></span>&#123;  System.out.println(<span class="string">"hello world"</span>); &#125;</span><br><span class="line">     <span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">name</span><span class="params">()</span></span>&#123; System.out.println(<span class="string">"name"</span>);&#125;  </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">InterfaceImplementExample</span> <span class="keyword">implements</span> <span class="title">InterfaceExample</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">func1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"func1"</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>从 <strong>Java 8</strong> 开始，接口也可以拥有默认的方法实现。在 Java 8 之前，<strong>如果一个接口想要添加新的方法，那么要修改所有实现了该接口的类</strong>，让它们都实现新增的方法。</p>
<ol start="2">
<li><strong><code>abstract</code>类和<code>interface</code>对比</strong><ol>
<li>Java 8之前,  <strong>implementations</strong>: Java interface are implicitly abstract and cannot have implementations. A Java abstract class can have instance methods that implements a default behavior.     Java 8及之后，interface中可以有default和public static方法</li>
<li><strong>Variables</strong> declared in a Java interface is by default <code>public static final</code>. An abstract class may contain <strong>non-final variables</strong>.</li>
<li><strong>Methods</strong> of a Java interface are <strong>public</strong> by default. A Java abstract class can have the usual flavors of class members like <strong>private, protected,</strong> etc..</li>
<li>Java interface should be implemented using <strong>keyword</strong> “implements”; A Java abstract class should be extended using <strong>keyword</strong> “extends”.</li>
<li>An interface can <strong>extend another Java interface only</strong>, an abstract class can extend <strong>another Java class</strong> and implement <strong>multiple Java interfaces</strong>.</li>
<li>A Java class can implement multiple interfaces but it can extend only one abstract class.</li>
<li>In comparison with java abstract classes, <strong>java interfaces are slow</strong> as it requires extra indirection.</li>
</ol>
</li>
</ol>
<ul>
<li><strong><em>Java8后引入default和 static final的目的：</em></strong></li>
</ul>
<p>The reason we have default methods in interfaces is to allow the developers to add new methods to the interfaces without affecting the classes that implements these interfaces.</p>
<p>在更改interface的时候，可以<strong>不影响已经实现的实现类</strong>。</p>
<table>
<thead>
<tr>
<th></th>
<th align="left">interface</th>
<th align="left">abstract class</th>
</tr>
</thead>
<tbody><tr>
<td>1 是否有<strong>实现方法</strong></td>
<td align="left">Java8之前不可以，之后可以有<code>static</code> 和<code>default</code>的方法</td>
<td align="left">可以有</td>
</tr>
<tr>
<td><strong>2 变量</strong>特性</td>
<td align="left">变量必须是 public static final（默认）</td>
<td align="left">可以有 non-final</td>
</tr>
<tr>
<td><strong>3 方法</strong>特性</td>
<td align="left">所有方法都是 <code>public</code>（默认）</td>
<td align="left">可以有 protect public private</td>
</tr>
<tr>
<td>4 关键字</td>
<td align="left">实现类用 implements</td>
<td align="left">用 extends</td>
</tr>
<tr>
<td>5</td>
<td align="left">关键字 interface</td>
<td align="left">在类名前面 加 abstract</td>
</tr>
<tr>
<td>6 interface和abstract class继承</td>
<td align="left">interface只能继承一个interface</td>
<td align="left">可以继承 另外一个class和多个interface                                                             public abstract class AbStract1  extends Employee  implements test,test1</td>
</tr>
<tr>
<td>7 实现类</td>
<td align="left">实现类可以implemets多个interface</td>
<td align="left">实现类只可以implements 一个 abstract class</td>
</tr>
<tr>
<td>8 速度</td>
<td align="left">较慢</td>
<td align="left">较快</td>
</tr>
</tbody></table>
<h2 id="super"><a href="#super" class="headerlink" title="super"></a>super</h2><ul>
<li>访问父类的构造函数：可以使用 super() 函数访问父类的构造函数.</li>
<li>访问父类的成员：如果子类重写了父类的某个方法，可以通过使用 super 关键字来引用父类的方法实现。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Father</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"this is father"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    Father(<span class="keyword">int</span> age,String name)&#123;<span class="keyword">this</span>.age=age; <span class="keyword">this</span>.name=name;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Child</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> len;</span><br><span class="line">    Child(<span class="keyword">int</span> age,String name,<span class="keyword">int</span> len)&#123;</span><br><span class="line">        <span class="keyword">super</span>(age,name);     <span class="comment">//调用父类构造函数</span></span><br><span class="line">        <span class="keyword">this</span>.len=len;</span><br><span class="line">    &#125;</span><br><span class="line">     <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span></span>&#123;    <span class="comment">//重写父类 print 方法</span></span><br><span class="line">        System.out.println(<span class="string">"this is child"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">printfather</span><span class="params">()</span></span>&#123;   </span><br><span class="line">        <span class="keyword">super</span>.print();    <span class="comment">//调用父类的 print方法，而非子类的</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> Father f=<span class="keyword">new</span> Child();</span><br><span class="line">        f.print();</span><br><span class="line">        Child child = (Child)f;</span><br><span class="line">        child.printfather();</span><br><span class="line">输出：</span><br><span class="line">    <span class="keyword">this</span> is child</span><br><span class="line">	<span class="keyword">this</span> is father</span><br></pre></td></tr></table></figure>

<h2 id="this"><a href="#this" class="headerlink" title="this"></a>this</h2><ol>
<li>指示自身类的变量</li>
<li>调用自身构造函数</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Employee</span> <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">Employee</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> nextId;</span><br><span class="line">    <span class="keyword">private</span> String name = <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        Random random = <span class="keyword">new</span> Random();</span><br><span class="line">        nextId = random.nextInt(<span class="number">1000</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    &#123;</span><br><span class="line">        id = nextId;</span><br><span class="line">        nextId++;      <span class="comment">//每次实例化对象的时候，nextid是递增的</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Employee</span><span class="params">(String n,<span class="keyword">double</span> s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name=n;</span><br><span class="line">    <span class="keyword">this</span>.salary=s;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Employee</span><span class="params">(<span class="keyword">double</span> s)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(<span class="string">"#Employee"</span>+nextId,s);    <span class="comment">//***************调用上面的构造器</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="重写与重载"><a href="#重写与重载" class="headerlink" title="重写与重载"></a>重写与重载</h2><h3 id="1-重写（Override）"><a href="#1-重写（Override）" class="headerlink" title="1. 重写（Override）"></a><strong>1. 重写（Override）</strong></h3><p>存在于<strong>继承体系</strong>中，指<strong>子类实现了一个与父类在方法声明上完全相同的一个方法</strong>。</p>
<p>重写有以下三个限制：</p>
<ul>
<li>子类方法的<strong>访问权限</strong>必须大于等于父类方法；</li>
<li>子类方法的<strong>返回类型</strong>必须是父类方法返回类型或为其子类型。</li>
<li>子类方法抛出的<strong>异常类型</strong>必须是<strong>父类抛出异常类型或为其子类型</strong>。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SuperClass</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">protected</span> List&lt;Integer&gt; <span class="title">func</span><span class="params">()</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SubClass</span> <span class="keyword">extends</span> <span class="title">SuperClass</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ArrayList&lt;Integer&gt; <span class="title">func</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在调用一个方法时，先从本类中查找看是否有对应的方法，如果没有再到父类中查看，看是否从父类继承来。否则就要对参数进行转型，转成父类之后看是否有对应的方法。总的来说，方法调用的优先级为：</p>
<ul>
<li>this.func(this)</li>
<li>super.func(this)</li>
<li>this.func(super)</li>
<li>super.func(super)</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestInheritance</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">(A obj)</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"A.show(A)"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">(C obj)</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"A.show(C)"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">(A obj)</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"B.show(A)"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">C</span> <span class="keyword">extends</span> <span class="title">B</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">D</span> <span class="keyword">extends</span> <span class="title">C</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">  TestInheritance testInheritance = <span class="keyword">new</span> TestInheritance();</span><br><span class="line">        TestInheritance.A a = testInheritance.<span class="keyword">new</span> A();</span><br><span class="line">        TestInheritance.B b = testInheritance.<span class="keyword">new</span> B();</span><br><span class="line">        TestInheritance.C c = testInheritance.<span class="keyword">new</span> C();</span><br><span class="line">        TestInheritance.D d = testInheritance.<span class="keyword">new</span> D();</span><br><span class="line">        a.show(a);       <span class="comment">// A.show(A)</span></span><br><span class="line">    </span><br><span class="line">        <span class="comment">// 在 A 中不存在 show(B obj)，将 B 转型成其父类 A</span></span><br><span class="line">        a.show(b); <span class="comment">// A.show(A)</span></span><br><span class="line">    </span><br><span class="line">        <span class="comment">// 在 B 中存在从 A 继承来的 show(C obj)，直接调用</span></span><br><span class="line">        b.show(c); <span class="comment">// A.show(C)</span></span><br><span class="line">    </span><br><span class="line">        <span class="comment">// 在 B 中不存在 show(D obj)，但是存在从 A 继承来的 show(C obj)，将 D 转型成其父类 C</span></span><br><span class="line">        b.show(d); <span class="comment">// A.show(C)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-重载（Overload）"><a href="#2-重载（Overload）" class="headerlink" title="2. 重载（Overload）"></a><strong>2. 重载（Overload）</strong></h3><p>同一个类中，一个方法与已经存在的方法<strong>名称</strong>上相同，但是 <strong>参数类型、个数、顺序</strong>至少有一个不同。</p>
<p>注意：返回值不可作为重载依据。</p>
]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java内部类</title>
    <url>/2020/09/14/java%E5%86%85%E9%83%A8%E7%B1%BB/</url>
    <content><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>定义在<strong>类内部的类</strong>就被称为 <strong>内部类</strong>。根据定义的方式不同，内部类分为 <strong>静态内部类，成员内部类，局部内部类，匿名内部类</strong> 四种。</p>
<a id="more"></a>

<h1 id="静态内部类"><a href="#静态内部类" class="headerlink" title="静态内部类"></a>静态内部类</h1><p>实例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticInner</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> d; </span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">inner</span></span>&#123;    <span class="comment">//class前面 有static 关键字</span></span><br><span class="line">         <span class="keyword">int</span> c;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">inner</span><span class="params">(<span class="keyword">int</span> c)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.c = c;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span></span>&#123;</span><br><span class="line">            System.out.println(c);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>申请方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">StaticInner.inner sti =  <span class="keyword">new</span> StaticInner.inner(<span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<ol>
<li><p>静态内部类可以访问 <strong>外部类所有的静态变量和方法</strong>，即使是 private 的也一样。</p>
</li>
<li><p>静态内部类和<strong>一般类一致</strong>，可以定义静态变量、方法，构造方法等。</p>
</li>
<li><p>其它类使用静态内部类需要使用“<strong>外部类.静态内部类</strong>”方式，如下所示：Out.Inner inner = </p>
</li>
</ol>
<p>new Out.Inner();inner.print();</p>
<h1 id="成员内部类"><a href="#成员内部类" class="headerlink" title="成员内部类"></a>成员内部类</h1><p>定义在类内部的非静态类，就是成员内部类。成员内部类<strong>不能定义静态方法和变量</strong>（<strong>final 修饰的除外</strong>）。</p>
<p>这是因为成员内部类是<strong>非静态的</strong>，类初始化的时候<strong>先初始化静态成员</strong>，如果允许成员内部类定义静态变量，那么成员内部类的静态变量初始化顺序是<strong>有歧义</strong>的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ParterInner</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> a=<span class="number">10</span>;</span><br><span class="line">    <span class="keyword">private</span> String s;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">inner</span></span>&#123;</span><br><span class="line">        <span class="keyword">private</span>  <span class="keyword">int</span> b;</span><br><span class="line">         <span class="keyword">int</span> c;</span><br><span class="line">         <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> d=<span class="number">10</span>;   <span class="comment">//不可 有 static ，但是加上final 就可以了</span></span><br><span class="line">        <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">()</span></span>&#123;</span><br><span class="line">            System.out.println(a+d);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>申请：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ParterInner.inner inner1 = <span class="keyword">new</span> ParterInner().<span class="keyword">new</span> inner();    <span class="comment">//两个都要new</span></span><br><span class="line">inner1.print();</span><br></pre></td></tr></table></figure>



<h1 id="局部内部类"><a href="#局部内部类" class="headerlink" title="局部内部类"></a>局部内部类</h1><p>定义在方法中的类，就是局部类。如果<strong>一个类</strong>只在<strong>某个方法中</strong>使用，则可以考虑使用局部类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JuBuNeibu</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span>  <span class="keyword">void</span> <span class="title">test</span><span class="params">( <span class="keyword">final</span> <span class="keyword">int</span> c)</span></span>&#123;</span><br><span class="line">         <span class="class"><span class="keyword">class</span> <span class="title">Inner</span></span>&#123;</span><br><span class="line">            <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">()</span></span>&#123;</span><br><span class="line">                System.out.println(<span class="string">"c="</span>+c);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        Inner inner = <span class="keyword">new</span> Inner();</span><br><span class="line">        inner.print();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>申请</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">JuBuNeibu jbn = <span class="keyword">new</span> JuBuNeibu();</span><br><span class="line">jbn.test(<span class="number">4</span>);       </span><br><span class="line"><span class="comment">//输出 4</span></span><br></pre></td></tr></table></figure>



<h1 id="匿名内部类"><a href="#匿名内部类" class="headerlink" title="匿名内部类"></a>匿名内部类</h1><p>匿名内部类我们必须要继承<strong>一个父类或者实现一个接口</strong>。同时它也是没有 class 关键字，这是因为 <strong>匿名内部类是直接使用 new 来生成一个对象</strong>的引用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestNonNameInner</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Test test = <span class="keyword">new</span> Test();</span><br><span class="line">        test.test(<span class="keyword">new</span> Bird() &#123;     <span class="comment">//这里就是调用匿名内部类的地方</span></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">fly</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">3000</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="string">"鸿鹄"</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">(Bird bird)</span></span>&#123;</span><br><span class="line">        System.out.println(bird.getName()+<span class="string">"能飞"</span>+(bird.fly())+<span class="string">"米"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Bird</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">int</span> <span class="title">fly</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//鸿鹄能飞3000米</span></span><br></pre></td></tr></table></figure>

<h1 id="内部类的使用场景和好处"><a href="#内部类的使用场景和好处" class="headerlink" title="内部类的使用场景和好处"></a>内部类的使用场景和好处</h1><ol>
<li><p><strong>每个内部类都能独立的继承一个接口的实现</strong>，所以无论外部类是否已经继承了某个(接口的)实现，<strong>对于内部类都没有影响</strong>。内部类使得<strong>多继承的解决方案</strong>变得完整，</p>
</li>
<li><p>方便将存在一<strong>定逻辑关系的类组织在一起</strong>，又可以对外界隐藏。</p>
</li>
<li><p>方便编写事件驱动程序</p>
</li>
<li><p>方便编写线程代码</p>
</li>
</ol>
]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java IO</title>
    <url>/2020/09/03/java-IO/</url>
    <content><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>Java IO 是一套Java用来读写数据（输入和输出）的API。 </p>
<a id="more"></a>

<p>Java IO中的一个<strong>核心概念</strong>是<strong>流</strong>（Stream），从外部（包括磁盘文件、键盘、网络套接字）读入到内存中的<strong>数据序列</strong>称为<strong>输入流</strong>，从内存写入到外部设备的<strong>数据序列</strong>称为<strong>输出流</strong>。</p>
<p>按照流传输的单位，Java中的流分为两种：</p>
<ul>
<li><strong>字节流</strong>： 最小单元是<strong>字节</strong>，多用于 <strong>二进制数据</strong>。</li>
<li><strong>字符流</strong>：数据流中最小的数据单元是<strong>字符</strong>。</li>
</ul>
<h1 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h1><h2 id="File类"><a href="#File类" class="headerlink" title="File类"></a>File类</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">常用构造：</span><br><span class="line">File(String pathname) </span><br><span class="line">          通过将给定路径名字符串转换为抽象路径名来创建一个新 File 实例。 </span><br><span class="line">File(String parent, String child) </span><br><span class="line">          根据 parent 路径名字符串和 child 路径名字符串创建一个新 File 实例。 </span><br><span class="line">常用方法：</span><br><span class="line">    file.isDirectory();   <span class="comment">//是否是 路径</span></span><br><span class="line">    file.isFile();		 <span class="comment">//是否是 文件</span></span><br><span class="line">    file.canExecute());  <span class="comment">//是否可运行</span></span><br><span class="line">    file.canRead();     </span><br><span class="line">    file.canWrite();</span><br><span class="line">    file.getAbsolutePath();     <span class="comment">//全路径</span></span><br><span class="line"></span><br><span class="line">    String name = file.getName(); <span class="comment">//文件的名字（不包含路径）  </span></span><br><span class="line">    File[] files = file.listFiles(); <span class="comment">//file 下一级 包含的文件夹</span></span><br></pre></td></tr></table></figure>

<h2 id="输入输出"><a href="#输入输出" class="headerlink" title="输入输出"></a>输入输出</h2><h3 id="字节输入输出"><a href="#字节输入输出" class="headerlink" title="字节输入输出"></a>字节输入输出</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="string">"D:\\temp"</span>);</span><br><span class="line">FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(<span class="string">"D:\\tmp"</span>);</span><br><span class="line">对应的方法:</span><br><span class="line">fis.read(b);</span><br><span class="line">fos.write(b,<span class="number">0</span>,len);</span><br></pre></td></tr></table></figure>

<p>该方法有异常，需要处理，因为路径、文件可能不存在，这些异常是可以预见的，属于<strong>非运行时异常</strong>，所以必须处理。</p>
<p>例：<strong>实现文件复制</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CopyFilePrac</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileInputStream fis=<span class="keyword">new</span> FileInputStream(<span class="string">"..略"</span>);</span><br><span class="line">        FileOutputStream fos=<span class="keyword">new</span> FileOutputStream(<span class="string">"..略"</span>);</span><br><span class="line">        <span class="keyword">int</span> res=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">byte</span> []b=<span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];         <span class="comment">//设置byte数组，实现缓冲作用</span></span><br><span class="line">        <span class="keyword">while</span>((res=fis.read(b))!=-<span class="number">1</span>)&#123;    <span class="comment">//若到达结尾，返回-1</span></span><br><span class="line">            fos.write(b,<span class="number">0</span>,res);          <span class="comment">//指定 范围 0,res</span></span><br><span class="line">        &#125;</span><br><span class="line">        fos.close();</span><br><span class="line">        fis.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//改进：若要进一步增加缓存的功能，减少磁盘的读写次数，可使用BufferedOutputStream和 BufferedInputStream</span></span><br><span class="line"><span class="comment">//注意 构造方法  参数是 InputStream 或者 OutputStream</span></span><br><span class="line">BufferedOutputStream bos=<span class="keyword">new</span> BufferedOutputStream(<span class="keyword">new</span> FileOutputStream(<span class="string">"..."</span>));  </span><br><span class="line">BufferedInputStream bis = <span class="keyword">new</span> BufferedInputStream(<span class="keyword">new</span> FileInputStream (<span class="string">"..."</span>));</span><br></pre></td></tr></table></figure>

<h3 id="字符输入输出"><a href="#字符输入输出" class="headerlink" title="字符输入输出"></a>字符输入输出</h3><ul>
<li>与字节输入输出相对应，有</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">FileReader fr = <span class="keyword">new</span> FileReader(FilePath);</span><br><span class="line">FileWriter fw = <span class="keyword">new</span> FileWriter(FilePath);</span><br><span class="line">对应的方法：</span><br><span class="line"><span class="keyword">char</span> []c;</span><br><span class="line">fr.read(c);</span><br><span class="line">fw.writeLine(c,<span class="number">0</span>,len);</span><br></pre></td></tr></table></figure>

<ul>
<li>对应的缓存类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> BufferedReader bufr = <span class="keyword">new</span> BufferedReader(fr);</span><br><span class="line"> BufferedWriter bufw = <span class="keyword">new</span> BufferedWriter(fr);</span><br><span class="line"> bufr.readLine();</span><br><span class="line"> bufw.writeLine();</span><br><span class="line"></span><br><span class="line"><span class="comment">//数据从主存写会磁盘</span></span><br><span class="line"> bufr.flush();</span><br><span class="line"> bufw.flush();</span><br></pre></td></tr></table></figure>

<p>所谓的字符，就是由一个或多个字节按照一定规则组成的。  <strong>字符转换为字节就是编码，字节转换为字符就是解码。</strong></p>
<ul>
<li>常见编码所占大小</li>
</ul>
<table>
<thead>
<tr>
<th>编码类型</th>
<th>中文</th>
<th>英文</th>
</tr>
</thead>
<tbody><tr>
<td>GBK</td>
<td>2 Byte</td>
<td>1 Byte</td>
</tr>
<tr>
<td>UTF-8</td>
<td>3 Byte</td>
<td>2 Byte</td>
</tr>
<tr>
<td>UTF-16be(big endian)</td>
<td>2 Byte</td>
<td>2 Byte</td>
</tr>
</tbody></table>
<h3 id="序列化"><a href="#序列化" class="headerlink" title="序列化"></a>序列化</h3><p>IO 对象序列化： 将一个对象转换为<strong>字节序列</strong>，以<strong>二进制</strong>的形式<strong>保存在硬盘</strong>上, 方便运输和传输。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ObjectOutputStream(<span class="keyword">new</span> FileOutputStream(...));   <span class="comment">//序列化</span></span><br><span class="line">ObjectInputStream(<span class="keyword">new</span> FileInputStream(...));     <span class="comment">//反序列化</span></span><br><span class="line"><span class="comment">//主要方法：</span></span><br><span class="line">objectOutputStream.writeObject(person);</span><br><span class="line">objectInputStream.readObject();</span><br></pre></td></tr></table></figure>

<ul>
<li>实例：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException, ClassNotFoundException </span>&#123;</span><br><span class="line">        String  ObjectFile = <span class="string">"C:\\person1"</span>;        <span class="comment">//序列化输出路径</span></span><br><span class="line">        Person person = <span class="keyword">new</span> Person(<span class="number">13</span>, <span class="string">"achris"</span>);  <span class="comment">//创建对象</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//序列化</span></span><br><span class="line">        ObjectOutputStream objectOutputStream = <span class="keyword">new</span> ObjectOutputStream(<span class="keyword">new</span> FileOutputStream(ObjectFile));</span><br><span class="line">        objectOutputStream.writeObject(person);</span><br><span class="line">        objectOutputStream.close();</span><br><span class="line"></span><br><span class="line">        ObjectInputStream objectInputStream = <span class="keyword">new</span> ObjectInputStream(<span class="keyword">new</span> FileInputStream(ObjectFile));</span><br><span class="line">        Person o =(Person) objectInputStream.readObject();   <span class="comment">//需要手动强制转换</span></span><br><span class="line">        System.out.println(o);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> <span class="keyword">implements</span> <span class="title">Serializable</span></span>&#123;...&#125;    <span class="comment">//相应的对象需要继承 Serializable接口</span></span><br></pre></td></tr></table></figure>

<p>序列话的类需要继承<code>Serializable</code>接口， 只是用于<strong>标识可序列化的语义</strong>。实现了Serializable接口的类可以被</p>
<p><code>ObjectOutputStream</code>转换为<strong>字节流</strong>，同时也可以通过<code>ObjectInputStream</code>再将其解析为对象。</p>
<ul>
<li>序列化针对的是<strong>对象</strong>，属于类的特性（static 属性等） 不会被序列化。</li>
<li>有关键字<strong><code>transient</code>的属性</strong> 将不会被序列化。transient关键字<strong>只能</strong>修饰<strong>变量</strong>，而<strong>不能修饰方法和类</strong>.</li>
</ul>
<h3 id="网络socket"><a href="#网络socket" class="headerlink" title="网络socket"></a>网络socket</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//服务端  </span></span><br><span class="line">ServerSocket ss = <span class="keyword">new</span> ServerSocket(<span class="number">8000</span>);  </span><br><span class="line">   Socket server = ss.accept();</span><br><span class="line"><span class="comment">//客户端</span></span><br><span class="line">Socket client=<span class="keyword">new</span> Socket(<span class="string">"localhost"</span>,<span class="number">8000</span>);</span><br></pre></td></tr></table></figure>



<h3 id="NIO"><a href="#NIO" class="headerlink" title="NIO"></a>NIO</h3><p>NIO是引入的 <strong>高速的</strong>、 <strong>面向块</strong>的IO，弥补普通IO的不足（IO速度慢，耗费大）。</p>
<ul>
<li><p>I/O 与 NIO 最重要的区别是数据<strong>打包和传输</strong>的方式，<strong>I/O 以流的方式</strong>处理数据，而 <strong>NIO 以块的方式</strong>处理数据。<strong>I/O是阻塞</strong>的，<strong>NIO非阻塞</strong>。</p>
</li>
<li><p>面向流的 I/O 一次处理<strong>一个字节数据</strong>：一个输入流<strong>产生</strong>一个字节数据，一个输出流<strong>消费</strong>一个字节数据。</p>
<ul>
<li>优点： 为流式数据创建<strong>过滤器</strong>非常容易，链接几个过滤器，以便每个过滤器只负责复杂处理机制的一部分。</li>
<li>缺点：<strong>面向流的 I/O通常相当慢。</strong></li>
</ul>
</li>
<li><p>NIO 利用Channel 复制文件实例</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NIOPrac</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileChannel fcin = <span class="keyword">new</span> FileInputStream(<span class="string">"C:\\3.txt"</span>).getChannel();</span><br><span class="line">        FileChannel fcout = <span class="keyword">new</span> FileOutputStream(<span class="string">"C:\\4.txt"</span>).getChannel();</span><br><span class="line"></span><br><span class="line">        ByteBuffer buffer = ByteBuffer.allocate(<span class="number">1024</span>);  <span class="comment">//缓冲区设置</span></span><br><span class="line">        <span class="keyword">int</span> len;</span><br><span class="line">        <span class="keyword">while</span>((len=fcin.read(buffer))!=-<span class="number">1</span>)&#123;</span><br><span class="line">            buffer.flip();           <span class="comment">//切换读写</span></span><br><span class="line">            fcout.write(buffer);</span><br><span class="line">            buffer.clear();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>   关于：<code>buffer.flip()</code>的说明： 该操作是 进行<strong>读写切换</strong>（先读后写），整个缓冲区的变化：</p>
<p>① 新建一个大小为 8 个字节的缓冲区，此时 position 为 0，而 limit = capacity = 8。</p>
<p><img src="https://s1.ax1x.com/2020/08/24/drRMTO.png" alt="drRMTO.png"></p>
<p>② 从输入通道中读取 5 个字节数据写入缓冲区中，此时 position 为 5，limit 保持不变。</p>
<p><img src="https://s1.ax1x.com/2020/08/24/drRspj.png" alt="drRspj.png"></p>
<p>③ 在将缓冲区的数据写到输出通道之前，需要先调用 flip() 方法，这个方法将 limit 设置为当前 position，并将 position 设置为 0。</p>
<img src="https://s1.ax1x.com/2020/09/03/wiMTUK.png" alt="wiMTUK.png" border="0" />



<p>④ 从缓冲区中取 4 个字节到输出缓冲中，此时 position 设为 4。</p>
<p><img src="https://s1.ax1x.com/2020/08/24/drRT39.png" alt=""></p>
<p>⑤ 最后需要调用 clear() 方法来清空缓冲区，此时 position 和 limit 都被设置为最初位置。</p>
<p><img src="https://s1.ax1x.com/2020/08/24/drWavR.png" alt=""></p>
]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java并发锁详情，volatile和cache line</title>
    <url>/2020/09/20/java%E5%B9%B6%E5%8F%91%E4%B9%8B%E9%94%81/</url>
    <content><![CDATA[<h2 id="CAS底层"><a href="#CAS底层" class="headerlink" title="CAS底层"></a>CAS底层</h2><p>使用的是<code>lock comxchg</code> 指令，直接有硬件支持。 </p>
<p>但是<code>comxchg</code>指令没有原子性，也就是说在CAS操作 比较旧值和新值之后，写入之前 是可能被其他线程 改变值的，故不能保证原子性。</p>
<p>保证原子性的是前面的<code>lock</code>指令。</p>
<a id="more"></a>



<h2 id="Object大小"><a href="#Object大小" class="headerlink" title="Object大小"></a>Object大小</h2><p><code>Object o = new Object()</code>对象的大小 为 <strong>16个字节</strong>。</p>
<p>对象的内存分布图：</p>
<p><img src="https://s1.ax1x.com/2020/09/15/wyadun.png" alt="wyadun.png"></p>
<ul>
<li><p>markword： 始终是8个字节。  记录着 <strong>GC信息 、锁信息等</strong>。</p>
</li>
<li><p>classpointer： 开启压缩时：4字节（默认开启压缩） ， 否则 8字节</p>
</li>
<li><p>实例数据： 视具体情况而定</p>
</li>
<li><p>对齐： 为了提高访问速度，使得整体的字节大小 为 8 的整数倍，进行补齐。</p>
</li>
</ul>
<h2 id="sychronized加锁的含义："><a href="#sychronized加锁的含义：" class="headerlink" title="sychronized加锁的含义："></a>sychronized加锁的含义：</h2><p> 执行中断代码的时候，锁定这个对象，也就是说，执行被加锁的代码的时候，锁定了对象，注意！ 代码不能加锁， 加锁的是对象。</p>
<p>关于synchronized的信息是 保存在对象 的 markWord中的。</p>
<h2 id="锁的升级过程"><a href="#锁的升级过程" class="headerlink" title="锁的升级过程:"></a>锁的升级过程:</h2><p> <strong>偏向锁-&gt;轻量级锁（自旋锁）-&gt;重量级锁</strong></p>
<h2 id="markword的详细信息："><a href="#markword的详细信息：" class="headerlink" title="markword的详细信息："></a><strong>markword的详细信息</strong>：</h2><p>hashCode（）只有<strong>调用后才会有值，</strong>但是 不调用就不会有信息。</p>
<p><img src="https://s1.ax1x.com/2020/09/15/wyd4Mj.png" alt="wyd4Mj.png"></p>
<h2 id="synchronized最底层"><a href="#synchronized最底层" class="headerlink" title="synchronized最底层"></a>synchronized最底层</h2><p>就是   <code>lock comxchg .....指令</code>。</p>
<h2 id="高速缓存和-cache-line"><a href="#高速缓存和-cache-line" class="headerlink" title="高速缓存和 cache line"></a>高速缓存和 cache line</h2><p><img src="https://s1.ax1x.com/2020/09/16/wg0DYt.png" alt="wg0DYt.png"></p>
<h3 id="cache-line的概念"><a href="#cache-line的概念" class="headerlink" title="cache line的概念"></a><a href="https://www.jianshu.com/p/e338b550850f" target="_blank" rel="noopener">cache line的概念</a></h3><h3 id="计算机的组成"><a href="#计算机的组成" class="headerlink" title="计算机的组成"></a>计算机的组成</h3><p><img src="https://s1.ax1x.com/2020/09/16/wg0NOe.png" alt="wg0NOe.png"></p>
<h3 id="CPU的核心组成"><a href="#CPU的核心组成" class="headerlink" title="CPU的核心组成"></a>CPU的核心组成</h3><p><img src="https://s1.ax1x.com/2020/09/16/wg0ySf.png" alt="wg0ySf.png"></p>
<p>一个CPU最核心的部分就是 ALU（逻辑运算单元）+Register（寄存器）+ PC（程序计数器）</p>
<h3 id="超线程："><a href="#超线程：" class="headerlink" title="超线程："></a><strong>超线程</strong>：</h3><p>一个ALU + 两组Registers + 两组 PC</p>
<p>ALU的执行可以在两组Register之间切换，所以会有四核八线程，八核16线程。</p>
<p>cache line的存在，使得两个不相关的线程的相关内容存在同一 行 中，这样 每次每个线程对内容修改以后， <strong>整行内容失效</strong>即使另外一个线程并没有对其对应的变量进行操作，所有就得重新从 主存读入，造成时间浪费。 所以要注意。</p>
<p>cache line大小： 64字节。</p>
<h3 id="缓存一致性协议："><a href="#缓存一致性协议：" class="headerlink" title="缓存一致性协议："></a>缓存一致性协议：</h3><p><img src="https://s1.ax1x.com/2020/09/16/wgrRmD.png" alt="wgrRmD.png"></p>
<p> 如果有多个核，每个核又都有自己的缓存，那么我们就遇到问题了：如果某个 CPU 缓存段中对应的内存内容被另外一个 CPU 偷偷改了，会发生什么？</p>
<p>好吧，答案很简单：什么也不会发生。这很糟糕。因为如果一个 CPU 缓存了某块内存，那么在其他 CPU 修改这块内存的时候，我们希望得到通知。我们拥有多组缓存的时候，真的需要它们保持同步。或者说，系统的内存在各个 CPU 之间无法做到与生俱来的同步，我们实际上是需要一个大家都能遵守的方法来达到同步的目的。</p>
<p><code>MESI</code>是四种缓存段状态的首字母缩写，任何多核系统中的缓存段都处于这四种状态之一。 </p>
<ul>
<li>失效（Invalid）缓存段，要么已经不在缓存中，要么它的内容已经过时。为了达到缓存的目的，这种状态的段将会被忽略。一旦缓存段被标记为失效，那效果就等同于它从来没被加载到缓存中。</li>
<li>共享（Shared）缓存段，它是和主内存内容保持一致的一份拷贝，在这种状态下的缓存段只能被读取，不能被写入。多组缓存可以同时拥有针对同一内存地址的共享缓存段，这就是名称的由来。</li>
<li>独占（Exclusive）缓存段，和 S 状态一样，也是和主内存内容保持一致的一份拷贝。区别在于，如果一个处理器持有了某个 E 状态的缓存段，那其他处理器就不能同时持有它，所以叫“独占”。这意味着，如果其他处理器原本也持有同一缓存段，那么它会马上变成“失效”状态。</li>
<li>已修改（Modified）缓存段，属于<strong>脏段</strong>，它们已经被所属的处理器修改了。如果一个段处于已修改状态，那么它在其他处理器缓存中的拷贝马上会变成失效状态，这个规律和 E 状态一样。此外，<strong>已修改缓存段</strong>如果被丢弃或<strong>标记为失效</strong>，那么先要<strong>把它的内容回写到内存中</strong>——这和回写模式下常规的脏段处理方式一样。</li>
</ul>
<h3 id="伪共享"><a href="#伪共享" class="headerlink" title="伪共享:"></a>伪共享:</h3><p>CPU 缓存系统中是以<strong>缓存行（cache line）为单位存储的</strong>。目前主流的 CPU Cache 的 Cache Line 大小都是 <strong>64 Bytes</strong>。</p>
<p>在多线程情况下，如果需要<strong>修改“共享同一个缓存行的变量”</strong>，就会无意中<strong>影响彼此的性能</strong>，这就是伪共享（False Sharing）。</p>
<h4 id="Java-8-中的解决方案"><a href="#Java-8-中的解决方案" class="headerlink" title="Java 8 中的解决方案"></a>Java 8 中的解决方案</h4><p>Java 8 中已经提供了官方的解决方案，Java 8 中新增了一个注解：<code>@sun.misc.Contended</code>。加上这个注解的类会自动补齐缓存行，需要注意的是此注解默认是无效的，需要在 <strong>jvm 启动时设置</strong> <code>-XX:-RestrictContended</code> 才会生效。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@sun</span>.misc.Contended</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">VolatileLong</span> </span>&#123;</span><br><span class="line"> <span class="keyword">public</span> <span class="keyword">volatile</span> <span class="keyword">long</span> value = <span class="number">0L</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="java7以下"><a href="#java7以下" class="headerlink" title="java7以下"></a>java7以下</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">VolatileLong</span></span>&#123;</span><br><span class="line">      <span class="keyword">public</span> <span class="keyword">long</span> p1, p2, p3, p4, p5, p6, p7; <span class="comment">// cache line padding   缓存行填充</span></span><br><span class="line">      <span class="keyword">public</span> <span class="keyword">volatile</span> <span class="keyword">long</span> value = <span class="number">0L</span>;</span><br><span class="line">      <span class="keyword">public</span> <span class="keyword">long</span> p8, p9, p10, p11, p12, p13, p14; <span class="comment">// cache line padding</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>







<h2 id="DCL单例需不需要加volatile"><a href="#DCL单例需不需要加volatile" class="headerlink" title="DCL单例需不需要加volatile"></a>DCL单例需不需要加volatile</h2><p>DCL： (double check locking)</p>
<p><strong>答： 需要。</strong></p>
<p>可以防止 发生<strong>指令重排序</strong>   使用到<strong>半初始化</strong>状态的对象。</p>
<p><img src="https://s1.ax1x.com/2020/09/16/w2F84f.png" alt="w2F84f.png"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Singleton</span> </span>&#123;</span><br><span class="line"> <span class="function"><span class="keyword">private</span> <span class="title">Singleton</span><span class="params">()</span> </span>&#123; &#125;</span><br><span class="line"> <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Singleton INSTANCE = <span class="keyword">null</span>;</span><br><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Singleton <span class="title">getInstance</span><span class="params">()</span> </span>&#123;</span><br><span class="line"> <span class="comment">// 实例没创建，才会进入内部的 synchronized代码块</span></span><br><span class="line"> <span class="keyword">if</span> (INSTANCE == <span class="keyword">null</span>) &#123; </span><br><span class="line"> <span class="keyword">synchronized</span> (Singleton<span class="class">.<span class="keyword">class</span>) </span>&#123; <span class="comment">// t2</span></span><br><span class="line"> <span class="comment">// 也许有其它线程已经创建实例，所以再判断一次</span></span><br><span class="line"> <span class="keyword">if</span> (INSTANCE == <span class="keyword">null</span>) &#123; <span class="comment">// t1</span></span><br><span class="line"> INSTANCE = <span class="keyword">new</span> Singleton();</span><br><span class="line">       &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> <span class="keyword">return</span> INSTANCE;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="为什么要double-check-："><a href="#为什么要double-check-：" class="headerlink" title="为什么要double check ："></a>为什么要double check ：</h3><p>防止 在第一次判断后到加锁的过程中 单例已经被其他线程创建好的情况。</p>
<h2 id="volatile如何解决指令重排序"><a href="#volatile如何解决指令重排序" class="headerlink" title="volatile如何解决指令重排序"></a>volatile如何解决指令重排序</h2><p>1: volatile i</p>
<p>2: Java字节码 上： 加了ACC_VOLATILE标志</p>
<p>3: JVM的内存屏障</p>
<pre><code>屏障两边的指令不可以重排！保障有序！

happends-before 
as - if - serial</code></pre><p>4：hotspot实现</p>
<p>bytecodeinterpreter.cpp</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> field_offset = cache-&gt;f2_as_index();</span><br><span class="line">          <span class="keyword">if</span> (cache-&gt;is_volatile()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (support_IRIW_for_not_multiple_copy_atomic_cpu) &#123;</span><br><span class="line">              OrderAccess::fence();</span><br><span class="line">            &#125;</span><br></pre></td></tr></table></figure>

<p>orderaccess_linux_x86.inline.hpp</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">OrderAccess::fence</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (os::is_MP()) &#123;</span><br><span class="line">    <span class="comment">// always use locked addl since mfence is sometimes expensive</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> AMD64</span></span><br><span class="line">    <span class="function">__asm__ <span class="title">volatile</span> <span class="params">(<span class="string">"lock; addl $0,0(%%rsp)"</span> : : : <span class="string">"cc"</span>, <span class="string">"memory"</span>)</span></span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">    <span class="function">__asm__ <span class="title">volatile</span> <span class="params">(<span class="string">"lock; addl $0,0(%%esp)"</span> : : : <span class="string">"cc"</span>, <span class="string">"memory"</span>)</span></span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>LOCK 用于在多处理器中执行指令时对共享内存的独占使用。<br>它的作用是能够将当前处理器对应缓存的内容刷新到内存，并使其他处理器对应的缓存失效。</strong><br><strong>另外还提供了有序的指令无法越过这个内存屏障的作用。</strong></p>
</blockquote>
<p><strong>jvm层面</strong></p>
<p><img src="https://s1.ax1x.com/2020/09/16/w2koes.png" alt="w2koes.png"></p>
<p><img src="https://s1.ax1x.com/2020/09/16/w2A9T1.png" alt="w2A9T1.png"></p>
<h2 id="Java的四种引用类型"><a href="#Java的四种引用类型" class="headerlink" title="Java的四种引用类型"></a>Java的四种引用类型</h2><p><a href="https://blog.csdn.net/l540675759/article/details/73733763" target="_blank" rel="noopener">参考链接</a></p>
<h3 id="虚引用："><a href="#虚引用：" class="headerlink" title="虚引用："></a>虚引用：</h3><p><strong>虚引用</strong>顾名思义，就是<strong>形同虚设</strong>。与其他几种引用都不同，<strong>虚引用</strong>并<strong>不会</strong>决定对象的<strong>生命周期</strong>。如果一个对象<strong>仅持有虚引用</strong>，那么它就和<strong>没有任何引用</strong>一样，在<strong>任何时候</strong>都可能被垃圾回收器回收。</p>
<p><strong>虚引用</strong>主要用来<strong>跟踪对象</strong>被垃圾回收器<strong>回收</strong>的活动。 <strong>虚引用</strong>与<strong>软引用</strong>和<strong>弱引用</strong>的一个区别在于：</p>
<blockquote>
<p>虚引用必须和<strong>引用队列(ReferenceQueue)联合使用</strong>。当垃圾回收器准备回收一个对象时，如果发现它还有虚引用，就会在<strong>回收对象的内存之前</strong>，把这个虚引用加入到与之关联的<strong>引用队列中</strong>。</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String str = <span class="keyword">new</span> String(<span class="string">"abc"</span>);</span><br><span class="line">ReferenceQueue queue = <span class="keyword">new</span> ReferenceQueue();</span><br><span class="line"><span class="comment">// 创建虚引用，要求必须与一个引用队列关联</span></span><br><span class="line">PhantomReference pr = <span class="keyword">new</span> PhantomReference(str, queue);</span><br></pre></td></tr></table></figure>



<p>当<strong>虚引用被回收的时候，有消息写入队列</strong>，某个<strong>线程一直监视着队列</strong>，发现有消息后就可以进行后面的工作。</p>
<p><img src="https://s1.ax1x.com/2020/09/16/w2nSmD.png" alt="w2nSmD.png"></p>
<h3 id="弱引用"><a href="#弱引用" class="headerlink" title="弱引用"></a>弱引用</h3><p>如果一个对象具有弱引用，那其的性质也是<strong>可有可无</strong>的状态。</p>
<p>弱引用和<strong>软引用</strong>的区别在于：弱引用的对象拥有<strong>更短的生命周期</strong>，<strong>只要垃圾回收器扫描到它</strong>，<strong>不管内存空间充足与否</strong>，<strong>都会回收</strong>它的内存。</p>
<h4 id="ThreadLocal使用弱引用的例子："><a href="#ThreadLocal使用弱引用的例子：" class="headerlink" title="ThreadLocal使用弱引用的例子："></a>ThreadLocal使用弱引用的例子：</h4><p>对于以下代码，thread1 中设置 threadLocal 为 1，而 thread2 设置 threadLocal 为 2。过了一段时间之后，thread1 读取 threadLocal 依然是 1，不受 thread2 的影响。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadLocalExample</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ThreadLocal threadLocal = <span class="keyword">new</span> ThreadLocal();</span><br><span class="line">        Thread thread1 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            threadLocal.set(<span class="number">1</span>);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(threadLocal.get());</span><br><span class="line">            threadLocal.remove();</span><br><span class="line">        &#125;);</span><br><span class="line">        Thread thread2 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            threadLocal.set(<span class="number">2</span>);</span><br><span class="line">            threadLocal.remove();</span><br><span class="line">        &#125;);</span><br><span class="line">        thread1.start();</span><br><span class="line">        thread2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">1</span><br></pre></td></tr></table></figure>



<p>ThreadLocal 它所对应的底层结构图为：</p>
<div align="center"> <img src="https://cs-notes-1256109796.cos.ap-guangzhou.myqcloud.com/6782674c-1bfe-4879-af39-e9d722a95d39.png" width="500px"> </div><br>

<p>每个 Thread 都有一个 ThreadLocal.ThreadLocalMap 对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* ThreadLocal values pertaining to this thread. This map is maintained</span></span><br><span class="line"><span class="comment"> * by the ThreadLocal class. */</span></span><br><span class="line">ThreadLocal.ThreadLocalMap threadLocals = <span class="keyword">null</span>;</span><br></pre></td></tr></table></figure>

<p>当调用<strong>一个 ThreadLocal 的 set(T value) 方法时，先得到当前线程的 ThreadLocalMap 对象</strong>，然后将 <code>ThreadLocal</code>-<code>value</code>键值对插入到该 Map 中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">threadLocal.set(<span class="string">"adf"</span>);</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadLocal</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(T value)</span> </span>&#123;</span><br><span class="line">        Thread t = Thread.currentThread();</span><br><span class="line">        ThreadLocalMap map = getMap(t);</span><br><span class="line">        <span class="keyword">if</span> (map != <span class="keyword">null</span>) &#123;</span><br><span class="line">            map.set(<span class="keyword">this</span>, value);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            createMap(t, value);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;    </span><br><span class="line">&#125;</span><br><span class="line">map.set方法中有以下实现：</span><br><span class="line">tab[i] = <span class="keyword">new</span> Entry(key, value);</span><br><span class="line"></span><br><span class="line">而Entry 是 WeakReference的子类</span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Entry</span> <span class="keyword">extends</span> <span class="title">WeakReference</span>&lt;<span class="title">ThreadLocal</span>&lt;?&gt;&gt; </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>key使用弱引用指向 ThreadLocal对象</strong>，垃圾回收时可以<strong>避免内存泄漏</strong>。</p>
<p> ThreadLocal对象不用后， <strong>手动执行</strong> <code>Threadlocal.remove()</code>，目的：删除 <code>ThreadLocalMap</code>中的记录。</p>
<p><img src="https://s1.ax1x.com/2020/09/17/wRFrY6.png" alt="wRFrY6.png"></p>
<h3 id="强引用"><a href="#强引用" class="headerlink" title="强引用"></a>强引用</h3><p>属于不可回收的资源，<strong>垃圾回收器绝不会回收它。当内存空间不足，Java虚拟机宁愿抛出OutOfMemoryError错误</strong>，使程序异常终止，也不会靠回收具有强引用的对象，来解决内存不足的问题</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Object o = <span class="keyword">new</span> Object();</span><br></pre></td></tr></table></figure>



<h3 id="软引用"><a href="#软引用" class="headerlink" title="软引用"></a>软引用</h3><p>如果一个对象只具有<strong>软引用</strong>，则<strong>内存空间充足</strong>时，<strong>垃圾回收器</strong>就<strong>不会</strong>回收它；如果<strong>内存空间不足</strong>了，就会<strong>回收</strong>这些对象的内存。只要垃圾回收器没有回收它，该对象就可以被程序使用。</p>
<blockquote>
<p><strong>软引用可用来实现内存敏感的高速缓存。</strong></p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 强引用</span></span><br><span class="line">String strongReference = <span class="keyword">new</span> String(<span class="string">"abc"</span>);</span><br><span class="line"><span class="comment">// 软引用</span></span><br><span class="line">String str = <span class="keyword">new</span> String(<span class="string">"abc"</span>);</span><br><span class="line">SoftReference&lt;String&gt; softReference = <span class="keyword">new</span> SoftReference&lt;String&gt;(str);</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>java并发</category>
      </categories>
      <tags>
        <tag>java并发</tag>
      </tags>
  </entry>
  <entry>
    <title>TCP和UDP</title>
    <url>/2020/09/08/TCP%E5%92%8CUDP/</url>
    <content><![CDATA[<h1 id="TCP-amp-UDP区别"><a href="#TCP-amp-UDP区别" class="headerlink" title="TCP&amp;UDP区别"></a>TCP&amp;UDP区别</h1><p>1、<strong>TCP面向连接</strong>;<strong>UDP是无连接的</strong>。</p>
<p>2、TCP<strong>提供可靠的服务</strong>。也就是说，通过TCP连接传送的数据，无差错，不丢失，不重复，且按序到达;<strong>UDP不保证可靠交付</strong>。</p>
<a id="more"></a>

<p>3、<strong>TCP面向字节流</strong>，实际上是TCP把数据看成一连串无结构的字节流;<strong>UDP是面向报文</strong>的。</p>
<p>4、每一<strong>条TCP连接只能是点到点的</strong>;<strong>UDP支持一对一，一对多，多对一和多对多的交互通信</strong>。</p>
<p>5、<strong>TCP首固定部分20字节</strong>;<strong>UDP的首部开销小，只有8个字节</strong>。</p>
<p>6、<strong>TCP有拥塞控制，UDP没有拥塞控制</strong>。因此网络出现拥塞<strong>不会使源主机的发送速率降低</strong>（对实时应用很有用，如IP电话，实时视频会议等）</p>
<h1 id="UDP应用场景："><a href="#UDP应用场景：" class="headerlink" title="UDP应用场景："></a>UDP应用场景：</h1><p>  1.面向<strong>数据报方式</strong>。</p>
<p>  2.网络数据大多为<strong>短消息。</strong></p>
<p>  3.拥有<strong>大量Client</strong>。</p>
<p>  4.<strong>对数据安全性无特殊要求</strong>。</p>
<p>  5.<strong>网络负担非常重，但对响应速度要求高。</strong></p>
<h1 id="OSI七层模型"><a href="#OSI七层模型" class="headerlink" title="OSI七层模型"></a>OSI七层模型</h1><p>  <img src="http://c.biancheng.net/uploads/allimg/190124/1-1Z1241445324H.jpg" alt="image"></p>
<h1 id="TCP可靠传输的实现"><a href="#TCP可靠传输的实现" class="headerlink" title="TCP可靠传输的实现"></a>TCP可靠传输的实现</h1><h2 id="什么是可靠传输"><a href="#什么是可靠传输" class="headerlink" title="什么是可靠传输"></a>什么是可靠传输</h2><p>一是能<strong>够正确地传输数据</strong>，</p>
<p>二是接收方能够及时<strong>处理发送方发送的数据</strong>。</p>
<h2 id="怎么实现"><a href="#怎么实现" class="headerlink" title="怎么实现"></a>怎么实现</h2><p>（1）首先，采用<strong>三次握手</strong>来建立TCP连接，<strong>四次挥手</strong>来释放TCP连接，从而保证<strong>建立的传输信道是可靠的</strong>。</p>
<p>（2）其次，TCP采用了<strong>连续ARQ协议</strong>（回退N，Go-back-N；超时自动重传）来保证数据传输的正确性，使用<strong>滑动窗口协议</strong>来保证接方能够及时处理所接收到的数据，同时进行流量控制。</p>
<p>（3）最后，TCP使用<strong>慢启动、拥塞避免、快重传和快恢复</strong>来进行<strong>拥塞控制</strong>，避免网络拥塞。</p>
<p>（4）<strong>存在确认号和序号</strong>。</p>
<h3 id="三次握手"><a href="#三次握手" class="headerlink" title="三次握手"></a>三次握手</h3><p><img src="https://s1.ax1x.com/2020/03/13/8uTl7j.png" alt="8uTl7j.png"></p>
<p>（1）第一次握手</p>
<ul>
<li><strong>A的TCP客户进程向B发出建立连接请求报文段，其中SYN（同步位）=1，ACK（确认位）=0，seq（序号）=x。</strong>（SYN=1且ACK=0时，表明这是一个请求建立连接的；SYN报文段（SYN=1的报文段）不能携带数据，但是要消耗掉一个序号。）</li>
<li>在A发送完毕之后，A的TCP客户端进程进入SYN-SENT（同步已发送）状态。</li>
</ul>
<p>（2）第二次握手</p>
<ul>
<li><strong>B在收到连接请求报文段之后，向A发送确认报文段。其中SYN=1，ACK=1，ack（确认号）=x+1，同时设置自己的初始序号seq=y。</strong>（TCP规定，当报文段的SYN=1且ACK=1时，表明这是一个同一建立连接响应报文段；这个报文段也不能携带数据，同样需要消耗掉一个序号。）</li>
<li>在B发送完毕之后，B的TCP服务端进程进入SYN-RCVD（同步收到）状态。</li>
</ul>
<p>（3）第三次握手</p>
<ul>
<li><p><strong>A在收到B的确认报文段之后，还需要向B给出确认报文段。其中ACK=1，seq=x+1，ack=y+1</strong><br>（TCP规定，这个ACK报文段可以携带数据；如果<strong>不携带数据则不消耗序号</strong>，即A下一个数据报文段的序号仍然是seq=x+1。）</p>
</li>
<li><p>在A发送完毕之后，A的TCP客户端进程进入ESTABLISHED（已建立连接）状态；B在接收到A的确认报文段之后，B的服务端进程也进入ESTABLISHED（已建立连接）状态。</p>
</li>
</ul>
<h4 id="三次握手的原因"><a href="#三次握手的原因" class="headerlink" title="三次握手的原因"></a>三次握手的原因</h4><ul>
<li><p><strong>防止已失效的请求连接报文段突然又传送到了server，因而产生错误</strong>：</p>
<p>client发出的第一个连接请求报文段并没有丢失，<strong>而是在某个网络结点长时间的滞留了</strong>，以致延误到连接释放以后的某个时间才到达server。本来这是一个<strong>早已失效</strong>的报文段。但server收到此失效的连接请求报文段后，就误认为是client再次发出的一个新的连接请求。于是就向client发出确认报文段，同意建立连接。</p>
<p>假设不采用“三次握手”，那么只要server发出确认，新的连接就建立了。由于现在client并没有发出建立连接的请求，因此不会理睬server的确认，也不会向server发送数据。但server却以为新的运输连接已经建立，并一直等待client发来数据。这样，server的很多资源就白白浪费掉了。</p>
<p>因此，采用“三次握手”的办法可以防止上述现象发生。 <strong>client不会向server的确认进行回应</strong>。server由于收不到确认，就知道client并没有要求建立连接。<strong>避免server端一直等待，浪费资源</strong>。</p>
</li>
</ul>
<h3 id="四次挥手"><a href="#四次挥手" class="headerlink" title="四次挥手"></a>四次挥手</h3><p><img src="https://s1.ax1x.com/2020/03/13/8uTe9P.png" alt="8uTe9P.png"></p>
<h4 id=""><a href="#" class="headerlink" title=""></a></h4><p>1）第一次挥手  A→ B</p>
<ul>
<li><strong>A的TCP客户进程向B发出释放连接请求报文段，其中FIN（终止位）=1，seq（序号）=u</strong>。</li>
</ul>
<p>（TCP规定，当报文段<strong>的FIN=1时，表明此报文段的发送方的数据已发生完毕</strong>，<strong>并要求释放连接</strong>；FIN报文段（FIN=1的报文段）<strong>不能携带数据</strong>，但是要<strong>消耗掉一个序号</strong>。）</p>
<ul>
<li>在A发送完毕之后，A的TCP客户端进程进入FIN-WAIT-1（终止等待1）状态。</li>
</ul>
<p>（2）第二次挥手  B→A</p>
<ul>
<li><p><strong>B在收到连接释放连接请求报文段之后，随即向A发送确认报文段</strong>。其中ACK=1，seq=v，ack（确认号）=u+1。</p>
</li>
<li><p>在B发送完毕之后，B的TCP服务端进程进入<strong>CLOSE-WAIT</strong>（关闭等待）状态。</p>
</li>
<li><p>TCP服务器进程这时<strong>应该通知高层应用</strong>，从A到B这个方向的连接就要释放了，这时的TCP连接处于<strong>半关闭（half-close）状态</strong>。</p>
</li>
<li><p>A在收到B的确认报文段之后，就进入FIN-WAIT-2（终止等待2）状态，等待B发出的连接释放报文段。</p>
</li>
</ul>
<p>（3）第三次挥手 B→A</p>
<ul>
<li><p>若B已<strong>经没有要向A发送的数据</strong>，其应用进程就通知TCP释放连接。B向A发出释放连接报文段，其中FIN=1，ACK=1，seq=w，ack=u+1。</p>
</li>
<li><p>在B发送完毕之后，B的TCP服务端进程进入LAST-ACK（最后确认）状态。</p>
</li>
</ul>
<p>（4）第四次挥手 A→B</p>
<ul>
<li>A在接收到B的释放连接请求报文段之后，必须对此发出确认。其中ACK=1，seq=u+1，ack=w+1。</li>
<li>A在发送完毕之后，进入到TIME-WAIT（时间等待）状态。</li>
<li>B在接收到A的确认之后，进入到CLOSED（关闭）状态。</li>
<li>A在经过时间等待计时器设置的时间<strong>2MSL之后，A才进入到CLOSED状态</strong>。</li>
</ul>
<h4 id="四次挥手原因"><a href="#四次挥手原因" class="headerlink" title="四次挥手原因"></a>四次挥手原因</h4><ul>
<li>四次握手可以看成是两个二次握手，分别是A的TCP客户端进程请求释放连接和B的TCP服务端进程请求释放连接。</li>
</ul>
<h4 id="client等待2MSL的原因"><a href="#client等待2MSL的原因" class="headerlink" title="client等待2MSL的原因"></a>client等待2MSL的原因</h4><ul>
<li><strong>一是为了让B能够按照正常步骤进入CLOSED状态，二是为了防止已失效的请求连接报文段出现在下次连接中，因而产生错误</strong>。<ul>
<li>为了保证A发送的最后一个ACK报文段能够到达B。因为这个ACK报文段有可能丢失，这样B就无法接收到而进入CLOSED状态。于是B会重传请求释放连接的报文段，A在这段等待时间接收到了，重传ACK报文段，这样B还是会顺利接收到确认报文段，进入CLOSED状态。如果此时A已经关闭了，那么就无法收到B的请求报文段，也不会发送ACK报文段，这样B就无法进入CLOSED状态了。</li>
<li>第二，在这2MSL的等待时间内，本次连接的所有报文都已经从网络中消失，从<strong>而不会出现失效的报文出现在下次连接中</strong>。</li>
</ul>
</li>
<li>MSL （Maximum Segment Lifetime） 报文最大生存时间。</li>
</ul>
<h3 id="连续ARQ协议"><a href="#连续ARQ协议" class="headerlink" title="连续ARQ协议"></a>连续ARQ协议</h3><ul>
<li><p>ARQ协议，即<strong>自动重传请求</strong>（Automatic Repeat-reQuest）通过使用<strong>确认和超时</strong>这两个机制，在不可靠服务的基础上实现可靠的信息传输。</p>
</li>
<li><p>连续ARQ协议发送方采用流水线传输。流水线传输就是发送方可以连续发送多个分组，不必每发完一个分组就停下来等待对方确认。结合滑动窗口来实现，<br><img src="https://s1.ax1x.com/2020/03/13/8KcO1J.png" alt="8KcO1J.png"></p>
</li>
<li><p>位于发送窗口内的5个分组都可以连续发送出去，而不需要等待对方的确认，这样就提高了信道利用率。 </p>
</li>
<li><p>发送方每收到一个确认，就把发送窗口向前滑动一个分组的位置。例如上面的图（b），当发送方收到第一个分组的确认，就把发送窗口向前移动一个分组的位置。如果原来已经发送了前5个分组，则现在可以发送窗口内的第6个分组。 </p>
</li>
<li><p>接收方一般都是采用<strong>累积确认</strong>的方式。也就是说接收方不必对收到的分组逐个发送确认。而是<strong>在收到几个分组后，对按序到达的最后一个分组发送确认</strong>。<strong>如果收到了这个分组确认信息，则表示到这个分组为止的所有分组都已经正确接收到了</strong>。 </p>
</li>
<li><p>累积确认的<strong>优点</strong>：容易实现。</p>
</li>
<li><p><strong>缺点</strong>：不能正确的向发送方反映出接收方已经正确收到的所以分组的信息。比如发送方发送了前5个分组，而中间的第3个分组丢失了，这时候接收方只能对前2个发出确认。而不知道后面3个分组的下落，因此只能把后面的3个分组都重传一次，这种机制叫<strong>Go-back-N（回退N）</strong>，表示需要再退回来重传已发送过的N个分组。</p>
</li>
</ul>
<h3 id="超时重传和快速重传"><a href="#超时重传和快速重传" class="headerlink" title="超时重传和快速重传"></a>超时重传和快速重传</h3><ul>
<li>设置一个计时器，如果在计时器计算完的时间内没有收到确认消息，则重新发送。</li>
<li>快速重传，连续收到三个ACK的时候，立即重传，不必等待计时器到时。</li>
</ul>
<h3 id="滑动窗口协议"><a href="#滑动窗口协议" class="headerlink" title="滑动窗口协议"></a>滑动窗口协议</h3><ul>
<li><p><strong>概念</strong></p>
<ul>
<li>滑动窗口协议在在发送方和接收方之间各自维持一个滑动窗口，窗口是随着时间可以向前滑动的。它允许发送方发送多个分组而不需等待确认。TCP的滑动窗口是以<strong>字节为单位</strong>的。发送和接收窗口的结构如下：<br><img src="https://s1.ax1x.com/2020/03/13/8K2wZt.png" alt="8K2wZt.png"></li>
</ul>
</li>
<li><p><strong>规则</strong></p>
<ul>
<li><p>（1）凡是已经发送过的数据，在未收到确认之前，都必须<strong>暂时保留</strong>，以便在超时重传时使用。</p>
</li>
<li><p>（2）只有当发送方A<strong>收到了接收方的确认报文段时</strong>，发送方窗口才可以向前滑动几个序号。</p>
</li>
<li><p>（3）当发送方A发送的数据经过一段时间没有收到确认，就要使用回退N步协议，回到最后接收到确认号的地方，重新发送这部分数据。</p>
</li>
</ul>
</li>
<li><p><strong>可变窗口流量控制</strong><br><a href="https://imgchr.com/i/8KRmY8" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2020/03/13/8KRmY8.png" alt="8KRmY8.png"></a></p>
</li>
<li><p>滑动窗口可以进行流量控制，使得接受端可以及时处理收到的信息。</p>
</li>
</ul>
<h3 id="拥塞控制"><a href="#拥塞控制" class="headerlink" title="拥塞控制"></a>拥塞控制</h3><h4 id="拥塞控制与流量控制的区别"><a href="#拥塞控制与流量控制的区别" class="headerlink" title="拥塞控制与流量控制的区别"></a>拥塞控制与流量控制的区别</h4><ul>
<li><p>拥塞控制就是防止过多的数据注入到网络中，这样可以防止网络中的路由器或链路不致过载。拥塞控制所要做的都有一个前提，就是网络能够承受现有的网络负荷。拥塞控制是一个全局性的过程，涉及到所有的主机、所有的路由器，以及与降低网络传输性能有关的所有因素。</p>
</li>
<li><p>流量控制往往指点对点通信量的控制，是个端到端的问题。流量控制所要做的就是抑制发送端发送数据的速率，以便使接收端来得及接收。</p>
</li>
</ul>
<h4 id="相关算法"><a href="#相关算法" class="headerlink" title="相关算法"></a>相关算法</h4><ul>
<li>cwnd&lt;ssthresh  慢启动算法</li>
<li>cwnd=ssthresh  慢启动算法/拥塞避免算法</li>
<li>cwnd&gt;ssthresh  拥塞避免算法</li>
<li>cwnd–congestion window拥塞控制窗口  ssthresh– slow start thresh 慢启动门限</li>
</ul>
<p>（1）慢启动算法</p>
<p>启动时，cwnd设置为一，每个传输轮次后将 cwnd 大小加倍。</p>
<p>（3）ssthresh设置</p>
<p>只要发生拥塞，ssthresh就在当前cwnd的大小下折半。</p>
<p>（3）拥塞避免算法</p>
<p>每个传输轮次后将 <strong>cwnd的大小加一</strong>（线性增加），如果发现出现网络拥塞的话就按照上面的方法重新设置ssthresh的大小（乘法减小），<strong>并从cwnd=1开始重新执行慢开始算法</strong>。</p>
<p>（4）快重传</p>
<ul>
<li>快重传是允许发送方再连续收到<strong>3个重复的确认后就可以立即重传丢失的报文</strong>而<strong>不必再等待所设置的重传计时器到时</strong>。</li>
</ul>
<p>（5）快恢复</p>
<p>发生拥塞后，ssthresh折半，cwnd并不从一开始，而是直接恢复到门限值ssthresh执行拥塞避免算法。</p>
<p><img src="https://upload-images.jianshu.io/upload_images/6333705-0341036bd6f18bc2?imageMogr2/auto-orient/strip%7CimageView2/2/format/webp" alt="image"></p>
]]></content>
      <categories>
        <category>计算机网络</category>
      </categories>
      <tags>
        <tag>计算机网络</tag>
      </tags>
  </entry>
  <entry>
    <title>java并发问题(1)</title>
    <url>/2020/09/11/java%E5%B9%B6%E5%8F%91%E9%97%AE%E9%A2%98-1/</url>
    <content><![CDATA[<h1 id="线程执行顺序相关"><a href="#线程执行顺序相关" class="headerlink" title="线程执行顺序相关"></a>线程执行顺序相关</h1><ol>
<li><strong>JOIN使用</strong></li>
</ol>
<p><strong>现在有 T1、T2、T3 三个线程，你怎样保证 T2 在 T1 执行完后执行，T3 在 T2 执行完后执行？</strong></p>
<a id="more"></a>

<p>答：可以用 join 方法实现。T2中调用<code>T1.join()</code>, T3中调用<code>T2.join()</code></p>
<p>在线程中调用另一个线程的 join() 方法，会将<strong>当前线程挂起</strong>，而<strong>不是忙等待</strong>，直到目标线程结束。</p>
<ol start="2">
<li><strong>wait()  notify()  notifyall()</strong></li>
</ol>
<p>调用 wait() 使得线程等待某个条件满足，线程在等待时会被<strong>挂起</strong>，当其他线程的运行使得这个条件满足时，其它线程会调用 notify() 或者 notifyAll() 来唤醒挂起的线程。</p>
<p>它们都属于 <code>Object</code> 的一部分，而<strong>不属于 Thread</strong>。</p>
<p><strong>需要配合<code>synchronized</code>使用。</strong></p>
<ol start="3">
<li><p><strong>await() signal() signalAll()</strong></p>
<p><strong>属于 Condition 类 ，可以在 Condition 上调用 await() 方法使线程等待，其它线程调用 signal() 或 signalAll() 方法唤醒等待的线程。</strong></p>
<p>相比于 wait() 这种等待方式，await() 可<strong>以指定等待的条件，因此更加灵活</strong>。</p>
<p>如下，可分别指定多个条件，这是优于<code>wait</code>方法的主要地方。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AwaitSignalExample</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">    <span class="keyword">private</span> Condition condition1 = lock.newCondition();</span><br><span class="line">    <span class="keyword">private</span> Condition condition2 = lock.newCondition();</span><br><span class="line">    </span><br><span class="line">     <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ExecutorService executorService = Executors.newCachedThreadPool();</span><br><span class="line">        AwaitSignalExample example = <span class="keyword">new</span> AwaitSignalExample();</span><br><span class="line">        executorService.execute(() -&gt; example.after1());</span><br><span class="line">        executorService.execute(() -&gt; example.before1());</span><br><span class="line">        executorService.execute(() -&gt; example.after2());</span><br><span class="line">        executorService.execute(() -&gt; example.before2());</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">before1</span><span class="params">()</span></span>&#123;</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">"before1 "</span> );</span><br><span class="line">            condition1.signalAll();</span><br><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">before2</span><span class="params">()</span></span>&#123;</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">"before2"</span>);</span><br><span class="line">            Sleeper.sleep(<span class="number">1</span>);</span><br><span class="line">            condition2.signal();</span><br><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">after1</span><span class="params">()</span></span>&#123;</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            condition1.await();</span><br><span class="line">            System.out.println(<span class="string">"after1 "</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">after2</span><span class="params">()</span></span>&#123;</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            condition2.await();</span><br><span class="line">            System.out.println(<span class="string">"after2 "</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





</li>
</ol>
<h1 id="lock和synchronized"><a href="#lock和synchronized" class="headerlink" title="lock和synchronized"></a>lock和synchronized</h1><h2 id="lock的优势"><a href="#lock的优势" class="headerlink" title="lock的优势"></a>lock的优势</h2><p>Lock接口在多线程和并发编程中最大的优势是它们<strong>分别为读和写提供了锁</strong>。</p>
<p>读写锁ReentrantReadWriteLock，它表示有两个锁，一个是读操作相关的锁，也称为<strong>共享锁</strong>；另一个是写操作相关的锁，也叫<strong>排他锁</strong>。只有”<strong>读读</strong>“操作是<strong>非互斥</strong>的可以同时执行的，只要<strong>涉及”写</strong>“操作都是<strong>排他互斥</strong>的。</p>
<p>利用这一个特点，可以实现 <strong>高效的缓存</strong>，它允许<strong>多个用户读，但只允许一个用户写</strong>，同时保持正确性。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReadWriteLockTest</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">            ReadWrite readWrite = <span class="keyword">new</span> ReadWrite();</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">2</span>;i++)&#123;</span><br><span class="line">                ThreadWrite write = <span class="keyword">new</span> ThreadWrite(readWrite,<span class="keyword">new</span> Random().nextInt(<span class="number">100</span>));</span><br><span class="line">                write.setName(<span class="string">"write"</span>+i+<span class="string">":"</span>);</span><br><span class="line">                write.start();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">2</span>;i++)&#123;</span><br><span class="line">                ThreadRead read = <span class="keyword">new</span> ThreadRead(readWrite);</span><br><span class="line">                read.setName(<span class="string">"read"</span>+i+<span class="string">":"</span>);</span><br><span class="line">                read.start();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">ReadWrite</span></span>&#123;</span><br><span class="line">        <span class="keyword">private</span> ReentrantReadWriteLock lock = <span class="keyword">new</span> ReentrantReadWriteLock();</span><br><span class="line">        <span class="keyword">private</span> Object data=<span class="keyword">null</span>;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">read</span><span class="params">()</span></span>&#123;</span><br><span class="line">            <span class="keyword">try</span>&#123;</span><br><span class="line">                lock.readLock().lock();</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"is ready to read"</span>);</span><br><span class="line">                    Thread.sleep(<span class="keyword">new</span> Random().nextInt(<span class="number">100</span>));</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"have read date "</span>+data);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">finally</span>&#123;</span><br><span class="line">                lock.readLock().unlock();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">(Object data)</span></span>&#123;</span><br><span class="line">            <span class="keyword">try</span>&#123;</span><br><span class="line">                lock.writeLock().lock();</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"is ready to write"</span>);</span><br><span class="line">                    <span class="keyword">this</span>.data = data;</span><br><span class="line">                    Thread.sleep(<span class="keyword">new</span> Random().nextInt(<span class="number">100</span>));</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">"have write data "</span>+data);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">finally</span>&#123;</span><br><span class="line">                lock.writeLock().unlock();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">ThreadRead</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">        <span class="keyword">private</span> ReadWrite readWrite;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">ThreadRead</span><span class="params">(ReadWrite readWrite)</span></span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.readWrite = readWrite;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">super</span>.run();</span><br><span class="line">            readWrite.read();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">ThreadWrite</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">        <span class="keyword">private</span> ReadWrite readWrite;</span><br><span class="line">        <span class="keyword">private</span> Object data;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">ThreadWrite</span><span class="params">(ReadWrite readWrite,Object data)</span></span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.readWrite = readWrite;</span><br><span class="line">            <span class="keyword">this</span>.data = data;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">super</span>.run();</span><br><span class="line">            readWrite.write(data);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">out:</span><br><span class="line">write1:is ready to write</span><br><span class="line">write1:have write date <span class="number">55</span></span><br><span class="line">write0:is ready to write</span><br><span class="line">write0:have write date <span class="number">99</span></span><br><span class="line">read0:is ready to read</span><br><span class="line">read1:is ready to read</span><br><span class="line">read1:have read data <span class="number">99</span></span><br><span class="line">read0:have read data <span class="number">99</span></span><br></pre></td></tr></table></figure>

<p>可见 ，write操作都是互斥的，同一时刻只能有一个线程执行， 而 read操作同时可以有多个线程。</p>
<h1 id="阻塞队列的实现"><a href="#阻塞队列的实现" class="headerlink" title="阻塞队列的实现"></a>阻塞队列的实现</h1><ul>
<li>实现功能： 实现一个队列， 当队列中元素个数达到上界，阻塞。 元素个数达到下届也阻塞。 否则就可以实现加入和删除元素的功能。</li>
<li>可用于解决 <strong>生产者消费者</strong> 问题。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBlockingQueue</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> LinkedList&lt;Object&gt; list = <span class="keyword">new</span> LinkedList&lt;Object&gt;();</span><br><span class="line">    <span class="keyword">private</span> AtomicInteger count = <span class="keyword">new</span> AtomicInteger(<span class="number">0</span>);   <span class="comment">//计数器</span></span><br><span class="line">    <span class="comment">//指定队列大小上下限</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> maxSize;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> minSize=<span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyQueue</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.maxSize = size;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Object lock = <span class="keyword">new</span> Object();  <span class="comment">//初始化一个对象用于加锁</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count.get();</span><br><span class="line">    &#125;</span><br><span class="line">  </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">put</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span> (lock) &#123;</span><br><span class="line">            <span class="comment">//如果容器大小刚好等于最大长度，则阻塞</span></span><br><span class="line">            <span class="keyword">while</span>(size() == maxSize) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    lock.wait();<span class="comment">//阻塞</span></span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            list.add(obj);</span><br><span class="line">            count.incrementAndGet();</span><br><span class="line">            System.out.println(<span class="string">"新加入的元素为："</span>+obj);</span><br><span class="line">            lock.notify();   <span class="comment">//通知另外一个线程去取元素</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">take</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span> (lock) &#123;</span><br><span class="line">            <span class="comment">//如果容器的大小刚好等于队列最小长度，则阻塞</span></span><br><span class="line">            <span class="keyword">while</span>(minSize == size()) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    lock.wait();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            Object obj = list.removeFirst();</span><br><span class="line">            count.decrementAndGet(); <span class="comment">//计数减一</span></span><br><span class="line">            lock.notify();          <span class="comment">//通知另外一个线程进行添加元素</span></span><br><span class="line">            <span class="keyword">return</span> obj;     </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="阻塞队列解决生产者消费者问题"><a href="#阻塞队列解决生产者消费者问题" class="headerlink" title="阻塞队列解决生产者消费者问题"></a>阻塞队列解决生产者消费者问题</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProducerConsumer</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> BlockingQueue&lt;String&gt; queue = <span class="keyword">new</span> ArrayBlockingQueue&lt;&gt;(<span class="number">5</span>);</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Producer</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                queue.put(<span class="string">"product"</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.print(<span class="string">"produce.."</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Consumer</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;String product = queue.take();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.print(<span class="string">"consume.."</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123; </span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++) &#123;</span><br><span class="line">            Producer producer = <span class="keyword">new</span> Producer();</span><br><span class="line">            producer.start(); </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            Consumer consumer = <span class="keyword">new</span> Consumer(); consumer.start();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">            Producer producer = <span class="keyword">new</span> Producer();</span><br><span class="line">            producer.start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//produce..produce..consume..consume..produce..consume..produce..produce..consume..consume..</span></span><br></pre></td></tr></table></figure>



<h1 id="Java中的各种锁"><a href="#Java中的各种锁" class="headerlink" title="Java中的各种锁"></a>Java中的各种锁</h1><p><a href="https://tech.meituan.com/2018/11/15/java-lock.html" target="_blank" rel="noopener">各种锁</a></p>
<h1 id="死锁例子及解决"><a href="#死锁例子及解决" class="headerlink" title="死锁例子及解决"></a>死锁例子及解决</h1><ul>
<li>产生死锁的例子</li>
<li>同时满足 <strong>互斥，不可抢占，请求和保持，循环等待</strong> 条件时会发生死锁。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LockTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String obj1 = <span class="string">"obj1"</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String obj2 = <span class="string">"obj2"</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Thread t1 = <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"LockA 开始执行"</span>);</span><br><span class="line">                <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</span><br><span class="line">                    <span class="keyword">synchronized</span> (LockTest.obj1) &#123;</span><br><span class="line">                        System.out.println( <span class="string">" LockA 锁住 obj1"</span>);</span><br><span class="line">                        Thread.sleep(<span class="number">3000</span>); <span class="comment">// 此处等待是给B能锁住机会</span></span><br><span class="line">                        <span class="keyword">synchronized</span> (LockTest.obj2) &#123;</span><br><span class="line">                            System.out.println(  <span class="string">" LockA 锁住 obj2"</span>);</span><br><span class="line">                            Thread.sleep(<span class="number">60</span> * <span class="number">1000</span>); <span class="comment">// 为测试，占用了就不放</span></span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        Thread t2 = <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                System.out.println( <span class="string">"LockB 开始执行"</span>);</span><br><span class="line">                <span class="keyword">while</span>(<span class="keyword">true</span>)&#123;</span><br><span class="line">                    <span class="keyword">synchronized</span> (LockTest.obj2) &#123;</span><br><span class="line">                        System.out.println(  <span class="string">" LockB 锁住 obj2"</span>);</span><br><span class="line">                        Thread.sleep(<span class="number">3000</span>); <span class="comment">// 此处等待是给A能锁住机会</span></span><br><span class="line">                        <span class="keyword">synchronized</span> (LockTest.obj1) &#123;</span><br><span class="line">                            System.out.println( <span class="string">" LockB 锁住 obj1"</span>);</span><br><span class="line">                            Thread.sleep(<span class="number">60</span> * <span class="number">1000</span>); <span class="comment">// 为测试，占用了就不放</span></span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        t1.start();</span><br><span class="line">        t2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="原子性和原子操作"><a href="#原子性和原子操作" class="headerlink" title="原子性和原子操作"></a>原子性和原子操作</h1><p><strong>1. 原子性</strong></p>
<p>　　原子性：即一个操作或者多个操作 <strong>要么全部执行并且执行的过程不会被任何因素打断</strong>，要么就<strong>都不执行</strong>。</p>
<p><strong>2.Java中的原子操作</strong></p>
<p>原子操作(atomic operation)是<strong>不需要<code>synchronized</code></strong>，所谓原子操作是指不会被线程调度机制打断的操作；这种操作一旦开始，就一直运行到结束，中间<strong>不会有任何 context switch</strong>；</p>
<h1 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h1><h2 id="volatile作用"><a href="#volatile作用" class="headerlink" title="volatile作用"></a>volatile作用</h2><p>volatile 可以 保证<strong>有序性、可见性</strong>。</p>
<p>volatile 的底层实现原理是<strong>内存屏障</strong>，Memory Barrier（Memory Fence） 对 volatile 变量的<strong>写指令后</strong>会加入<strong>写屏障</strong>，对 volatile 变量的<strong>读指令前</strong>会加入<strong>读屏障</strong>。</p>
<ul>
<li><strong>保证可见性</strong>：</li>
</ul>
<p>写屏障（sfence）保证在该屏障之前的，对共享变量的改动，都<strong>同步到主存当中</strong></p>
<p>读屏障（lfence）保证在该屏障之后，<strong>对共享变量的读取，加载的是主存中最新数据</strong>。</p>
<ul>
<li><strong>保证有序性：</strong></li>
</ul>
<p>写屏障会确保指令重排序时，不会将写屏障之前的代码排在写屏障之后。</p>
<p>读屏障会确保指令重排序时，不会将读屏障之后的代码排在读屏障之前。</p>
<p><strong>volatile解决的是可见性和有序性的问题，不能保证原子性</strong>。只能保证<strong>单个线程</strong>中的指令不会重排序，不能保证多个线程之间的</p>
<p>顺序。synchronized三种性质都可以保证。（被synchronized完全保护的内容不会有有序性问题，但是并不能阻止指令重排序，只</p>
<p>是不会因为指令重排序产生问题）</p>
<h2 id="volatile和synchronized的区别"><a href="#volatile和synchronized的区别" class="headerlink" title="volatile和synchronized的区别"></a>volatile和synchronized的区别</h2><ol>
<li>volatile本质是在<strong>告诉jvm</strong>当前变量在寄存器（工作内存）中的值是不确定的，需要<strong>从主存中读取</strong>； synchronized则是<strong>锁定当前变量，只有当前线程可以访问该变量</strong>，其他线程被阻塞住。</li>
<li>volatile仅能使用在<strong>变量级别</strong>；synchronized则可以使用在变量、方法、和类级别的</li>
<li>volatile仅能保证变量的<strong>可见性</strong>，<strong>不能保证原子性</strong>；而synchronized则可以保证变量的 <strong>可见性和原子性</strong>。</li>
<li>volatile不会造成线程的阻塞；synchronized可能会造成线程的阻塞。</li>
<li>volatile标记的变量不会被编译器优化；synchronized标记的变量可以被编译器优化。</li>
</ol>
<h1 id="竞争条件及其解决"><a href="#竞争条件及其解决" class="headerlink" title="竞争条件及其解决"></a>竞争条件及其解决</h1><p>​    Java中的多线程，<strong>当多个线程对一个数据进行操作</strong>时，可能会产生“竞争条件”的现象。</p>
<p>​    这时候需要对线程的操作<strong>进行加锁</strong>，来解决多线程操作一个数据时可能产生问题。加锁方式有两种，一个是申明Lock对象来对语句快进行加锁，另一种是通过synchronized 关键字来对方法进行加锁。以上两种方法都可以有效解决Java多线程中存在的竞争条件的问题。</p>
<h1 id="Java-中唤醒一个阻塞的线程"><a href="#Java-中唤醒一个阻塞的线程" class="headerlink" title="Java 中唤醒一个阻塞的线程"></a>Java 中唤醒一个阻塞的线程</h1><p><strong>1. sleep()</strong> 方法</p>
<p>​    sleep 使线程在该时间内进入线程<strong>time_wating状态</strong>，期间得不到cpu的时间片，等到时间过去了，线程重新进入可执行状态。 </p>
<p><strong>2. yield()</strong> 方法</p>
<p>​    会使<strong>线程放弃当前分得的cpu时间片</strong>，但此时线程仍然<strong>处于可执行状态</strong>，<strong>随时可以再次分得cpu时间片</strong>。yield()方法只能使同优先级的线程有执行的机会。调用 yield()的效果等价于调度程序认为<strong>该线程已执行了足够的时间从而转到另一个线程</strong>。</p>
<p><strong>3.wait()</strong> 和<strong>notify()</strong>方法</p>
<p>​    两个方法搭配使用，wait()使线程进入<strong>阻塞状态</strong>，调用notify()时，线程进入可执行状态。wait()内可加或不加参数，加参数时是以毫秒为单位，当到了指定时间或调用notify()方法时，进入可执行状态。（属于Object类，而不属于Thread类，wait()会先释放锁住的对象，然后再执行等待的动作。由于wait()所等待的对象必须先锁住，因此，它只能用在同步化程序段或者同步化方法内，否则，会抛出异常IllegalMonitorStateException.）</p>
<p><strong>4.join()</strong> 方法</p>
<p>​    叫线程加入。是当前线程A调用另一个线程B的join()方法，当前线程转A入<strong>阻塞状态</strong>，直到<strong>线程B运行结束</strong>，线程A才由阻塞状态转为可执行状态。</p>
<p> wait() 和 notify()是其中<strong>功能最强大、使用最灵活</strong>的方法，但这也导致了它们效率较低、较容易出错的特性，因此，在实际应用中应灵活运用各种方法。</p>
<ol start="5">
<li><strong>await和  signal</strong> </li>
</ol>
<p>属于 <strong>Condition</strong>类，await后线程变为 <strong>阻塞状态</strong>， 调用signal 后变为Runnable。</p>
<h1 id="CycliBarriar-和-CountdownLatch"><a href="#CycliBarriar-和-CountdownLatch" class="headerlink" title="CycliBarriar 和 CountdownLatch"></a>CycliBarriar 和 CountdownLatch</h1><h2 id="CountdownLatch"><a href="#CountdownLatch" class="headerlink" title="CountdownLatch"></a>CountdownLatch</h2><p>用来控制<strong>一个线程等待多个线程</strong>。</p>
<p>维护了一个计数器 cnt，每次调用 <strong>countDown() 方法会让计数器的值减 1</strong>，减到 0 的时候，那些<strong>因为调用 await() 方法而在等待</strong>的线程就会被<strong>唤醒</strong>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CountdownLatchExample</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">int</span> totalThread = <span class="number">10</span>;</span><br><span class="line">        CountDownLatch countDownLatch = <span class="keyword">new</span> CountDownLatch(totalThread);</span><br><span class="line">        ExecutorService executorService = Executors.newCachedThreadPool();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; totalThread; i++) &#123; </span><br><span class="line">            executorService.execute(() -&gt; &#123;</span><br><span class="line">            System.out.print(<span class="string">"run.."</span>);</span><br><span class="line">            countDownLatch.countDown(); &#125;);</span><br><span class="line">        &#125;</span><br><span class="line">            countDownLatch.await();     <span class="comment">//阻塞， 当countDown的计数器 cnt 值为0时 唤醒</span></span><br><span class="line">            System.out.println(<span class="string">"end"</span>);</span><br><span class="line">            executorService.shutdown(); &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//输出： run..run..run..run..run..run..run..run..run..run..end</span></span><br></pre></td></tr></table></figure>



<h2 id="CyclicBarrier"><a href="#CyclicBarrier" class="headerlink" title="CyclicBarrier"></a><strong>CyclicBarrier</strong></h2><p>用来控制<strong>多个线程互相等待</strong>，只有当多个线程都到达时，这些线程才会继续执行。</p>
<p>和 CountdownLatch 相似，都是通过<strong>维护计数器</strong>来实现的。线程执行 <strong>await() 方法之后计数器会减 1</strong>，并进行等待，直到<strong>计数器为 0</strong>，所有调用 await() 方法而在等待的线程才能继续执行。</p>
<p><strong>CyclicBarrier 和 CountdownLatch 的一个区别</strong>是，CyclicBarrier 的计数器通过调用 <strong>reset() 方法可以循环使用</strong>，所以</p>
<p>它才叫做循环屏障。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CyclicBarrierExample</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">int</span> totalThread = <span class="number">3</span>;</span><br><span class="line">        CyclicBarrier cyclicBarrier = <span class="keyword">new</span> CyclicBarrier(totalThread,<span class="keyword">new</span> Run());</span><br><span class="line">        ExecutorService executorService = Executors.newCachedThreadPool();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; totalThread; i++) &#123;</span><br><span class="line">            executorService.execute(() -&gt; &#123; System.out.print(<span class="string">"before.."</span>);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                    cyclicBarrier.await();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException | BrokenBarrierException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.print(<span class="string">"after.."</span>); &#125;);</span><br><span class="line">        &#125;</span><br><span class="line">        executorService.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Run</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"runnable"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//before..before..before..runnable</span></span><br><span class="line"><span class="comment">//after..after..after..</span></span><br></pre></td></tr></table></figure>

<p>上面的例子中，在计数器减为0之后，执行输出了’runnable’字样，这是为什么呢？</p>
<p>原来<code>CyclicBarrier</code>有两个构造方法:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="function"><span class="keyword">public</span> <span class="title">CyclicBarrier</span><span class="params">(<span class="keyword">int</span> parties, Runnable barrierAction)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (parties &lt;= <span class="number">0</span>) <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">        <span class="keyword">this</span>.parties = parties;</span><br><span class="line">        <span class="keyword">this</span>.count = parties;</span><br><span class="line">        <span class="keyword">this</span>.barrierCommand = barrierAction;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">CyclicBarrier</span><span class="params">(<span class="keyword">int</span> parties)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(parties, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>当调用<code>await</code>多次后计数器减为0，这时就会执行 <code>barrierAction</code>中的操作。</p>
<h1 id="线程为什么调用start-而不是直接调用run"><a href="#线程为什么调用start-而不是直接调用run" class="headerlink" title="线程为什么调用start()而不是直接调用run()"></a>线程为什么调用start()而不是直接调用run()</h1><p>run()方法只是一个类中的<strong>普通方法</strong>，直接执行和普通的方法没有设么两样。</p>
<p>start()方法则不同，它首先做了<strong>创建线程等一系列工作，然后调用行的run()方法</strong></p>
<p><strong>直接调用run方法 ，它并不会开启新的线程，而是在调用run的当前的线程当中执行操作。</strong></p>
<h1 id="什么是不可变对象，它对写并发应用有什么帮助？"><a href="#什么是不可变对象，它对写并发应用有什么帮助？" class="headerlink" title="什么是不可变对象，它对写并发应用有什么帮助？"></a>什么是不可变对象，它对写并发应用有什么帮助？</h1><h2 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h2><p>不可变类是其<strong>实例不能被修改的类</strong>。每个实例中包含的所有信息都必须在创建该实例的时候就提供，并且在对象的整个生命周期内<strong>固定不变</strong>。</p>
<h2 id="不可变的好处"><a href="#不可变的好处" class="headerlink" title="不可变的好处"></a>不可变的好处</h2><p><strong>不可变（Immutable）的对象一定是线程安全的，不需要再采取任何的线程安全保障措施</strong>。</p>
<p>只要一个不可变的对象被正确地构建出来，永远也不会看到它在多个线程之中处于不一致的状态。多线程环境下，应当<strong>尽量使对象成为不可变</strong>，来满足线程安全。</p>
<h2 id="不可变的类型："><a href="#不可变的类型：" class="headerlink" title="不可变的类型："></a>不可变的类型：</h2><ul>
<li><p>final 关键字修饰的<strong>基本数据类型</strong></p>
</li>
<li><p>String</p>
</li>
<li><p>枚举类型</p>
</li>
<li><p>所有基本类型的包装类都是不可变类，如 *<em>Long 和 Double 等 *</em>，以及 BigInteger 和 BigDecimal 等大数据类型。但</p>
</li>
</ul>
<p>原子类 AtomicInteger 和 AtomicLong 则是<strong>可变</strong>的。</p>
<ul>
<li>对于集合类型，可以使用 <code>Collections.unmodififiableXXX()</code> 方法来获取一个不可变的集合。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ImmutableExample</span> </span>&#123; </span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123; </span><br><span class="line">Map&lt;String, Integer&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;(); </span><br><span class="line">Map&lt;String, Integer&gt; unmodifiableMap = Collections.unmodifiableMap(map); </span><br><span class="line">unmodifiableMap.put(<span class="string">"a"</span>, <span class="number">1</span>); &#125;   <span class="comment">//此时会发生异常</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java并发</category>
      </categories>
      <tags>
        <tag>java并发</tag>
      </tags>
  </entry>
  <entry>
    <title>java集合</title>
    <url>/2020/08/26/java%E9%9B%86%E5%90%88/</url>
    <content><![CDATA[<h1 id="Java集合框架"><a href="#Java集合框架" class="headerlink" title="Java集合框架"></a>Java集合框架</h1><a id="more"></a>



<p><img src="https://beginnersbook.com/wp-content/uploads/2013/12/Java-collection-framework-hierarchy.png" alt=""></p>
<p><a href="https://imgchr.com/i/d2bB0f" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2020/08/26/d2bB0f.png" alt="d2bB0f.png"></a></p>
<h1 id="基本操作举例"><a href="#基本操作举例" class="headerlink" title="基本操作举例"></a>基本操作举例</h1><h2 id="collection类"><a href="#collection类" class="headerlink" title="collection类"></a>collection类</h2><ol>
<li>插入删除</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList&lt;Integer&gt; arr=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) </span><br><span class="line">   arr.add(i+<span class="number">1</span>);    <span class="comment">//插入（在末尾）</span></span><br><span class="line">arr.add(<span class="keyword">int</span> index, E element) ; <span class="comment">//在index处 插入element</span></span><br><span class="line">arr.remove();  <span class="comment">//删除末尾</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>遍历</li>
</ol>
<ul>
<li>迭代器遍历</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Iterator&lt;Integer&gt; it = arr.iterator();</span><br><span class="line">       <span class="keyword">while</span>(it.hasNext())&#123;</span><br><span class="line">           System.out.print(it.next());</span><br><span class="line">           System.out.print(<span class="string">" "</span>);</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>for each语句</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(Integer tmp:arr)</span><br><span class="line">          System.out.println(tmp);</span><br></pre></td></tr></table></figure>



<h2 id="Map类"><a href="#Map类" class="headerlink" title="Map类"></a>Map类</h2><p>Map中插入的是 key-value对。</p>
<ol>
<li>插入</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> Map&lt;Integer, Integer&gt; map = <span class="keyword">new</span> HashMap&lt;Integer, Integer&gt;()&#123;</span><br><span class="line"> &#123; put(<span class="number">1</span>, <span class="number">10</span>); put(<span class="number">2</span>, <span class="number">20</span>); &#125; <span class="comment">//用 括号 括起来</span></span><br><span class="line"> &#125;;   <span class="comment">//在创建的时候就初始化的方法</span></span><br><span class="line">map.get(Object key) ;</span><br><span class="line">map.remove(Object key) ;</span><br><span class="line">map.keySet();   <span class="comment">//key集合</span></span><br><span class="line">map.values();  <span class="comment">//值集合</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>遍历</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//方式1  for Each</span></span><br><span class="line"><span class="keyword">for</span> (Map.Entry&lt;Integer, Integer&gt; entry : map.entrySet()) </span><br><span class="line">	System.out.println(<span class="string">"Key = "</span> + entry.getKey() + <span class="string">", Value = "</span> + entry.getValue());</span><br><span class="line"><span class="comment">/*entrySet() ：返回此映射中包含的映射关系的 Set 视图。*/</span>		</span><br><span class="line"></span><br><span class="line"><span class="comment">//方式2</span></span><br><span class="line">Iterator&lt;Map.Entry&lt;Integer, Integer&gt;&gt; entries = map.entrySet().iterator();</span><br><span class="line"> <span class="keyword">while</span> (entries.hasNext()) &#123;</span><br><span class="line">	 Map.Entry&lt;Integer, Integer&gt; entry = entries.next();</span><br><span class="line">	 System.out.println(<span class="string">"Key = "</span> + entry.getKey() + <span class="string">", Value = "</span> + entry.getValue());</span><br><span class="line">		&#125;</span><br><span class="line"><span class="comment">/*迭代器是在 entryset里*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//方式3 Lambda表达式+ map.forEach</span></span><br><span class="line">map.forEach((k, v) -&gt; System.out.println(<span class="string">"key: "</span> + k + <span class="string">" value:"</span> + v));</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">//方式4 遍历key 或者 value</span></span><br><span class="line"> <span class="keyword">for</span> (Integer key : map.keySet()) </span><br><span class="line">            System.out.println(<span class="string">"Key = "</span> + key);</span><br><span class="line">  <span class="keyword">for</span>(Integer i:map.values())</span><br><span class="line">            System.out.println(<span class="string">"values= "</span>+i);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>值得一提的是，Map是一个数组结构，根据key计算 index ，在数组index中存入value。</li>
</ol>
<p>index的计算：由key得到hashCode，由hashCode计算index。</p>
<h1 id="Collections中常用方法"><a href="#Collections中常用方法" class="headerlink" title="Collections中常用方法"></a>Collections中常用方法</h1><ol>
<li>排序</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList&lt;Integer&gt; list=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">Collections.addAll(list,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>);</span><br><span class="line">       Collections.sort(list, <span class="keyword">new</span> Comparator&lt;Integer&gt;() &#123;  <span class="comment">//创建匿名内部类</span></span><br><span class="line">           <span class="meta">@Override</span></span><br><span class="line">           <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Integer o1, Integer o2)</span> </span>&#123;</span><br><span class="line">               <span class="keyword">return</span> o2-o1;   <span class="comment">//第二个减去第一个，降序</span></span><br><span class="line">                               <span class="comment">//第一个减去第二个，升序</span></span><br><span class="line">           &#125;</span><br><span class="line">       &#125;);</span><br><span class="line"></span><br><span class="line">Collections.sort(list, (i1,i2)-&gt;&#123;<span class="keyword">return</span> i1-i2;&#125;);  <span class="comment">//使用 lambda表达式</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>最值</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer max = Collections.max(list);</span><br><span class="line">Integer min = Collections.min(list);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>反转</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span>  Collections.reverse(list);   <span class="comment">//将集合反转</span></span><br></pre></td></tr></table></figure>

<ol start="4">
<li>随机打乱</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span>  Collections.shuffle(list);   <span class="comment">//将集合反转</span></span><br></pre></td></tr></table></figure>



<p><a href="https://imgchr.com/i/dW8JDe" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2020/08/26/dW8JDe.jpg" alt="dW8JDe.jpg" border="0" /></a></p>
<h1 id="详解"><a href="#详解" class="headerlink" title="详解"></a>详解</h1><h2 id="Map"><a href="#Map" class="headerlink" title="Map"></a>Map</h2><figure class="highlight dts"><table><tr><td class="code"><pre><span class="line">可重复性： key 均不可重复</span><br><span class="line">key是否可为Null：</span><br><span class="line"><span class="symbol">        HashMap:</span>   可   (只允许一个key为NULL，多个不会报错，但是会覆盖）</span><br><span class="line"><span class="symbol">        LinkedHashMap:</span> 可  (只允许一个key为NULL，多个不会报错，但是会覆盖）</span><br><span class="line"><span class="symbol">        Hashtable:</span>   不可</span><br><span class="line"><span class="symbol">        TreeMap:</span>     不可</span><br><span class="line"></span><br><span class="line">排序性：</span><br><span class="line"><span class="symbol">        HashMap:</span>   无序</span><br><span class="line"><span class="symbol">        Hashtable:</span>   无序</span><br><span class="line"><span class="symbol">        LinkedHashMap:</span> 按照插入顺序  （因为有链表辅助）</span><br><span class="line"><span class="symbol">        TreeMap:</span>     按照key升序     （因为结构是红黑树）</span><br></pre></td></tr></table></figure>

<h3 id="HashTable不允许-NULL作为键值原因："><a href="#HashTable不允许-NULL作为键值原因：" class="headerlink" title="HashTable不允许 NULL作为键值原因："></a>HashTable不允许 NULL作为键值原因：</h3><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line"><span class="keyword">To</span> successfully store <span class="keyword">and</span> retrieve objects <span class="keyword">from</span> a Hashtable, the objects used <span class="keyword">as</span> </span><br><span class="line">keys must implement the hashCode <span class="function"><span class="keyword">method</span> <span class="title">and</span> <span class="title">the</span> <span class="title">equals</span> <span class="title">method</span>.</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">任何非 <span class="title">null</span> 对象都可以用作键或值。</span></span><br><span class="line"><span class="function">为了成功地在哈希表中存储和获取对象，用作键的对象必须实现 <span class="title">hashCode</span> 方法和 <span class="title">equals</span> 方法. <span class="title">null</span>不是对象，</span></span><br><span class="line"><span class="function">不可以重写从<span class="title">Object</span>继承的  .<span class="title">equals</span><span class="params">()</span> <span class="title">or</span> .<span class="title">hashCode</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">Since</span> <span class="title">null</span> <span class="title">isn</span>'<span class="title">t</span> <span class="title">an</span> <span class="title">object</span>, <span class="title">you</span> <span class="title">can</span>'<span class="title">t</span> <span class="title">call</span> .<span class="title">equals</span><span class="params">()</span> <span class="title">or</span> .<span class="title">hashCode</span><span class="params">()</span> <span class="title">on</span> <span class="title">it</span>, <span class="title">so</span> <span class="title">the</span></span></span><br><span class="line"><span class="function"><span class="title">Hashtable</span> <span class="title">can</span>'<span class="title">t</span> <span class="title">compute</span> <span class="title">a</span> <span class="title">hash</span> <span class="title">to</span> <span class="title">use</span> <span class="title">it</span> <span class="title">as</span> <span class="title">a</span> <span class="title">key</span>.</span></span><br></pre></td></tr></table></figure>



<h3 id="HashMap-详解"><a href="#HashMap-详解" class="headerlink" title="HashMap 详解"></a>HashMap 详解</h3><ul>
<li><p><strong>容量</strong> 和 <strong>加载因子</strong></p>
<p>HashMap 的实例有两个参数影响其性能：<strong>初始容量</strong> 和<strong>加载因子</strong>。</p>
<p>容量 是哈希表中<strong>桶的数量</strong>，初始容量只是哈希表在创建时的容量。</p>
<p><strong>加载因子</strong> 是哈希表在其容量自动增加之前可以达到<strong>多满的一种尺度</strong>。当哈希表中的条目数超出了加载因子与当前容量的乘积时，则要对该哈希表进行 rehash 操作（即重建内部数据结构），从而哈希表将具有大约<strong>两倍</strong>的桶数。 (容量一般是$2^n$大小，可以使<strong>散列更加均匀</strong>)</p>
<p>通常，默认加载因子 (.75) 在时间和空间成本上寻求一种折衷。加载因子过高虽然减少了空间开销，但同时也增加了查询成本.</p>
</li>
<li><p><strong>Java8 的优化</strong></p>
<p>HashMap 在 JDK1.8 的版本中引入了<strong>红黑树结构</strong>做优化，当链表元素个数<strong>大于等于 8</strong> 时，链表转 换成<strong>树结构</strong>；若桶中链表元素个数<strong>小于等于 6 时</strong>，树结构还<strong>原成链表</strong>。因为红黑树的平均查找长 度是 log(n)，长度为 8 的时候，平均查找长度为 3，如果继续使用链表，平均查找长度为 <strong>8/2=4</strong>， 这才有转换为树的必要。链表长度如果是小于等于 6，6/2=3，虽然速度也很快的，转化为 树结构和生成树的时间并不会太短。还有选择 6 和 8，中间有<strong>个差值 7 可以有效防止链表和树频 繁转换</strong>。</p>
</li>
</ul>
<h3 id="ConcurentMap"><a href="#ConcurentMap" class="headerlink" title="ConcurentMap"></a>ConcurentMap</h3><ul>
<li><p>HashMap<strong>非线程安全</strong>。</p>
</li>
<li><p><code>ConcurentMap</code>操作都是<strong>线程安全</strong>的。</p>
</li>
</ul>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line">HashMap <span class="keyword">is</span> non-Synchronized <span class="keyword">in</span> nature. HashMap <span class="keyword">is</span> <span class="keyword">not</span> Thread-safe whereas ConcurrentHashMap </span><br><span class="line"><span class="keyword">is</span> Thread-safe <span class="keyword">in</span> nature.</span><br><span class="line"></span><br><span class="line">HashMap performance <span class="keyword">is</span> relatively high because it <span class="keyword">is</span> non-synchronized <span class="keyword">in</span> nature <span class="keyword">and</span> <span class="keyword">any</span></span><br><span class="line">number <span class="keyword">of</span> threads can <span class="keyword">perform</span> simultaneously（同时）. But ConcurrentHashMap performance <span class="keyword">is</span> </span><br><span class="line">low sometimes because sometimes Threads are required <span class="keyword">to</span> wait <span class="keyword">on</span> ConcurrentHashMap.</span><br><span class="line"></span><br><span class="line"><span class="keyword">While</span> one thread <span class="keyword">is</span> Iterating the HashMap <span class="keyword">object</span>, <span class="keyword">if</span> other thread try <span class="keyword">to</span> <span class="keyword">add</span>/modify the</span><br><span class="line">contents <span class="keyword">of</span> <span class="keyword">Object</span> <span class="keyword">then</span> we will <span class="keyword">get</span> Run-<span class="type">time</span> <span class="keyword">exception</span> saying ConcurrentModificationException.</span><br><span class="line">Whereas <span class="keyword">In</span> ConcurrentHashMap we wont <span class="keyword">get</span> <span class="keyword">any</span> <span class="keyword">exception</span> <span class="keyword">while</span> performing <span class="keyword">any</span> modification at the <span class="type">time</span> <span class="keyword">of</span> Iteration.</span><br></pre></td></tr></table></figure>



<p>HashMap迭代时候 修改 会 抛出异常 ， ConcurentMap 不会，因为它线程安全</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConCurentHashMapTest</span> <span class="keyword">extends</span> <span class="title">Thread</span> </span>&#123;</span><br><span class="line">        <span class="keyword">static</span> ConcurrentHashMap&lt;Integer,String&gt; l =   <span class="keyword">new</span> ConcurrentHashMap&lt;Integer,String&gt;();</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            l.put(<span class="number">103</span>,<span class="string">"D"</span>);</span><br><span class="line">            <span class="keyword">try</span></span><br><span class="line">            &#123;</span><br><span class="line">                Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">catch</span>(InterruptedException e)</span><br><span class="line">            &#123;</span><br><span class="line">                System.out.println(<span class="string">"Child Thread going to add element"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException</span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            l.put(<span class="number">100</span>,<span class="string">"A"</span>);</span><br><span class="line">            l.put(<span class="number">101</span>,<span class="string">"B"</span>);</span><br><span class="line">            l.put(<span class="number">102</span>,<span class="string">"C"</span>);</span><br><span class="line">            ConCurentHashMapTest t=<span class="keyword">new</span> ConCurentHashMapTest();</span><br><span class="line">            t.start();</span><br><span class="line">            <span class="keyword">for</span> (Object o : l.entrySet())</span><br><span class="line">            &#123;</span><br><span class="line">                Object s=o;</span><br><span class="line">                System.out.println(s);</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line">输出：</span><br><span class="line">    <span class="number">100</span>=A</span><br><span class="line">    <span class="number">101</span>=B</span><br><span class="line">    <span class="number">102</span>=C</span><br><span class="line">    <span class="number">103</span>=D</span><br></pre></td></tr></table></figure>

<p>把<code>ConcurrentHashMap</code>换成<code>HashMap</code>结果如下</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">Exception <span class="keyword">in</span> thread <span class="string">"main"</span> java<span class="selector-class">.util</span>.ConcurrentModificationException</span><br><span class="line">同时修改异常</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="支持并发的原理："><a href="#支持并发的原理：" class="headerlink" title="支持并发的原理："></a><strong>支持并发的原理：</strong></h4><p>​     整个 ConcurrentHashMap 由一个个 <strong>Segment</strong> 组成，Segment 代表<strong>”部分“或”一段“</strong>的意思，所以很多地方都会将其描述为<strong>分段锁</strong>。</p>
<p>Segment<strong>继承</strong> 了<strong>ReentrantLock</strong> 。</p>
<p>​    简单理解就是，ConcurrentHashMap 是一个 <strong>Segment 数组</strong>，<strong>Segment 通过继承 ReentrantLock 来进行加锁</strong>，所以每次需要加锁的操作<strong>锁住的是一个 segment</strong>，这样只要保证每个 Segment 是线程安全的，也就实现了全局的线程安全。</p>
<p>​    <strong>concurrencyLevel</strong>：并行级别、并发数、Segment 数， 默认是 16。也就是说 ConcurrentHashMap 有 16 个 Segments，所以理论上，这个时候，最多可以同时支持 <strong>16 个线程并发写</strong>，只要它们的操作分别分布在不同的 Segment 上。这个值可以在初始化的时</p>
<p>候设置为其他值，但是<strong>一旦初始化以后，它是不可以扩容的</strong>。</p>
<h3 id="TreeMap"><a href="#TreeMap" class="headerlink" title="TreeMap"></a>TreeMap</h3><p>TreeMap 的实现就是<strong>红黑树</strong>数据结构，也就说是一棵<strong>自平衡的排序二叉树</strong>，这样就可以保证 当需要快速检索指定节点。 </p>
<p>红黑树的插入、删除、遍历时间复杂度都为 O(lgN)，所以<strong>性能上低于哈希表</strong>。但是哈希表 无法提供键值对的有序输出，红黑树因为是排序插入的，可以按照键的值的大小有序输出。 </p>
<h2 id="list"><a href="#list" class="headerlink" title="list"></a>list</h2><h3 id="1-ArrayList"><a href="#1-ArrayList" class="headerlink" title="1. ArrayList"></a>1. ArrayList</h3><p>ArrayList 是最常用的 List 实现类，内部是通过数组实现的，它允许对元素进行快速随机访问。数组的缺点是每个元素之间不能有间隔，当数组大小不满足时需要增加存储能力，就要将已经有数组的数据复制到新的存储空间中。当从 ArrayList 的中间位置插入或者删除元素时，需要对数组进行复制、移动、代价比较高。因此，它适合随机查找和遍历，不适合插入和删除。</p>
<h3 id="2-LinkedList"><a href="#2-LinkedList" class="headerlink" title="2. LinkedList"></a>2. LinkedList</h3><p>LinkedList 是用链表结构存储数据的，很适合数据的动态插入和删除，随机访问和遍历速度比较慢。另外，他还提供了 List 接口中没有定义的方法，专门用于操作<strong>表头和表尾</strong>元素，可以当作 <strong>栈、队列</strong>和<strong>双向队列</strong>使用</p>
<ul>
<li>eg.</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        LinkedList&lt;Integer&gt; queue = <span class="keyword">new</span> LinkedList&lt;&gt;();   <span class="comment">//用作队列</span></span><br><span class="line">        LinkedList&lt;Integer&gt; stack = <span class="keyword">new</span> LinkedList&lt;&gt;();  <span class="comment">//用作栈</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) </span><br><span class="line">            queue.add(i);  </span><br><span class="line">        System.out.println(<span class="string">"queue============================"</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) </span><br><span class="line">            System.out.println(queue.removeFirst());        </span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) </span><br><span class="line">            stack.add(i);</span><br><span class="line">    </span><br><span class="line">        System.out.println(<span class="string">"stack============================"</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) </span><br><span class="line">            System.out.println(stack.removeLast());     </span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">peekFirst() :	获取 但 不移除 列表的 第一 个元素； </span></span><br><span class="line"><span class="comment">peekLast() : 	获取 但 不移除 列表的 最后 一个元素； </span></span><br><span class="line"><span class="comment">removeFirst():	获取 且 移除   列表的 第一 个元素</span></span><br><span class="line"><span class="comment">removeLast(): 	获取 且 移除   列表的 最后 一个元素</span></span><br><span class="line"><span class="comment">addFirst(E e)： 将指定元素插入列表的 开头 。</span></span><br><span class="line"><span class="comment">add(E e):插入列表 末尾</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure>



<h2 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h2><p>Set 注重<strong>独一无二</strong>的性质,即：存储的元素没有 重复。</p>
<p>用于存储<strong>无序</strong>(存入和取出的顺序不一定相同)元素。对象的相等性本质是对象 <code>hashCode</code> 值 （默认根据<strong>对象地址</strong>计算）判断的。如果想要让<strong>两个不同的对象视为相等的</strong>，就必须覆盖 Object 的 <code>hashCode</code> 方法和 <code>equals</code> 方法。</p>
<h3 id="1-HashSet"><a href="#1-HashSet" class="headerlink" title="1. HashSet"></a>1. HashSet</h3><ul>
<li><p>HashSet 首先判断两个<strong>元素的哈希值</strong>，如果哈希值一样，接着会<strong>比较equals</strong> 方法 如果 equls 结果为 true ，HashSet 就视为同一个元素。如果 equals 为 false 就不是同一个元素。</p>
</li>
<li><p>哈希值相同 equals 为 false 的元素存储：  存在<strong>同一个哈希桶</strong>中。</p>
</li>
</ul>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line"><span class="builtin-name">add</span>(E e) 如果此 <span class="builtin-name">set</span> 中尚未包含指定元素，则添加指定元素。          </span><br><span class="line">boolean isEmpty()  如果此 <span class="builtin-name">set</span> 不包含任何元素，则返回 <span class="literal">true</span>。  </span><br><span class="line">boolean <span class="builtin-name">remove</span>(Object o)  如果指定元素存在于此 <span class="builtin-name">set</span> 中，则将其移除。</span><br></pre></td></tr></table></figure>



<h3 id="2-LinkedHashSet"><a href="#2-LinkedHashSet" class="headerlink" title="2. LinkedHashSet"></a>2. LinkedHashSet</h3><ul>
<li><p><code>LinkedHashSet</code>保证输出按照插入的顺序，而 <code>hashSet</code>无序，这是最大不同，其余相同。</p>
</li>
<li><p>它继承与 HashSet、又基于 LinkedHashMap 来实现的。</p>
</li>
</ul>
<h3 id="3-TreeSet"><a href="#3-TreeSet" class="headerlink" title="3. TreeSet"></a>3. TreeSet</h3><ul>
<li><p>TreeSet()是使用<strong>二叉排序树</strong>的原理对新 add()的对象按照指定的顺序排序（升序、降序），每增加一个对象都会进行排序，将对象插入的二叉树指定的位置。</p>
</li>
<li><p>Integer 和 String 对象都可以进行默认的 TreeSet 排序。</p>
</li>
<li><p>自定义的类必须实现<code>Comparable</code> 接口，并且覆写相应的 <code>compareTo()</code>函数，才可以正常使用。</p>
</li>
<li><p><strong>此实现不是线程同步的</strong></p>
</li>
</ul>
<ul>
<li>eg:TreeSet使用自定义类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PersonForTreeset</span> <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">PersonForTreeset</span>&gt;</span>&#123;  <span class="comment">//实现 Comparable接口</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(PersonForTreeset personForTreeset)</span> </span>&#123;  <span class="comment">//重写 compareTo</span></span><br><span class="line">        <span class="keyword">return</span> personForTreeset.age-<span class="keyword">this</span>.age;    </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object o)</span> </span>&#123;        <span class="comment">//hashcode和equals作为判断 是否 等价的依据</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span> == o) <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">if</span> (o == <span class="keyword">null</span> || getClass() != o.getClass()) <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        PersonForTreeset that = (PersonForTreeset) o;</span><br><span class="line">        <span class="keyword">return</span> age == that.age &amp;&amp;</span><br><span class="line">                Objects.equals(name, that.name);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Objects.hash(age, name);</span><br><span class="line">    &#125;</span><br><span class="line"> <span class="comment">//省略构造方法 、 toString等非重点代码</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestTreeSet</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        TreeSet&lt;PersonForTreeset&gt; treeset = <span class="keyword">new</span> TreeSet&lt;&gt;();</span><br><span class="line">        treeset.add(<span class="keyword">new</span> PersonForTreeset(<span class="number">2</span>,<span class="string">"d"</span>));</span><br><span class="line">        treeset.add(<span class="keyword">new</span> PersonForTreeset(<span class="number">3</span>,<span class="string">"b"</span>));</span><br><span class="line">        treeset.add(<span class="keyword">new</span> PersonForTreeset(<span class="number">5</span>,<span class="string">"b"</span>));</span><br><span class="line">        treeset.add(<span class="keyword">new</span> PersonForTreeset(<span class="number">5</span>,<span class="string">"b"</span>));</span><br><span class="line"></span><br><span class="line">        System.out.println(treeset);     </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">输出;</span><br><span class="line">[PersonForTreeset&#123;age=<span class="number">5</span>, name=<span class="string">'b'</span>&#125;, PersonForTreeset&#123;age=<span class="number">3</span>, name=<span class="string">'b'</span>&#125;, PersonForTreeset&#123;age=<span class="number">2</span>, name=<span class="string">'d'</span>&#125;]</span><br><span class="line"><span class="comment">//不包含等价对象， 按照对应队则compareTo输出</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>Java基础小结</title>
    <url>/2020/08/07/java%E5%9F%BA%E7%A1%80%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h2 id="IDEA快捷操作"><a href="#IDEA快捷操作" class="headerlink" title="IDEA快捷操作"></a>IDEA快捷操作</h2><table>
<thead>
<tr>
<th><strong>ctrl+Y</strong></th>
<th align="left"><strong>删除一行</strong></th>
</tr>
</thead>
<tbody><tr>
<td><strong>ctrl+D</strong></td>
<td align="left"><strong>复制一行</strong></td>
</tr>
<tr>
<td><strong>ctrl+/</strong></td>
<td align="left"><strong>快速注释</strong></td>
</tr>
<tr>
<td><strong>Ctrl+shift+/</strong></td>
<td align="left"><strong>多行注释</strong></td>
</tr>
<tr>
<td>alt+insert</td>
<td align="left">插入函数</td>
</tr>
<tr>
<td><strong>alt+shift+方向键</strong></td>
<td align="left"><strong>整行上下移动</strong></td>
</tr>
<tr>
<td>alt+enter</td>
<td align="left">导入包依赖</td>
</tr>
<tr>
<td><strong>shift+f6</strong></td>
<td align="left"><strong>重命名文件</strong></td>
</tr>
</tbody></table>
<a id="more"></a>



<ul>
<li><code>5.fori</code>自动生成<code>for(int i=0;i&lt;5;i++)</code>循环体</li>
<li><code>psvm</code>-&gt;<code>public static void main()</code></li>
<li><code>数组名.fori</code>+回车-&gt; 遍历数组</li>
</ul>
<h2 id="输入输出"><a href="#输入输出" class="headerlink" title="输入输出"></a>输入输出</h2><h3 id="输入Scanner"><a href="#输入Scanner" class="headerlink" title="输入Scanner"></a>输入Scanner</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Scanner sc = <span class="keyword">new</span> Scanner(System.in);</span><br><span class="line">String str = sc.nextLine();; <span class="comment">//整整一行，遇到空格不结束</span></span><br><span class="line">String str = sc.next(); <span class="comment">//遇到空格结束</span></span><br><span class="line"><span class="keyword">int</span> num = sc.nextInt();  <span class="comment">//整数</span></span><br></pre></td></tr></table></figure>

<h3 id="输出"><a href="#输出" class="headerlink" title="输出"></a>输出</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">System.out.println(...);  <span class="comment">//输出内容搭配屏幕 并且 换行</span></span><br><span class="line">System.out.print(...);  <span class="comment">//输出内容 但 不换行</span></span><br><span class="line">System.out.printf(<span class="string">"6.3f"</span>,<span class="number">3.24555</span>d);<span class="comment">//控制输出格式： 这里是： 占6位，小数点后保留3位</span></span><br></pre></td></tr></table></figure>



<h2 id="常用集合使用"><a href="#常用集合使用" class="headerlink" title="常用集合使用"></a>常用集合使用</h2><h3 id="Stack"><a href="#Stack" class="headerlink" title="Stack"></a>Stack</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Stack&lt;Integer&gt; s = <span class="keyword">new</span> Stack&lt;&gt;();  <span class="comment">//  &lt;&gt;里面只能写封装类型</span></span><br><span class="line">s.push();   <span class="comment">//将元素压入栈</span></span><br><span class="line">s.pop();   <span class="comment">//获取并弹出栈顶值</span></span><br><span class="line">s.peek();  <span class="comment">//获取栈顶值 但不弹出</span></span><br><span class="line">s.empty();  <span class="comment">//是否为空</span></span><br></pre></td></tr></table></figure>

<h3 id="Queue"><a href="#Queue" class="headerlink" title="Queue"></a>Queue</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Queue&lt;Integer&gt; q = <span class="keyword">new</span> LinkedList&lt;&gt;();  <span class="comment">//new的是一个 linkedList</span></span><br><span class="line">q.add();   <span class="comment">//添加元素</span></span><br><span class="line">q.remove(); <span class="comment">//删除队首元素</span></span><br><span class="line">q.peek();  <span class="comment">//获取但不删除队首元素</span></span><br><span class="line">q.poll();   <span class="comment">//获取且删除队首元素</span></span><br><span class="line">q.isEmpty(); <span class="comment">//判断是否为空</span></span><br></pre></td></tr></table></figure>

<h3 id="ArrayList"><a href="#ArrayList" class="headerlink" title="ArrayList"></a>ArrayList</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">list.add();</span><br><span class="line">list.remove(<span class="keyword">int</span> index);</span><br><span class="line">list.remove(object o); <span class="comment">//移除此列表中首次出现的指定元素</span></span><br><span class="line">list.size();  <span class="comment">//集合的大小</span></span><br><span class="line">list.toArray();<span class="comment">//转变为数组</span></span><br><span class="line">list.isEmpty(); <span class="comment">//是否为空</span></span><br></pre></td></tr></table></figure>

<h3 id="Collections中的方法"><a href="#Collections中的方法" class="headerlink" title="Collections中的方法"></a>Collections中的方法</h3><ol>
<li>排序</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ArrayList&lt;Integer&gt; list=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">Collections.addAll(list,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>);</span><br><span class="line"></span><br><span class="line">       Collections.sort(list, <span class="keyword">new</span> Comparator&lt;Integer&gt;() &#123;  <span class="comment">//创建匿名内部类</span></span><br><span class="line">           <span class="meta">@Override</span></span><br><span class="line">           <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Integer o1, Integer o2)</span> </span>&#123;</span><br><span class="line">               <span class="keyword">return</span> o2-o1;   <span class="comment">//第二个减去第一个，降序</span></span><br><span class="line">                               <span class="comment">//第一个减去第二个，升序</span></span><br><span class="line">           &#125;</span><br><span class="line">       &#125;);</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>最值</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer max = Collections.max(list);</span><br><span class="line">Integer min = Collections.min(list);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>反转</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span>  Collections.reverse(list);   <span class="comment">//将集合反转</span></span><br></pre></td></tr></table></figure>

<ol start="4">
<li>随机打乱</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span>  Collections.shuffle(list);   <span class="comment">//将集合反转</span></span><br></pre></td></tr></table></figure>

<h3 id="Arrays中的方法"><a href="#Arrays中的方法" class="headerlink" title="Arrays中的方法"></a>Arrays中的方法</h3><p><code>Arrays</code>针对的操作对象是数组。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">E [] arr = <span class="keyword">new</span> E[<span class="number">100</span>];</span><br><span class="line">Arrays.sort(<span class="keyword">int</span> []arr);   <span class="comment">//数组正序排列</span></span><br><span class="line">Arrays.fill(E []arr, E val); <span class="comment">//用 val填充 arr数组 </span></span><br><span class="line">Arrays.equals(E a,E b) ;  <span class="comment">//判断 a b 是否相等</span></span><br><span class="line">Arrays.toString(arr) ;  <span class="comment">// 把数组转换为 String类型</span></span><br><span class="line">Arrays.binarySearch(E []arr, <span class="keyword">int</span> val) <span class="comment">//二分法查找val在arr中对应的下标</span></span><br></pre></td></tr></table></figure>



<h2 id="常用类"><a href="#常用类" class="headerlink" title="常用类"></a>常用类</h2><h3 id="Random产生随机数"><a href="#Random产生随机数" class="headerlink" title="Random产生随机数"></a>Random产生随机数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> Random r = <span class="keyword">new</span> Random();</span><br><span class="line">        <span class="keyword">int</span> num = r.nextInt();</span><br><span class="line">        System.out.println(num);</span><br><span class="line"> <span class="comment">/*匿名：*/</span>  System.out.println(<span class="keyword">new</span> Random().nextDouble());</span><br><span class="line"></span><br><span class="line"><span class="comment">/*指定范围*/</span> r.nextInt(<span class="number">10</span>); <span class="comment">//0-10之间的随机数</span></span><br></pre></td></tr></table></figure>

<h3 id="String类"><a href="#String类" class="headerlink" title="String类"></a>String类</h3><ul>
<li>String内容<strong>不可改变</strong>，属于<strong>常量</strong></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String s=<span class="string">"123455,6"</span>;</span><br><span class="line"></span><br><span class="line">s.substring(<span class="number">1</span>,<span class="number">4</span>); <span class="comment">//结果是 "234"  是左闭右开（即左边下标对应元素可取到，右边取不到)</span></span><br><span class="line">String [] res = s.split(<span class="string">","</span>);  <span class="comment">//以 响应的符号为标志 分割字符串</span></span><br><span class="line"></span><br><span class="line">s1+s2; <span class="comment">//字符串可以直接进行+操作</span></span><br><span class="line"><span class="keyword">boolean</span> s1.equals(s2);  <span class="comment">// 默认的 equals方法是比较内容是否相同,而"==“ 是地址是否相同</span></span><br><span class="line"></span><br><span class="line">s.charAt(<span class="keyword">int</span> index);  <span class="comment">//s在index处的元素</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//转变为Integer类型</span></span><br><span class="line">Integer.parseInt(<span class="string">"2323"</span>);</span><br></pre></td></tr></table></figure>

<p><img src="https://pic2.zhimg.com/80/v2-d9dc4362f0334802413e3a3ff2f53c2e_1440w.jpg?source=1940ef5c" alt="equals和==的区别"></p>
<h4 id="其他类型到String的转换"><a href="#其他类型到String的转换" class="headerlink" title="其他类型到String的转换"></a>其他类型到String的转换</h4><ul>
<li>例如： int i-123; 转换为String类型，有以下方式<ul>
<li><code>String s = i+&quot;&quot;</code></li>
<li><code>String s = String.valueof(i);</code></li>
</ul>
</li>
</ul>
<h4 id="其他类型到String"><a href="#其他类型到String" class="headerlink" title="其他类型到String"></a>其他类型到String</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String s=<span class="string">"123"</span>;</span><br><span class="line">Integer.parseInt(s);   <span class="comment">//转换为Integer</span></span><br><span class="line">Float.parseFloat(s);  <span class="comment">//转化为Float</span></span><br></pre></td></tr></table></figure>

<h4 id="StringBuilder"><a href="#StringBuilder" class="headerlink" title="StringBuilder"></a>StringBuilder</h4><ul>
<li>String需要频繁操作时，效率十分低下， 这时候应该采用 <code>StringBuilder</code>。</li>
</ul>
<p>​    <code>StringBuilder</code>是 一个可变的字符序列。此类提供一个与 <code>StringBuffer</code> 兼容的 API，但不保证同步</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//StringBuilder功能十分强大</span></span><br><span class="line">StringBuilder stb = <span class="keyword">new</span> StringBuilder(<span class="string">"aaaa"</span>);  <span class="comment">//构造</span></span><br><span class="line">stb.append(o);<span class="comment">//括号内可添加 int  char  boolean  String byte short等各种类型数据</span></span><br><span class="line">stb.deleteCharAt(<span class="keyword">int</span> inex);   <span class="comment">//删除索引处的字符</span></span><br><span class="line">stb.toString();  <span class="comment">//最后转换成String类型</span></span><br></pre></td></tr></table></figure>

<h3 id="Integer类"><a href="#Integer类" class="headerlink" title="Integer类"></a>Integer类</h3><ul>
<li>自动装箱拆箱,可直接与 基本数据类型 int进行 大小比较 ，加减操作</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer ii=<span class="number">12</span>;</span><br><span class="line">    System.out.println(ii+<span class="number">11</span>);</span><br><span class="line">    <span class="keyword">int</span> res = ii-<span class="number">13</span>;</span><br><span class="line">    System.out.println(res);</span><br></pre></td></tr></table></figure>

<h3 id="时间戳和Date类"><a href="#时间戳和Date类" class="headerlink" title="时间戳和Date类"></a>时间戳和Date类</h3><ol>
<li>时间戳</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">System.currentTimeMillis()  <span class="comment">//返回long类型数据 ，是从1970.01.01到运行时的 ms数值</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>Date使用</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取指定类型日期 </span></span><br><span class="line"><span class="keyword">long</span> val = System.currentTimeMillis()-<span class="number">100000000</span>;</span><br><span class="line"> Date date = <span class="keyword">new</span> Date(val);  <span class="comment">//构造date， val是ms值</span></span><br><span class="line"> System.out.println(date);   <span class="comment">//输出date  Thu Aug 06 13:14:50 CST 2020</span></span><br><span class="line"> SimpleDateFormat sdf = <span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-dd "</span>); <span class="comment">//指定解析格式</span></span><br><span class="line"> String s=sdf.format(date);    <span class="comment">//解析成为字符串</span></span><br><span class="line"> System.out.println(s);</span><br><span class="line"></span><br><span class="line"><span class="comment">//format相反的操作是parse</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">//根据字符串获取指定的Date和ms值</span></span><br><span class="line"> SimpleDateFormat sdf=<span class="keyword">new</span> SimpleDateFormat(<span class="string">"yyyy-MM-dd"</span>);</span><br><span class="line"> Date BirthDate = sdf.parse(BirthDaytxt);    <span class="comment">//将输入的String类型的数据解析成 Date类型</span></span><br><span class="line"> <span class="keyword">long</span> birthDateTime = BirthDate.getTime();   <span class="comment">//对应的ms值</span></span><br></pre></td></tr></table></figure>



<h2 id="继承方式与访问权限"><a href="#继承方式与访问权限" class="headerlink" title="继承方式与访问权限"></a>继承方式与访问权限</h2><ol>
<li>java中都是<strong>单继承</strong>的，即：每个子类只有<strong>一个直接父类</strong>， 但是可以<strong>多重继承</strong>，即垂直继承。单继承可以避免出现菱形继承的问题， 避免数据出现混乱。</li>
<li>权限：<code>public&gt;protected&gt;default&gt;private</code></li>
</ol>
<p><img src="https://uploadfiles.nowcoder.com/files/20170518/6589111_1495116648005_163162-20160121105831078-570590712.png" alt=""></p>
]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java注解</title>
    <url>/2020/08/30/java%E6%B3%A8%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="什么是注解"><a href="#什么是注解" class="headerlink" title="什么是注解"></a>什么是注解</h1><p>​    Annotation（注解）是 Java 提供的一种<strong>对元程序中元素关联信息和元数据</strong>（metadata）的途径和方法。Annatation(注解)是一个<strong>接口</strong>，程序可以通过<strong>反射</strong>来获取指定程序中元素的 <strong>Annotation对象</strong>，然后<strong>通过该 Annotation 对象来获取注解中的元数据信息</strong>。</p>
<a id="more"></a>

<p>注解对<strong>运行代码的操作没有影响</strong>。 典型用例是：</p>
<ul>
<li><strong>在编译时进行格式检查</strong> 。 </li>
<li><strong>代替配置文件</strong>等。</li>
<li><strong>生成文档</strong>。</li>
</ul>
<h1 id="Java中的注解"><a href="#Java中的注解" class="headerlink" title="Java中的注解"></a>Java中的注解</h1><h2 id="用于编译的注解"><a href="#用于编译的注解" class="headerlink" title="用于编译的注解"></a>用于编译的注解</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Deprecated</span>                       <span class="comment">//不推荐使用</span></span><br><span class="line"><span class="meta">@SuppressWarnings</span>(...)    <span class="comment">//告知编译器阻止特定类型的警告</span></span><br><span class="line">如： <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)  <span class="comment">//阻止 unchecked类型警告</span></span><br><span class="line"><span class="meta">@Override</span></span><br></pre></td></tr></table></figure>

<h2 id="管理资源的注解"><a href="#管理资源的注解" class="headerlink" title="管理资源的注解"></a>管理资源的注解</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Resource</span>()   <span class="comment">//用于资源注入</span></span><br></pre></td></tr></table></figure>

<h2 id="元注解"><a href="#元注解" class="headerlink" title="元注解"></a>元注解</h2><p>元注解的作用是  <strong>负责注解其他注解</strong> 。 Java5.0 定义了 4 个标准的 <code>meta-annotation</code>类型，它们被用来提供对其它 <code>annotation</code>类型<strong>作说明</strong>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span> </span><br><span class="line"><span class="meta">@Retention</span> </span><br><span class="line"><span class="meta">@Inherited</span>  </span><br><span class="line"><span class="meta">@Documented</span></span><br></pre></td></tr></table></figure>

<h3 id="Target"><a href="#Target" class="headerlink" title="@Target"></a>@Target</h3><p>声明该注解用于<strong>使用的地方</strong>。默认值为任何元素，表示该注解用于<strong>任何</strong>地方。可用的<code>ElementType</code> 参数包括<br> ● <code>CONSTRUCTOR</code>: 用于描述构造器<br> ●  <code>FIELD</code>: 成员变量、对象、属性（包括enum实例）<br> ●  <code>LOCAL_VARIABLE</code>: 用于描述<strong>局部变量</strong><br> ●  <code>METHOD</code>: 用于<strong>描述方法</strong><br> ● <code>PACKAGE</code>: <strong>用于描述包</strong><br> ● <code>PARAMETER</code>: 用于<strong>描述参数</strong><br> ● <code>TYPE</code>: 用于描述类、接口(包括注解类型) 或enum声明</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">如：</span><br><span class="line"><span class="meta">@Target</span>(ElementType.METHOD)</span><br><span class="line"><span class="meta">@Target</span>(ElementType.FIELD)</span><br></pre></td></tr></table></figure>

<h3 id="Retention"><a href="#Retention" class="headerlink" title="@Retention"></a>@Retention</h3><p>用于指定该注解可以<strong>保留多长时间</strong>。使用 <code>RetentionPolicy</code> 中的选项</p>
<p> ●   <code>SOURCE</code> : 在<strong>编译阶段</strong>丢弃。这些注解在<strong>编译结束之后就不再有任何意义</strong>，所以它们<strong>不会写入字节码</strong>。 </p>
<p>如<code>SuppressWarnings</code>和<code>override</code>的源码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.SOURCE)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> SuppressWarnings &#123;</span><br><span class="line">    String[] value();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Target</span>(&#123;ElementType.METHOD&#125;)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.SOURCE)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Override &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> ●  <code>CLASS</code> :   在<strong>类加载的时候丢弃</strong>。 注解<strong>默认使用</strong>这种方式<br> ● <code>RUNTIME</code> : <strong>始终不会丢弃</strong>，运行期也保留该注解，因此可以使用反射机制读取该注解的信息。我们自定义的注解通常使用这种方式。</p>
<p>如<code>Deprecated</code>的源码，始终不会丢弃。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(&#123;ElementType.CONSTRUCTOR, ElementType.FIELD, ElementType.LOCAL_VARIABLE, ElementType.METHOD, ElementType.PACKAGE, ElementType.MODULE, ElementType.PARAMETER, ElementType.TYPE&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Deprecated &#123;</span><br><span class="line">    <span class="function">String <span class="title">since</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">forRemoval</span><span class="params">()</span> <span class="keyword">default</span> <span class="keyword">false</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Inherited"><a href="#Inherited" class="headerlink" title="@Inherited"></a>@Inherited</h3><ul>
<li>子类是否继承该类的注解，只能用于标记类，如果一个类有<code>@Inherited</code>注解，那么<strong>这个类的所有子类</strong> 则 拥有该类的<strong>全部注解</strong>。</li>
</ul>
<h3 id="Documented"><a href="#Documented" class="headerlink" title="@Documented"></a>@Documented</h3><ul>
<li>一个简单的Annotations 标记注解，表示是否将注解信息添加在java 文档中。</li>
</ul>
<h1 id="自定义注解"><a href="#自定义注解" class="headerlink" title="自定义注解"></a>自定义注解</h1><p>Java虽然提供了很多标准注解，但是有时仍然需要自定义一些注解来满足要求，自定义注解<strong>需要使用到元注解</strong></p>
<p>自定义注解类编写的一些规则:</p>
<ol>
<li>Annotation 型定义为<code>@interface</code>, 所有的Annotation 会自动继承<code>java.lang.Annotation</code>这一接口,并且<strong>不能再去继承别的类或是接口</strong>.</li>
<li>参数成员只能用<code>public</code> 或默认(<code>default</code>) 这两个<strong>访问权修饰</strong>.</li>
<li>参数成员只能用基本类型<code>byte、short、char、int、long、float、double、boolean</code>八种<strong>基本数据类型</strong>和<code>String、Enum、Class、annotations</code>等数据类型，以及这一些类型的数组.</li>
<li>要<strong>获取类方法和字段的注解信息</strong>，必须通过<strong>Java的反射技术</strong>来获取 Annotation 对象，因为你除此之外没有别的获取注解对象的方法。</li>
</ol>
<h1 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h1><ul>
<li>编写注解</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(FIELD)</span><br><span class="line"><span class="meta">@Retention</span>(RUNTIME)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> FruitColor &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">enum</span> Color&#123; BLUE,RED,GREEN&#125;;</span><br><span class="line">    <span class="function">Color <span class="title">fruitColor</span><span class="params">()</span> <span class="keyword">default</span> Color.GREEN</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Target</span>(FIELD)</span><br><span class="line"><span class="meta">@Retention</span>(RUNTIME)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> FruitName &#123;</span><br><span class="line">    <span class="function">String <span class="title">value</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Target</span>(FIELD)</span><br><span class="line"><span class="meta">@Retention</span>(RUNTIME)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> FruitProvider &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">id</span><span class="params">()</span> <span class="keyword">default</span> -1</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">name</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">address</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>编写Apple</code>类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Apple</span> </span>&#123;</span><br><span class="line">    <span class="meta">@FruitName</span>(<span class="string">"Apple"</span>)</span><br><span class="line">    <span class="keyword">private</span> String appleName;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@FruitColor</span>(fruitColor= FruitColor.Color.RED)</span><br><span class="line">    <span class="keyword">private</span> String appleColor;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@FruitProvider</span>(id=<span class="number">1</span>,name=<span class="string">"陕西红富士集团"</span>,address=<span class="string">"陕西省西安市延安路89号红富士大厦"</span>)</span><br><span class="line">    <span class="keyword">private</span> String appleProvider;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>注解处理器</li>
</ul>
<p>使用反射得到 <code>filed</code>对象-&gt;使用<code>filed.getAnnotation()</code>得到注解信息</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FruitInfoUtil</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">getFruitInfo</span><span class="params">(Class&lt;?&gt; clazz)</span></span>&#123;</span><br><span class="line">        String strFruitName=<span class="string">" 水果名称："</span>;</span><br><span class="line">        String strFruitColor=<span class="string">" 水果颜色："</span>;</span><br><span class="line">        String strFruitProvicer=<span class="string">"供应商信息："</span>;</span><br><span class="line"></span><br><span class="line">        Field[] fields = clazz.getDeclaredFields();  <span class="comment">//使用反射      </span></span><br><span class="line"> <span class="comment">/*</span></span><br><span class="line"><span class="comment"> Field[] getDeclaredFields() 返回 Field  对象  的一个数组，这些对象反映此Class 对象所表示的类或接口所声明的所有字段。 </span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(Field field :fields)&#123;</span><br><span class="line">            <span class="keyword">if</span>(field.isAnnotationPresent(FruitName<span class="class">.<span class="keyword">class</span>))</span>&#123;</span><br><span class="line">           <span class="comment">//如果指定类型的注释(@FruitName)存在于此元素(filed) 上，则返回 true，否则返回 false</span></span><br><span class="line">                FruitName fruitName = (FruitName) field.getAnnotation(FruitName<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">   <span class="comment">/*</span></span><br><span class="line"><span class="comment">     getAnnotation(Class&lt;T&gt; annotationClass)如果存在该元素的指定类型的注释，则返回这些注释，否则返回 null。</span></span><br><span class="line"><span class="comment">     需要强制转换</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">                strFruitName=strFruitName+fruitName.value();</span><br><span class="line">                System.out.println(strFruitName);</span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(field.isAnnotationPresent(FruitColor<span class="class">.<span class="keyword">class</span>))</span>&#123;</span><br><span class="line">                FruitColor fruitColor= (FruitColor) field.getAnnotation(FruitColor<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">                strFruitColor=strFruitColor+fruitColor.fruitColor().toString();</span><br><span class="line">                System.out.println(strFruitColor);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(field.isAnnotationPresent(FruitProvider<span class="class">.<span class="keyword">class</span>))</span>&#123;</span><br><span class="line">                FruitProvider fruitProvider= (FruitProvider) field.getAnnotation(FruitProvider<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">                strFruitProvicer=<span class="string">" 供应商编号："</span>+fruitProvider.id()+<span class="string">" 供应商名称："</span>+fruitProvider.name()+<span class="string">" 供应商地址："</span>+fruitProvider.address();</span><br><span class="line">                System.out.println(strFruitProvicer);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>测试</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FruitRun</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span>   </span>&#123;</span><br><span class="line">        FruitInfoUtil.getFruitInfo(Apple<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>声明：<a href="https://www.cnblogs.com/acm-bingzi/p/javaAnnotation.html" target="_blank" rel="noopener">代码参考自</a></p>
<h1 id="可扩展性"><a href="#可扩展性" class="headerlink" title="可扩展性"></a>可扩展性</h1><p>注解可以额外扩展吗？<br>注解总是扩展<code>java.lang.annotation.Annotation</code>。</p>
<p>如果我们尝试在注解声明中使用extends子句，<strong>我们将得到一个编译错误</strong>：,所有是<strong>不允许</strong>额外扩展的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> AnAnnotation extends OtherAnnotation &#123;</span><br><span class="line">    <span class="comment">// Compilation error</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java并发问题(2)</title>
    <url>/2020/09/13/java%E5%B9%B6%E5%8F%91%E9%97%AE%E9%A2%98-2/</url>
    <content><![CDATA[<p>注：本文内容转于《并发问题专题》，原文链接已不可知，若有侵权，请联系删除。</p>
<h1 id="Synchronized相关问题"><a href="#Synchronized相关问题" class="headerlink" title="Synchronized相关问题"></a>Synchronized相关问题</h1><p> ##问题一:Synchronized原理<br>  <a id="more"></a><br>  答：<br>    Synchronized是由<strong>JVM实现</strong>的一种实现互斥同步的一种方式，被Synchronized修饰过的程序块，在编译前后被编译器生成了<code>monitorenter</code>和<code>monitorexit</code>两个字节码指令。这两个指令是什么意思呢？-在虚拟机执行到<code>monitorenter</code>指令时，首先要<strong>尝试获取对象的锁</strong>：如果这个对象<strong>没有锁定</strong>，或者当前线程<strong>已经拥有了这个对象的锁</strong>，把锁的<strong>计数器+1</strong>；-当执行monitorexit指令时<strong>锁计数器-1</strong>；当计数器为0时，锁就被<strong>释放</strong>了。Java中Synchronize通过在<strong>对象头设置标记</strong>，达到了<strong>获取锁和释放锁</strong>的目的。</p>
<h2 id="问题二-获取对象的锁这个“锁”到底是什么？"><a href="#问题二-获取对象的锁这个“锁”到底是什么？" class="headerlink" title="问题二:获取对象的锁这个“锁”到底是什么？"></a>问题二:获取对象的锁这个“锁”到底是什么？</h2><p>“锁”的<strong>本质是一种资源</strong>，是<code>monitorenter</code>和<code>monitorexit</code>字节码指令的一个Reference类型的参数，即<strong>要锁定和解锁的对象。</strong>1.如果Synchronized明确指定了锁对象，比如Synchronized（变量名）、Synchronized(this)等，说明加解锁<strong>对象为该对象。</strong>2.如果没有明确指定：若Synchronized修饰的方法为<strong>非静态</strong>方法，表示<strong>此方法对应的对象</strong>为<strong>锁对象</strong>；若Synchronized修饰的方法为<strong>静态</strong>方法，则表示<strong>此方法对应的类</strong>对象为锁对象。注意，当一个对象被锁住时，对象里所有用Synchronized修饰的方法都将产生堵塞，而对象里<strong>非Synchronized修饰的方法可正常被调用</strong>，不受锁影响。</p>
<p>问题三:Synchronized为什么是可重入锁<br>可重入性是<strong>锁的一个基本要求</strong>，是为了<strong>解决自己锁死自己的情况</strong>。比如，一个类中的同步方法调用另一个同步方法，假如Synchronized不支持重入，进入<code>method2</code>方法时当前线程获得锁,<code>method2</code>方法里面执行<code>method1</code>时当前线程又要去尝试获取锁，这时如果不支持重入，它就要等待释放，把自己阻塞，导致<strong>自己锁死自己</strong>。对Synchronized来说，在执行monitorenter指令时，如果这个对象没有锁定，就获对象的取锁，如果当前线程<strong>已经拥有了</strong>这个对象的锁，就<strong>把锁的计数器+1</strong>，而不是阻塞等待。本质上就通过这种方式实现了<strong>可重入性</strong>。</p>
<h2 id="问题四-JVM对Java的原生锁做了哪些优化"><a href="#问题四-JVM对Java的原生锁做了哪些优化" class="headerlink" title="问题四:JVM对Java的原生锁做了哪些优化"></a>问题四:JVM对Java的原生锁做了哪些优化</h2><p>在Java6之前，Monitor的实现<strong>完全依赖底层操作系统的互斥锁</strong>来实现。如果要将一个线程进行阻塞或唤起都需要操作系统的协助，这就需要从<strong>用户态切换到内核态</strong>来执行，这种切换代价十分昂贵，很耗处理器时间。一种优化是使用<strong>自旋锁</strong>，即在把线程进行阻塞操作之前先让线程自旋等待一段时间，可能在等待期间其他线程已经解锁，这时就无需再让线程执行阻塞操作，避免了用户态到内核态的切换。现代JDK中还提供了三种不同的Monitor实现，也就是三种不同的锁：<br> -<strong>偏向锁（BiasedLocking）</strong><br> -<strong>轻量级锁</strong><br> -<strong>重量级锁</strong></p>
<ul>
<li><p>这三种锁使得JDK得以优化Synchronized的运行，当JVM检测到<strong>不同的竞争状况时，会自动切换到适合的锁实现</strong>，这就是锁的升级、降级。</p>
</li>
<li><p>当没有竞争出现时，默认会使用<strong>偏向锁</strong>。JVM会利用<strong>CAS操作</strong>，在对象头上的<strong>MarkWord部分设置线程ID</strong>，以表示<strong>这个对象偏向当前线程</strong>，所以并不涉及真正的互斥锁，因为在很多应用场景中，大部分对象生命周期中最多会被一个线程锁定，使用偏向锁可以降低<strong>无竞争开销</strong>。</p>
</li>
<li><p>如果有<strong>另一线程试图锁定</strong>某个被偏斜过的对象，JVM就<strong>撤销偏向锁</strong>，切换到轻量级锁实现。轻量级锁依赖<strong>CAS操作MarkWord来试图获取锁</strong>，如果重试成功，就使用普通的轻量级锁；否则，进一步升级为<strong>重量级锁</strong>，升级为重量级锁也叫<strong>锁膨胀</strong>。</p>
</li>
</ul>
<h2 id="问题五-Synchronized为什么是非公平锁？"><a href="#问题五-Synchronized为什么是非公平锁？" class="headerlink" title="问题五:Synchronized为什么是非公平锁？"></a>问题五:Synchronized为什么是非公平锁？</h2><p>非公平主要表现在获取锁的行为上，并非是按照申<strong>请锁的时间</strong>前后给等待线程分配锁的，每当锁被释放后，<strong>任何一个线程都有机会</strong>竞争到锁，<br>这样做的目的是为了<strong>提高执行性能</strong>，缺点是可能会产生<strong>线程饥饿现象</strong>。</p>
<h2 id="问题六-什么是锁消除和锁粗化？"><a href="#问题六-什么是锁消除和锁粗化？" class="headerlink" title="问题六:什么是锁消除和锁粗化？"></a>问题六:什么是锁消除和锁粗化？</h2><ul>
<li>锁消除：指虚拟机即时编译器在运行时，对一些<strong>代码上要求同步</strong>，但<strong>被检测到不可能存在共享数据竞争</strong>的锁进行消除。</li>
<li>锁粗化：原则上，同步块的作用范围要尽量小。但是如果一系列的连续操作都<strong>对同一个对象反复加锁和解锁</strong>，频繁地进行互斥同步操作也会导致不必要的性能损耗。<strong>锁粗化就是增大锁的作用域</strong>。以降低加锁解锁的频率。</li>
</ul>
<h2 id="问题七：为什么Synchronized是一个悲观锁？"><a href="#问题七：为什么Synchronized是一个悲观锁？" class="headerlink" title="问题七：为什么Synchronized是一个悲观锁？"></a>问题七：为什么Synchronized是一个悲观锁？</h2><p>乐观锁的实现原理Synchronized的并发策略是悲观的：<strong>不管是否会产生竞争，任何的数据操作都必须要加锁</strong>、用户态核心态转换、维护锁计数器和检查是否有被阻塞的线程需要被唤醒等操作。</p>
<p>乐观锁：<strong>先进行操作</strong>，如果没有其他线程征用数据，那操作就成功了；如果共享数据有<strong>征用</strong>，产生冲突，那就再进行其他的<strong>补偿措施</strong>。这种乐观的并发策略<strong>不需要线程挂起</strong>，所以被称为<strong>非阻塞同步</strong>。</p>
<p>乐观锁的核心算法是CAS：它涉及到三个操作数：<strong>内存值、预期值、新值</strong>。<br>当且仅当<strong>预期值和内存值相等</strong>时才将内存值修改为新值。这样处理的逻辑是:首先检查某块内存值是否跟之前我读取时的一样，如不一样则表示期间此内存值已经被别的线程更改过，<strong>舍弃本次操作</strong>，否则说明期间没有其他线程对此内存值操作，可以把新值设置给此块内存。</p>
<p>CAS具有<strong>原子性</strong>，它的原子性由<strong>CPU硬件指令</strong>实现保证。</p>
<h2 id="问题八-乐观锁一定就是好的吗？"><a href="#问题八-乐观锁一定就是好的吗？" class="headerlink" title="问题八: 乐观锁一定就是好的吗？"></a>问题八: 乐观锁一定就是好的吗？</h2><p>乐观锁避免了悲观锁独占对象的现象，同时也提高了并发性能，但它也有缺点：</p>
<p>1.乐观锁只能保证<strong>一个共享变量的原子操作</strong>。如果<strong>多一个或几个变量</strong>，乐观锁将变得力不从心，但<strong>互斥锁</strong>能易解决，不管对象数量多少及对象颗粒度大小。</p>
<p>2.<strong>长时间自旋</strong>可能导致开销大。假如CAS长时间不成功而一直自旋，<strong>会给CPU带来很大的开销</strong>。</p>
<p>3.<strong>ABA问题</strong>。CAS的核心思想是通过比对内存值与预期值是否一样而判断内存值是否被改过，但<strong>这个判断逻辑不严谨</strong>，假如内存值原来是A后来被一条线程改为B，最后又被改成了A，则CAS认为此内存值并没有发生改变，但实际上是有被其他线程改过的，这种情况对依赖过值的情景的运算结果影响很大。解决的思路是<strong>引入版本号</strong>，每次变量更新都把版本号加一。</p>
<h1 id="ReentrantLock及其他显式锁相关问题"><a href="#ReentrantLock及其他显式锁相关问题" class="headerlink" title="ReentrantLock及其他显式锁相关问题"></a>ReentrantLock及其他显式锁相关问题</h1><h2 id="问题一-Synchronized和ReentrantLock实现原理的不同"><a href="#问题一-Synchronized和ReentrantLock实现原理的不同" class="headerlink" title="问题一:Synchronized和ReentrantLock实现原理的不同"></a>问题一:Synchronized和ReentrantLock实现原理的不同</h2><p>其实，锁的实现原理基本是为了达到一个目的，<strong>让所有的线程都能看到某种标记</strong>。Synchronized通过在<strong>对象头中设置标记</strong>实现了这一目的，是一种<strong>JVM原生的锁</strong>实现方式。</p>
<p>ReentrantLock以及所有的基于Lock接口的实现类，都是通过用一个<strong>volitile修饰的int型变量</strong>，<strong>并保证每个线程都能拥有对该int的可见性和原子修改</strong>，其本质是基于所谓的<strong>AQS框架</strong>。</p>
<h2 id="问题二-AQS框架"><a href="#问题二-AQS框架" class="headerlink" title="问题二:AQS框架"></a>问题二:AQS框架</h2><p>AQS（AbstractQueuedSynchronizer类）是一个用来<strong>构建锁和同步器的框架</strong>，各种Lock包都是基于AQS来构建。</p>
<ol>
<li><p>AQS在内部定义了一个<code>volatile intstate</code>变量，表示<strong>同步状态</strong>：当线程调用lock方法时，如果state=0，说明没有任何线程占有共享资源的锁，可以获得锁并将state=1  ; 如果state=1，则说明有线程目前正使用共享变量，其他线程必须<strong>加入同步队列进行等待</strong>。</p>
</li>
<li><p>AQS通过Node内部类构成的一个双向链表结构的<strong>同步队列</strong>，来完成<strong>线程获取锁的排队工作</strong>，当有线程获取锁<strong>失败</strong>后，就被添加到<strong>队列末尾</strong>。</p>
<ul>
<li>Node类是对要访问同步代码的线程的封装，包含了线程本身及其状态叫waitStatus（有五种不同取值，分别表示是否被阻塞，是否等待唤醒，是否已经被取消等），每个Node结点关联其prev结点和next结点，方便线程释放锁后快速唤醒下一个在等待的线程，是一个FIFO的过程</li>
<li>Node类有两个常量，SHARED和EXCLUSIVE，分别代表共享模式和独占模式。所谓共享模式是一个锁允许多条线程同时操作（信号量Semaphore就是基于AQS的共享模式实现的），独占模式是同一个时间段只能有一个线程对共享资源进行操作，多余的请求线程需要排队等待（如ReentranLock）。</li>
</ul>
</li>
<li><p>AQS通过内部类<code>ConditionObject</code>构建<strong>等待队列</strong>,当Condition调用<strong>wait()方法后</strong>，线程将会加<strong>入等待队列中</strong>，而Condition<strong>调用signal()方法后</strong>，线程将从<strong>等待队列</strong>转移动<strong>同步队列</strong>中进行锁竞争。</p>
</li>
<li><p>AQS和Condition各自维护了不同的队列，在使用Lock和Condition的时候，其实就是<strong>两个队列的互相移动</strong>。</p>
</li>
</ol>
<h2 id="问题三-SynchronizedReentrantLock的异同"><a href="#问题三-SynchronizedReentrantLock的异同" class="headerlink" title="问题三:SynchronizedReentrantLock的异同"></a>问题三:SynchronizedReentrantLock的异同</h2><p>ReentrantLock是Lock的实现类,是一个互斥的同步锁。<br><strong>从功能角度,</strong>ReentrantLock比Synchronized的同步操作<strong>更精细</strong>：</p>
<ul>
<li><strong>等待可中断</strong>：当持有锁的线程长期不释放锁的时候，正在等待的线程可以选择放弃等待，对处理执行时间非常长的同步块很有用。</li>
<li><strong>带超时的获取锁尝试</strong>：在指定的时间范围内获取锁，如果时间到了仍然无法获取则返回。</li>
<li>可以判断是否有线程在排队等待获取锁。    </li>
<li>可以<strong>响应中断请求</strong>：与Synchronized不同，当获取到锁的线程被中断时，能够响应中断，中断异常将会被抛出，同时锁会被释放。</li>
<li>可以<strong>实现公平锁</strong>。</li>
</ul>
<p><strong>从锁释放角度:</strong><br>Synchronized在<strong>JVM层面上实现的</strong>，不但可以通过一些监控工具监控Synchronized的锁定，而且在<strong>代码执行出现异常</strong>时，<strong>JVM会自动释放锁定</strong>；<br>Lock是通过代码实现的，要保证锁定一定会被释放，就必须将<strong>unLock()放到finally{}中</strong>。</p>
<p><strong>从性能角度</strong>:<br>Synchronized早期实现比较低效，对比ReentrantLock，大多数场景性能都相差较大。改进后在竞争不激烈时，Synchronized的性能要优于ReetrantLock；在高竞争情况下，Synchronized的性能会下降几十倍，但是ReetrantLock的性可以维持常态。</p>
<h2 id="问-题-四-ReentrantLock如何实现可重入性"><a href="#问-题-四-ReentrantLock如何实现可重入性" class="headerlink" title="问 题 四: ReentrantLock如何实现可重入性"></a>问 题 四: ReentrantLock如何实现可重入性</h2><p>ReentrantLock内部自定义了<strong>同步器Sync</strong> ,其实就是<strong>加锁的时候通过CAS算法 ，将线程对象放到一个双向链表中 ，每次获取锁的时候 ，看下当前维护的那个线程ID和当前请求的线程ID是否一样 ,一样就可重入了</strong> 。</p>
<h2 id="问-题-五-JUC-中-的其他并-发-工-具-？"><a href="#问-题-五-JUC-中-的其他并-发-工-具-？" class="headerlink" title="问 题 五 :  JUC 中 的其他并 发 工 具 ？"></a>问 题 五 :  JUC 中 的其他并 发 工 具 ？</h2><p>通常所说的并发包（JUC）也就是java.util.concurrent及其子包，集中了Java并发的各种基础工具类，具体主要包括几个方面：</p>
<ul>
<li><p>提供了<code>CountDownLatch、CyclicBarrier、Semaphore</code>等，比Synchronized更加高级，可以实现更加丰富多线程操作的同步结构。</p>
</li>
<li><p>提供了<code>ConcurrentHashMap、有序的ConcunrrentSkipListMap</code>，或者通过类似快照机制实现线程安全的动态数组CopyOnWriteArrayList等，各种线程安全的容器。</p>
</li>
<li><p>提供了<code>ArrayBlockingQueue、SynchorousQueue</code>或针对特定场景的PriorityBlockingQueue等，各种并发队列实现。</p>
</li>
<li><p>强大的<code>Executor</code>框架，可以创建各种不同类型的线程池，调度任务运行等。</p>
</li>
</ul>
<h2 id="问-题-六-ReadWriteLock-和-StampedLock"><a href="#问-题-六-ReadWriteLock-和-StampedLock" class="headerlink" title="问 题 六 :ReadWriteLock 和 StampedLock"></a>问 题 六 :ReadWriteLock 和 StampedLock</h2><p>虽然ReentrantLock和Synchronized简单实用，但是行为上有一定局限性，<strong>要么不占，要么独占</strong>。实际应用场景中，有时候不需要大量竞争的写操作，<strong>而是以并发读取为主</strong>，为了进一步优化并发操作的粒度，<strong>Java提供了读写锁</strong>。</p>
<hr>
<h3 id="读写锁"><a href="#读写锁" class="headerlink" title="读写锁"></a>读写锁</h3><p>读写锁基于的原理是<strong>多个读操作不需要互斥</strong>，如果<strong>读锁试图锁定</strong>时，<strong>写锁是被某个线程持有，读锁将无法获得</strong>，而只好等待对方操作结束，这样就可以<strong>自动保证不会读取到有争议的数据</strong>。</p>
<p>ReadWriteLock代表了<strong>一对锁</strong>，当数据量较大，<strong>并发读多、并发写少</strong>的时候，能够比纯同步版本凸显出优势。</p>
<h3 id="StampedLock"><a href="#StampedLock" class="headerlink" title="StampedLock"></a>StampedLock</h3><p><strong>读写锁也会引入相对比较大的开销</strong>。所以，JDK在后期引入了<strong>StampedLock</strong>，在提供类似读写锁的同时，还支持优化读模式。<strong>优化读基于假设</strong>，大多数情况下读操作并不会和写操作冲突。</p>
<p>其逻辑是<strong>先试着修改</strong>，然后通过<strong>validate方法确认是否进入了写模式</strong>，如果<strong>没有进入，就成功避免了开销；如果进入，则尝试获取读锁</strong>。</p>
<h2 id="问题七-Java的线程如何彼此同步"><a href="#问题七-Java的线程如何彼此同步" class="headerlink" title="问题七: Java的线程如何彼此同步"></a>问题七: Java的线程如何彼此同步</h2><p>JUC中的同步器三个主要的成员：CountDownLatch、CyclicBarrier和Semaphore。</p>
<ol>
<li><code>CountDownLatch</code>叫倒计数，允许一个或多个线程等待某些操作完成。</li>
</ol>
<p>看几个场景：</p>
<ul>
<li><p>跑步比赛，裁判需要等到所有的运动员（“其他线程”）都跑到终点（达到目标），才能去算排名和颁奖。</p>
</li>
<li><p>模拟并发，我需要启动100个线程去同时访问某一个地址，我希望它们能<strong>同时并发</strong>，而不是一个一个的去执行。</p>
</li>
</ul>
<ol start="2">
<li><p><code>CyclicBarrier</code>叫循环栅栏，它实现让一组线程等待至某个状态之后再全部同时执行，而且当所有等待线程被释放后，CyclicBarrier可以被重复使用。CyclicBarrier的典型应用场景是用来<strong>等待并发线程结束</strong>。</p>
<p><strong>CyclicBarrier的主要方法</strong>是<strong>await()</strong>， 每被调用一次，计数便会减少1，并阻塞住当前线程。当计数减至0时，阻塞解除，所有在此CyclicBarrier上面阻塞的线程开始运行。<strong>在这之后，如果再次调用await()，计数就又会变成N-1，新一轮重新开始</strong>，这便是Cyclic的含义所在。 </p>
</li>
<li><p>Semaphore，Java版本的信号量实现，用于<strong>控制同时访问的线程个数</strong>，来达到限制通用资源访问的目的，其原理是通过<strong>acquire()获取一个许可，如果没有就等待，而release()释放一个许可。</strong></p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SemaphoreTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Semaphore semaphore = <span class="keyword">new</span> Semaphore(<span class="number">2</span>);    <span class="comment">//限制同时只能有两个线程访问</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;             <span class="comment">//开启10个线程取竞争</span></span><br><span class="line">            <span class="keyword">new</span> Worker(i,semaphore).start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Worker</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> num;</span><br><span class="line">    <span class="keyword">private</span> Semaphore semaphore;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Worker</span><span class="params">(<span class="keyword">int</span> num,Semaphore semaphore)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.num=num;</span><br><span class="line">        <span class="keyword">this</span>.semaphore=semaphore;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            semaphore.acquire();          <span class="comment">//申请获得 一个资源</span></span><br><span class="line">            System.out.println(<span class="string">"acquired"</span>);</span><br><span class="line">            Sleeper.sleep(<span class="number">2</span>); </span><br><span class="line">            semaphore.release();         <span class="comment">//释放资源</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="问题八-对比CyclicBarrier和CountDownLatch"><a href="#问题八-对比CyclicBarrier和CountDownLatch" class="headerlink" title="问题八:对比CyclicBarrier和CountDownLatch"></a>问题八:对比CyclicBarrier和CountDownLatch</h2><ul>
<li><p>是否可重置</p>
<ul>
<li><strong>CountDownLatch是不可以重置的</strong>，所以<strong>无法重用</strong>。</li>
<li>CyclicBarrier没有这种限制, 可以重用。</li>
</ul>
</li>
<li><p>基本操作：</p>
<ul>
<li><p>CountDownLatch的基本操作组合是<code>countDown</code>和<code>await</code>，调用<strong>await的线程阻塞等待countDown足够的次数</strong>才会继续向下执行。</p>
</li>
<li><p>CyclicBarrier的基本操作组合就是<code>await</code>，<strong>当所有的伙伴都调用了await</strong>，才会继续进行任务，并自动进行重置。</p>
</li>
</ul>
</li>
<li><p>目的：</p>
<ul>
<li>CountDownLatch目的是让<strong>一个线程等待其他N个线程达到某个条件后，自己再去做某个事</strong>。</li>
<li>而CyclicBarrier的目的是让<strong>N多线程互相等待直到所有的都达到某个状态</strong>，然后这N个线程再继续执行<strong>各自后续</strong> 。</li>
</ul>
</li>
</ul>
<h1 id="Java线程池相关问题"><a href="#Java线程池相关问题" class="headerlink" title="Java线程池相关问题"></a>Java线程池相关问题</h1><p><a href="https://tech.meituan.com/2020/04/02/java-pooling-pratice-in-meituan.html" target="_blank" rel="noopener">美团线程池总结</a></p>
<h2 id="问题一：Java中的线程池是如何实现的"><a href="#问题一：Java中的线程池是如何实现的" class="headerlink" title="问题一：Java中的线程池是如何实现的"></a>问题一：Java中的线程池是如何实现的</h2><p>java中线程池中的<strong>“线程”</strong>，其实是被抽象为了<strong>一个静态内部类Worker</strong>，它基于AQS实现，存放在线程池的<code>HashSet &lt;Worker&gt;workers</code> 成员变量中。</p>
<p><strong>需要执行的任务</strong>则存放在成员变量<code>workQueue</code>（<code>BlockingQueue&lt;Runnable&gt;workQueue</code>）中。</p>
<p>这样，整个线程池实现的基本思想就是：<strong>从workQueue中不断取出需要执行的任务，放在Workers（线程的抽象）中进行处理</strong>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> HashSet&lt;Worker&gt; workers = <span class="keyword">new</span> HashSet&lt;&gt;(); </span><br><span class="line">BlockingQueue&lt;Runnable&gt;workQueue</span><br></pre></td></tr></table></figure>



<h2 id="问题二：创建线程池的几个核心构造参数"><a href="#问题二：创建线程池的几个核心构造参数" class="headerlink" title="问题二：创建线程池的几个核心构造参数"></a>问题二：创建线程池的几个核心构造参数</h2><p>Java中的线程池的创建其实非常灵活，我们可以通过配置不同的参数，创建出行为不同的线程池，这几个参数包括：</p>
<ul>
<li><p><code>corePoolSize</code>：线程池的<strong>核心线程数</strong>。</p>
</li>
<li><p><code>maximumPoolSize</code>：<strong>线程池允许的最大线程数</strong>。</p>
</li>
<li><p><code>keepAliveTime</code>：超过核心线程数时<strong>闲置线程的存活时间</strong>。</p>
</li>
<li><p><code>workQueue</code>：<strong>需要执行任务的队列</strong>。任务执行前保存任务的队列，保存由execute方法提交的Runnable任务。</p>
</li>
</ul>
<p>下面看一下源码中的一个构造方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ThreadPoolExecutor</span><span class="params">(<span class="keyword">int</span> corePoolSize,</span></span></span><br><span class="line"><span class="function"><span class="params">                          <span class="keyword">int</span> maximumPoolSize,</span></span></span><br><span class="line"><span class="function"><span class="params">                          <span class="keyword">long</span> keepAliveTime,</span></span></span><br><span class="line"><span class="function"><span class="params">                          TimeUnit unit,</span></span></span><br><span class="line"><span class="function"><span class="params">                          BlockingQueue&lt;Runnable&gt; workQueue,</span></span></span><br><span class="line"><span class="function"><span class="params">                          ThreadFactory threadFactory,</span></span></span><br><span class="line"><span class="function"><span class="params">                          RejectedExecutionHandler handler)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (corePoolSize &lt; <span class="number">0</span> ||</span><br><span class="line">        maximumPoolSize &lt;= <span class="number">0</span> ||</span><br><span class="line">        maximumPoolSize &lt; corePoolSize ||</span><br><span class="line">        keepAliveTime &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">    <span class="keyword">if</span> (workQueue == <span class="keyword">null</span> || threadFactory == <span class="keyword">null</span> || handler == <span class="keyword">null</span>)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="keyword">this</span>.corePoolSize = corePoolSize;</span><br><span class="line">    <span class="keyword">this</span>.maximumPoolSize = maximumPoolSize;</span><br><span class="line">    <span class="keyword">this</span>.workQueue = workQueue;</span><br><span class="line">    <span class="keyword">this</span>.keepAliveTime = unit.toNanos(keepAliveTime);</span><br><span class="line">    <span class="keyword">this</span>.threadFactory = threadFactory;</span><br><span class="line">    <span class="keyword">this</span>.handler = handler;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="问题三：线程池中的线程创建"><a href="#问题三：线程池中的线程创建" class="headerlink" title="问题三：线程池中的线程创建"></a>问题三：线程池中的线程创建</h2><p><strong>问：线程池中的线程是怎么创建的？是一开始就随着线程池的启动创建好的吗？</strong></p>
<p>答：</p>
<p>不是的。<strong>线程池默认初始化后不启动Worker，等待有请求时才启动</strong>。每当我们调用<strong>execute()方法添加一个任务时</strong>，线程池会做如下判断：</p>
<ul>
<li><p>如果正在运行的线程数量<strong>小于</strong>corePoolSize，那么<strong>马上创建</strong>线程运行这个任务；</p>
</li>
<li><p>如果正在运行的线程数量<strong>大于或等于</strong>corePoolSize，那么将这个任务<strong>放入队列</strong>；</p>
</li>
<li><p>如果这时候<strong>队列满了</strong>，而且正在运行的线程<strong>数量小于maximumPoolSize</strong>，那么还是要创建<strong>非核心线程</strong>立刻运行这个任务；</p>
</li>
<li><p>如果<strong>队列满了</strong>，而且正在运行的线程数量<strong>大于或等于maximumPoolSize</strong>，那么线程池会抛出异常<code>RejectExecutionException</code>。 </p>
</li>
<li><p>当一个线程完成任务时，它会从<strong>队列中取下一个任务来执行</strong>。当一个线程无事可做，<strong>超过一定的时间</strong>（keepAliveTime）时，线程池会判断。如果当前运行的线程数<strong>大于</strong>corePoolSize，那么这个<strong>线程就被停掉</strong>。<strong>所以线程池的所有任务完成后，它最终会收缩到corePoolSize 的大小。</strong></p>
</li>
</ul>
<h2 id="问题四：Java实现好的线程池"><a href="#问题四：Java实现好的线程池" class="headerlink" title="问题四：Java实现好的线程池"></a>问题四：Java实现好的线程池</h2><p><strong>问：既然提到可以通过配置不同参数创建出不同的线程池，那么Java中默认实现好的线程池又有哪些呢？请比较它们的异同。</strong></p>
<p><strong>答：</strong></p>
<ol>
<li><p><code>SingleThreadExecutor</code> 线程池:  <strong>一个核心线程</strong>在工作，也就是相当于单线程<strong>串行</strong>执行所有任务。如果这个唯一的线程因为异常结束，那么会有一个<strong>新的线程来替代它</strong>。此线程池<strong>保证所有任务的执行顺序按照任务的提交顺序执行</strong>。</p>
<ul>
<li><p><code>corePoolSize：1</code>，只有<strong>一个核心线程</strong>在工作。</p>
</li>
<li><p><code>maximumPoolSize：1</code>。</p>
</li>
<li><p><code>keepAliveTime：0L</code>。</p>
</li>
<li><p><code>workQueue：new LinkedBlockingQueue&lt;Runnable&gt;()</code> <strong>缓冲队列无界</strong>。</p>
</li>
</ul>
</li>
</ol>
<ol start="2">
<li><p><code>FixedThreadPool</code> 线程池:  <strong>固定大小</strong>的线程池，<strong>只有核心线程</strong>。<strong>每次提交一个任务就创建一个线程，直到线程达到线程池的最大</strong>大小。线程池的大小一旦达到<strong>最大值就会保持不变</strong>，如果某个线程因为执行<strong>异常</strong>而结束，那么线程池会<strong>补充一个新线程</strong>。FixedThreadPool多数针对一些<strong>很稳定很固定的正规并发线程</strong>，多用于服务器。</p>
<ul>
<li><p><code>corePoolSize：n Threads</code></p>
</li>
<li><p><code>maximumPoolSize：n Threads</code></p>
</li>
<li><p><code>keepAliveTime：0L</code></p>
</li>
<li><p><code>workQueue：new LinkedBlockingQueue&lt;Runnable&gt;()</code>，<strong>缓冲队列无界</strong>。</p>
</li>
</ul>
</li>
</ol>
<ol start="3">
<li><p><code>CachedThreadPool</code> 线程池: <strong>无界线程池</strong>，如果线程池的大小超过了处理任务所需要的线程，那么就会回收部分空闲线程，当<strong>任务数增加时</strong>，此线程池又可以<strong>智能的添加新线程</strong>来处理任务。线程池大小完全依赖于 <strong>操作系统</strong> 能够创建的<strong>最大线程</strong>大小。 </p>
<ul>
<li><p><code>corePoolSize：0</code></p>
</li>
<li><p><code>maximumPoolSize：Integer.MAX_VALUE</code></p>
</li>
<li><p><code>keepAliveTime：60L</code></p>
</li>
<li><p><code>workQueue：newSynchronousQueue&lt;Runnable&gt;()</code>   <strong>缓冲区为1的阻塞队列</strong>。</p>
</li>
</ul>
</li>
</ol>
<ol start="4">
<li><p><code>ScheduledThreadPool</code> 线程池 ：<strong>核心线程池固定</strong>，<strong>大小无限</strong>的线程池。此线程池支持定时以及周期性执行任务的需求。创建一个周期性执行任务的线程池。如果闲置，非核心线程池会在<code>DEFAULT_KEEPALIVEMILLIS</code>时间内回收。</p>
<ul>
<li><p><code>corePoolSize：corePoolSize</code></p>
</li>
<li><p><code>maximumPoolSize：Integer.MAX_VALUE</code></p>
</li>
<li><p><code>keepAliveTime：DEFAULT_KEEPALIVE_MILLIS</code></p>
</li>
<li><p><code>workQueue：newDelayedWorkQueue()</code></p>
</li>
</ul>
</li>
</ol>
<h2 id="问题六：Java线程池中如何提交线程"><a href="#问题六：Java线程池中如何提交线程" class="headerlink" title="问题六：Java线程池中如何提交线程"></a>问题六：Java线程池中如何提交线程</h2><p>   线程池最常用的提交任务的方法有两种：</p>
<p>   1.<code>execute()</code>：ExecutorService.execute方法接收一个Runable实例，它用来执行一个任务：</p>
<p>   2.submit()：ExecutorService.submit()方法返回的是Future对象。可以用isDone()来查询Future是否已经完成，当任务完成时，它具有一个结果，可以调用get()来获取结果。也可以不用isDone()进行检查就直接调用get()，在这种情况下，get()将阻塞，直至结果准备就绪。</p>
<h1 id="Java内存模型"><a href="#Java内存模型" class="headerlink" title="Java内存模型"></a>Java内存模型</h1><h2 id="问题一：Java的内存模型，各个线程怎么看到对方的变量的"><a href="#问题一：Java的内存模型，各个线程怎么看到对方的变量的" class="headerlink" title="问题一：Java的内存模型，各个线程怎么看到对方的变量的"></a>问题一：Java的内存模型，各个线程怎么看到对方的变量的</h2><p>   Java的内存模型定义了程序中各个变量的访问规则，即在虚拟机中将变量存储到内存和从内存中取出这样的底层细节。此处的变量包括实例字段、静态字段和构成数组对象的元素，但是不包括局部变量和方法参数，因为这些是线程私有的，不会被共享，所以不存在竞争问题。Java中各个线程是怎么彼此看到对方的变量的呢？Java中定义了主内存与工作内存的概念：所有的变量都存储在主内存，每条线程还有自己的工作内存，保存了被该线程使用到的变量的主内存副本拷贝。线程对变量的所有操作（读取、赋值）都必须在工作内存中进行，不能直接读写主内存的变量。不同的线程之间也无法直接访问对方工作内存的变量，线程间变量值的传递需要通过主内存。</p>
<h2 id="问题二：volatile-特点，为什么能保证可见性"><a href="#问题二：volatile-特点，为什么能保证可见性" class="headerlink" title="问题二：volatile 特点，为什么能保证可见性"></a>问题二：volatile 特点，为什么能保证可见性</h2><p>   关键字volatile是Java虚拟机提供的最轻量级的同步机制。当一个变量被定义成volatile之后，具备两种特性：1.保证此变量对所有线程的可见性。当一条线程修改了这个变量的值，新值对于其他线程是可以立即得知的。而普通变量做不到这一点。2.禁止指令重排序优化。普通变量仅仅能保证在该方法执行过程中，得到正确结果，但是不保证程序代码的执行顺序。Java的内存模型定义了8种内存间操作：lock和unlock把一个变量标识为一条线程独占的状态。把一个处于锁定状态的变量释放出来，释放之后的变量才能被其他线程锁定。read和write把一个变量值从主内存传输到线程的工作内存，以便load。把store操作从工作内存得到的变量的值，放入主内存的变量中。load和store把read操作从主内存得到的变量值放入工作内存的变量副本中。把工作内存的变量值传送到主内存，以便write。use和assgin把工作内存变量值传递给执行引擎。将执行引擎值传递给工作内存变量值。volatile的实现基于这8种内存间操作，保证了一个线程对某个volatile变量的修改，一定会被另一个线程看见，即保证了可见性。</p>
<h2 id="问题三：-volatile变量的运算就是并发安全的吗"><a href="#问题三：-volatile变量的运算就是并发安全的吗" class="headerlink" title="问题三： volatile变量的运算就是并发安全的吗"></a>问题三： volatile变量的运算就是并发安全的吗</h2><p>   显然不是的。基于volatile变量的运算在并发下不一定是安全的。volatile变量在各个线程的工作内存，不存在一致性问题（各个线程的工作内存中volatile变量，每次使用前都要刷新到主内存）。但是Java里面的运算并非原子操作，导致volatile变量的运算在并发下一样是不安全的。</p>
<h2 id="问题四：volatile对比Synchronized的异同。"><a href="#问题四：volatile对比Synchronized的异同。" class="headerlink" title="问题四：volatile对比Synchronized的异同。"></a>问题四：volatile对比Synchronized的异同。</h2><p>   Synchronized既能保证可见性，又能保证原子性，而volatile只能保证可见性，无法保证原子性。ThreadLocal和Synchonized都用于解决多线程并发访问，防止任务在共享资源上产生冲突。但是ThreadLocal与Synchronized有本质的区别。Synchronized用于实现同步机制，是利用锁的机制使变量或代码块在某一时该只能被一个线程访问，是一种“以时间换空间”的方式。而ThreadLocal为每一个线程都提供了变量的副本，使得每个线程在某一时间访问到的并不是同一个对象，根除了对变量的共享，是一种“以空间换时间”的方式。</p>
<h2 id="问题五：ThreadLocal怎么解决并发安全的？"><a href="#问题五：ThreadLocal怎么解决并发安全的？" class="headerlink" title="问题五：ThreadLocal怎么解决并发安全的？"></a>问题五：ThreadLocal怎么解决并发安全的？</h2><p>   ThreadLocal这是Java提供的一种保存线程私有信息的机制，因为其在整个线程生命周期内有效，所以可以方便地在一个线程关联的不同业务模块之间传递信息，比如事务ID、Cookie等上下文相关信息。ThreadLocal为每一个线程维护变量的副本，把共享数据的可见范围限制在同一个线程之内，其实现原理是，在ThreadLocal类中有一个Map，用于存储每一个线程的变量的副本。问题六：很多人都说要慎用ThreadLocal，谈谈你的理解，使用ThreadLocal需要注意些什么？使用ThreadLocal要注意remove！ThreadLocal的实现是基于一个所谓的ThreadLocalMap，在ThreadLocalMap中，它的key是一个弱引用。通常弱引用都会和引用队列配合清理机制使用，但是ThreadLocal是个例外，它并没有这么做。这意味着，废弃项目的回收依赖于显式地触发，否则就要等待线程结束，进而回收相应ThreadLocalMap！这就是很多OOM的来源，所以通常都会建议，应用一定要自己负责remove，并且不要和线程池配合，因为worker线程往往是不会退出的。</p>
]]></content>
      <categories>
        <category>java并发</category>
      </categories>
      <tags>
        <tag>java并发</tag>
      </tags>
  </entry>
  <entry>
    <title>执行计划explain</title>
    <url>/2020/09/24/%E6%89%A7%E8%A1%8C%E8%AE%A1%E5%88%92explain/</url>
    <content><![CDATA[<h1 id="什么是explain"><a href="#什么是explain" class="headerlink" title="什么是explain"></a>什么是explain</h1><p> EXPLAIN 关键字可以<strong>模拟优化器执行 SQL 查询语句</strong>，根据返回信息得知 MySQL 是<strong>如何处理对应的SQL语句的</strong>。 进而可以对语句的性能进行分析优化。</p>
<a id="more"></a>

<p>用法： Explain+SQL 语句。 </p>
<p><img src="https://s1.ax1x.com/2020/09/24/0p84IJ.png" alt="0p84IJ.png"></p>
<p>这些表里的选项都是些什么意思呢？</p>
<h1 id="表格分析"><a href="#表格分析" class="headerlink" title="表格分析"></a>表格分析</h1><h2 id="id"><a href="#id" class="headerlink" title="id"></a>id</h2><p>表示查询中执行 select 子句或操作表的顺序。</p>
<ul>
<li>id相同时， 执行顺序从上到下</li>
<li>id不同时， id越大，越先执行。</li>
<li>id相同和不同的，<strong>同时存在</strong>：<strong>相同的可以认为是一组</strong>，从上往下顺序执行，在所有组中，<strong>id值越大，优先级越高</strong>，越先执行。</li>
</ul>
<hr>
<h2 id="select-type"><a href="#select-type" class="headerlink" title="select_type"></a>select_type</h2><p><code>select_type</code> 代表<strong>查询的类型</strong>，主要是用于区别<strong>普通查询、联合查询、子查询</strong>等的复杂查询。</p>
<table>
<thead>
<tr>
<th align="left">Value</th>
<th align="left">Meaning</th>
</tr>
</thead>
<tbody><tr>
<td align="left">SIMPLE</td>
<td align="left">简单的select 不包括UNION或者子查询</td>
</tr>
<tr>
<td align="left"><strong>PRIMARY</strong></td>
<td align="left">子查询的外层被标</td>
</tr>
<tr>
<td align="left">UNION</td>
<td align="left">第二个 select 出现在union之后</td>
</tr>
<tr>
<td align="left"><strong>SUBQUERY</strong></td>
<td align="left">在<strong>SELECT或WHERE列表</strong>中包含了<strong>子查询</strong></td>
</tr>
<tr>
<td align="left">UNCACHEABLE SUBQUERY</td>
<td align="left">A subquery for which the result cannot be cached and must be re-evaluated for each row of the outer query</td>
</tr>
<tr>
<td align="left"><strong>DERIVED</strong></td>
<td align="left">在 <strong>FROM 列表中包含的子查询被标记为</strong> DERIVED</td>
</tr>
<tr>
<td align="left">UNION RESULT</td>
<td align="left"><strong>从UNION表获取结果的SELECT</strong></td>
</tr>
</tbody></table>
<h2 id="table"><a href="#table" class="headerlink" title="table"></a><strong>table</strong></h2><p>对应行<strong>正在访问哪一个表</strong>，表名或者别名，可能是临时表或者union合并结果集</p>
<h2 id="type"><a href="#type" class="headerlink" title="type"></a>type</h2><p> <strong>访问类型</strong>， 表示我是<strong>以何种方式去访问我们的数据</strong> ，性能从高到低对应依次是：</p>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">system</span> &gt; const &gt; eq_ref &gt; <span class="keyword">ref</span> &gt; fulltext &gt; ref_or_null &gt; index_merge &gt; unique_subquery &gt; index_subquery &gt; range &gt; <span class="keyword">index</span> &gt; <span class="keyword">ALL</span></span><br></pre></td></tr></table></figure>

<p>常用的几个：</p>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">system</span> &gt; const &gt; <span class="keyword">ref</span>&gt; range &gt; <span class="keyword">index</span> &gt; <span class="keyword">ALL</span></span><br></pre></td></tr></table></figure>

<p>一般情况下，得保证查询至少达到<strong>range级别，最好能达到ref</strong>.</p>
<ul>
<li><code>system</code>:系统级别</li>
<li><code>const</code>:常量级别，表示<strong>通过索引一次</strong>就找到了。</li>
<li><code>ref</code>: 返回所有<strong>匹配某个单独值的行</strong>， <strong>使用了非唯一性索引扫描</strong> ，本质上也是索引访问。</li>
<li><code>range</code>:范围访问，只检索给定范围的行， 一般是使用了<code>between  &gt; &lt;</code>等范围约束条件。</li>
<li><code>inedex</code>:使用了<strong>覆盖索引</strong>或者是利用索引进行了<strong>排序分组</strong>。</li>
<li><code>ALL</code>:全表扫描</li>
</ul>
<h2 id="key相关"><a href="#key相关" class="headerlink" title="key相关"></a>key相关</h2><ul>
<li><code>possible keys</code>: 可能用到的索引。</li>
<li><code>key</code>： 实际用到的 索引</li>
<li><code>key_len</code> :  索引中使用的<strong>字节数</strong>， 越长，说明<strong>索引使用的越充分。</strong></li>
</ul>
<h2 id="ref"><a href="#ref" class="headerlink" title="ref"></a>ref</h2><p>显示<strong>索引的哪一列被使用了</strong>。</p>
<p>注意不要同前面的  访问类型 <code>type</code>中的ref<strong>造成混淆</strong>（表示使用了 非唯一性索引扫描）</p>
<h2 id="rows"><a href="#rows" class="headerlink" title="rows"></a>rows</h2><p>rows 列显示 MySQL 认为它执行查询时<strong>必须检查的行数</strong>。越少越好！</p>
<h2 id="Extra"><a href="#Extra" class="headerlink" title="Extra"></a>Extra</h2><p>额外的信息，常见的内容和说明如下：</p>
<ul>
<li><p><code>using filesort</code>  : <strong>无法利用索引进行排序</strong>，只能利用<strong>排序算法进行排序</strong>，会消耗额外的空间 </p>
</li>
<li><p><code>using temporary</code>:建立<strong>临时表</strong>来保存中间结果，查询完成之后把<strong>临时表删除</strong>。 </p>
</li>
<li><p><code>using index</code>:  当前的查询是<strong>索引覆盖</strong>（要查询的内容<strong>通过索引完全获取</strong>，而不用访问数据表）。如果同时出现<code>using where</code> 表名索引被用来执行<strong>索引键值</strong>的查找，如果没有，表面索引被用来<strong>读取数据</strong> 。 </p>
</li>
<li><p>using where:<strong>使用where进行条件过滤</strong>。 </p>
</li>
<li><p>using join buffer:使用连接缓存（ 在JOIN的 <strong>Blocked Nested Loop 算法</strong>中适用（驱动表的数据加入到了 join buffer中))。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>mysql</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>java异常</title>
    <url>/2020/08/27/java%E5%BC%82%E5%B8%B8/</url>
    <content><![CDATA[<h1 id="什么是异常"><a href="#什么是异常" class="headerlink" title="什么是异常"></a>什么是异常</h1><p>​        异常指出现的各种<strong>非正常</strong>的状况，如：文件找不到、网络连接失败、除0操作、非法参数等。异常是一个事件，它发生在<strong>程序运行期间</strong> 。</p>
<p>​        当发生异常情况时，一个<strong>代表该异常</strong>的<strong>对象</strong>被创建并且<strong>在导致该异常的方法中被抛出</strong>，而该方法可以选择<strong>自己处理异常</strong>或者<strong>传递</strong>该异常。</p>
<a id="more"></a>



<h1 id="Java异常体系结构"><a href="#Java异常体系结构" class="headerlink" title="Java异常体系结构"></a>Java异常体系结构</h1><h2 id="结构图"><a href="#结构图" class="headerlink" title="结构图"></a>结构图</h2><img src="https://img.favpng.com/19/1/6/exception-handling-java-class-hierarchy-method-static-variable-png-favpng-aCfKQLTaPNt32TX6mfSJ0twBx.jpg" style="zoom:80%;" />

<h2 id="详解"><a href="#详解" class="headerlink" title="详解"></a>详解</h2><ol>
<li><code>Throwable</code>分为两个分支：<code>Error</code> 和<code>Exception</code></li>
</ol>
<ul>
<li><code>Error</code>，指 java 运行时系统的<strong>内部错误和资源耗尽错误</strong>。应用程序不会抛出该类对象。 时候<strong>程序无法捕获或者 无法处理</strong>的错误， 出现了这样的错误，除了告知用户，剩下的就是尽力使程序安全的终止。</li>
<li>另一个分支是<code>Exception</code>，是用户程序<strong>可能捕捉</strong>或者<strong>处理</strong>的异常情况 。<ul>
<li>按照<strong>发生时间</strong>分为：<strong>运行时异常</strong>(<code>RuntimeException</code>)和<strong>非运行时异常</strong>。 </li>
<li>按照是<strong>否受到检查</strong>分为：<strong>不受检查异常</strong>（<code>Unchecked Exception</code>）和<strong>检查异常</strong>（<code>Checked Exception</code>）。 </li>
</ul>
</li>
</ul>
<ol start="2">
<li><code>RuntimeException</code>一般在 <strong>程序运行期间</strong> 发生，<strong>编译时期</strong>是无法获悉是否会发生异常的，因此<strong>若不处理也可以编译通过</strong>，因为它不受检查。  而非运行时异常，是可以提前预见到的，必须在代码中进行处理，否则 <strong>编译不会通过</strong>。</li>
</ol>
<ol start="3">
<li><p>派生 于 <code>Erro</code>r 类 或 <code>RuntimeException</code> 类的所有异常称为<strong>非受查( unchecked ) 异常</strong>，所有<strong>其他的异常称为受查</strong>（ checked) 异常。</p>
<ul>
<li><p>出现 <code>RuntimeException</code>一定是<strong>程序编写</strong>出现了问题；</p>
</li>
<li><p>而<strong>程序本身没有问题</strong>， 但由于像 <strong>I</strong>/<strong>O</strong> 错误这类问题导致的异常属于<strong>非运行时异常</strong>。</p>
</li>
</ul>
<p><strong>所有的受查异常 一定要在程序中得到处理</strong>。要么使用<code>try-catch</code>语句进行捕获，要么用<code>throws</code>子句抛出，否则编译无法通过。</p>
</li>
</ol>
<h1 id="如何处理异常"><a href="#如何处理异常" class="headerlink" title="如何处理异常"></a>如何处理异常</h1><h2 id="抛出异常"><a href="#抛出异常" class="headerlink" title="抛出异常"></a>抛出异常</h2><pre><code>对于异常情形， 无法继续运行下去，在当前环境下无法解决，所能做的就是**从当前环境中跳出**，并把问题提**交给上一级**环境，这就是抛出异常时所发生的事情。</code></pre><p>抛出异常后：</p>
<ol>
<li>像创建普通的java对象一样 将使用<code>new</code>在堆上<strong>创建</strong>一个异常对象；</li>
<li>从当前环境中<strong>弹出对异常对象的引用</strong>。此时，<strong>异常处理机制接管程序</strong>， <strong>异常处理器</strong>继续执行程序， 它的任务是将程序<strong>从错误状态中恢复</strong>，以使程序要么换一种方式运行，要么继续运行下去。</li>
</ol>
<h2 id="捕获异常"><a href="#捕获异常" class="headerlink" title="捕获异常"></a>捕获异常</h2><p>​        在方法抛出异常之后，运行时系统将转为寻找合适的异常处理器（exception  handler）。当异常处理器所能处理的异常类型与方法抛出的异常类型相符时，即为合适的异常处理器。</p>
<ol>
<li><code>try catch</code>语句</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>&#123;</span><br><span class="line">    <span class="comment">//可能发生异常的代码    又叫监控区域</span></span><br><span class="line">&#125;<span class="keyword">catch</span>(Exception e)&#123;</span><br><span class="line">   <span class="comment">//catch 用来捕获异常       若异常类型与括号内匹配，则按照原则，不再向后面进行判断</span></span><br><span class="line">&#125;<span class="keyword">catch</span>(Exception e)&#123;        <span class="comment">//故要求 有多个catch 时，异常类型级别越低，越要写到前面，防止被短路。</span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li><code>try</code>嵌套</li>
</ol>
<p><code>嵌套try语句</code>：<code>try</code>语句可以被嵌套。也就是说，一个<code>try</code>语句可以在另一个<code>try</code>块的内部。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NestTry</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        <span class="keyword">try</span>&#123;</span><br><span class="line">            <span class="keyword">int</span> a = args.length;</span><br><span class="line">            <span class="keyword">int</span> b = <span class="number">42</span> / a;</span><br><span class="line">            System.out.println(<span class="string">"a = "</span>+ a);</span><br><span class="line">            <span class="keyword">try</span>&#123;</span><br><span class="line">                <span class="keyword">if</span>(a == <span class="number">1</span>)&#123;</span><br><span class="line">                a = a/(a-a);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span>(a == <span class="number">2</span>)&#123;</span><br><span class="line">                    <span class="keyword">int</span> c[] = &#123;<span class="number">1</span>&#125;;</span><br><span class="line">                    c[<span class="number">42</span>] =<span class="number">99</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">catch</span>(ArrayIndexOutOfBoundsException e)&#123;    <span class="comment">//若 内部的 异常和catch 不匹配，则将异常抛出到外部</span></span><br><span class="line">                System.out.println(<span class="string">"ArrayIndexOutOfBounds :"</span>+e);</span><br><span class="line">            &#125;    </span><br><span class="line">        &#125;<span class="keyword">catch</span>(ArithmeticException e)&#123;</span><br><span class="line">            System.out.println(<span class="string">"Divide by 0"</span>+ e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><p><code>throw</code>：应用于某个语句，<strong>抛出明确的异常</strong>。程序执行完<code>throw</code>语句之后<strong>立即停止</strong>；<code>throw</code>后面的<strong>任何语句不被执行</strong>，最邻近的<code>try</code>块用来检查它是否含有一个与异常类型匹配的<code>catch</code>语句</p>
</li>
<li><p><strong><code>throws</code>子句</strong>:   应用于<strong>方法</strong>， 使该方法的 <strong>调用者</strong> 可以  <strong>保护它们自己</strong>  而不发生异常。</p>
</li>
</ol>
<h1 id="创建自己的异常"><a href="#创建自己的异常" class="headerlink" title="创建自己的异常"></a>创建自己的异常</h1><p> 在程序中，可能会遇到任何标准异常类都没有能够充分地描述清楚的问题。 在这种情况下，创建自己的异常类就是一件顺理成章的事情了。 我们需要做的只是定义一个派生于Exception 的类，或者派生于 Exception 子类的类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyException</span> <span class="keyword">extends</span> <span class="title">Exception</span> </span>&#123;   <span class="comment">//继承 Exception</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> detail;</span><br><span class="line">    MyException(<span class="keyword">int</span> a)&#123;</span><br><span class="line">        detail = a;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"MyException ["</span>+ detail + <span class="string">"]"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestMyException</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">compute</span><span class="params">(<span class="keyword">int</span> a)</span> <span class="keyword">throws</span> MyException</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Called compute("</span> + a + <span class="string">")"</span>);</span><br><span class="line">        <span class="keyword">if</span>(a &gt; <span class="number">10</span>)&#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> MyException(a);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"Normal exit!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String [] args)</span></span>&#123;</span><br><span class="line">        <span class="keyword">try</span>&#123;</span><br><span class="line">            compute(<span class="number">1</span>);</span><br><span class="line">            compute(<span class="number">20</span>);</span><br><span class="line">        &#125;<span class="keyword">catch</span>(MyException me)&#123;</span><br><span class="line">            System.out.println(<span class="string">"Caught "</span> + me);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h1><h3 id="比较throw和throws"><a href="#比较throw和throws" class="headerlink" title="比较throw和throws"></a>比较throw和throws</h3><ol>
<li><p>throws出现在<strong>方法函数头</strong>；而throw出现在<strong>函数体</strong>。</p>
</li>
<li><p>throws表示出现<strong>异常的一种可能性，并不一定会发生这些异常</strong>；throw<strong>则是抛出了异常</strong>，执行throw则一<strong>定抛出了某种异常对象</strong>。执行到throw ，功能也就结束了</p>
</li>
<li><p>两者都是消极处理异常的方式（这里的消极并不是说这种方式不好），只是抛出或者可能抛出异常，但是<strong>不会由函数去处理异常</strong>，真正的处理异常由<strong>函数的上层调用</strong>处理。</p>
</li>
</ol>
<h3 id="try-catch-finally省略"><a href="#try-catch-finally省略" class="headerlink" title="try-catch-finally省略"></a>try-catch-finally省略</h3><p>try-catch-finally 其中<code>catch</code> 和 <code>finally</code> 都可以被省略，但是<strong>不能同时省略</strong>，也就是说有 <strong>try 的时候，必须后面跟一个 catch 或者 finally。</strong></p>
<h3 id="try-catch-finally-中return"><a href="#try-catch-finally-中return" class="headerlink" title="try-catch-finally 中return"></a>try-catch-finally 中return</h3><ul>
<li>try-catch-finally 中，如果 catch 中 return 了，finally 还会执行吗？</li>
</ul>
<p>finally <strong>一定会执行</strong>，即使是 catch 中 return 了，<strong>catch 中的 return</strong> <strong>会等 finally 中的代码执行完之后，</strong>才会执行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FinallyDemo2</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(getInt());</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">getInt</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> a = <span class="number">10</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(a / <span class="number">0</span>);  <span class="comment">//异常发生</span></span><br><span class="line">            a = <span class="number">20</span>;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ArithmeticException e) &#123;</span><br><span class="line">            a = <span class="number">30</span>;</span><br><span class="line">            <span class="keyword">return</span> a;</span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">             * return a 在程序执行到这一步的时候，这里不是return a 而是 return 30；这个返回路径就形成了</span></span><br><span class="line"><span class="comment">             * 但是呢，它发现后面还有finally，所以继续执行finally的内容，a=40</span></span><br><span class="line"><span class="comment">             * 再次回到以前的路径,继续走return 30，形成返回路径之后，这里的a就不是a变量了，而是常量30</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            a = <span class="number">40</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> a;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">结果: <span class="number">30</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>单调栈</title>
    <url>/2020/09/20/%E5%8D%95%E8%B0%83%E6%A0%88/</url>
    <content><![CDATA[<h1 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h1><p>在leetcode上做了一些关于栈的题目，发现栈里面的单调栈频率特别高，而且也比较锻炼思维能力， 本文找了几个经典的、思路又极其相似的几个题目进行总结一下。</p>
<a id="more"></a>

<p>这几个题目分别是：</p>
<p><a href="https://leetcode-cn.com/problems/trapping-rain-water/" target="_blank" rel="noopener">1. 接雨水</a></p>
<p><a href="https://leetcode-cn.com/problems/largest-rectangle-in-histogram/" target="_blank" rel="noopener">2. 柱状图中最大的矩形</a></p>
<p><a href="https://leetcode-cn.com/problems/maximal-rectangle/" target="_blank" rel="noopener">3.数组中1组成的最大矩形</a></p>
<h1 id="接雨水"><a href="#接雨水" class="headerlink" title="接雨水"></a>接雨水</h1><p><img src="https://assets.leetcode-cn.com/aliyun-lc-upload/uploads/2018/10/22/rainwatertrap.png" alt=""></p>
<p>可以使用动态规划，也可以使用栈来解决。 主要分析栈的思想。</p>
<p>维护一个栈，其中记录 <strong>下标</strong> ，下标用于记录 尚未找到 右边比本身高的柱子的下标，当找到以后，弹出当前 元素，和高度相同的元素，这样栈顶保存的值 就是 左边的界限柱子下标，而height[i]就是右边的界限下标， 取二者高度的较小值， 与当前宽度相乘 即为 当前这层可以存储的雨水量。</p>
<p>其实更简单的理解就是， <strong>每次都要帮栈顶的元素 从 剩余的柱子中 找到右边的界限，因为左边的界限是在栈中的</strong>，如果不符合右边界限的条件，那么也将它的下标加入栈中等待寻找。</p>
<p>找到右边界限后，因为<strong>它可能是栈中很多元素的右界限，</strong>所以用<strong>while循环多次判断</strong>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">trap</span><span class="params">(<span class="keyword">int</span>[] height)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(height.length==<span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        LinkedList&lt;Integer&gt; s = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">        <span class="keyword">int</span> res=<span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;height.length;i++)&#123;</span><br><span class="line">            <span class="keyword">while</span>(!s.isEmpty() &amp;&amp; height[s.getLast()]&lt;height[i])&#123;  <span class="comment">//当前的柱子可能是 栈中很多元素的右边界，用while</span></span><br><span class="line">                <span class="keyword">int</span> curId = s.removeLast();</span><br><span class="line">                <span class="keyword">while</span>(!s.isEmpty() &amp;&amp; height[s.getLast()]==height[curId])</span><br><span class="line">                    s.removeLast();</span><br><span class="line">                </span><br><span class="line">                <span class="keyword">if</span>(!s.isEmpty())&#123;</span><br><span class="line">                    <span class="keyword">int</span> stackTop = s.getLast();</span><br><span class="line">                    res+=(Math.min(height[stackTop],height[i])-height[curId])*(i-stackTop-<span class="number">1</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            s.add(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h1 id="柱状图中最大的矩形"><a href="#柱状图中最大的矩形" class="headerlink" title="柱状图中最大的矩形"></a>柱状图中最大的矩形</h1><p><img src="https://assets.leetcode-cn.com/aliyun-lc-upload/uploads/2018/10/12/histogram.png" alt=""></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">largestRectangleArea</span><span class="params">(<span class="keyword">int</span>[] heights)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> len=heights.length;</span><br><span class="line">        <span class="keyword">if</span>(len==<span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> res=<span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        LinkedList&lt;Integer&gt; stack = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">            <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; heights[i]&lt;heights[stack.getLast()])&#123;</span><br><span class="line">                <span class="keyword">int</span> curHeight = heights[stack.removeLast()];</span><br><span class="line">                <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; curHeight==heights[stack.getLast()])</span><br><span class="line">                    stack.removeLast();</span><br><span class="line">                </span><br><span class="line">                <span class="keyword">int</span> width;</span><br><span class="line">                <span class="keyword">if</span>(stack.isEmpty())</span><br><span class="line">                    width=i;</span><br><span class="line">                <span class="keyword">else</span> width = i- stack.getLast()-<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">                res = Math.max(res,(width*curHeight));</span><br><span class="line">            &#125;</span><br><span class="line">            stack.add(i);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(!stack.isEmpty())&#123;    <span class="comment">//处理 stack 中剩余的元素</span></span><br><span class="line">            <span class="keyword">int</span> curHeight = heights[stack.removeLast()];</span><br><span class="line">            <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; curHeight== heights[stack.getLast()])</span><br><span class="line">                stack.removeLast();</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">int</span> width;</span><br><span class="line">            <span class="keyword">if</span>(stack.isEmpty())  width=len;</span><br><span class="line">            <span class="keyword">else</span> width=len-stack.getLast()-<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            res=Math.max(res,(width*curHeight));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其实跟上一个题目：接雨水一样， 维护一个单调栈，是一个单调递增栈，也是没有找到右边界（高度比当前小的）元素的下标。 接雨水是在右边找 高的， 而最大矩形面积是在右边找矮的，总之就是<strong>符合边界条件</strong>的元素</p>
<p>这里维护一个 高度单调递增对应的下标栈， 当栈顶对应高度遇到比自身小的高度时， 栈顶对应高度的矩形最大面积就确定了。</p>
<p>最后stack里面还有剩余元素，这些元素对应的高度没有在右边遇到比它小的高度， 所以要<strong>进行额外处理</strong>。</p>
<h1 id="数组中1组成的最大矩形"><a href="#数组中1组成的最大矩形" class="headerlink" title="数组中1组成的最大矩形"></a>数组中1组成的最大矩形</h1><p>这个题目和上一个更像了，是 上面的拓展</p>
<figure class="highlight prolog"><table><tr><td class="code"><pre><span class="line">输入:</span><br><span class="line">[</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"0"</span>],</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>],</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>,<span class="string">"1"</span>],</span><br><span class="line">  [<span class="string">"1"</span>,<span class="string">"0"</span>,<span class="string">"0"</span>,<span class="string">"1"</span>,<span class="string">"0"</span>]</span><br><span class="line">]</span><br><span class="line">输出：<span class="number">6</span></span><br></pre></td></tr></table></figure>

<p>这里把 从上到下每一层分解为上面的 求矩形最大面积的题目， 按层来算， 若果当前值为0，那么相当于矩形高度为0， 值为1，那么就是上一层的高度+1. <code>dp[j]=dp[j]+1</code>;</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">help</span><span class="params">(<span class="keyword">int</span>[] heights)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">int</span> len=heights.length;</span><br><span class="line">        <span class="keyword">if</span>(len==<span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> res=<span class="number">0</span>;</span><br><span class="line">        LinkedList&lt;Integer&gt; stack = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;len;i++)&#123;</span><br><span class="line">            <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; heights[i]&lt;heights[stack.getLast()])&#123;</span><br><span class="line">                <span class="keyword">int</span> curHeight = heights[stack.removeLast()];</span><br><span class="line">                <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; curHeight==heights[stack.getLast()])</span><br><span class="line">                    stack.removeLast();</span><br><span class="line">                </span><br><span class="line">                <span class="keyword">int</span> width;</span><br><span class="line">                <span class="keyword">if</span>(stack.isEmpty())</span><br><span class="line">                    width=i;</span><br><span class="line">                <span class="keyword">else</span> width = i- stack.getLast()-<span class="number">1</span>;</span><br><span class="line">                res = Math.max(res,(width*curHeight));</span><br><span class="line">            &#125;</span><br><span class="line">            stack.add(i);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span>(!stack.isEmpty())&#123;</span><br><span class="line">            <span class="keyword">int</span> curHeight = heights[stack.removeLast()];</span><br><span class="line">            <span class="keyword">while</span>(!stack.isEmpty() &amp;&amp; curHeight== heights[stack.getLast()])</span><br><span class="line">                stack.removeLast();</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">int</span> width;</span><br><span class="line">            <span class="keyword">if</span>(stack.isEmpty())  width=len;</span><br><span class="line">            <span class="keyword">else</span> width=len-stack.getLast()-<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            res=Math.max(res,(width*curHeight));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">maximalRectangle</span><span class="params">(<span class="keyword">char</span>[][] matrix)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (matrix.length == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> maxarea = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[matrix[<span class="number">0</span>].length];</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; matrix.length; i++) &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; matrix[<span class="number">0</span>].length; j++) &#123;</span><br><span class="line">                dp[j] = matrix[i][j] == <span class="string">'1'</span> ? dp[j] + <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            maxarea = Math.max(maxarea, help(dp));</span><br><span class="line">        &#125; <span class="keyword">return</span> maxarea;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>数据结构与算法</category>
      </categories>
      <tags>
        <tag>数据结构与算法</tag>
      </tags>
  </entry>
  <entry>
    <title>搭建博客记录</title>
    <url>/2020/07/26/%E5%8D%9A%E5%AE%A2%E5%88%9D%E7%BA%A7%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h3 id="安装nodejs-安装gitbash"><a href="#安装nodejs-安装gitbash" class="headerlink" title="安装nodejs,安装gitbash"></a>安装nodejs,安装gitbash</h3><a id="more"></a>

<figure class="highlight coffeescript"><table><tr><td class="code"><pre><span class="line">node -v <span class="comment">#查看node版本</span></span><br><span class="line"><span class="built_in">npm</span> -v <span class="comment">#查看npm版本</span></span><br><span class="line"><span class="built_in">npm</span> install -g cnpm --registry=http://registry.<span class="built_in">npm</span>.taobao.org <span class="comment">#安装淘宝的cnpm 管理器</span></span><br><span class="line">cnpm -v <span class="comment">#查看cnpm版本</span></span><br><span class="line">cnpm install -g hexo-cli <span class="comment">#安装hexo框架</span></span><br><span class="line">hexo -v <span class="comment">#查看hexo版本</span></span><br><span class="line">mkdir blog <span class="comment">#创建blog目录</span></span><br><span class="line"></span><br><span class="line">cd blog <span class="comment">#进入blog目录</span></span><br><span class="line">hexo init <span class="comment"># 初始化hexo框架</span></span><br><span class="line">hexo s <span class="comment">#启动本地博客服务</span></span><br><span class="line">http:<span class="regexp">//localhost:4000/</span> <span class="comment">#本地访问地址`</span></span><br></pre></td></tr></table></figure>
<h3 id="写博客"><a href="#写博客" class="headerlink" title="写博客"></a>写博客</h3><figure class="highlight nginx"><table><tr><td class="code"><pre><span class="line"><span class="attribute">hexo</span> clean <span class="comment"># 清理</span></span><br><span class="line">hexo n <span class="string">"title"</span> <span class="comment"># 新建文档</span></span><br><span class="line">hexo g <span class="comment"># 生成</span></span><br></pre></td></tr></table></figure>
<h3 id="部署到github仓库"><a href="#部署到github仓库" class="headerlink" title="部署到github仓库"></a>部署到github仓库</h3><ul>
<li>在blog目录下找到_config.yml，vim打开</li>
<li>找到 Deployment(在末尾)<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="string">deploy:</span></span><br><span class="line"><span class="string">type:</span> git</span><br><span class="line"><span class="string">repo:</span> <span class="string">https:</span><span class="comment">//github.com/...(这里就是github仓库的地址)</span></span><br><span class="line"><span class="string">branch:</span> master</span><br></pre></td></tr></table></figure></li>
<li><code>hexo  d</code>上传到github</li>
</ul>
<h3 id="文档归类等操作"><a href="#文档归类等操作" class="headerlink" title="文档归类等操作"></a>文档归类等操作</h3><p><a href="[http://www.iooeo.com/2017/07/20/Hexo-%E6%96%B0%E5%BB%BA%E8%8F%9C%E5%8D%95-menu-%E5%AD%98%E6%94%BE%E5%BD%92%E6%A1%A3/](http://www.iooeo.com/2017/07/20/Hexo-新建菜单-menu-存放归档/)">参考链接</a></p>
<h3 id="添加搜索功能（win）"><a href="#添加搜索功能（win）" class="headerlink" title="添加搜索功能（win）"></a>添加搜索功能（win）</h3><p><a href="https://blog.csdn.net/weixin_44381143/article/details/104552635" target="_blank" rel="noopener">参考</a></p>
]]></content>
      <categories>
        <category>各种安装</category>
      </categories>
      <tags>
        <tag>各种安装</tag>
      </tags>
  </entry>
  <entry>
    <title>死锁</title>
    <url>/2020/09/07/%E6%AD%BB%E9%94%81/</url>
    <content><![CDATA[<h1 id="什么是死锁"><a href="#什么是死锁" class="headerlink" title="什么是死锁"></a>什么是死锁</h1><p><strong>多个进程的并发执行</strong>，改善了系统资源的<strong>利用率并提高了系统的处理能力</strong>，为了保证并发的安全运行，也带来了新的问题——死锁。</p>
<p>所谓死锁是指<strong>多个进程因竞争资源</strong>而造成的一种<strong>互相等待</strong>资源的情况，若无外力作用，这些进程都将无法向前推进。</p>
<a id="more"></a>

<h1 id="死锁产生的必要条件"><a href="#死锁产生的必要条件" class="headerlink" title="死锁产生的必要条件"></a>死锁产生的必要条件</h1><ul>
<li><strong>互斥条件</strong>：进程要求对所分配的资源（如打印机）进行排他性控制，即在一段时间内某 资源仅为一个进程所占有。此时若有其他进程请求该资源，则请求进程只能等待。</li>
<li><strong>不剥夺条件</strong>：进程所获得的资源在未使用完毕之前，<strong>不能被其他进程强行夺走</strong>，即只能 由获得该资源的进程自己来释放（只能是主动释放)。</li>
<li><strong>请求和保持条件</strong>：进程已经保持了<strong>至少一个资源</strong>，但又提出了<strong>新的资源请求</strong>，同时对自己已获得的<strong>资源保持不放</strong>。</li>
<li><strong>循环等待条件</strong>：有两个或者两个以上的进程组成一条<strong>环路</strong>，该环路中的每个进程都在<strong>等待下一个进程所占有的资源</strong>。</li>
</ul>
<h1 id="死锁处理策略"><a href="#死锁处理策略" class="headerlink" title="死锁处理策略"></a>死锁处理策略</h1><h2 id="死锁预防"><a href="#死锁预防" class="headerlink" title="死锁预防"></a>死锁预防</h2><h4 id="破坏互斥条件"><a href="#破坏互斥条件" class="headerlink" title="破坏互斥条件"></a>破坏互斥条件</h4><p>如果<strong>允许系统资源都能共享使用</strong>，则系统不会进入死锁状态。不过，在有的场合应该保护这种互斥性，所以<strong>破坏互斥条件而预防死锁的方法不太可行</strong>。</p>
<h4 id="破坏不剥夺条件"><a href="#破坏不剥夺条件" class="headerlink" title="破坏不剥夺条件"></a>破坏不剥夺条件</h4><p>一个已保持了资源的进程，请求新的资源而得不到满足时，它<strong>必须释放已经保持的所有资源</strong>，待以后需要时再重新申请。</p>
<h4 id="破坏请求和保持条件"><a href="#破坏请求和保持条件" class="headerlink" title="破坏请求和保持条件"></a>破坏请求和保持条件</h4><p>釆用预先静态分配方法，即进程<strong>在运行前一次申请完它所需要的全部资源</strong>，在它的资源未满足前，不把它投入运行。一旦投入运行后，这些资源就一直归它所有，<strong>也不再提出其他资源请求</strong>，这样就可以保证系统不会发生死锁。</p>
<h4 id="破坏循环等待条件"><a href="#破坏循环等待条件" class="headerlink" title="破坏循环等待条件"></a>破坏循环等待条件</h4><p>为了破坏循环等待条件，可<strong>釆用顺序资源分配法</strong>。首先给系统中的资源编号，规定每个进程，<strong>必须按编号递增的顺序请求资源</strong>，同类资源一次申请完。</p>
<p>这种方法存在的问题是<strong>限制了新类型设备的增</strong>加；而且容易<strong>造成资源的浪费</strong>；</p>
<h2 id="死锁避免"><a href="#死锁避免" class="headerlink" title="死锁避免"></a>死锁避免</h2><p>避免死锁同样是属于<strong>事先预防</strong>的策略，是在资源动态分配过程中，<strong>防止系统进入不安全状态</strong>，以避免发生死锁。</p>
<h4 id="系统安全状态"><a href="#系统安全状态" class="headerlink" title="系统安全状态"></a>系统安全状态</h4><p>避免死锁的方法中，允许进程<strong>动态地申请资源</strong>，但系统在进行资源分配之前，应先计算此次资源分配的<strong>安全性</strong>。若此次分配不会导致系统进入不安全状态，则将资源分配给进程； 否则，让进程等待。</p>
<p>所谓安全状态，是指系统能按<strong>某种进程推进顺序</strong> ，为每个进程Pi分配其所需资源，直至满足每个进程对资源的<strong>最大需求</strong>，使每个进程都可顺序地完成。此时称 P1, P2, …, Pn 为安全序列。如果系统无法找到一个安全序列，则称系统处于<strong>不安全状态</strong>。</p>
<p>假设系统中有三个进程P1、P2和P3,共有12 台磁带机。进程P1总共需要10台磁带机，P2和P3 分别需要4台和9台。假设在T0时刻，进程P1、P2 和P3已分别获得5合、2台和2台，尚有3台未分配，见表2-15。</p>
<p><img src="https://s2.ax1x.com/2020/03/10/8iVETg.png" alt="8iVETg.png"></p>
<p>则在T0时刻是安全的，因为存在一个安全序列P2、Pl、P3，即<strong>只要系统按此进程序列分配资源</strong>，则每个进程都能顺利完成。若在T0时刻后，系统分配1台磁带机给P3，则此时系统便进入不安全状态，因为此时已<strong>无法再找到一个安全序列</strong>。</p>
<p><strong>并非所有的不安全状态都是死锁状态，但当系统进入不安全状态后，便可能进入死锁状态</strong>；反之，只要系统处于安全状态，系统便可以避免进入死锁状态。</p>
<h4 id="银行家算法"><a href="#银行家算法" class="headerlink" title="银行家算法"></a>银行家算法</h4><p>把<strong>操作系统看做是银行家</strong>，操作系统管理的<strong>资源</strong>相当于银行家管理的<strong>资金</strong>，进程向操作系统请求分<strong>配资源相当于用户向银行家贷款</strong>。 </p>
<p>当进<strong>程首次</strong>申请资源时，要测试该进程对资源的<strong>最大需求量</strong>，如果系统现存的资源可以满足它的<strong>最大需求量</strong>则按当前的申请量分配资源，<strong>否则就推迟分配</strong>。</p>
<p>当进程在执行中继续申请资源时，先测试该进程<strong>已占用的资源数与本次申请的资源数之和是否超过了该进程对资源的最大需求量</strong>。若<strong>超过则拒绝分配资源</strong>，若没有超过则再测试系统现存的资源能否满足该进程尚需的最大资源量，若能满足则按当前的申请量分配资源，否则也要推迟分配。</p>
<h2 id="死锁检测与解除"><a href="#死锁检测与解除" class="headerlink" title="死锁检测与解除"></a>死锁检测与解除</h2><p>思想：不阻止死锁，而是当检测到<strong>死锁发生时，采取措施进行恢复</strong>。</p>
<p> 资源分配图描述如下：圆圈代表<strong>一个进程</strong>，用框代表<strong>一类资源</strong>。 </p>
<p><img src="http://c.biancheng.net/cpp/uploads/allimg/140630/1-1406301I940246.jpg" alt="image"></p>
<p>可以通过将资源分配图简化的方法来检测系统状态是否为死锁状态。简化方法如下：</p>
<p>1) 在资源分配图中，找出<strong>既不阻塞又不是孤点</strong>的进程Pi。消去它所有的<strong>请求边和分配边</strong>，使之成为<strong>孤立的结点</strong>。</p>
<p>2) 进程Pi所释放的资源，可以<strong>唤醒</strong>某些因等待这些资源而<strong>阻塞的进程</strong>，原来的阻塞进程可能变为非阻塞进程。进行一系列简化后,若能消去图中所有的边，则称该图是<strong>可完全简化的</strong>。</p>
<p>死锁的条件是：当且仅当 <strong>资源分配图是 不可完全简化的</strong>,该条件为死锁定理。</p>
<p><img src="http://c.biancheng.net/cpp/uploads/allimg/140630/1-1406301JP45A.jpg" alt="image"></p>
<h4 id="死锁的解除"><a href="#死锁的解除" class="headerlink" title="死锁的解除"></a>死锁的解除</h4><p> <strong>资源剥夺法</strong>：挂起某些死锁进程，并<strong>抢占它的资源</strong>，但<strong>不撤销</strong>进程。将这些资源分配给<strong>其他的死锁进程</strong>。</p>
<p> <strong>撤销进程法</strong>：<strong>强制撤销部分、甚至全部死锁进程</strong>并剥夺这些进程的资源。</p>
<p> <strong>进程回退法</strong>：让一（多）个进程回退到足以<strong>回避死锁</strong>的地步，进程回退时<strong>自愿释放</strong>资源而不是被剥夺。</p>
<h2 id="比较"><a href="#比较" class="headerlink" title="比较"></a>比较</h2><p><img src="https://s2.ax1x.com/2020/03/10/8PHBct.png" alt="8PHBct.png"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
      </tags>
  </entry>
  <entry>
    <title>java反射</title>
    <url>/2020/08/28/java%E5%8F%8D%E5%B0%84/</url>
    <content><![CDATA[<h1 id="什么是反射"><a href="#什么是反射" class="headerlink" title="什么是反射"></a>什么是反射</h1><ul>
<li>Java 中的反射机制是指在<strong>运行状态</strong>中，对于<strong>任意一个类</strong>都能够知道<strong>这个类所有的属性和方法</strong>，并且对于<strong>任意一个对象</strong>，都能够调用它的<strong>任意一个方法</strong>。这种<strong>动态获取信息以及动态调用对象方法</strong>的功能称为 Java 语言的反射机制。                                                                      </li>
<li>简而言之，Java程序中一般的<strong>对象的类型都是在编译期</strong>就确定下来的，而 Java 反射机制可以<strong>动态地创建对象并调用其属性</strong>， 这样的对的类型在<strong>编译期是未知</strong>的，但是可以通过<strong>反射机制</strong>直接创建对象。</li>
</ul>
<a id="more"></a>

<p><strong>反射的原理：</strong></p>
<p>JVM 在<strong>运行时</strong>才<strong>动态加载类或调用方法/访问属性</strong>，它<strong>不需要事先</strong>（写代码的时候或编译期）知道运行对象是谁。</p>
<h1 id="反射的基本使用"><a href="#反射的基本使用" class="headerlink" title="反射的基本使用"></a>反射的基本使用</h1><h2 id="使用步骤"><a href="#使用步骤" class="headerlink" title="使用步骤"></a>使用步骤</h2><ol>
<li>获取class对象 -&gt; 2. 调用 class 中的方法-&gt; 3. 调用反射API操作</li>
</ol>
<h2 id="获取class对象"><a href="#获取class对象" class="headerlink" title="获取class对象"></a>获取class对象</h2><p>有三种方法获取class对象，第三中较好。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//通过对象的 getClass方法获取</span></span><br><span class="line">Class&lt;? extends Employee&gt; aClass = employee.getClass();  </span><br><span class="line"></span><br><span class="line"><span class="comment">//直接调用类名.class获取</span></span><br><span class="line"> Class&lt;Employee&gt; aClass = Employee<span class="class">.<span class="keyword">class</span></span>;     </span><br><span class="line"></span><br><span class="line"><span class="comment">//通过 Class.forName +"类的路径名" 获取</span></span><br><span class="line"> Class aClass = Class.forName(<span class="string">"com.company.demo0823.Employee"</span>);</span><br></pre></td></tr></table></figure>

<h2 id="调用class的常见方法"><a href="#调用class的常见方法" class="headerlink" title="调用class的常见方法"></a>调用class的常见方法</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String name = aClass.getConstructor().getName();  <span class="comment">//获取 构造方法名称</span></span><br><span class="line"></span><br><span class="line">Method[] methods = aClass.getMethods();  <span class="comment">//获取 class中的所有method</span></span><br><span class="line"> 	</span><br><span class="line"><span class="function"><span class="keyword">public</span> Method <span class="title">getMethod</span><span class="params">(String name, Class&lt;?&gt;... parameterTypes)</span></span>;</span><br><span class="line">Method m = aClass.getMethod(...);    <span class="comment">//根据 括号中的参数 获取方法</span></span><br></pre></td></tr></table></figure>

<ul>
<li><code>method</code>方法的调用：<code>invoke</code></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Method m = aClass.getMethod(String<span class="class">.<span class="keyword">class</span>,<span class="title">Integer</span>.<span class="title">class</span>)</span>; </span><br><span class="line">Object res = m.invoke(<span class="string">"李四"</span>,<span class="string">"23"</span>)；   <span class="comment">//invoke调用</span></span><br></pre></td></tr></table></figure>



<h2 id="创建对象的两种方法"><a href="#创建对象的两种方法" class="headerlink" title="创建对象的两种方法"></a>创建对象的两种方法</h2><ol>
<li>直接 newInstance ：需要有默认构造方法</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Employee e= (Employee)aClass.newInstance();</span><br></pre></td></tr></table></figure>

<ol start="2">
<li><code>getDeclaredConstructor</code>获得构造方法</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> Constructor declaredConstructor = aClass.getDeclaredConstructor(<span class="keyword">int</span><span class="class">.<span class="keyword">class</span>, <span class="title">String</span>.<span class="title">class</span>, <span class="title">double</span>.<span class="title">class</span>)</span>;</span><br><span class="line"><span class="comment">//括号内 与 Employee的某个构造方法相当应</span></span><br><span class="line"></span><br><span class="line"> Employee e = (Employee)declaredConstructor.newInstance(<span class="number">3</span>, <span class="string">"李四"</span>, <span class="number">3999.23</span>d);   <span class="comment">//创建</span></span><br></pre></td></tr></table></figure>

<h1 id="反射的用途"><a href="#反射的用途" class="headerlink" title="反射的用途"></a>反射的用途</h1><ol>
<li><p><strong>反射最重要的用途就是开发各种通用框架。</strong>很多框架（比如 Spring）都是配置化的（比如通过 XML 文件配置 Bean），为了保证框架的通用性，它们可能需要根据配置文件加载不同的对象或类，调用不同的方法，这个时候就<strong>必须用到反射</strong>。</p>
</li>
<li><p>编译器里的自动提示功能。</p>
</li>
</ol>
<h1 id="反射与new"><a href="#反射与new" class="headerlink" title="反射与new"></a>反射与new</h1><p>除了使用new创建对象之外， 使用<code>Java反射</code>同样可以创建对象! </p>
<p>通过new创建对象<strong>的效率比较高</strong>。通过反射时，需要：先找<strong>查找类资源</strong>，使用<strong>类加载器</strong>创建，过程比较繁琐，所以效率较低</p>
<h1 id="反射机制的优缺点："><a href="#反射机制的优缺点：" class="headerlink" title="反射机制的优缺点："></a>反射机制的优缺点：</h1><p>优点：</p>
<p>1）能够<strong>运行时动态获取</strong>类的实例，<strong>提高灵活性</strong>；</p>
<p>2）与动态编译结合 </p>
<p>缺点：</p>
<p>1）使用反射<strong>性能较低</strong>，需要<strong>解析字节码</strong>，将内存中的对象进行解析。</p>
<p>2)   相对<strong>不安全</strong>，破坏了<strong>封装性</strong>（因为<strong>通过反射可以获得私有方法和属性</strong>）。</p>
<p>解决方案：</p>
<p>1.通过setAccessible(true)<strong>关闭JDK的安全检查</strong>来提升反射速度；</p>
<p>2.多次创建一个类的实例时，有<strong>缓存</strong>会快很多。</p>
<p>3.通过<strong>优化字节码生成的方式</strong>加快反射速度。</p>
]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>进程管理</title>
    <url>/2020/09/04/%E8%BF%9B%E7%A8%8B%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="大内核和微内核"><a href="#大内核和微内核" class="headerlink" title="大内核和微内核"></a>大内核和微内核</h1><p><strong>大内核</strong>系统： 将操作系统的主要功能模块都作为一个紧密联系的整体运行在核心态。由于各管理模块之间共享信息，能有效利用相互之间的有效特性，所以具有无可比拟的<strong>性能优势</strong>。</p>
<a id="more"></a>

<p>微内核：它将内核中<strong>最基本的功能</strong>（如进程管理等）保留在内核，将不需要在核心态执行的功能移到<strong>用户态</strong>执行，从而降低了内核的设计复杂性。</p>
<p>微内核缺点： 需要频繁地在核心态和用户态之间<strong>进行切换</strong>，执行开销偏大，性能降低。</p>
<h1 id="进程和线程"><a href="#进程和线程" class="headerlink" title="进程和线程"></a>进程和线程</h1><h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><ul>
<li>概念： <strong>进程是程序实体的一次运行过程，是系统进行  资源分配的基本单位</strong> 。</li>
<li><strong>进程与程序的区别</strong>：程序就是一堆代码和数据，是静态的，而进程是程序的运行实例，是动态的。</li>
<li>特征<ul>
<li><strong>动态性</strong>：进程是程序的一次执行，它有着创建、活动、暂停、终止等过程，具有一定的生命周期，是动态地产生、变化和消亡的。动态性是进程最基本的特征。</li>
<li><strong>并发性</strong>：指多个进程实体，同存于内存中，能在一段时间内同时运行，并发性是进程的重要特征，同时也是操作系统的重要特征。引入进程的目的就是为了使程序能与其他进程的程序并发执行，以提高资源利用率。</li>
<li><strong>独立性</strong>：指进程实体是一个能独立运行、独立获得资源和独立接受调度的基本单位。凡未建立PCB的程序都不能作为一个独立的单位参与运行。</li>
<li><strong>异步性</strong>：由于进程的相互制约，使进程具有执行的间断性，即进程按各自独立的、 不可预知的速度向前推进。异步性会导致执行结果的不可再现性，为此，在操作系统中必须配置相应的进程同步机制。</li>
<li><strong>结构性</strong>：每个进程都配置一个PCB（进程控制块Process Control Block)）对其进行描述。从结构上看，进程实体是由程序段、数据段和进程控制段三部分组成的。</li>
</ul>
</li>
</ul>
<h2 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h2><ul>
<li>概念 ：一个进程可分为多个线程，线程是<strong>独立调度</strong>的基本单位</li>
<li>特点： <ul>
<li>程自己<strong>不拥有系统资源</strong>，只拥有一点在运行中必不可少的资源如（<strong>函数栈帧</strong>等）。</li>
<li>可与同属一个进程的其他线程<strong>共享进程所拥有的全部资源</strong>。</li>
<li>一个线程可以创建和撤销另一个线程，同一进程中的多个线程之间可以并发执行。由于线程之间的相互制约，致使线程在运行中呈现出<strong>间断性</strong>。</li>
</ul>
</li>
</ul>
<h2 id="进程和线程的比较"><a href="#进程和线程的比较" class="headerlink" title="进程和线程的比较"></a>进程和线程的比较</h2><ol>
<li><p>拥有资源</p>
<p>进程是资源分配的基本单位，但是线程不拥有资源，线程可以访问隶属进程的资源。</p>
</li>
<li><p>调度</p>
<p>线程是独立调度的基本单位，在同一进程中，线程的切换不会引起进程切换，从一个进程中的线程切换到另一个进程</p>
<p>中的线程时，会引起进程切换。</p>
</li>
<li><p>系统开销</p>
<p>由于创建或撤销进程时，系统都要为之分配或回收资源 ,开销比较大。类似地，在进行进程切换时，涉及当前执行进程 CPU 环境的保存及新调度进程 CPU 环境的设置，而</p>
<p>线程切换时只需保存和设置少量寄存器内容，开销很小。</p>
</li>
<li><p>通信方面</p>
<p>线程间可以通过直接读写同一进程中的数据进行通信，但是进程通信需要借助 IPC。</p>
</li>
</ol>
<h2 id="进程的状态"><a href="#进程的状态" class="headerlink" title="进程的状态"></a>进程的状态</h2><ul>
<li><strong>运行状态</strong> ：正在处理器上运行。单处理器器下，每个时刻最多一个进程在运行。</li>
<li><strong>就绪状态</strong> ：获得了<strong>除处理机外</strong>的一切资源。</li>
<li><strong>阻塞状态</strong> ：<strong>等待某资源的分配</strong>（不包括处理机），<strong>即使处理机空闲</strong>，也不能运行。</li>
<li>创建状态：进程正在被创建。</li>
<li>结束状态：进程正在从系统中消失。</li>
</ul>
<p><img src="https://s1.ax1x.com/2020/09/04/wF9BZ9.png" alt="wF9BZ9.png"></p>
<h1 id="进程调度"><a href="#进程调度" class="headerlink" title="进程调度"></a>进程调度</h1><h2 id="1-进程调度的概念"><a href="#1-进程调度的概念" class="headerlink" title="1.进程调度的概念"></a>1.进程调度的概念</h2><ul>
<li>操作系统中，用户进程数一般都多于处理机数、这将导致它<strong>们互相争夺处理机</strong>。另外，系统进程也同样需要使用处理机。这就要求进程调度程序按一定的策略，动态地把处理机分配给处于就绪队列中的某一个进程，以使之执行。<h2 id="2-具体类型"><a href="#2-具体类型" class="headerlink" title="2.具体类型"></a>2.具体类型</h2><h3 id="CFS"><a href="#CFS" class="headerlink" title="CFS"></a>CFS</h3></li>
<li>CFS （completely fair scheduler<strong>完全公平调度器</strong>）并不采用严格规则来为一个优先级分配某个长度的时间片，而是为每个任务分配<strong>一定比例的CPU处理时间</strong>。</li>
<li>每个任务分配的具体比例是<strong>根据nice值来计算的</strong>。nice值的范围从 <strong>-20 到+19</strong>，<strong>数值较低的nice值表示较高的相对优先级</strong>。具有较低nice值的任务，与具有较高nice值的任务相比，会得到更高比例的处理器处理时间。<strong>默认nice值为 0</strong>。</li>
</ul>
<h3 id="FCFS-先来先服务"><a href="#FCFS-先来先服务" class="headerlink" title="FCFS 先来先服务"></a>FCFS 先来先服务</h3><ul>
<li>每次调度是从就绪队列中选择一个<strong>最先进入该队列的进程</strong>，为之分配处理机。</li>
</ul>
<h3 id="短进程优先"><a href="#短进程优先" class="headerlink" title="短进程优先"></a>短进程优先</h3><ul>
<li>从就绪队列中选出一个<strong>估计运行时间最短</strong>的进程，将处理机分配给它</li>
</ul>
<h3 id="时间片轮转"><a href="#时间片轮转" class="headerlink" title="时间片轮转"></a>时间片轮转</h3><ul>
<li>系统将所有的就绪进程按<strong>先来先服务的原则排成一个队列</strong>、每次调度时，把CPU分<strong>配给队首进程</strong>，并令其执行一个时间片。</li>
<li>当执行的时间片用完时，停止该进程的执行，并将它<strong>送往就绪队列的末尾</strong>；</li>
<li>然后，再把处理机分配给就绪队列<strong>中新的队首进程</strong>，同时也让它执行一个时间片。</li>
</ul>
<p><strong>优点：系统能在给定的时间内响应所有用户的请求。</strong></p>
<h3 id="优先级调度"><a href="#优先级调度" class="headerlink" title="优先级调度"></a>优先级调度</h3><p>把处理机分配给就绪队列中<strong>优先权最高</strong>的进程。具体分为两种：</p>
<p> <strong>1. 抢占式优先权算法</strong></p>
<ul>
<li>一旦把处理机分配给就绪队列中优先权最高的进程后，该进程<strong>便一直执行下去，直至完成</strong>- 这种调度算法主要用于<strong>批处理系统</strong>中；也可用于<strong>某些对实时性要求不严的实时系统</strong>中。</li>
</ul>
<p><strong>2.  非抢占式优先权调度算法</strong></p>
<ul>
<li>系统同样是把处理机分配给优先权最高的进程，使之执行。<strong>但在其执行期间，只要又出现了另一个其优先权更高的进程</strong>，进程调度程序就立即停止当前进程的执行，<strong>重新将处理机分配</strong>给新到的优先权最高的进程。</li>
<li>常用于比较严格的实时系统中，以及对性能要求较高的批处理和分时系统中。</li>
</ul>
<h3 id="多级反馈队列调度"><a href="#多级反馈队列调度" class="headerlink" title="多级反馈队列调度"></a>多级反馈队列调度</h3><p>多级反馈队列调度算法则不必事先知道各种进程所需的执行时间，而且还可以满足各种类型进程的需要，因而它是目前被公认的一种<strong>较好的进程调度算法</strong>。调度算法的实施过程如下所述：</p>
<p>1）<strong>应设置多个就绪队列，并为各个队列赋予不同的优先级</strong>。第一个队列的优先级最高，第二个队列次之，其余各队列的优先权逐个降低。该算法赋予各个队列中进程执行<strong>时间片的大小也各不相同</strong>，在优先权愈高的队列中，为每个进程所规定的执行时间片就愈小。</p>
<p>2）当一个新进程进入内存后，<strong>首先将它放入第一队列的末尾</strong>，按<strong>FCFS原则</strong>排队等待调度。当轮到该进程执行时，如它能在该时间片内<strong>完成</strong>，便可准<strong>备撤离系统</strong>；如果它在一个时间片结束时<strong>尚未完成</strong>，调度程序便将该<strong>进程转入第二队列的末尾</strong>，再同样地按FCFS原则等待调度执行；如果它在第二队列中运行一个时间片后仍未完成，再依次将它放入第三队列，……，如此下去。</p>
<p>3）<strong>仅当第1～(i-1)队列均空时，才会调度第i队列中的进程运行</strong>。如果处理机正在第i队列中为某进程服务时，又有新进程进入优先权较高的队列(第1～(i-1)中的任何一个队列)，则此时新进程将<strong>抢占正在运行进程的处理机</strong>，<strong>即第i队列中某个正在运行的进程的时间片用完后，由调度程序选择优先权较高的队列中的那一个进程，把处理机分配给它</strong>。</p>
<h1 id="进程同步"><a href="#进程同步" class="headerlink" title="进程同步"></a>进程同步</h1><h2 id="信号量机制"><a href="#信号量机制" class="headerlink" title="信号量机制"></a>信号量机制</h2><p><strong>PV操作</strong></p>
<p>信号量机制是一种功能较强的机制，可用来解决<strong>互斥与同步</strong>的问题，它只能被两个标准的原语wait(S)和signal(S)来访问，也可以记为“P操作”和“V操作”。</p>
<ul>
<li>P操作，S–; 表示申请</li>
<li>V操作，S++; 表示释放</li>
</ul>
<p><strong>P操作的主要动作</strong>：</p>
<ol>
<li>S减1； </li>
<li>若S减1后仍大于或等于0，则<strong>进程继续执行</strong>；</li>
<li>若S减1后小于0，则该进程被<strong>阻塞后放入</strong>等待该信号量的等待队列中，然后转进程调度。</li>
</ol>
<p><strong>V操作的主要动作</strong>：</p>
<ol>
<li>S加1； </li>
<li>若相加后结果大于0<strong>，则进程继续执行</strong>；</li>
<li>若相加后结果小于或等于0，则从该信号的等待队列中<strong>释放一个等待进程</strong>，然后再返回原进程继续执行或转进程调度。 </li>
</ol>
<h3 id="整型信号量"><a href="#整型信号量" class="headerlink" title="整型信号量"></a>整型信号量</h3><p>定义： 用一个<strong>整型变量</strong>S表示整型信号量</p>
<figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">wait(S)&#123;</span><br><span class="line">    <span class="keyword">while</span> (S&lt;=0);</span><br><span class="line">    <span class="attribute">S</span>=S-1;</span><br><span class="line">&#125;</span><br><span class="line">signal(S)&#123;</span><br><span class="line">    <span class="attribute">S</span>=S+1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>缺点：wait操作中，只要<strong>信号量S&lt;=0即：某些进程得不到处理机，就会不断地测试</strong>。因此，该机<strong>制并未遵循“让权等待” 的准则，而是使进程处于“忙等”的状态</strong>。</p>
<h3 id="记录型信号量"><a href="#记录型信号量" class="headerlink" title="记录型信号量"></a>记录型信号量</h3><ul>
<li>用一个<strong>链表</strong>表示记录型信号量,L指向等待该信号量的下一个进程。</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span>&#123;</span></span><br><span class="line">    <span class="keyword">int</span> value;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">process</span> *<span class="title">L</span>;</span></span><br><span class="line">&#125; semaphore;</span><br></pre></td></tr></table></figure>

<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">wait</span> (<span class="params">semaphore S</span>)</span> &#123; <span class="comment">//相当于申请资源</span></span><br><span class="line">    S.<span class="keyword">value</span>--;</span><br><span class="line">    <span class="keyword">if</span>(S.<span class="keyword">value</span>&lt;<span class="number">0</span>) &#123;</span><br><span class="line">        block(S.L);  <span class="comment">//插入阻塞队列</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">signal</span> (<span class="params">semaphore S</span>)</span> &#123;  <span class="comment">//相当于释放资源</span></span><br><span class="line">    S.<span class="keyword">value</span>++;   </span><br><span class="line">    <span class="keyword">if</span>(S.<span class="keyword">value</span>&lt;=<span class="number">0</span>)&#123;</span><br><span class="line">        wakeup(S.L);  <span class="comment">//进程从阻塞队列释放</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>wait操作，S.value–，当S.value&lt;0时，表示该类资源已分配完毕，因此<strong>进程应调用block原语，进行自我阻塞，放弃处理机</strong>，并插入到该类资源的等待队列S.L中，可见该机制<strong>遵循了“让权等待”的准则</strong>。</li>
<li>signal操作：value++，表示资源释放一个，因此wake up<strong>唤醒队列中的第一个进程</strong>。</li>
</ul>
<h3 id="信号量实现同步"><a href="#信号量实现同步" class="headerlink" title="信号量实现同步"></a>信号量实现同步</h3><ul>
<li>同步：<strong>一种合作关系，为完成某种任务而建立的多个进程或者线程之间的协调调用，次序等待</strong>，传递消息告知资源占用情况。</li>
<li>必须保证<strong>一前一后地执行两个操作</strong>。<ul>
<li>实现方法：</li>
<li><strong>先执行语句的后面</strong>+<code>V操作</code>，</li>
<li><strong>后执行的语句前面</strong>+<code>P操作</code></li>
</ul>
</li>
</ul>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">semaphore S = <span class="number">0</span>;  <span class="comment">//初始值为0</span></span><br><span class="line">P<span class="number">1</span> <span class="comment">( )</span> &#123;</span><br><span class="line">    <span class="comment">// …</span></span><br><span class="line">    x;  <span class="comment">//语句x</span></span><br><span class="line">    V<span class="comment">(S)</span>;  <span class="comment">//告诉进程P2,语句乂已经完成</span></span><br><span class="line">&#125;</span><br><span class="line">P<span class="number">2</span><span class="comment">()</span>）&#123;</span><br><span class="line">    <span class="comment">// …</span></span><br><span class="line">    P<span class="comment">(S)</span> ;  <span class="comment">//检查语句x是否运行完成</span></span><br><span class="line">    y;  <span class="comment">// 检查无误，运行y语句</span></span><br><span class="line">    <span class="comment">// …</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 以上实现的功能就是，保证x在y之前执行</span></span><br></pre></td></tr></table></figure>

<h3 id="信号量实现互斥"><a href="#信号量实现互斥" class="headerlink" title="信号量实现互斥"></a>信号量实现互斥</h3><ul>
<li>互斥：<strong>一种制约关系，当一个进程或者多个进程进入临界区后会进行加锁操作，此时其他进程（线程）无法进入临界区，只有当该进程（线程）使用后进行解锁其他人才可以使用</strong>，这种技术往往是通过阻塞完成。</li>
</ul>
<figure class="highlight reasonml"><table><tr><td class="code"><pre><span class="line">semaphore mutex = <span class="number">1</span>;  <span class="comment">//初化信号量</span></span><br><span class="line">P1 ( ) &#123;</span><br><span class="line">    <span class="comment">// …</span></span><br><span class="line">    <span class="constructor">P(<span class="params">mutex</span>)</span>;  <span class="comment">// 准备开始访问临界资源，加锁</span></span><br><span class="line">    <span class="comment">// 进程P1的临界区</span></span><br><span class="line">    <span class="constructor">V(<span class="params">mutex</span>)</span>;  <span class="comment">// 访问结束，解锁</span></span><br><span class="line">    <span class="comment">// …</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="constructor">P2()</span> &#123;</span><br><span class="line">    <span class="comment">// …</span></span><br><span class="line">    <span class="constructor">P(<span class="params">mutex</span>)</span>; <span class="comment">//准备开始访问临界资源，加锁</span></span><br><span class="line">    <span class="comment">// 进程P2的临界区；</span></span><br><span class="line">    <span class="constructor">V(<span class="params">mutex</span>)</span>;  <span class="comment">// 访问结束，解锁</span></span><br><span class="line">    <span class="comment">// …</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h2 id="管程"><a href="#管程" class="headerlink" title="管程"></a>管程</h2><p><strong>管程定义</strong></p>
<ul>
<li>引入原因：信号量机制编写程序困难、容易出错。</li>
<li>管程是一种<strong>高级的同步机制</strong>。</li>
</ul>
<p><strong>管程组成</strong></p>
<p>1) 局部于管程的共享<strong>结构数据</strong>。</p>
<p>2) 对该数据结构<strong>进行操作的一组过程</strong>。</p>
<p>3) 对局部于管程的共享数据<strong>设置初始值的语句</strong>。</p>
<p>4) 管程的名字</p>
<p><strong>管程基本特性</strong></p>
<p>1) 局部于<strong>管程的数据只能被局部于管程内的过程所访问</strong>。</p>
<p>2) 一个进程只有通过<strong>调用管程内的过程才能进入管程访问共享数据</strong>。（和Ｃ＋＋类有点类似哦！）</p>
<p>3) 每次仅允许<strong>一个进程</strong>在管程内执行某个内部过程。</p>
<h2 id="自旋锁"><a href="#自旋锁" class="headerlink" title="自旋锁"></a>自旋锁</h2><ul>
<li>自旋锁是一种<strong>基础的同步原语</strong>，用<strong>于保障对共享数据的互斥访问</strong>。与互斥锁的相比，在获取锁失败的时候<strong>不会使得线程阻塞而是一直自旋尝试获取锁</strong>。当线程等待自旋锁的时候，CPU不能做其他事情，而是一直处于<strong>轮询忙等的状态</strong>。</li>
<li>由于一直要占用CPU，所有<strong>单核CPU不适合该</strong>机制，适合<strong>多核</strong>的情况。</li>
</ul>
<h1 id="进程通信"><a href="#进程通信" class="headerlink" title="进程通信"></a>进程通信</h1><p><strong>进程的通信方式</strong></p>
<h2 id="匿名管道-pipe"><a href="#匿名管道-pipe" class="headerlink" title="匿名管道(pipe)"></a>匿名管道(pipe)</h2><ul>
<li>管道是<strong>半双工</strong>的，数据只能向一个方向流动；</li>
<li>需要双方通信时，需要建立起两个管道。</li>
<li>只能用于<strong>父子进程或者兄弟进程</strong>之间(具有亲缘关系的进程);<h2 id="有名管道-FIFO"><a href="#有名管道-FIFO" class="headerlink" title="有名管道(FIFO)"></a>有名管道(FIFO)</h2></li>
<li>匿名管道，由于没有名字，只能用于亲缘关系的进程间通信。为了克服这个缺点，提出了有名管道(FIFO)。</li>
<li>有名管道不同于匿名管道之处在于<strong>它提供了一个路径名与之关联</strong>，即使与有名管道的创建进程不存在亲缘关系的进程，只<strong>要可以访问该路径</strong>，<strong>就能够彼此通过有名管道相互通信</strong>.<h2 id="信号-sinal"><a href="#信号-sinal" class="headerlink" title="信号(sinal)"></a>信号(sinal)</h2></li>
<li>用于通知接收进程某个事件已经发生。</li>
<li>信号是Linux系统中用于<strong>进程间互相通信</strong>或者操作的一种机制，信号可以在<strong>任何时候发给某一进程</strong>，而无需知道该进程的状态。</li>
</ul>
<h2 id="共享内存-share-memory"><a href="#共享内存-share-memory" class="headerlink" title="共享内存(share memory)"></a>共享内存(share memory)</h2><ul>
<li>是<strong>最快</strong>的可用IPC形式。是针对其他通信机制运行效率较低而设计的。</li>
<li><strong>内核专门留出了一块内存区</strong>，可以由<strong>需要访问的进程将其映射到自己的私有地址空间</strong>。进程就可以直接读写这一块内存而<strong>不需要进行数据的拷贝</strong>，从而大大提高效率。</li>
<li>由于多个进程共享一段内存，因此需要依靠某种同步机制（如<strong>信号量</strong>）来达到<strong>进程间的同步及互斥</strong>。</li>
</ul>
<h2 id="信号量-semaphore"><a href="#信号量-semaphore" class="headerlink" title="信号量(semaphore)"></a>信号量(semaphore)</h2><ul>
<li><p><strong>信号量是一个计数器</strong>，用于多进程对共享数据的访问，信号量的<strong>意图在于进程间互斥与同步，而不是用于存储进程间通信数据，若要在进程间传递数据需要结合共享内存</strong>。</p>
</li>
<li><p>为了获得共享资源，进程需要执行下列操作：</p>
<p>（1）<strong>创建一个信号量</strong>：这要求调用者指定初始值，对于二值信号量来说，它通常是1，也可是0。</p>
<p>（2）<strong>等待一个信号量</strong>：该操作会测试这个信号量的值，如<strong>果小于0，就阻塞</strong>。也称为P操作。</p>
<p>（3）挂出一个信号量：该操作将信号量的<strong>值加1，也称为V操作</strong>。</p>
</li>
</ul>
<h2 id="套接字-socket"><a href="#套接字-socket" class="headerlink" title="套接字(socket)"></a>套接字(socket)</h2><ul>
<li>套接字是支持TCP/IP的网络通信的基本操作单元，可以看做是<strong>不同主机之间的进程进行双向通信的端点</strong>，简单的说就是通信的两方的一种约定，用套接字中的相关函数来完成通信过程。<h2 id="消息队列-message"><a href="#消息队列-message" class="headerlink" title="消息队列(message)"></a>消息队列(message)</h2></li>
<li>消息队列是存放在<strong>内核中的消息链表</strong>，每个消息队列由消息队列标识符表示。</li>
<li>消息队列在某个进程往一个队列写入消息之前，并<strong>不需要另外某个进程在该队列上等待</strong>消息的到达。</li>
</ul>
<h1 id="僵尸进程"><a href="#僵尸进程" class="headerlink" title="僵尸进程"></a>僵尸进程</h1><h2 id="什么是僵尸进程"><a href="#什么是僵尸进程" class="headerlink" title="什么是僵尸进程"></a>什么是僵尸进程</h2><ul>
<li>僵尸进程是当<strong>子进程比父进程先结束</strong>，而<strong>父进程又没有回收子进程</strong>，释放子进程占用的资源，此时<strong>子进程</strong>将成为一个僵尸进程。</li>
<li>僵尸进程已经不再运行，但是仍然占用系统资源，积累过多会造成内存泄漏。<h2 id="如何避免"><a href="#如何避免" class="headerlink" title="如何避免"></a>如何避免</h2>1) 父进程通过<strong>wait和waitpid等函数等待子进程结束</strong>，这会导致父进程<strong>挂起</strong>。<br>2) 如果父进程很忙，那么可以在子进程结束时发送<strong>SIGCHLD</strong>信号，父进程会<strong>收到该信号</strong>，<strong>调用wait回收</strong>。<br>3)kill发送SIGTERM或者SIGKILL信号，使产生的僵死进程就变成了孤儿进程，这些孤儿进程会被init进程接管</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
      </tags>
  </entry>
  <entry>
    <title>进程和线程</title>
    <url>/2020/07/26/%E8%BF%9B%E7%A8%8B%E5%92%8C%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><ul>
<li>概念： 进程是进程实体的运行过程，是系统进行  ==资源分配的基本单位== 。 </li>
<li>进程与程序的区别：程序就是一堆代码和数据，是静态的，而进程是程序的运行实例，是动态的。 <a id="more"></a></li>
<li>特征<ul>
<li><strong>动态性</strong>：进程是程序的一次执行，它有着创建、活动、暂停、终止等过程，具有一定的生命周期，是动态地产生、变化和消亡的。动态性是进程最基本的特征。</li>
<li><strong>并发性</strong>：指多个进程实体，同存于内存中，能在一段时间内同时运行，并发性是进程的重要特征，同时也是操作系统的重要特征。引入进程的目的就是为了使程序能与其他进程的程序并发执行，以提高资源利用率。</li>
<li><strong>独立性</strong>：指进程实体是一个能独立运行、独立获得资源和独立接受调度的基本单位。凡未建立PCB的程序都不能作为一个独立的单位参与运行。</li>
<li><strong>异步性</strong>：由于进程的相互制约，使进程具有执行的间断性，即进程按各自独立的、 不可预知的速度向前推进。异步性会导致执行结果的不可再现性，为此，在操作系统中必须配置相应的进程同步机制。</li>
<li><strong>结构性</strong>：每个进程都配置一个PCB（进程控制块Process Control Block)）对其进行描述。从结构上看，进程实体是由程序段、数据段和进程控制段三部分组成的。</li>
</ul>
</li>
</ul>
<h2 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h2><ul>
<li>概念 ：==独立调度的基本单位==</li>
<li>特点： <ul>
<li>程自己<strong>不拥有系统资源</strong>，只拥有一点在运行中必不可少的资源如（函数栈帧等）。</li>
<li>可与同属一个进程的其他线程<strong>共享进程所拥有的全部资源</strong>。</li>
<li>一个线程可以创建和撤销另一个线程，同一进程中的多个线程之间可以并发执行。由于线程之间的相互制约，致使线程在运行中呈现出间断性。</li>
</ul>
<hr>
</li>
<li>模型：<ul>
<li>多对一模型： 多个用户级线程映射到一个内核级线程。 </li>
<li>一对一模型：将每个用户级线程映射到一个内核级线程。</li>
<li>多对多模型：将 n 个用户级线程映射到 m 个内核级线程上，要求 m &lt;= n。</li>
</ul>
</li>
</ul>
<h2 id="进程VS线程"><a href="#进程VS线程" class="headerlink" title="进程VS线程"></a>进程VS线程</h2><ul>
<li><strong>引入进程的目的</strong>，是为==了使多道程序并发执行==，以提高资源利用率和系统吞吐量；</li>
<li><strong>引入线程</strong>，则是为了==减小程序在并发执行时所付出的时空开销==，提高操作系统的并发性能。</li>
<li><strong>比较</strong></li>
</ul>
<p>1) 调度。<strong>线程是独立调度的基本单位，进程是资源拥有的基本单位</strong>。在同一进程中，线程的切换不会引起进程切换。在不同进程中进行线程切换,如从一个进程内的线程切换到另一个进程中的线程时，会引起进程切换。</p>
<p>2) <strong>进程有独立的系统资源或地址空间</strong>，而同一进程内的线程<strong>共享</strong>进程的大部分系统资源,包括<strong>堆、代码段、数据段</strong>，每个线程只拥有一些在运行中必不可少的私有属性，比==如线程Id,栈、寄存器、程序计数器PC==(或者说IP)。</p>
<p>3) 并发性。在引入线程的操作系统中，不仅进程之间可以并发执行，而且多个线程之间也可以并发执行，从而使操作系统具有更好的并发性，提高了系统的吞吐量。</p>
<p>4) 系统开销。创建或撤销进程时，系统都要为之分配或回收资源，如内存空间、I/O设备等，因此操作系统所付出的<strong>开销远大于创建或撤销线程时的开销</strong>。此外，由于同一进程内的多个线程共享进程的地址空间，因此，这些线程之间的同步与通信非常容易实现，甚至无需操作系统的干预。</p>
<p>5) 地址空间和其他资源（如打开的文件）：进程的地址空间之间互相独立，同一进程的各线程间共享进程的资源，某进程内的线程对于其他进程不可见。</p>
<p>6) 通信方面：<strong>进程：进程间通信</strong>；而<strong>线程间可以直接读/写进程数据段</strong>（如全局变量）来进行通信–（<strong>需要进程同步和互斥手段的辅助</strong>）</p>
<h2 id="进程的状态"><a href="#进程的状态" class="headerlink" title="进程的状态"></a>进程的状态</h2><ul>
<li><strong>运行状态</strong> ：正在处理器上运行。单处理器器下，每个时刻最多一个进程在运行。</li>
<li><strong>就绪状态</strong> ：获得了除处理机外的一切资源。</li>
<li><strong>阻塞状态</strong> ：等待某资源的分配（不包括处理机），即使处理机空闲，也不能运行。</li>
<li>创建状态：进程正在被创建。</li>
<li>结束状态：进程正在从系统中消失。</li>
<li>前三种是<strong>基本运行状态</strong></li>
</ul>
<h1 id="多线程共享的资源"><a href="#多线程共享的资源" class="headerlink" title="多线程共享的资源"></a>多线程共享的资源</h1><ul>
<li>共享： 堆、全局变量、静态变量、文件</li>
<li>独有： 栈、寄存器</li>
<li>个性： ID</li>
</ul>
<h1 id="父子进程共享资源"><a href="#父子进程共享资源" class="headerlink" title="父子进程共享资源"></a>父子进程共享资源</h1><ul>
<li><p>共享： </p>
<ul>
<li><strong>打开的文件</strong></li>
<li>实际用户ID、实际组ID、有效用户ID、有效组ID</li>
<li>添加组ID</li>
<li><strong>进程组ID</strong></li>
<li>会话期ID</li>
</ul>
</li>
<li><p>区别：</p>
</li>
<li><p>fork的返回值</p>
</li>
<li><p>进程ID</p>
</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
      </tags>
  </entry>
  <entry>
    <title>java泛型</title>
    <url>/2020/08/30/java%E6%B3%9B%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="泛型的概念"><a href="#泛型的概念" class="headerlink" title="泛型的概念"></a>泛型的概念</h1><ul>
<li><p>泛型，即“<strong>参数化类型</strong>”，就是将类型由原来的<strong>具体的类型参数化</strong>，类似于<strong>方法中的变量参数</strong>，此时类型也定义成参数形式（可以称之为类型形参），然后在<strong>使用/调用时</strong>传入具体的类型（类型实参）。</p>
<a id="more"></a>
</li>
<li><p>泛型的目的是在<strong>不创建新的类型</strong>的情况下，<strong>通过泛型指定的不同类型来控制形参具体限制的类型</strong>。也就是说在泛型使用过程中，<strong>操作的数据类型被指定为一个参数</strong> 。</p>
</li>
<li><p>泛型对于<strong>集合类</strong>尤其有用，它提供了<strong>编译期</strong>的类型安全，确保你只能把正确类型的对象放入集合中。在集合类中使用泛型机制编写的程序代码要比那些杂乱地使用Object 变量，然后再进行强制类型转换的代码具有更好的安全性和可读性。</p>
</li>
</ul>
<h1 id="使用泛型"><a href="#使用泛型" class="headerlink" title="使用泛型"></a>使用泛型</h1><p>按照泛型作用的类型，分为<strong>泛型类</strong>、<strong>泛型方法</strong>、<strong>泛型接口</strong>。</p>
<h2 id="泛型类"><a href="#泛型类" class="headerlink" title="泛型类"></a>泛型类</h2><p>在类名后面 加<code>&lt;T&gt;</code>来指代该类是泛型类型，并且类中所有泛型都要使用对应的<code>T</code>来修饰，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Generic</span>&lt;<span class="title">T</span>&gt;</span>&#123;  </span><br><span class="line">    <span class="keyword">private</span> T key;  <span class="comment">//key这个成员变量的类型为T,T的类型由外部指定  </span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Generic</span><span class="params">(T key)</span> </span>&#123; <span class="comment">//泛型构造方法形参key的类型也为T，T的类型由外部指定</span></span><br><span class="line">        <span class="keyword">this</span>.key = key;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getKey</span><span class="params">()</span></span>&#123; <span class="comment">//泛型方法getKey的返回值类型为T，T的类型由外部指定</span></span><br><span class="line">        <span class="keyword">return</span> key;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="泛型方法"><a href="#泛型方法" class="headerlink" title="泛型方法"></a>泛型方法</h2><ul>
<li>在方法前面加上<code>&lt;E&gt;</code>泛型。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt; E &gt; <span class="function"><span class="keyword">void</span> <span class="title">printArray</span><span class="params">( E[] inputArray )</span></span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line"><span class="keyword">for</span> ( E element : inputArray )&#123; </span><br><span class="line">System.out.printf( <span class="string">"%s "</span>, element );</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">   Integer[] arr_int = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>&#125;;</span><br><span class="line">    printArray(arr_int);</span><br><span class="line">    System.out.println(<span class="string">" "</span>);</span><br><span class="line">    String [] arr_str=&#123;<span class="string">"hello"</span>, <span class="string">"world"</span>,<span class="string">"java"</span>,<span class="string">"generic"</span>&#125;;</span><br><span class="line">    printArray(arr_str);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>



<ul>
<li><strong>泛型类中的泛型方法</strong></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GenericClass</span>&lt;<span class="title">T</span>&gt;</span>&#123;</span><br><span class="line">       <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show_1</span><span class="params">(T t)</span></span>&#123;</span><br><span class="line">           System.out.println(t.toString());</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="comment">//在泛型类中声明了一个泛型方法，使用泛型E，这种泛型E可以为任意类型。可以类型与T相同，也可以不同。</span></span><br><span class="line">       <span class="comment">//由于泛型方法在声明的时候会声明泛型&lt;E&gt;，因此即使在泛型类中并未声明泛型，编译器也能够正确识别泛型方法中识别的泛型。</span></span><br><span class="line">       <span class="keyword">public</span> &lt;E&gt; <span class="function"><span class="keyword">void</span> <span class="title">show_3</span><span class="params">(E t)</span></span>&#123;</span><br><span class="line">           System.out.println(t.toString());</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">public</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">show_2</span><span class="params">(T t)</span></span>&#123;</span><br><span class="line">           System.out.println(t.toString());</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>show_1</code><strong>不是</strong>一个泛型方法，只是一个普通方法，因为它的泛型类型取决于<strong>泛型类</strong>，并没有自己的<strong>参数化类型</strong>。</li>
<li><code>show_2</code>和<code>show_3</code>都是泛型方法，无论他们的<strong>泛型说明符</strong>(<T>或者<E>)是否与泛型类冲突，只要它符合在方法名前声明 泛型说明符，那么它就是一个泛型。</li>
</ul>
<p>比如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    Generic&lt;String&gt; stringGeneric = <span class="keyword">new</span> Generic&lt;&gt;();  <span class="comment">//指定 类 的泛型为String类型</span></span><br><span class="line">    stringGeneric.show_1(<span class="string">"show_1"</span>);   <span class="comment">//由于 show_1方法并非泛型方法，而是要与 类 保持一致，所以 参数必须为String</span></span><br><span class="line">    <span class="comment">//        stringGeneric.show_1(11515);  //报错</span></span><br><span class="line">    stringGeneric.show_2(<span class="number">1234</span>);    <span class="comment">//泛型方法，类型随意</span></span><br><span class="line">    stringGeneric.show_2(<span class="number">12.5655</span>);  <span class="comment">//泛型方法，类型随意</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<ul>
<li>静态泛型方法</li>
</ul>
<p>由于 <strong>静态方法 属于类</strong>的内容，所有如果一个<strong>静态方法要设置为泛型方法</strong>，那么其对应的类也应该是<strong>泛型类</strong>。</p>
<h2 id="泛型接口"><a href="#泛型接口" class="headerlink" title="泛型接口"></a>泛型接口</h2><p>接口名后面加<code>&lt;T&gt;</code>指代泛型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Generator</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">next</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>接口的实现类分为两种情况，一种是指定了具体的类型，一种是仍然保持泛型。</p>
<ul>
<li>指定具体类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> <span class="keyword">implements</span> <span class="title">Generator</span>&lt;<span class="title">String</span>&gt;</span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>保持泛型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span>&lt;<span class="title">T</span>&gt; <span class="keyword">implements</span> <span class="title">Generator</span>&lt;<span class="title">T</span>&gt;</span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





</li>
</ul>
<h2 id="通配符"><a href="#通配符" class="headerlink" title="通配符"></a>通配符</h2><p>类型通配符一般是使用<code>?</code>代替<strong>具体的类型参数</strong>。例如 List&lt;?&gt; 在逻辑上是<code>List&lt;String&gt;``,List&lt;Integer&gt;</code> 等所有 List&lt;具体类型实参&gt;的<strong>父类</strong>。</p>
<p><code>?</code>代表的是 <strong>具体</strong> 的一种类型实参，而<strong>不是类型参数</strong>，和可以把<code>?</code>看成所有<strong>类型的父类</strong>，是一种真实的类型。</p>
<p><strong>使用场景：</strong></p>
<ul>
<li><p>具体类型<strong>不确定</strong>的时候，这个通配符就是 <strong>?</strong> ；</p>
</li>
<li><p>当 不需要使用类型的<strong>具体功能</strong>时，只使用<strong>通用方法</strong>时。那么可以用 <code>?</code>通配符来表未知类型。</p>
</li>
</ul>
<p><strong>例如</strong>：在上一篇文章Java 注解的例子中就有使用 通配符</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">getFruitInfo</span><span class="params">(Class&lt;?&gt; clazz)</span></span>&#123;</span><br><span class="line">    ... </span><br><span class="line">     Field[] fields = clazz.getDeclaredFields();  <span class="comment">//使用反射   </span></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>不能确定具体的class类型，并且 使用的方法也是 类的通用方法。</p>
<h1 id="类型擦除"><a href="#类型擦除" class="headerlink" title="类型擦除"></a>类型擦除</h1><p>泛型只<strong>在编译阶段有效</strong>。 也就是说，<strong>泛型在编译过后就会被抛弃</strong>。在虚拟机没有<strong>泛型类型对象</strong>，所有对象都属于<strong>普通类</strong>。</p>
<p> <strong>类型擦除</strong>就是：<strong>擦除（ erased) 类型变量</strong>, 并<strong>替换为限定类型（第一个限定类型的变量）</strong>（无限定的变量用 <code>Object</code>替换)。</p>
<ul>
<li>无限定类型，例如，<code>Pair&lt;T&gt;</code> 的原始类型如下所示：</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Pair</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Object first;</span><br><span class="line">    <span class="keyword">private</span> Object second;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Pair</span><span class="params">(Object first, Object second)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.first = first;</span><br><span class="line">        <span class="keyword">this</span>.second = second;</span><br><span class="line">    &#125;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> Object <span class="title">getFirst</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> first;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> Object <span class="title">getSecond</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> second;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setFirst</span> <span class="params">(Object newValue)</span></span>&#123;</span><br><span class="line">            first = newValue;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setSecond</span> <span class="params">(Object newValue)</span></span>&#123;</span><br><span class="line">            second = newValue;</span><br><span class="line">        &#125;   </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>限定类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Interval</span> &lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">Comparable</span> &amp; <span class="title">Thread</span>&gt;</span>&#123;  <span class="comment">// &amp; 来隔开多个限定</span></span><br><span class="line">    <span class="keyword">private</span> T first;</span><br><span class="line">    <span class="keyword">private</span> T second;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>类型擦除后, 用第一个限定类型 <code>Comparable</code>代替 T</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">原始类型 Interval 如下所示：</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Interval</span> <span class="keyword">implements</span> <span class="title">Serializable</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line"><span class="keyword">private</span> Comparable lower;</span><br><span class="line"><span class="keyword">private</span> Coiparable upper;</span><br></pre></td></tr></table></figure>



<h1 id="强制转换"><a href="#强制转换" class="headerlink" title="强制转换"></a>强制转换</h1><ul>
<li>程序调用泛型方法的时候，如果 <strong>返回类型</strong> 被擦除了，那么编译器将插入<strong>强制类型转换</strong>，以保证 结果的正确性和安全性。</li>
</ul>
<p>例如上面的<code>Pair&lt;T&gt;</code>泛型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pair&lt;String&gt; s=...;</span><br><span class="line">String str = s.getFirst();</span><br></pre></td></tr></table></figure>

<p>过程是这样的:</p>
<ol>
<li><p>在发生 类型 擦除后，用 <code>Object</code>类型替换了<code>String</code>类型，首先调用原始类型的<code>getFirst()</code> 返回<code>Object</code>类型的结果。</p>
</li>
<li><p>插入<strong>强制类型转换</strong>，转换为 <code>String</code>类型。</p>
</li>
</ol>
<p>翻译泛型方法</p>
<h1 id="类型擦除与多态的冲突问题"><a href="#类型擦除与多态的冲突问题" class="headerlink" title="类型擦除与多态的冲突问题"></a>类型擦除与多态的冲突问题</h1><p>着重讲解 一个<strong>类继承了泛型类</strong>，然后<strong>多态调用</strong>引发的 方法的冲突问题及解决。比如下面这种情况。(例子来源于Java核心技术卷一8.5.3节)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DateInterval</span> <span class="keyword">extends</span> <span class="title">Pair</span>&lt;<span class="title">LocalDate</span>&gt;</span>&#123;</span><br><span class="line">    ... ...</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> LocalDate <span class="title">getFirst</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">super</span>.getFirst();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setFirst</span><span class="params">(LocalDate first)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.setFirst(first);</span><br><span class="line">    &#125;</span><br><span class="line">    ... ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是在重写了父类 ，pair中的方法。由于Pair是泛型的使用，所以会有类型擦除现象， 那么 会有下面这种情况</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DateInterval</span> <span class="keyword">extends</span> <span class="title">Pair</span></span>&#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>同样会从<strong>Pair原始类型</strong>中 继承 原始类型的方法，例如，会继承</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">getFirst</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.first; &#125;</span><br></pre></td></tr></table></figure>

<p>在虚拟机看来<code>Object getFirst()和 LocalDate getFirst()</code>是不同的，当我们 使用 多态调用就会产生一些问题，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LocalDate first = LocalDate.of(<span class="number">1990</span>, <span class="number">10</span>, <span class="number">12</span>); LocalDate second = LocalDate.of(<span class="number">2000</span>, <span class="number">11</span>, <span class="number">23</span>);</span><br><span class="line"> </span><br><span class="line">Pair&lt;LocalDate&gt; pair=<span class="keyword">new</span> DateInterval(first,second);   <span class="comment">//父类类型调用 子类 ，典型的多态调用</span></span><br><span class="line">System.out.println(pair.getFirst());</span><br></pre></td></tr></table></figure>

<p>那么就会有问题了：<code>pair</code>中既有``Object getFirst()<code>，又有</code> LocalDate getFirst()`，到底该调用哪一个才正确呢？</p>
<p>解决方法：编译器会 在 <code>DateInterval</code>中生成 一个<strong>桥方法(bridge method)</strong>，变成：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> LocalDate <span class="title">getFirst</span><span class="params">()</span></span>&#123;<span class="keyword">return</span> (Date) <span class="keyword">super</span>.getFirst().clone();&#125;</span><br></pre></td></tr></table></figure>

<p>也就是说，返回类型仍然保持 <code>LocalDate</code>，但是在 方法体中调用 从 Pair继承 的方法，并且对它进行<strong>强制类型</strong>转换。</p>
<p><strong>这样就解决了 类型 擦除 与 继承覆写方法 不一致 的问题</strong>。</p>
<h1 id="Java-泛型转换的事实"><a href="#Java-泛型转换的事实" class="headerlink" title="Java 泛型转换的事实"></a>Java 泛型转换的事实</h1><ul>
<li>java虚拟机中<strong>没有泛型</strong>，只有普通类、普通方法</li>
<li>所有类型参数在编译后，都会被<strong>限定类型（原始类型） 替换</strong>。</li>
<li>用 <strong>合成桥方法</strong> 来保持多态</li>
<li>为保持安全性，有时需要插入<strong>强制类型转换</strong>。</li>
</ul>
]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>内存管理</title>
    <url>/2020/09/07/%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>一个程序<strong>不需要全部调入内存</strong>就可以运行，只调入一部分，当需要的部分不在主存中时，发生缺页中断，从磁盘中调入所需部分。这样使得有限的物理内存可以运行<strong>大的多的程序</strong>，好像<strong>内存增大</strong>了一样。这就是虚拟内存技术。</p>
<a id="more"></a>

<h2 id="内存分配方式"><a href="#内存分配方式" class="headerlink" title="内存分配方式"></a>内存分配方式</h2><h3 id="基本分页存储"><a href="#基本分页存储" class="headerlink" title="基本分页存储"></a>基本分页存储</h3><p>为了尽量避免碎片的产生，引入了分页的思想，把主存<strong>划分为大小固定的块</strong>，块相对较小，作为主存的基本单位。</p>
<p>进程中的块称为页，页的大小应该适中。太大会碎片化加剧，太小置换次数太多，效率降低。</p>
<p>页由<strong>页号</strong>（高地址）和<strong>页内偏量</strong>组成（低地址）。</p>
<p><strong>页表</strong>：操作系统建立的，实现<strong>页号到物理块号</strong>的映射。</p>
<h3 id="基本分段管理"><a href="#基本分段管理" class="headerlink" title="基本分段管理"></a>基本分段管理</h3><p>按照用户进程中的<strong>自然段</strong>来划分逻辑空间。段内空间是连续的，段间空间不要求。</p>
<p>由<strong>段号</strong>（高地址）和<strong>段内偏移</strong>（低地址）组成。</p>
<h3 id="段页式管理"><a href="#段页式管理" class="headerlink" title="段页式管理"></a>段页式管理</h3><p>页式存储能够提高<strong>内存利用率</strong>，段式存储有利于<strong>段的共享</strong>，二者结合起来就是段页存储。</p>
<p>作业的地址空间首先被<strong>划分为多个段</strong>，每个段有自己的<strong>段号</strong>。<strong>每个段中划分为多个页</strong>，每个页有自己的页号。每个进程有<strong>段表</strong>，每个<strong>段有页表</strong>。</p>
<h3 id="比较"><a href="#比较" class="headerlink" title="比较"></a>比较</h3><p><strong>分段分页的比较：</strong></p>
<p>对程序员的透明性：分页透明，但是分段需要程序员显式划分每个段。 </p>
<p>大小是否可变：页的大小不可变，段的大小可以动态改变。</p>
<p>出现原因：分页主要用于实现<strong>虚拟内存</strong>，从而获得更大的地址空间；分段主要是为了有助于<strong>程序和数据</strong>的<strong>共享和保护</strong>。</p>
<h3 id=""><a href="#" class="headerlink" title=""></a></h3><h1 id="虚拟内存管理"><a href="#虚拟内存管理" class="headerlink" title="虚拟内存管理"></a>虚拟内存管理</h1><p>请<strong>求分页管理</strong>是当前最流行的实现虚拟存储器的方法。</p>
<h2 id="页面置换算法"><a href="#页面置换算法" class="headerlink" title="页面置换算法"></a>页面置换算法</h2><h3 id="什么是页面置换"><a href="#什么是页面置换" class="headerlink" title="什么是页面置换"></a>什么是页面置换</h3><p>当发生缺页中断时，如果操作系统内存中<strong>没有空闲页面</strong>，操作系统必须在内存<strong>选择一个页面将其移出内存</strong>，以便为即将调入的页面让出空间。而<strong>用来选择淘汰哪一页的规则叫做页面置换算法</strong>。</p>
<h3 id="最佳置换算法"><a href="#最佳置换算法" class="headerlink" title="最佳置换算法"></a>最佳置换算法</h3><p>这是一种<strong>理想情况下的页面置换算法</strong>，但实际上是<strong>不可能实现</strong>的。</p>
<p><strong>基本思想是</strong>：选择距离下一次被访问间隔指令数目最多的页面进行置换。</p>
<p>这个算法唯一的一个问题就是它无法实现。但是最佳页面置换算法可以用于对可实现算法的<strong>性能进行衡量比较</strong>。</p>
<h3 id="先进先出置换算法（FIFO）"><a href="#先进先出置换算法（FIFO）" class="headerlink" title="先进先出置换算法（FIFO）"></a>先进先出置换算法（FIFO）</h3><p>实质：总是<strong>选择在主存中停留时间最长的一页置换</strong>，即<strong>先进入内存的页，先退出内存</strong>。</p>
<p>理由：最早调入内存的页，其<strong>不再被使用</strong>的可能性比刚调入内存的可能性大。</p>
<p>实现：建立一个<strong>FIFO队列</strong>，收容所有在内存中的页。<strong>被置换页面总是在队列头上进行</strong>。当一个页面被放入内存时，就把它插在队尾上。</p>
<p>缺点：这种算法只是在按<strong>线性顺序访问地址空间时才是理想的</strong>，否则效率不高。因为那些<strong>常被访问的页，往往在主存中也停留得最久</strong>，结果它们因变“老”而不得不被置换出去。</p>
<h3 id="最近最久未使用（LRU）算法"><a href="#最近最久未使用（LRU）算法" class="headerlink" title="最近最久未使用（LRU）算法"></a>最近最久未使用（LRU）算法</h3><p>实质：<strong>当需要置换一页时，选择在最近一段时间里最久没有使用过的页面</strong>予以置换。</p>
<p>LRU算法需要<strong>实际硬件的支持</strong>。其问题是<strong>怎么确定最后使用时间的顺</strong>序，对此有两种可行的办法：</p>
<ul>
<li><p>1.<strong>计数器</strong>:  使每个页表项对应一个使用<strong>时间字段</strong>，并给CPU增加一个计数器。每次存储访问，该时钟都加1。在置换页面时，选择该<strong>计数值最小</strong>的页面。</p>
</li>
<li><p>2.<strong>栈</strong>。用一个<strong>栈保留页号</strong>。每当<strong>访问一个页面时，就把它从栈中取出放在栈顶上</strong>。这样一来，<strong>栈顶总是放有目前使用最多的页，而栈底放着目前最少使用的页</strong>。</p>
</li>
</ul>
<h3 id="未使用算法-NRU"><a href="#未使用算法-NRU" class="headerlink" title="未使用算法(NRU)"></a>未使用算法(NRU)</h3><ul>
<li><p>因实现LRU算法必须有大量硬件支持，一定的软件开销。</p>
<p><strong>最近未使用算法</strong> ： 它在存储分块表的每一表项中<strong>增加一个引用位</strong>，<strong>操作系统定期地将它们置为0</strong>。当某一页被访问时，由<strong>硬件将该位置1</strong>。需要置换时将对应<strong>位置为是的页淘汰出去</strong>，</p>
</li>
</ul>
<h2 id="抖动"><a href="#抖动" class="headerlink" title="抖动"></a>抖动</h2><p>页面的<strong>过于频繁的调度</strong>行为成为抖动，造成处理机大部分时间都是在进行页面的置换，而<strong>不执行进程的指令</strong>，造成效率降低。</p>
<p>原因：<strong>实际可用的物理页面数目小于频繁使用的页面数目</strong>。</p>
<h1 id="地址空间"><a href="#地址空间" class="headerlink" title="地址空间"></a>地址空间</h1><h2 id="逻辑地址"><a href="#逻辑地址" class="headerlink" title="逻辑地址"></a>逻辑地址</h2><p>编译后，<strong>每个模块</strong>都是从0号单元开始编址，称为模板模块的相对地址。</p>
<p>链接器将各个模块连接以后，按照各个模块的顺序，统一从0开始同一编址。</p>
<h2 id="物理地址"><a href="#物理地址" class="headerlink" title="物理地址"></a>物理地址</h2><p>物理地址空间是指内存单元中物理单元的集合，是地址转换的<strong>最终地址</strong>，是物理上存在的。程序运行的时候，<strong>必须把逻辑地址映射到物理地址上执行</strong>，这个过程叫做<strong>地址重定位</strong>。</p>
<h1 id="堆区内存申请"><a href="#堆区内存申请" class="headerlink" title="堆区内存申请"></a>堆区内存申请</h1><h2 id="malloc原理"><a href="#malloc原理" class="headerlink" title="malloc原理"></a>malloc原理</h2><p>为了减少内存碎片和系统调用的开销，malloc其采用<strong>内存池的方式</strong>，<strong>先申请大块内存作为堆区，然后将堆区分为多个内存块</strong>，以<strong>块</strong>作为内存管理的基本单位。</p>
<p>当用户申请内存时，直接从堆区<strong>分配一块合适的空闲块</strong>。</p>
<p>malloc在申请内存时，一般会通<strong>过brk或者mmap系统调用进行申请</strong>。其中当申请内存小于<strong>128K</strong>时，会使用系统函数<strong>brk在堆区中</strong>分配；而当申请内存<strong>大于128K</strong>时，会使用系统函数<strong>mmap在映射区分配</strong>。</p>
<hr>
<p>malloc采用<strong>隐式链表</strong>结构将堆区分成连续的、大小不一的块，包含已分配块和未分配块；</p>
<p>同时malloc采用<strong>显示链表结构</strong>来管理所有的<strong>空闲块</strong>。</p>
<p>当进行内存分配时，malloc会通过<strong>链表遍历所有的空闲块</strong>，选择满足要求的块进行分配；</p>
<p>当进行内存合并时，malloc<strong>采用边界标记法</strong>，根据每个块的前后块是否已经分配来决定是否进行块合并。</p>
<h2 id="内存分配原理brk-mmap"><a href="#内存分配原理brk-mmap" class="headerlink" title="内存分配原理brk/mmap"></a>内存分配原理brk/mmap</h2><p>1、malloc<strong>小于128k的内存</strong>，使用<strong>brk分配内存</strong>，将_edata往高地址推(只分配虚拟空间，不对应物理内存(因此没有初始化)。</p>
<p>2、malloc<strong>大于128k的内存，使用mmap分配内存</strong>，在<strong>堆和栈之间</strong>找一块空闲内存分配(对应独立内存，而且初始化为0)</p>
<hr>
<p>这两种方式分配的<strong>都是虚拟内存</strong>，没有分配物理内存。在第一次访问已分配的虚拟地址空间的时候，发生缺页中断，操作系统负责分配物理内存，然后建立虚拟内存和物理内存之间的映射关系。</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
      </tags>
  </entry>
  <entry>
    <title>centos7安装jdk</title>
    <url>/2020/08/27/centos7%E5%AE%89%E8%A3%85jdk/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>​    网上找了很多Linux 配置 Java开发环境的，但是找到的中文资料博客不是抄袭就是 好多已经过时了，配置起来很麻烦，坑也很多，本文参考一篇了<a href="https://computingforgeeks.com/install-oracle-java-openjdk-14-on-centosfedora-linux/" target="_blank" rel="noopener">英文博客</a>，安装起来畅通无阻。</p>
<a id="more"></a>

<h1 id="官网下载"><a href="#官网下载" class="headerlink" title="官网下载"></a>官网下载</h1><img src="https://s1.ax1x.com/2020/08/27/dfxDne.png" alt="dfxDne.png" border="0" />

<p>选择图中所标记的选项，下载到本地。</p>
<h1 id="传输到虚拟机"><a href="#传输到虚拟机" class="headerlink" title="传输到虚拟机"></a>传输到虚拟机</h1><p>​     我这里用的是secure-CRT 的SFTP 传输。具体步骤可以百度。稍微说一下简单的操作语法。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">lls   --查看本机目录下的文件</span><br><span class="line">lpwd  --查看本机当前路径</span><br><span class="line">lcd   --进行本机目录切换</span><br><span class="line">ls    --虚拟机当前目录下的文件</span><br><span class="line">pwd   -- 查看虚拟机当前路径</span><br><span class="line">cd    -- 虚拟机目录切换</span><br></pre></td></tr></table></figure>

<ul>
<li>在root用户下，创建java文件夹,并且切换到该目录下</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdir /usr/java</span><br></pre></td></tr></table></figure>

<ul>
<li>连接SFTP, 切换到jdk压缩包下载目录下</li>
<li>比如说我的压缩包目录在<code>D:\tmp</code>目录下</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">lcd D:\tmp;</span><br><span class="line">cd /usr/java</span><br><span class="line">put jdk14-bin_tar.gz    #本机文档传输到虚拟机</span><br></pre></td></tr></table></figure>



<h1 id="解压安装"><a href="#解压安装" class="headerlink" title="解压安装"></a>解压安装</h1><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">tar  zxvf 压缩包名称 (tar  zxvf jdk14-bin_tar.gz  )</span><br><span class="line">rm -rf jdk14-bin_tar.gz  #删除安装包</span><br></pre></td></tr></table></figure>



<h1 id="环境配置"><a href="#环境配置" class="headerlink" title="环境配置"></a>环境配置</h1><ul>
<li>编写添加 一个 脚本文件</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cat &lt;&lt;EOF | sudo tee /etc/profile.d/jdk14.sh</span><br><span class="line">export JAVA_HOME=/usr/java/default</span><br><span class="line">export PATH=\$PATH:\$JAVA_HOME/bin</span><br><span class="line">EOF</span><br></pre></td></tr></table></figure>

<ul>
<li>执行脚本</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">source /etc/profile.d/jdk14.sh</span><br></pre></td></tr></table></figure>

<h1 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h1><p>基本安装完成，进行测试</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">java -version</span><br><span class="line">java version "14.0.2" 2020-07-14</span><br><span class="line">Java(TM) SE Runtime Environment (build 14.0.2+12-46)</span><br><span class="line">Java HotSpot(TM) 64-Bit Server VM (build 14.0.2+12-46, mixed mode, sharing)</span><br><span class="line">[root@localhost ~]# javac -version</span><br><span class="line">javac 14.0.2</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">$ vi HelloWorld.java </span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorld</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Hello, World"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"> javac HelloWorld.java</span><br><span class="line"> java HelloWorld</span><br><span class="line">     </span><br><span class="line"> 输出 Hello, World</span><br></pre></td></tr></table></figure>

<p>大功告成</p>
]]></content>
      <categories>
        <category>各种安装</category>
      </categories>
      <tags>
        <tag>各种安装</tag>
      </tags>
  </entry>
  <entry>
    <title>视图存储过程触发器</title>
    <url>/2020/09/27/%E6%95%B0%E6%8D%AE%E5%BA%93%E8%A7%86%E5%9B%BE%E5%AD%98%E5%82%A8%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h1><h2 id="什么是视图"><a href="#什么是视图" class="headerlink" title="什么是视图"></a>什么是视图</h2><p>视图是<strong>虚拟的表</strong>。与包含数据的表不一样，视图只包含使用时动态检索数据的<strong>查询</strong>。视图就是查询，它包含了一系列的SQL语句。</p>
<p>可以将类比 Java中面向对象特性中的 <strong>封装</strong>， 就是屏蔽了一些细节，只暴露出一个简单了对外调用的接口。</p>
<a id="more"></a>

<p>下面举个栗子。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">VIEW</span> selctid3 <span class="keyword">AS</span> <span class="keyword">select</span> * <span class="keyword">from</span> department <span class="keyword">where</span> <span class="keyword">id</span>=<span class="number">3</span>;</span><br></pre></td></tr></table></figure>

<p>使用 <code>CREATE VIEW</code>为操作 创建一个视图，选出 id=3的department 的相关信息。</p>
<p>那么下次调用的时候，如下操作:</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"> <span class="keyword">select</span> * <span class="keyword">from</span> selctid3;   </span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">| id | dep_name | dep_location |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">|  3 | c        | cccc         |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br></pre></td></tr></table></figure>

<p>这就是视图简单的例子。</p>
<h2 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h2><h3 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h3><ul>
<li><p><code>CREATE VIEW</code>语句来创建。</p>
</li>
<li><p>使用<code>SHOW CREATE VIEW viewname</code>；来查看创建视图的语句。</p>
</li>
<li><p><code>DROP VIEW viewname;</code>删除视图。 </p>
</li>
<li><p>更新视图：可以先用<code>DROP</code>再用<code>CREATE</code>，也可以直接用<code>CREATE OR REPLACE VIEW</code>。</p>
</li>
</ul>
<h3 id="限制"><a href="#限制" class="headerlink" title="限制"></a>限制</h3><ul>
<li><p>视图必须<strong>唯一命名</strong>  。</p>
</li>
<li><p>对于可以创建的视图<strong>数目没有限制</strong>。</p>
</li>
<li><p>视图<strong>可以嵌套</strong>，即可以<strong>利用从其他视图中检索数据的查询来构造一个视图。</strong></p>
</li>
<li><p>ORDER BY覆盖： ORDER BY可以用在视图中，但如果从该视图检索数据SELECT中也含有ORDER BY，那么该<strong>视图中的ORDER BY将被覆盖</strong>。</p>
</li>
<li><p>视图<strong>可以和表一起使用</strong>。 </p>
</li>
</ul>
<h2 id="视图的更新"><a href="#视图的更新" class="headerlink" title="视图的更新"></a>视图的更新</h2><p>并非所有视图都是可更新的。 如果MySQL不能正确地确定被更新的基数据，则不允许更新（<strong>包括插入和删除</strong>）。这实</p>
<p>如果视图定义中有以下操作，则不能进行视图的更新：</p>
<ul>
<li><p><strong>分组</strong>（使用GROUP BY和HAVING）；</p>
</li>
<li><p><strong>联结</strong>  (JOIN)</p>
</li>
<li><p><strong>子查询</strong></p>
</li>
<li><p><strong>并</strong> (UNION  UNION ALL)</p>
</li>
<li><p><strong>聚集函数</strong>（Min()、Count()、Sum()等）</p>
</li>
</ul>
<p>视图更新的时候，<strong>不是作用在 视图上</strong>，而是<strong>通过视图 作用在了基表上</strong>， 因为 永远要记住视图只是一个虚表，是一组逻辑，真正的实体落在<strong>基表</strong>上面。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> selIdBiggerThan7;</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">| id | dep_name | dep_location |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">|  8 | adf      | weihai       |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line"></span><br><span class="line">mysql&gt; insert into selIdBiggerThan7(dep_name,dep_location) values('dfadfda','qingdao');</span><br><span class="line">Query OK, 1 row affected (0.00 sec)</span><br><span class="line"></span><br><span class="line">mysql&gt; select * from selIdBiggerThan7;</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">| id | dep_name | dep_location |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br><span class="line">|  8 | adf      | weihai       |</span><br><span class="line">|  9 | dfadfda  | qingdao      |</span><br><span class="line">+<span class="comment">----+----------+--------------+</span></span><br></pre></td></tr></table></figure>



<h2 id="视图的作用"><a href="#视图的作用" class="headerlink" title="视图的作用"></a>视图的作用</h2><ol>
<li><strong>重用 SQL语句</strong>， 创建视图后相同的操作就可以通过视图重复使用。</li>
<li>简化操作， 将复杂的操作语句通过视图封装起来，屏蔽了底层的复杂操作。</li>
<li>保证数据<strong>安全性</strong>， 在创建视图时加入一些条件限制，可以给用户授予表的<strong>特定部分</strong>的访问权限而<strong>不是整个表的</strong>访问权限，保证数据的安全。</li>
<li><strong>连接分区数据</strong>。 为了提升性能，许多数据库在设计时将 大表分成许多小块，使用视图可以很方便的将分区表数据联合查询出来，像在一张表中那样简单。</li>
</ol>
<h1 id="存储过程"><a href="#存储过程" class="headerlink" title="存储过程"></a>存储过程</h1><h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>有些功能<strong>通过一条 SQL语句不可能实现</strong>，需要<strong>多条语句一起执行</strong>， 这时候就需要一个叫做存储过程的功能。</p>
<p>存储过程简单来说，就是为以后的使用而保存的<strong>一条或多条MySQL语句的集合</strong>。 </p>
<h2 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">DELIMITER ;;</span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">PROCEDURE</span> selectItTest()</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">FROM</span> department <span class="keyword">order</span> <span class="keyword">by</span> <span class="keyword">id</span>;</span><br><span class="line"><span class="keyword">UPDATE</span> department <span class="keyword">SET</span> dep_name=<span class="string">'AliBaBa'</span> <span class="keyword">WHERE</span> <span class="keyword">id</span>=<span class="number">2</span> ;</span><br><span class="line"><span class="keyword">select</span> <span class="keyword">id</span>,dep_name <span class="keyword">FROM</span> department <span class="keyword">order</span> <span class="keyword">by</span> <span class="keyword">id</span>;</span><br><span class="line"><span class="keyword">END</span></span><br><span class="line">;;</span><br><span class="line">DELIMITER ;</span><br><span class="line"></span><br><span class="line">mysql&gt; call selectItTest;</span><br><span class="line">+<span class="comment">----+---------------+--------------+</span></span><br><span class="line">| id | dep_name      | dep_location |</span><br><span class="line">+<span class="comment">----+---------------+--------------+</span></span><br><span class="line">|  2 | ByteDance     | bbbb         |</span><br><span class="line">|  3 | c             | cccc         |</span><br><span class="line">|  4 | d             | ddd          |</span><br><span class="line">+<span class="comment">----+---------------+--------------+</span></span><br><span class="line"> </span><br><span class="line">+<span class="comment">----+---------------+--------------+</span></span><br><span class="line">| id | dep_name      | dep_location |</span><br><span class="line">+<span class="comment">----+---------------+--------------+</span></span><br><span class="line">|  2 | AliBaBa     | bbbb         |</span><br><span class="line">|  3 | c             | cccc         |</span><br><span class="line">|  4 | d             | ddd          |</span><br><span class="line">+<span class="comment">----+---------------+--------------+</span></span><br></pre></td></tr></table></figure>

<p><strong>语法解析：</strong></p>
<ul>
<li><p>存储过程名字前面 要加关键字<code>PROCEDURE</code>,</p>
</li>
<li><p>存储过程名字后要加括号<code>()</code></p>
</li>
<li><p>调用存储过程应该使用 :<code>call  procedure_name</code> </p>
</li>
</ul>
<h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><ol>
<li>简单： 使复杂的逻辑变得简单，只需要调用一下即可。</li>
<li>数据完整性： 不需要建立一系列的处理步骤，保证了数据的完整性。</li>
<li>简化变动管理： 变动时可以只修改底层逻辑实现，不影响调用者。</li>
<li>提高性能： 使用存储过程比单独的SQL语句要快。</li>
</ol>
<h1 id="触发器"><a href="#触发器" class="headerlink" title="触发器"></a>触发器</h1><h2 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h2><p>所谓触发器，就是<strong>某些事件发生的时候自动触执行的语句</strong>。</p>
<p><strong>Mysql</strong>只对一下语句进行触发响应：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">INSERT</span>;</span><br><span class="line"><span class="keyword">DELETE</span>;</span><br><span class="line"><span class="keyword">UPDATE</span>;</span><br></pre></td></tr></table></figure>

<h2 id="触发器分类"><a href="#触发器分类" class="headerlink" title="触发器分类"></a>触发器分类</h2><h3 id="INSERT触发器"><a href="#INSERT触发器" class="headerlink" title="INSERT触发器"></a>INSERT触发器</h3><p>MySQL5以后的触发器<strong>不允许返回值</strong>，应该如下使用：<strong>把返回值插入变量中</strong>，在对变量进行select。</p>
<p>有一个名为<strong>new的表</strong>，用于记录新插入的数据。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> newDepName <span class="keyword">AFTER</span> <span class="keyword">INSERT</span> <span class="keyword">ON</span> department</span><br><span class="line"><span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="keyword">ROW</span> <span class="keyword">SELECT</span> NEW.dep_name <span class="keyword">INTO</span>@asd;   <span class="comment">--选择NEW中的dep_name存入asd变量</span></span><br><span class="line"><span class="comment">-- 插入前，变量无值</span></span><br><span class="line"></span><br><span class="line"> <span class="keyword">select</span> @asd;</span><br><span class="line">+<span class="comment">------------+</span></span><br><span class="line">| @asd       |</span><br><span class="line">+<span class="comment">------------+</span></span><br><span class="line">| 0x         |</span><br><span class="line">+<span class="comment">------------+</span></span><br><span class="line"></span><br><span class="line">mysql&gt; insert into department(dep_name,dep_location) values('shehaijiudian','weihai');</span><br><span class="line"> </span><br><span class="line">mysql&gt; select @asd;  -- 查看存入变量的内容</span><br><span class="line">+<span class="comment">---------------+</span></span><br><span class="line">| @asd          |</span><br><span class="line">+<span class="comment">---------------+</span></span><br><span class="line">| shehaijiudian |</span><br><span class="line">+<span class="comment">---------------+</span></span><br></pre></td></tr></table></figure>



<h3 id="DELETE触发器"><a href="#DELETE触发器" class="headerlink" title="DELETE触发器"></a>DELETE触发器</h3><p>使用DELETE触发器前，需要先建立一张对应的 保存 删除内容的表。</p>
<p>有一张<strong>OLD表，用于保存刚刚删除的记录</strong>。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> depArchive(</span><br><span class="line"><span class="keyword">id</span> <span class="built_in">INT</span>,</span><br><span class="line">dep_name <span class="built_in">VARCHAR</span>(<span class="number">20</span>),</span><br><span class="line">dep_location <span class="built_in">VARCHAR</span>(<span class="number">20</span>)</span><br><span class="line">);  <span class="comment">-- 创建 用于保存 删除的记录的表</span></span><br><span class="line"><span class="comment">---------------------------------------------------------------</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TRIGGER</span> DeleDepartment <span class="keyword">BEFORE</span> <span class="keyword">DELETE</span> <span class="keyword">on</span> department  <span class="comment">-- BEFORE DELETE 在删除前触发</span></span><br><span class="line"><span class="keyword">FOR</span> <span class="keyword">EACH</span> <span class="keyword">ROW</span></span><br><span class="line"><span class="keyword">BEGIN</span> </span><br><span class="line"><span class="keyword">insert</span> <span class="keyword">into</span>  depArchive(<span class="keyword">id</span>,dep_name,dep_location) <span class="keyword">VALUES</span>   <span class="comment">-- 删除的值在 OLD表中 ，同new（新插入的）类似</span></span><br><span class="line">(OLD.id,OLD.dep_name,OLD.dep_location);</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span>  <span class="keyword">from</span> department <span class="keyword">where</span> dep_name = <span class="string">'huaweihaisi'</span>;</span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> deparchive;   <span class="comment">-- 此时，表格中已经有被删除的内容了</span></span><br><span class="line">+<span class="comment">------+-------------+--------------+</span></span><br><span class="line">| id   | dep_name    | dep_location |</span><br><span class="line">+<span class="comment">------+-------------+--------------+</span></span><br><span class="line">|   12 | huaweihaisi | shanghai     |</span><br><span class="line">+<span class="comment">------+-------------+--------------+</span></span><br></pre></td></tr></table></figure>

<p><strong>OLD表中的 值是只读的，不允许更新。</strong></p>
<h3 id="UPDATE触发器"><a href="#UPDATE触发器" class="headerlink" title="UPDATE触发器"></a>UPDATE触发器</h3><p>UPDATE触发器在UPDATE语句执行之前或之后执行。</p>
<ul>
<li>在UPDATE触发器代码中，可以引用一个名为OLD的虚拟表访问<strong>UPDATE语句前的值</strong>，引用一个名为NEW的<strong>虚拟表</strong>访问<strong>新</strong></li>
</ul>
<p><strong>更新的值；</strong></p>
<ul>
<li>在BEFORE UPDATE触发器中，<strong>NEW中的值可能也被更新</strong>（允许更改将要用于UPDATE语句中的值）； </li>
</ul>
<h2 id="语法分析"><a href="#语法分析" class="headerlink" title="语法分析"></a>语法分析</h2><p><code>CREATE TRIGGER+名称</code>: 创建 触发器。</p>
<p><code>AFTER INSERT</code>： 在插入之后触发</p>
<p><code>BEFORE INSERT</code>: 在删除之前触发</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>HashMap的使用</title>
    <url>/2020/09/29/HashMap%E7%9A%84%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>HashMap是个使用频率非常高的数据结构， 但是对它的一些 API 和操作还不是很清楚，造成每次使用都迷迷糊糊，所以决定好好整理一下它的一些常用方法。</p>
<a id="more"></a>

<h1 id="增删"><a href="#增删" class="headerlink" title="增删"></a>增删</h1><ol>
<li>定义HashMap</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HashMap&lt;Integer,Integer&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br></pre></td></tr></table></figure>

<p>hashmap存放的是 key-value键值对， 在 &lt;&gt;中指定对应的类型， 只能使用包装类型， 不可以使用基本类型。</p>
<ol start="2">
<li>加入元素</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">map.put(<span class="number">1</span>,<span class="number">2</span>);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>删除元素</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">map.remove(<span class="number">1</span>);   <span class="comment">// 删除 key值 为1 的内容</span></span><br></pre></td></tr></table></figure>

<ol start="4">
<li><strong>使用hashMap计数</strong></li>
</ol>
<p>我们经常使用 HashMap进行元素的个数计数， 用<code>key</code>存放元素，用<code>value</code>存放 出现的次数， 这个时候因为要累计计数，所以使用下面的方法十分方便</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">map.put(key, getOrDefault(map.get(key),<span class="number">0</span>)+<span class="number">1</span>);  </span><br><span class="line"><span class="comment">//getOrDefault的意思是说， 如果之前有key 在map中，则获取它的value值，否则就是为 0</span></span><br></pre></td></tr></table></figure>

<ol start="5">
<li>全部删除</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">map.clear();   <span class="comment">//删除 map中的全部内容</span></span><br></pre></td></tr></table></figure>

<h1 id="判断"><a href="#判断" class="headerlink" title="判断"></a>判断</h1><ol>
<li><strong>根据键值判断</strong></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">boolean</span> map.containsKey(key);   <span class="comment">//判断map中是否有 键值 为key的元素</span></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>根据内容判断</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">boolean</span> map.containsValue(value);  <span class="comment">//判断map 中是否有 值为 value的元素</span></span><br></pre></td></tr></table></figure>

<ol start="3">
<li>是否为空</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">boolean</span> map.isEmpty();</span><br></pre></td></tr></table></figure>

<h1 id="遍历"><a href="#遍历" class="headerlink" title="遍历"></a>遍历</h1><ol>
<li><p>通过 key 获得 value</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(Integer key:map.keySet())    </span><br><span class="line">    System.out.println(<span class="string">"Key: "</span>+key+<span class="string">" Value: "</span>+map.get(key));</span><br></pre></td></tr></table></figure>
</li>
<li><p>通过entrySet的迭代器遍历 entry</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Iterator it=map.entrySet().iterator();  </span><br><span class="line">    <span class="keyword">while</span>(it.hasNext())&#123;</span><br><span class="line">      Map.Entry&lt;Integer, Integer&gt; entry=it.next();</span><br><span class="line">      System.out.println(<span class="string">"Key: "</span>+entry.getKey()+<span class="string">" Value: "</span>+entry.getValue());</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>遍历 entrySet</strong>  </p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(Map.Entry&lt;Integer, Integer&gt; entry: map.entrySet())</span><br><span class="line">System.out.println(<span class="string">"Key: "</span>+ entry.getKey()+ <span class="string">"Value:"</span>+entry.getValue());</span><br></pre></td></tr></table></figure>


<ol start="4">
<li>只获得 value  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(Integer v:map.values())</span><br><span class="line">     System.out.println(<span class="string">"The value is "</span>+v);</span><br></pre></td></tr></table></figure>



</li>
</ol>
<p><strong>推荐使用 第三钟方法</strong>，或者第二种。 牢记<code>Map.Entry&lt; Integer, Integer&gt;entry</code>的用法。</p>
<h1 id="map中的方法（重要）"><a href="#map中的方法（重要）" class="headerlink" title="map中的方法（重要）"></a>map中的方法（重要）</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Set&lt;Integer&gt; integers = map.keySet();   <span class="comment">//获取 key 的set集合</span></span><br><span class="line">Collection&lt;Integer&gt; values = map.values(); <span class="comment">//获取所有的value</span></span><br><span class="line">Set&lt;Map.Entry&lt;Integer, Integer&gt;&gt; entries = map.entrySet();  <span class="comment">//获取 entry集合</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>java基础</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>主从复制</title>
    <url>/2020/09/28/%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6/</url>
    <content><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>一直在说主从复制，这到底是个什么东西？到底是为了什么提出？ 其实从字面上就可以知道， 这是个<strong>数据的复制过程， 把一个服务器数据库上的数据 复制 到另外一个或者几个服务器上</strong> ，数据源服务器就是“主”， 数据目的服务器就是“从”。</p>
<a id="more"></a>

<h1 id="用处"><a href="#用处" class="headerlink" title="用处"></a>用处</h1><ol>
<li><strong>实时备份数据</strong>，发生意外时，让从库随时接管有故障的主库。</li>
<li><strong>读写分离</strong>，主服务器性能较高，可以用作<strong>数据写操作</strong>，从服务器做查询读操作。 读写分离提高性能（主从服务器各自负责读写，<strong>减缓了锁的争用</strong>；  从服务器可以使用<strong>MyIsam存储引擎</strong>，提高查询速度）</li>
</ol>
<h1 id="模式"><a href="#模式" class="headerlink" title="模式"></a>模式</h1><ol>
<li>单向主从（单向数据传输， 主服务器到从服务器）</li>
<li>双向主从 （双向数据传输， 服务器互为对方的master 和 slave）</li>
<li>级联主从 （从服务器后面接着 从服务器）</li>
<li>一主多从 （一台主服务器 多态从服务器）</li>
<li>多主一从     </li>
</ol>
<h1 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h1><p>拿单向主从复制举例， 复制的过程主要涉及三个线程：</p>
<ol>
<li>主机的 <strong>binlog线程</strong>（<strong>IO Dump Thread)</strong>： 把主库接收到的<strong>SQL记录存入 bin_log中</strong>。</li>
<li>从机的 <strong>IO Thread</strong>：  负责请求主机的binlog日志，将得到的内容写入 <strong>从机的 Relay log(中继日志)中</strong>。</li>
<li>从机的 <strong>SQL Thread</strong>：重做Relay log 中的SQL语句。实现内容复制。</li>
</ol>
<p>可见，传递的不是直接的数据，而是传递的SQL操作，<strong>在从机中重做了。</strong></p>
<p>这三个过程中<strong>，前两个都是 顺序 IO，速度较快。 而最后一步则是 随机IO，速度较慢。</strong></p>
<p><img src="https://s1.ax1x.com/2020/09/28/0V6CIf.png" alt="0V6CIf.png"></p>
<h1 id="延迟"><a href="#延迟" class="headerlink" title="延迟"></a>延迟</h1><p>由于种种原因，主从服务器上的数据不可能完全没有延迟，延迟的原因主要有以下几点 ：</p>
<ol>
<li><p>从服务器的 SQL Thread只有<strong>单线程</strong>执行任务。</p>
</li>
<li><p><strong>大事务执行</strong>： binlog的写入必须在事务完成之后，如果事务执行时间过长，那么就会造成在主机写入 binlog时长时间等待，造成延迟。</p>
</li>
<li><p>主库写binlog是顺序IO， IO Thread请求binlog也是顺序IO， 而SQL Thread执行是随机IO，效率低。</p>
</li>
<li><p>网络抖动。</p>
</li>
<li><p>从服务器性能较低。</p>
</li>
</ol>
<h1 id="解决延迟"><a href="#解决延迟" class="headerlink" title="解决延迟"></a>解决延迟</h1><h2 id="并行复制"><a href="#并行复制" class="headerlink" title="并行复制"></a>并行复制</h2><p>上面提到，最主要的原因就是 在 SQL Thread执行阶段效率低，且只有单线程，所以针对这个缺点提出了 并行复制的方案。</p>
<p>从 realy log读取SQL 记录 重做的时候，我们创建多个线程去执行，如下图所示：</p>
<p><img src="https://s1.ax1x.com/2020/09/28/0V2lJf.png" alt="0V2lJf.png"></p>
<p>所谓的并行复制，就是在中间添加了<code>coordinator</code>分发过程，<strong>将任务分发给多个worker同时执行</strong>（人多力量大，很简单的道理）。worker是真正的执行者。</p>
<h2 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h2><p>并行操作的时候，如果不遵守一定的规则，就会造成执行混乱， 那么应该遵守什么样的规则呢？</p>
<ol>
<li><strong>同一行的多个事务必须要分发到同一个woker中执行</strong>（ 若分发到不同的 woker, 可能会造成执行顺序不遵守原来的顺序，产生错误）</li>
<li>同一个事务不能被拆开到不同的事务中， 必须在同一个事务中。</li>
</ol>
<h1 id="分组提交二阶段提交"><a href="#分组提交二阶段提交" class="headerlink" title="分组提交二阶段提交"></a>分组提交二阶段提交</h1><p>我们知道，磁盘的读写速度相较于CPU 或者主存而言，速度极慢，而且过多的IO 也会造成性能下降，所以写日志的时候，如果不进行相应的改良，那么将比较耗费时间和资源。</p>
<p>分组的提交：为了尽量<strong>减少 IO的次数，同时保证数据的 持久性和一致性</strong>，提出了<strong>分组的提交</strong>的概念。 大致思想就是写redolog和 binlog的时候，将操作以组的方式 进行提交。</p>
<p>redolog是 存储引擎级别的，里面记录的是一些具体的物理操作，比如去磁盘的哪个地方执行什么操作</p>
<p>而binlog是属于 服务层次级别的 ， 里面记录的是SQL逻辑操作。</p>
<p>参考<a href="https://mp.weixin.qq.com/s/_LK8bdHPw9bZ9W1b3i5UZA" target="_blank" rel="noopener">图解分组提交</a>写的，</p>
<p><img src="https://s1.ax1x.com/2020/09/29/0ekrAe.png" alt="0ekrAe.png"></p>
<p><strong>Flush 阶段 (图中第一个渡口)</strong></p>
<ul>
<li>首先获取队列中的<strong>事务组</strong></li>
<li>将Redo log中prepare阶段的数据<strong>刷盘(图中Flush Redo log)</strong></li>
<li>将binlog数据写入文件，当然此时只是写入<strong>文件系统的缓冲</strong>，并不能保证数据库崩溃时binlog不丢失 (图中Write binlog)</li>
<li><strong>Flush阶段队列</strong>的作用是提供了<strong>Redo log的组提交</strong>。</li>
<li>如果在这一步完成后数据库崩溃，由于协调者binlog中不保证有该组事务的记录，所以MySQL可能会在重启后回滚该组事务</li>
</ul>
<p><strong>Sync 阶段 (图中第二个渡口)</strong></p>
<ul>
<li>这里为了增加一组事务中的事务数量，提高刷盘收益，MySQL使用两个参数控制获取队列事务组的时机： </li>
</ul>
<p>​    binlog_group_commit_sync_delay=N：在等待N μs后，开始事务刷盘(图中Sync binlog)</p>
<p>​    binlog_group_commit_sync_no_delay_count=N：如果队列中的事务数达到N个，就忽视binlog_group_commit_sync_delay的设置，直接开始刷盘(图中Sync binlog)</p>
<ul>
<li><strong>Sync阶段队列</strong>的作用是<strong>支持binlog的组提交</strong></li>
<li>如果在这一步完成后数据库崩溃，由于协调者binlog中<strong>已经有了事务记录</strong>，MySQL会在重启后通过Flush 阶段中Redo log刷盘的数据继续进行事务的提交</li>
</ul>
<p><strong>Commit 阶段 (图中第三个渡口)</strong></p>
<ul>
<li>首先获取队列中的事务组</li>
<li>依次将Redo log中已经prepare的事务在引擎层提交(图中InnoDB Commit)</li>
<li>Commit阶段不用刷盘，如上所述，Flush阶段中的Redo log刷盘已经足够保证数据库崩溃时的数据安全了</li>
<li>Commit阶段队列的作用是承接Sync阶段的事务，完成最后的引擎提交，使得Sync可以尽早的处理下一组事务，最大化组提交的效率</li>
</ul>
<p>这种实现的<strong>优势在于三个阶段可以并发执行，从而提升效率</strong>。注意prepare阶段没有变，还是write/sync redo log.</p>
<p>由上面可知，<strong>binlog的提交方式是二阶段提交（flush阶段是 binlog 的prepare阶段，完成数据的一些操作， sync阶段是 commit阶段，进行提交，只不过是多了分组操作。</strong></p>
<h2 id="二阶段提交："><a href="#二阶段提交：" class="headerlink" title="二阶段提交："></a><strong>二阶段提交：</strong></h2><p><strong>可以保证数据的一致性。</strong></p>
<ol>
<li><p><strong>prepare</strong>阶段：执行数据<strong>修改和日志记录等处理</strong>，处理完成后只是把事务的状态改成”可以提交”,然后把<strong>结果返回给事务管理器</strong>。</p>
</li>
<li><p><strong>commit</strong>阶段： 如果在第一阶段内有<strong>任何一个数据库的操作发生了错误</strong>，或者事务管理器收不到某个数据库的回应，则认为事务失败，<strong>回撤所有数据库的事务</strong>。数据库服务器收不到第二阶段的确认提交请求，也会把”可以提交”的事务回撤。如果第一阶段中所有数据库都提交成功，那么事务管理器向数据库服务器发出”确认提交”请求，数据库服务器把事务的”可以提交”状态改为”提交完成”状态，然后返回应答。</p>
</li>
<li><p><strong>binlog就是二阶段提交。</strong></p>
</li>
</ol>
<h2 id="二阶段提交参考"><a href="#二阶段提交参考" class="headerlink" title="二阶段提交参考"></a><a href="https://juejin.im/post/6844903929847742478" target="_blank" rel="noopener">二阶段提交参考</a></h2>]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>mysql概念杂记</title>
    <url>/2020/09/29/mysql%E6%A6%82%E5%BF%B5%E6%9D%82%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="预写日志"><a href="#预写日志" class="headerlink" title="预写日志"></a>预写日志</h1><p>WAL：(write ahead  log) 就是在写数据之前 先写日志的情况。</p>
<a id="more"></a>

<p>在使用WAL的系统中，所有的修改在<strong>提交之前都要先写入log文件中</strong>，log文件中通常包括<strong>redo和undo信息</strong>，通过<strong>日志记录</strong>描述好数据的改变后(redo和undo)，再<strong>写入缓存</strong>，等缓存区写满后，最后再<strong>往持久层修改数据</strong>。</p>
<h1 id="GTID"><a href="#GTID" class="headerlink" title="GTID"></a>GTID</h1><h1 id="MTS"><a href="#MTS" class="headerlink" title="MTS"></a>MTS</h1><p>就是<strong>并行复制</strong>  <strong>multi-threaded slave（简称MTS）</strong></p>
<h1 id="TPS"><a href="#TPS" class="headerlink" title="TPS"></a>TPS</h1><p><strong>Transactions Per Second：每秒处理事务数</strong></p>
<h1 id="MySql的整体架构图"><a href="#MySql的整体架构图" class="headerlink" title="MySql的整体架构图"></a>MySql的整体架构图</h1><p><img src="https://user-gold-cdn.xitu.io/2017/10/22/436c6beeb41e0b3980c57df6d5ed4dca?imageView2/0/w/1280/h/960/format/webp/ignore-error/1" alt=""></p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>MVCC</title>
    <url>/2020/09/29/MVCC/</url>
    <content><![CDATA[<h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p><code>MVCC (Multiversion Concurrency Control)</code> 中文全程叫<strong>多版本并发控制</strong>， 是数据库存储引擎实现中常用的<strong>处理读写冲突</strong>的手段，<strong>目的在于提高数据库高并发场景下的吞吐性能</strong>。</p>
<a id="more"></a>

<p> <code>MySQL</code> 中的 <code>MVCC</code> 实现，从 <code>《高性能 MySQL》</code>一书中对 <code>MVCC</code> 的介绍可知：</p>
<ol>
<li><code>MySQL</code> 中 <code>InnoDB</code> 引擎支持 <code>MVCC</code></li>
<li>应对高并发事务, <code>MVCC</code> <strong>比单纯的加行锁更有效, 开销更小</strong></li>
<li><code>MVCC</code> 在<strong>读已提交</strong><code>（Read Committed）</code>和<strong>可重复读</strong><code>（Repeatable Read）</code>隔离级别下起作用</li>
<li><code>MVCC</code> 既可以基于<strong>乐观锁</strong>又可以基于<strong>悲观锁</strong>来实现</li>
</ol>
<h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><p>MVCC的实现，是通过<strong>保存数据在某一个结点的快照实</strong>现的。</p>
<p>MVCC只应用 在 <strong>读已提交</strong>和<strong>可重复读</strong>   隔离级别， 而   <strong>未提交读</strong>  隔离级别总是<strong>读取最新的数据行</strong>，无需使用MVCC。<strong>可串行化隔离</strong>级别需要对  所有读取的行<strong>都加锁</strong>，单纯使用 MVCC 无法实现。</p>
<p>不同的存储引擎，其MVCC的实现原理也不同， 这里主要学习 主流存储引擎InnoDB的实现方式。</p>
<hr>
<p>MVCC 在每行记录后面都保存着<strong>两个隐藏的列</strong>，用来存储两个版本号：</p>
<p><strong>创建版本号</strong>：指示创建一个数据行的快照时的系统版本号；</p>
<p><strong>删除版本号：</strong>如果该快照的删除版本号大于当前事务版本号表示该快照有效，否则表示该快照已经被删除了</p>
<hr>
<p>版本号： 标志当前版本的<strong>连续递增</strong>的号码， <strong>每开启一个事务，系统版本号都会递增</strong>。</p>
<h2 id="SELECT"><a href="#SELECT" class="headerlink" title="SELECT"></a>SELECT</h2><ol>
<li>InnoDB只查找  <strong>版本早于</strong>   当前事务版本的<strong>数据行</strong> ，这样可以确保事务读取的行，要么是在事务<strong>开始前</strong>已经存在的，要么是事务<strong>自身插入或者修改</strong>过的。</li>
<li>行的删除版本要么<strong>未定义</strong>，要么<strong>大于</strong>当前事务版本号。（小于等于的话说明已经被删除了） 这可以确保事务读取到的行，在事务开始之前<strong>未被删除</strong>。</li>
</ol>
<p>只有符合上述两个条件的记录，才能返回作为查询结果</p>
<h2 id="INSERT"><a href="#INSERT" class="headerlink" title="INSERT"></a>INSERT</h2><p>InnoDB为<strong>新插入的每一行</strong>保存<strong>当前系统版本号</strong>作为行版本号。</p>
<h2 id="DELETE"><a href="#DELETE" class="headerlink" title="DELETE"></a>DELETE</h2><p>InnoDB为<strong>删除的每一行</strong>保存<strong>当前系统版本号</strong>作为<strong>行删除标识</strong>。</p>
<h2 id="UPDATE"><a href="#UPDATE" class="headerlink" title="UPDATE"></a>UPDATE</h2><p>InnoDB为<strong>插入一行新记录</strong>，保存当<strong>前系统版本号作为行版本号</strong>，同时保存当前系统版本号到原来的行作为<strong>行删除标识</strong>。</p>
<p>可以理解为 新 插入一条数据，同时作废（删除） 之前旧的数据。</p>
<h1 id="MVCC优缺点"><a href="#MVCC优缺点" class="headerlink" title="MVCC优缺点"></a>MVCC优缺点</h1><h2 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h2><p>由于MVCC的 存在，使得很多操作不用加锁就可以正确的完成，简单高效， 极大的提高了<strong>并发的效率</strong>。</p>
<h2 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h2><p>由于要为每行记录都增加 创建和删除版本号， 所以增加了存储空间</p>
<p>要进行版本号的比较，需要做额外的 <strong>维护工作</strong>和  <strong>查询比较</strong>工作。</p>
<h1 id="乐观锁悲观锁"><a href="#乐观锁悲观锁" class="headerlink" title="乐观锁悲观锁"></a>乐观锁悲观锁</h1><p>上面提到，不同的存储引擎其MVCC实现思想是不一样的，有悲观锁，有乐观锁，那么二者有什么区别呢？</p>
<ul>
<li>悲观锁（Pessimistic Lock），顾名思义，就是很悲观，<strong>每次去拿数据的时候都认为别人会修改</strong>，所以每次<strong>在拿数据的时候都会上锁</strong>，这样别人想拿这个数据就会block直到它拿到锁。<strong>假定会发生并发冲突</strong>，<strong>事先屏蔽一切</strong>可能违反数据完整性的操作。</li>
</ul>
<ul>
<li>乐观锁（Optimistic Lock），顾名思义，就是很乐观，<strong>每次去拿数据的时候都认为别人不会修改</strong>，所以<strong>不会上锁</strong>，但是在<strong>提交更新的时候会判断一下</strong>在此期间别人有没有去更新这个数据。乐观锁适用于<strong>读多写少</strong>的应用场景，这样可以提高吞吐量。乐观锁假设不会发生并发冲突，<strong>只在提交操作时检查是否违反数据是否和预期的一致</strong></li>
</ul>
<h1 id="当前读快照读"><a href="#当前读快照读" class="headerlink" title="当前读快照读"></a>当前读快照读</h1><ol>
<li>快照读： 读取的是 快照中的内容， 不需要加锁。</li>
</ol>
<p><strong>使用场景：</strong></p>
<p><strong>单纯的select操作</strong>，<strong>不包括</strong> </p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> ... <span class="keyword">lock</span> <span class="keyword">in</span> <span class="keyword">share</span> <span class="keyword">mode</span> </span><br><span class="line"><span class="keyword">select</span> ... <span class="keyword">for</span> <span class="keyword">update</span></span><br></pre></td></tr></table></figure>



<p> Read Committed隔离级别：<strong>每次select都生成一个快照读。</strong></p>
<p> Read Repeatable隔离级别：<strong>开启事务后第一个select语句才是快照读的地方，而不是一开启事务就快照读。</strong></p>
<ol start="2">
<li>当前读： <strong>读取的是当前内容，需要加锁</strong></li>
</ol>
<p><strong>使用场景：</strong></p>
<figure class="highlight n1ql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span>...lock <span class="keyword">in</span> share mode (共享读锁)</span><br><span class="line"><span class="keyword">select</span>...<span class="keyword">for</span> <span class="keyword">update</span></span><br><span class="line"><span class="keyword">update</span> , <span class="keyword">delete</span> , <span class="keyword">insert</span></span><br></pre></td></tr></table></figure>

<p>因为<strong>必须获取 最新的信息</strong>，所以必须 使用 当前读。</p>
<p>实现方式： <strong>next -key -locks</strong>, 由两部分组成 gap_lock 和 record_lock组成。</p>
<table>
<thead>
<tr>
<th>record lock</th>
<th>锁定记录上的索引</th>
</tr>
</thead>
<tbody><tr>
<td>gap lock</td>
<td>锁定 索引间的间隙，不包括索引本身</td>
</tr>
<tr>
<td>next -key -locks</td>
<td>由record lock+gap lock组成， 既锁定索引本身也锁定索引的间隙</td>
</tr>
</tbody></table>
<h1 id="解决幻读"><a href="#解决幻读" class="headerlink" title="解决幻读"></a>解决幻读</h1><p>MVCC并没有从根本上解决 幻读的问题， MVCC+next_key_locks 二者一起可解决 幻读的问题。</p>
<p><img src="https://pic4.zhimg.com/80/v2-118bd607f107b4e7e333aa91a81161c3_1440w.jpg" alt=""></p>
<p>幻读： <strong>T1 读取某个范围的数据，T2 在这个范围内插入新的数据，T1 再次读取这个范围的数据，此时读取的结果和和第一次</strong></p>
<p><strong>读取的结果不同。</strong></p>
<p>由于有<strong>next_key_locks</strong>的存在，T1读取某个范围的时候，T2不能对其进行修改，所以解决了幻读这个问题。</p>
<p>实际上就是 <strong>MVCC+当前读，</strong> 这样就不会产生读取的时候被插入新数据的情况了。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>MyIsam和InnoDB比较</title>
    <url>/2020/09/27/MyIsam%E5%92%8CInnoDB%E6%AF%94%E8%BE%83/</url>
    <content><![CDATA[<h3 id="MYISAM和INNODB"><a href="#MYISAM和INNODB" class="headerlink" title="MYISAM和INNODB"></a>MYISAM和INNODB</h3><p><a href="http://www.expertphp.in/article/what-are-the-main-differences-between-innodb-and-myisam#:~:text=InnoDB%20does%20not%20support%20FULLTEXT,is%20suitable%20for%20small%20project." target="_blank" rel="noopener">参考链接</a></p>
<p>Mysql 数据库中，最常用的两种引擎是 innordb 和 myisam。InnoDB 是 Mysql 的默认存储引擎。</p>
<a id="more"></a>

<ol>
<li><p>事务处理上方面<br>MyISAM 强调的是<strong>性能</strong>，查询的速度比 InnoDB 类型更快，但是<strong>不提供事务支持</strong>。<br><strong>InnoDB 提供事务支持事务。</strong></p>
</li>
<li><p>外键<br>MyISAM 不支持外键，InnoDB 支持外键。</p>
</li>
<li><p><strong>锁</strong><br>MyISAM <strong>只支持表级锁</strong>，InnoDB 支持<strong>行级锁和表级锁</strong>，默认是<strong>行级锁</strong>，行锁大幅度提高了多用户并发操作的性能。</p>
<p>innodb 比较适合于<strong>插入和更新操作比较多的情况</strong>，而 myisam 则适合用于<strong>频繁查询</strong>的情况。另外，InnoDB 表的<strong>行锁</strong>也不是绝对的，如果在执行一个 SQL 语句时，MySQL <strong>不能确定要扫描的范围</strong>，InnoDB 表同样会锁全表。</p>
</li>
<li><p><strong>表主键</strong><br>MyISAM：<strong>允许没有主键</strong>的表存在。<br>InnoDB：如果没有设定主键，就会自动生成一个 <strong>6 字节的主键(</strong>用户不可见)。</p>
</li>
<li><p><strong>表的具体行数</strong><br>MyISAM：select count(<em>) from table,MyISAM 只要简单的读出保存好的行数。因为<br>MyISAM <strong>内置了一个计数器</strong>，count(</em>)时它直接从计数器中读。<br>InnoDB：<strong>不保存表的具体行数</strong>，也就是说，执行 select count(*) from table 时，InnoDB要扫描一遍整个表来计算有多少行。</p>
</li>
<li><p>索引的结构</p>
</li>
</ol>
<p>​        MyISAM 的索引是<strong>非聚簇索引</strong>，<strong>可以加载更多索引</strong> ，内存利用率高</p>
<p>​        InnoDB 的索引是 <strong>聚簇索引</strong> ， 体积庞大。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>数据库</tag>
      </tags>
  </entry>
</search>
